// 제공자 공식 로고 — CLAUDE.md Invariants 의 제공자 로고 예외, 이 파일 밖에 색 값을 옮기지 않는다. 받은 곳·날짜: 아직 받지 않음(빈 값이면 /login 버튼은 글자만) (specs/features/F-2033.md 6.4)

export const GOOGLE_LOGO_SVG = ''
export const GITHUB_LOGO_SVG = ''
