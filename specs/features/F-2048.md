---
id: F-2048
title: 이미지 사용법 글
milestone: M2
status: done
created: 2026-09-27
approved: 2026-09-27
implemented: 986f266
depends: [F-2039, F-156, F-157, F-158, F-209, F-218, F-219, F-220, F-221, F-406, F-2020, F-2047]
---

# F-2048 이미지 사용법 글

작성일: 2026-09-27
상태: 승인(2026-09-27). 따로 검토하지 않았고, 열린 질문이 모두 기본값을 가져 상시 지시로 승인했다 — 기본값대로 구현한다. 11장 C1(EXIF 방향 버그)은 main 이 명세 밖 버그 수정으로 먼저 고쳤다
범위: `specs/features/F-2039.md` 10장 "후속 사용법 글 후보" 넷째 행 "이미지와 첨부". `specs/ia.md` 6.1 R9 에 따라 **글 1편(`content/guides/images.md`) + 도움말 `## 이미지` 절 고쳐 쓰기와 끝 `사용법 글:` 줄 + 수치 대조 테스트(앱 쪽·worker 쪽) + 대조에 필요한 상수 7개에 `export` 붙이기**가 이번 구현 단위다
범위 확인: `specs/product.md` 7장 "공개 사이트" 표에 F-2039·F-2045~F-2047 행이 있고, 8장(범위 밖)에 사용법 글·이미지를 빼는 문장이 없다(2026-09-27 grep). 원천 명세가 모두 `done` 이고 구현 커밋이 모두 `origin/deploy`(`fedc340`)의 조상이다 — **글에 쓰는 기능은 모두 사용자에게 나가 있다**(m9)

사용자가 이미 정한 것(바꾸지 않는다, 2026-09-27 main 전달):

| # | 결정 | 이 명세 |
| --- | --- | --- |
| 1 | 글은 **한 편**. 형식 모델은 F-2045·F-2046·F-2047 — 글 전문 초안, 사실 표, 수치 대조, U*/E*, 파일 소유, 사람 확인, 스크래치 실측 | 전체 |
| 2 | 이 명세 → 바로 구현(ship-feature). 갈림길마다 기본값 | 9장 |
| 3 | 금고 이미지는 이 글에서 짧게 + `/guides/encryption` 링크(R8) | 3장 "인터넷 주소의 이미지와 금고 문서의 이미지" |
| 4 | EXIF 등 개인정보 문장은 코드로 확인한 것만 | 4.2 마지막 행, 11장 C3 |
| 5 | 명세와 코드가 다르면 코드를 따르고 갱신 대상에 적는다 | 4.8, 1.1 |

근거(2026-09-27 HEAD `19bd23c` 에서 읽은 것. 작업 트리에 다른 변경 없음 — `git status` 는 `.playwright-mcp/` 하나):
명세: `specs/ia.md` 3.26·6장 표·6.1 R1~R9, `F-2047.md` 전문(형식 모델), `F-2039.md` 10장, `F-156.md` 2장, `F-157.md` 2·3장, `F-158.md` 1~3장, `F-218.md` 2장, `F-219.md` 2장, `F-220.md` 2장, `F-257.md` 2장 표, `specs/product.md` 5·7장 F-156·F-209 행, `content/guides/*.md` 10편 전문(특히 `markdown-portability.md` "이미지가 있으면 zip 으로", `obsidian-vault.md` "이미지는 이렇게 바뀝니다", `offline-sync.md` 이미지 행 두 개, `encryption.md` 이미지 행, `sharing.md` 이미지 행).
코드: `src/app/attachImages.ts` 전문(`MAX_INPUT_BYTES`·`MAX_RESULT_BYTES`·`MAX_DIM`·`MAX_AREA`·`MESSAGES`·`altFromFile`), `src/lib/shrinkImage.ts` 전문(`MAX_SIDE`·`QUALITY`·`fitWithin`·`shrinkImage`), `src/lib/imageFile.ts` 전문(`inspectImageBytes`), `src/storage/toWebp.ts`, `src/editor/imageInsert.ts` 전문(`computeImagePlacement`·`isBlockedPosition`·붙여넣기 `text/plain` 갈래·`isViewOnly`), `src/lib/imageBlock.ts`(`buildImageBlock`·`parseImageBlock`·`extractAttachmentRefs`), `src/editor/preview/imageWidget.ts` 전문(도구 묶음 글자·손잡이·키보드·`이미지를 찾을 수 없습니다`), `src/editor/preview/blocks.ts` 이미지 갈래, `src/styles/image.css` 도구 묶음 위치, `src/app/App.tsx`(`handleImageFiles`·창 전체 드롭 알림 두 개·`scheduleGc`·`attachmentResolverFor`), `src/app/attachmentGc.ts` 전문(`GRACE_MS`), `worker/attachmentGc.ts` 전문(`GRACE_MS`·`MAX_DELETE`), `worker/index.ts` `scheduled`, `wrangler.jsonc` `triggers.crons`, `worker/attachments.ts`(`MAX_ATTACHMENT_BYTES`·`ATTACHMENT_QUOTA_BYTES`·`getUsedBytes`·`storeAttachment`), `worker/imageSniff.ts` `MAX_PIXELS`, `src/storage/serverStore.ts`(`putAttachment`·`getAttachment`·`sendOne` 의 `upload` 오류 갈래·`migrateLocalAttachments`·`ATTACHMENT_QUOTA_MESSAGE`), `src/lib/usageLimits.ts` `formatMegabytes`, `src/app/AccountMenu.tsx` 사용량 줄, `src/app/ShareMenu.tsx`·`SharedView.tsx` 문구, `src/app/exportDoc.ts`(`.md` zip·HTML·서식 있는 복사), `src/viewer/toHtmlDoc.ts` `buildExportBody`, `src/viewer/toPlainText.ts` 이미지 자리, `src/app/printDoc.ts`, `src/app/ExportMenu.tsx` 항목 글자, `src/viewer/renderMarkdown.ts` `image` 규칙, `src/editor/preview/links.ts` `Image` 갈래, `src/lib/e2eeLimits.ts`, `site/guard.ts` 전문, `src/app/helpDoc.ts` `GROUPS` 이미지 카드·`APP_SECTIONS`.
테스트: `src/app/helpDoc.test.ts` 전문, `site/helpGuides.test.ts` 전문, `worker/guideLimits.test.ts` 전문, `e2e/site.spec.js` F-2047 E1~E3·F-276 A10, `e2e/image.spec.js` 테스트 이름 전부, `site/pageNav.test.ts`·`site/render.test.ts` 42

**직접 돌리거나 재어 확인한 것** (2026-09-27, 이 PC, Node v24.12.0. 스크립트는 스크래치패드 `f2048/check.ts`·`build.mjs`·`patch.mjs`·`orient.mjs` 에만 있다 — esbuild 로 저장소의 `site/`·`src/`·`worker/` 모듈을 그대로 묶고, `HELP_DOC_CONTENT` 에서 `## 이미지` 절만 5장 after 로 바꿔 끼운다. 저장소 무변경)

| # | 무엇 | 결과 |
| --- | --- | --- |
| m1 | 지금 `## 이미지` 절 본문(제목 뺀, 코드 포인트) | **78자, 1문단**, `사용법 글:` 줄 없음. "한 장에 20MB까지 넣을 수 있고" 는 `MAX_INPUT_BYTES`(20 × 1024 × 1024, 고르는 파일 크기)와 **맞다.** 다만 줄인 뒤 5MB(`MAX_RESULT_BYTES`) 상한을 빠뜨려, GIF 6MB 처럼 20MB 아래인데 막히는 경우를 설명하지 못한다(4.8 D1) |
| m2 | 5장 after `## 이미지` | **326자, 3문단** + 링크 줄(R2 상한 600·4). 수치 3개(20MB·5MB·300MB, R5 절마다 3개 이하) |
| m3 | 도움말 제목 수 | 원래·after 모두 **42**(`siteHeadings`). `site/pageNav.test.ts` F-2036 A2·`site/render.test.ts` F-2036 A6 의 42 그대로 |
| m4 | 3장 글 초안 | 본문 **4,811자**, `##` **9개**, 111줄(R7 상한 6,000·10). `checkContentFile('guides/images.md', …)` 통과(`title` 반환 — 이미지 문법 가드에 걸리지 않음). `renderSitePage` HTML 26,229 B, `<script` 없음, `<table>` 3, `<h2>` 9, `<pre>` 1. `<img` 는 머리 아이콘 하나뿐(다른 글과 같다) |
| m5 | 3장 초안 + 지금 `content/**` 로 `buildSite` | 통과. `guides/images.html` 생성, `sitemap.xml` `<loc>`·`llms.txt` 줄에 들어간다. `/guides` 목록 첫 자리가 **`images`**(`date` 2026-09-27 인 네 글이 url 오름차순). 링크 줄 HTML `<p>사용법 글: <a href="/guides/images" target="_blank" rel="noopener noreferrer">문서에 이미지 넣기</a></p>`. 빌드된 `/help` 에는 `href="/guides/images"` 가 3개(본문 1 + 왼쪽 문서 목록·접는 목록 2 — `obsidian-vault`·`wiki-map` 도 3개로 같다). 앱 `.help-page` 안은 본문 1개 |
| m6 | R4(U5 방법) — after `## 이미지` ↔ 초안 | 절 7문장·글 53문장, **양방향 0**. 옛 절 ↔ 초안 0, 도움말 전체 ↔ 초안 0 |
| m7 | 초안 ↔ 다른 글 10편 20자 이상 문장 겹침 | **모두 0**(`markdown-portability`·`obsidian-vault`·`offline-sync`·`encryption`·`sharing` 포함) |
| m8 | 초안·after 절에 `FORBIDDEN_WORDS` 25개 + ia.md 6장 쓰지 않는 말(`첨부파일`·`첨부`·`사진`·`그림`·`불러오`·`업로드`·`다운로드`·`저장소`·`페이지`·`미리보기`·`캐시`·`옵션`·제품명 등) | **0개**. `vault` 는 링크 주소 `/guides/obsidian-vault` 안에만 있다(`markdown-portability.md` 와 같다) |
| m9 | 원천 명세가 사용자에게 나갔나 | `git merge-base --is-ancestor` — F-123 `a492f42`, F-156 `39cbf4b`, F-157 `b20654e`, F-158 `1809d11`, F-209 `366c2e4`, F-218 `2a08e9d`, F-219 `dd99c8d`, F-220 `8f74f1c`, F-221 `52de062`, F-280 `2f4099a`, F-406 `4dad0bf`, F-2019 `c767dad`, F-2020 `b0af139`, F-2021 `93004fc` 모두 `origin/deploy`(`fedc340`) 조상. `fedc340..HEAD` 의 `src`·`worker` 변경에 이미지 코드 없음 |
| m10 | 대조할 상수 값 | `MAX_INPUT_BYTES` 20,971,520 → `20MB`, `MAX_DIM` 10,000 → `10,000px`, `MAX_AREA` 40,000,000 → `4,000만`, `MAX_SIDE` 2,000 → `2,000px`, `MAX_RESULT_BYTES` 5,242,880 → `5MB`(= `E2EE_SERVER_MAX_ATTACHMENT_BYTES` = worker `MAX_ATTACHMENT_BYTES`), `GRACE_MS` 두 곳 모두 86,400,000 → `24시간`, `ATTACHMENT_QUOTA_BYTES` 314,572,800 → `300MB`. **앞 다섯과 두 `GRACE_MS` 는 지금 export 되지 않은 모듈 지역 상수다**(9장 Q3) |
| m11 | Chrome 에서 WebP 인코딩과 EXIF 방향(`orient.mjs`, Playwright 설치 Chrome, `about:blank`, 서버 없음) | ① `OffscreenCanvas.convertToBlob({ type: 'image/webp' })` 결과 `type` = `image/webp` ② 헤더 400×200 JPEG 에 EXIF Orientation=6 을 끼우면 `createImageBitmap` 이 **200×400**(돌린 크기)을 돌려준다 ③ `shrinkImage` 와 같게 헤더 크기(400×200) 캔버스에 그리면 돌린 그림이 **가로로 늘어나 찌그러진다**(위 빨강·아래 파랑). 11장 C1 |
| m12 | 기준선 | `helpDoc`·`helpGuides`·`pageNav`·`render`·`attachImages`·`shrinkImage`·`attachmentGc`(앱)·`guideLimits`·`attachmentGc`(worker) 9파일 **114개 통과**. `attachImages.ts` 는 node 에서 import 된다(`attachImages.test.ts` 가 이미 돈다) |

---

## 1. 파일 소유

| 파일 | 변경 |
| --- | --- |
| `content/guides/images.md` (신규) | 이미지 사용법 글. **3장 전문을 그대로** 넣는다 |
| `src/app/helpDoc.ts` | `APP_SECTIONS` 의 `## 이미지` 절만 5장 after 로 통째 바꾼다. 다른 절·`GROUPS`(마크다운 문법 이미지 카드)는 건드리지 않는다 |
| `src/app/helpDoc.test.ts` | 6.1 U1 추가 |
| `site/helpGuides.test.ts` | 6.2 U2·U3 추가. 다른 기존 테스트·도우미는 고치지 않는다 |
| `worker/guideLimits.test.ts` | 6.3 U4·U5 추가(F-2047 이 만든 파일에 `describe` 두 개). 기존 F-2047 U4·U5 는 건드리지 않는다 |
| `src/app/attachImages.ts` | `MAX_INPUT_BYTES`·`MAX_RESULT_BYTES`·`MAX_DIM`·`MAX_AREA` 네 선언 앞에 `export` 만 붙인다. 값·이름·다른 줄 변경 없음 |
| `src/lib/shrinkImage.ts` | `MAX_SIDE` 선언 앞에 `export` 만 |
| `src/app/attachmentGc.ts` | `GRACE_MS` 선언 앞에 `export` 만 |
| `worker/attachmentGc.ts` | `GRACE_MS` 선언 앞에 `export` 만 |
| `e2e/site.spec.js` | `F-2048 E1`~`E3` 추가. 기존 테스트는 건드리지 않는다 |

- `site/**` 소스·`vite.config.ts`·`src/app/HelpPage.tsx`·`src/viewer/**`·이미지 넣기·저장·정리 코드의 **동작 변경 없음.** `export` 넷(파일 넷)은 테스트가 상수를 읽으려는 것뿐이다. 글 하나를 더하면 목록·주소·`sitemap.xml`·`llms.txt`·왼쪽 문서 목록(F-2036)이 저절로 따라온다(m5)
- `worker/attachments.ts` 는 테스트가 `ATTACHMENT_QUOTA_BYTES` 를 **읽기만** 한다(이미 export)
- 도움말 제목은 늘지 않는다(m3)
- 새 의존성·저장소 식별자·설정 키 없음
- 줄 끝은 LF

### 1.1 갱신 대상 — 이번에 고치지 않는다

메인이 반영한다. 구현 에이전트는 건드리지 않는다.

| 파일 | 위치 | 반영할 내용 |
| --- | --- | --- |
| `specs/product.md` | 7장 "공개 사이트" 표, F-2047 행 아래 | `F-2048 | 이미지 사용법 글 | /guides/images, 도움말 이미지 절 고쳐 쓰기와 링크` |
| `specs/product.md` | 7장 F-209 행 "새로 넣는 이미지는 브라우저에서 WebP 변환(애니메이션 GIF 제외, EXIF 빠짐)" | "EXIF 빠짐" → "캔버스로 다시 그린 이미지만 EXIF 가 빠진다. GIF 와 WebP 로 바꿔도 작아지지 않은 이미지는 원래 바이트 그대로라 EXIF 가 남는다(F-220 2.1, F-2048 11장 C3)" |
| `specs/product.md` | 5장 F-156 행 "장당 5MB" | 끝에 "→ F-220: 고르는 파일 20MB, 줄인 결과 5MB" |
| `specs/ia.md` | 3.26 5번 "공유 링크에는 이미지가 담기지 않아 자리 표시로 보인다" | "`링크 복사` 주소에는 이미지가 담기지 않아 자리 표시로 보인다. 읽기 전용 링크·초대에서는 보인다(F-209·F-212)" — `sharing.md` 이미지 행과 같은 사실 |
| `specs/features/F-2039.md` | 10장 넷째 행 "이미지와 첨부" | 메모 칸 끝에 "→ F-2048 (`/guides/images`). 제목은 `문서에 이미지 넣기` — ia.md 6장이 `첨부파일` 을 쓰지 않는 말로 두어 행 이름을 쓰지 않았다(F-2048 9장 Q2)" |
| `specs/features/F-257.md` | 2장 절 표 `이미지` 행 | 내용 칸 → "붙여넣기·끌어넣기, 받는 형식, 고르는 파일 20MB·줄인 뒤 5MB, 정렬·`이미지 삭제`·손잡이, 원문 세 줄, 계정 300MB, 지운 이미지는 나중에 정리 — 사용법 글 링크(`images`, F-2048)". 근거 칸에 `attachImages.ts`·`shrinkImage.ts`·`imageWidget.ts` 추가 |
| `specs/human-checks.md` | 새 절 | 8장 H1~H4 |

확인한 결과 **고칠 것이 없는 문서**: `specs/design.md`(새 시각 요소 없음 — 표·목록·코드블록만), `specs/architecture.md`(모듈·키 변화 없음. `export` 넷은 공개 인터페이스 변경이 아니다), `F-276.md`(목록 문구가 주제를 나열하지 않는다), `F-2045.md`·`F-2046.md`·`F-2047.md`(세 글의 이미지 문장은 이 글과 겹치지 않고 이 글이 링크로 가리킨다 — m7), `specs/ia.md` 6.1(R1~R9 을 그대로 따랐다)

## 2. 글 — 목차와 판단

| 절 | 무엇을 | 왜 이 자리 |
| --- | --- | --- |
| (머리) | 붙여넣기·끌어 놓기, 파일은 따로·본문에는 HTML 세 줄 | "이미지가 원문에 어떻게 남나" 가 이 앱을 고른 이유(원문 보존)와 이어진다 |
| 넣는 방법 | 넣는 자리, 여러 장, 실행 취소, 글자 붙여넣기가 이김, 편집 영역 밖·`.md` 섞임, 넣지 못하는 자리 | 가장 먼저 부딪히는 동작 |
| 받는 형식과 크기 | 형식 판정, 4단계 표(20MB → 10,000px·4,000만 → 2,000px 줄이기 → 5MB), WebP·GIF, 파일 안 정보 | 도움말 20MB 와 5MB 의 관계를 여기서 풀어 준다(m1) |
| 원문에 들어가는 모양 | 세 줄 예시, `alt`·`width`, 모양이 깨지면 글자, 원문 들어가기 | 원문 보존 앱의 핵심 — 손으로 고쳐도 되는 곳과 깨지는 곳 |
| 정렬·크기 조절·삭제 | 버튼 표, 손잡이·Esc·←/→·Shift, 보기 모드 | 매일 쓰는 조작 |
| 저장되는 곳과 한도 | 로그인 전·후, 300MB, 계정 메뉴, 넘을 때 알림, 누구 공간으로 세나 | 한도의 이유와 결과(R1) |
| 지운 이미지가 정리되는 때 | 24시간·어느 문서도 쓰지 않음, 언제 정리하나, 문서 삭제, 복사, 정리 뒤 되살리기 | 쓴 양이 바로 안 주는 이유와 실행 취소의 한계 |
| 공유하고 내보낼 때 | 7줄 표 | 각 내보내기의 이미지 모양. 자세한 것은 세 글로 링크(R8) |
| 인터넷 주소의 이미지와 금고 문서의 이미지 | 표준 이미지 문법이 링크가 되는 이유, 금고 이미지 한 문단 + 링크, 터미널 링크 | 사용자 결정 3 |
| 정리 | 다섯 줄 | 다른 글과 같은 끝맺음 |

- **slug `images`**(9장 Q1): main 권장 그대로. 짧고, 주소만 보고 주제를 안다. `attachments` 는 원문 경로 이름과 같아 헷갈리고 ia.md 6장 "첨부파일" 금지와도 어긋난다
- **제목 `문서에 이미지 넣기`**(9장 Q2): F-2039 10장 행 이름 "이미지와 첨부" 는 ia.md 6장 `이미지` 행의 쓰지 않는 말 `첨부파일` 에 걸린다. 다른 글의 "…하기" 끝맺음(`금고로 문서 암호화하기`·`문서를 공유하고 함께 편집하기`)을 따랐다
- **겹치는 글과 나누기**(R8, 결정 3)
  - `markdown-portability.md` "이미지가 있으면 zip 으로": zip 안 모양·세 줄 HTML 예시가 이미 있다. 이 글의 원문 예시는 **다른 id·`alt` 가 있는 줄**로 두고(20자 이상 겹침 0, m7), `.md` 내보내기 모양은 표 한 칸 + 링크
  - `obsidian-vault.md`: 볼트 내보내기·가져오기의 이미지 변환은 표 한 칸 + 링크. 볼트 가져오기의 WebP 규칙(줄이지 않고 로그인 때만 바꾼다)은 붙여넣기 경로와 달라 이 글에 쓰지 않는다
  - `offline-sync.md`: 끊긴 동안 넣은 이미지·한 번 본 이미지만 보이는 것·오프라인에서 넣어 300MB 를 넘긴 경우는 한 문장 + 링크. 이 글은 "연결된 상태에서" 넘길 때만 쓴다
  - `sharing.md` 이미지 행("읽기 전용 링크와 초대에서는 보입니다"), "편집 권한인 사람이 넣은 이미지는 그 사람의 이미지 저장 공간" : 표 한 칸 + 링크, 공간 문장은 다른 말로 한 줄
  - `encryption.md` 이미지 행: 금고 이미지는 두 문장 + 링크(결정 3). `이미지를 찾을 수 없습니다` 로 보이는 경우는 그 글 몫
  - `cli.md` `rawdoc upload`: 링크 한 문장
- **넣지 않은 것**과 이유
  - `alt` 100자 자르기, 크기 조절 최소 48px, ←/→ 10px·Shift 50px: 모두 함수 안 리터럴이거나 export 되지 않은 모듈 상수(`imageBlock.ts` `sanitizeAlt` 의 `100`, `imageWidget.ts` `MIN_WIDTH`·`attachHandle` 의 `10`/`50`)라 R5 대조를 걸려면 `src/editor/**`·`src/lib/imageBlock.ts` 를 소유에 더해야 한다. 쓰다가 판단에 쓰는 수치가 아니다 — 글은 "조금씩", "한 번에 더 많이" 로 쓴다(9장 Q5)
  - WebP 품질 0.85·0.9(`shrinkImage.ts` `QUALITY`, `toWebp.ts` 리터럴): 사용자가 고를 수 없는 값이다. "원본과 픽셀까지 같지는 않습니다" 한 문장으로 쓴다
  - 서버 정리 시각(`wrangler.jsonc` `"0 18 * * *"` = 한국 시간 새벽 3시)·한 번에 500개(`MAX_DELETE`): 설정 파일 값이고 운영 사정으로 바뀐다. 글은 "하루에 한 번"
  - 금고 이미지의 한 장 상한(`E2EE_MAX_PLAIN_ATTACHMENT_BYTES` 5,242,811 바이트 — 일반과 69바이트 차이)·문서당 1,000개: 앞은 사용자에게 차이가 없고, 뒤는 `encryption.md` 가 쓰고 대조한다(F-2039 U7)
  - 공유 편집자가 남의 문서에 넣은 이미지를 문서 주인·다른 참여자가 받는 권한 판정(`handleGetAttachment` `?doc=`): `sharing.md` 몫
  - 서식 있는 복사의 이미지(`copyDocAsRichText` 가 data URI 로 넣는다): 붙여넣는 프로그램마다 결과가 다르고 이 세션에서 돌려 보지 않았다
  - Safari 에서의 WebP 변환(11장 C2): 확인하지 못했다
  - EXIF 방향 JPEG 가 찌그러지는 동작(11장 C1): 버그라 사용법 글에 적지 않는다

## 3. 글 전문 — `content/guides/images.md`

**아래를 그대로 파일로 만든다.** 구현하는 쪽이 문장을 새로 짓지 않는다. 문장의 근거는 4장 표에 있다. `date`·`updated` 는 구현 커밋 날짜가 2026-09-27 이 아니면 그 날짜로 두 줄 모두 바꾼다(R7). 줄 끝은 LF.

````markdown
---
title: 문서에 이미지 넣기
summary: 이미지를 붙여넣거나 끌어 놓아 넣는 방법, 넣을 때 줄이는 규칙과 크기 한도, 정렬·크기 조절·삭제, 지운 이미지가 정리되는 때, 공유하고 내보낼 때의 모양.
date: 2026-09-27
updated: 2026-09-27
---

# 문서에 이미지 넣기

이미지는 편집 영역에 붙여넣거나 파일을 끌어 놓아 넣습니다. 이미지 파일은 문서 본문과 따로 저장되고, 본문에는 그 파일을 가리키는 HTML 세 줄이 들어갑니다. 그래서 원문 모드에서도 이미지 자리가 보이고, 내보낸 파일에도 그 자리가 그대로 남습니다.

## 넣는 방법

- 복사해 둔 이미지를 편집 영역에 붙여넣으면 커서가 있는 줄 다음에 들어갑니다. 커서가 빈 줄에 있으면 그 줄에 들어갑니다
- 이미지 파일을 편집 영역으로 끌어다 놓으면 커서 대신 놓은 자리를 기준으로 같은 방식으로 들어갑니다. 여러 파일을 한 번에 놓으면 빈 줄을 사이에 두고 차례로 넣습니다
- 넣은 이미지는 여러 장이어도 Ctrl+Z 한 번으로 모두 빠집니다
- 복사한 내용에 글자도 들어 있으면 글자만 붙여넣습니다. 워드나 엑셀에서 복사할 때 딸려 오는 이미지 조각이 문서에 끼지 않게 하려는 것입니다
- 편집 영역 밖(사이드바·상단바·보기 모드 화면)에 놓으면 넣지 않고 `이미지는 편집 영역에 놓아 넣을 수 있습니다.`가 뜹니다. `.md` 파일과 섞어서 놓으면 `.md` 파일만 가져옵니다
- 코드블록·표·문서 맨 위 속성 안에 커서가 있거나 읽기만 할 수 있는 문서에서는 `이 위치에는 이미지를 넣을 수 없습니다.`가 뜹니다

## 받는 형식과 크기

PNG·JPEG·GIF·WebP 이미지를 받습니다. 형식은 파일 이름이 아니라 파일 앞부분의 내용을 보고 정하므로, 확장자만 바꾼 파일과 SVG는 받지 않습니다.

넣을 때는 아래 순서로 봅니다. 여러 장 가운데 몇 장만 걸리면 나머지는 넣고, 못 넣은 장 수를 알림으로 알려 줍니다.

| 순서 | 확인하는 것 | 결과 |
| --- | --- | --- |
| 1 | 고른 파일이 20MB를 넘는가 | 넘으면 넣지 않습니다 |
| 2 | 한 변이 10,000px를 넘거나, 가로와 세로를 곱한 화소 수가 4,000만을 넘는가 | 넘으면 넣지 않습니다 |
| 3 | 긴 변이 2,000px를 넘는 PNG·JPEG·WebP인가 | 긴 변을 2,000px에 맞춰 줄이고 WebP로 바꿉니다 |
| 4 | 3번을 거친 결과가 5MB를 넘는가 | 넘으면 넣지 않습니다 |

- 긴 변이 2,000px 이하인 PNG·JPEG·WebP도 한 번 WebP로 바꿔 보고, 파일이 작아질 때만 바꾼 쪽을 씁니다. 바꾼 이미지는 원본과 픽셀까지 같지는 않습니다
- GIF는 줄이지도 바꾸지도 않습니다. 움직이는 GIF는 그대로 움직이고, 그 대신 처음부터 5MB 이하여야 합니다
- 원래 파일을 그대로 저장할 때(GIF, 바꿔도 작아지지 않은 이미지)는 파일 안에 든 정보(찍은 날짜·위치 등)도 함께 남습니다. 이 앱에는 그 정보만 골라 지우는 기능이 없습니다

## 원문에 들어가는 모양

본문에는 이런 세 줄이 들어갑니다.

```
<div align="center">
  <img src="attachments/0f3a9c2e7b1d4a58.webp" alt="회의 화이트보드" width="480">
</div>
```

- `attachments/` 뒤의 이름은 넣을 때마다 새로 정합니다. 원래 파일 이름은 쓰지 않습니다
- `alt`는 이미지 설명입니다. 끌어다 놓은 파일은 확장자를 뗀 파일 이름이, 붙여넣은 이미지는 `이미지`가 들어갑니다. 원문에서 직접 고쳐도 됩니다
- `width`는 화면에 보이는 폭(px)입니다. 처음 값은 이미지 원래 폭과 편집 영역 글 폭 가운데 작은 쪽입니다
- 이 모양에서 벗어나게 고치면(다른 속성을 더하거나 줄을 합치면) 이미지 대신 적은 글자가 그대로 보입니다. 목록이나 인용 안에 들어간 세 줄도 글자로 남습니다
- 편집 모드에서 커서가 이 세 줄 안으로 들어가면 원문이 보입니다. 위 줄에서 ↓ 키로 내려오거나 이미지를 두 번 누르면 들어갑니다

## 정렬·크기 조절·삭제

편집 모드에서 이미지에 마우스를 올리면 이미지 안 오른쪽 위에 아이콘 버튼 네 개가 뜹니다. 버튼에 마우스를 올리면 이름이 보입니다.

| 버튼 | 하는 일 |
| --- | --- |
| `왼쪽 정렬`·`가운데 정렬`·`오른쪽 정렬` | 원문의 `align` 값만 바꿉니다 |
| `이미지 삭제` | 세 줄을 지웁니다. 묻지 않고 지우며, Ctrl+Z 한 번으로 되돌립니다 |

- 폭은 이미지 가장자리의 막대 손잡이나 아래 모서리 손잡이를 끌어 바꿉니다. 끄는 동안 이미지 안에 `480px`처럼 폭이 보이고, 손을 떼면 원문 `width`에 적힙니다. 높이는 비율을 따라가고, 편집 영역 글 폭보다 넓게는 늘어나지 않습니다
- 끄는 도중 Esc를 누르면 원래 폭으로 돌아갑니다. 끈 뒤에는 ←·→ 키로 폭을 조금씩 더 바꿀 수 있고, Shift를 같이 누르면 한 번에 더 많이 바뀝니다
- 이미지를 한 번 누르는 것으로는 커서가 움직이지 않습니다
- 보기 모드에서는 같은 정렬과 폭으로 보이고, 버튼과 손잡이는 없습니다

## 저장되는 곳과 한도

로그인하지 않았으면 이미지는 문서와 함께 이 브라우저에만 저장됩니다. 브라우저가 공간을 더 내주지 않으면 `저장 공간이 부족해 이미지를 넣지 못했습니다.`가 뜹니다.

로그인했으면 이 브라우저에 먼저 두고 서버로 올립니다. 다른 기기에서는 이미지를 처음 볼 때 서버에서 받아 그 브라우저에 둡니다. 연결이 끊긴 동안 넣거나 볼 때의 이야기는 [오프라인과 동기화](/guides/offline-sync)에 있습니다.

- 로그인한 계정의 이미지 저장 공간은 300MB입니다. 줄이고 바꾼 뒤의 크기로 셉니다
- 계정 메뉴를 열면 `이미지 42MB / 300MB`처럼 지금까지 쓴 양이 보입니다
- 연결된 상태에서 새 이미지가 공간을 넘기면 넣지 않고 `이미지 저장 공간(300MB)이 가득 찼습니다. 문서에서 지운 이미지는 하루 뒤 정리됩니다.`가 뜹니다
- 금고 문서에 넣은 이미지와, 다른 사람 문서에 `편집` 권한으로 넣은 이미지도 넣은 사람의 공간에 들어갑니다
- 이미지를 지우거나 문서를 지워도 쓴 양은 바로 줄지 않습니다. 아래 정리가 끝난 뒤에 줄어듭니다

## 지운 이미지가 정리되는 때

`이미지 삭제`를 누르거나 원문에서 세 줄을 지워도 이미지 파일은 곧바로 지우지 않습니다. 어느 문서의 원문에도 그 파일 이름이 없고, 넣은 지 24시간이 지난 이미지만 정리합니다. 지운 직후 되돌리거나 다른 문서로 잘라 붙이는 동안 파일이 사라지지 않게 하려는 여유입니다.

- 로그인했으면 서버가 하루에 한 번 정리합니다. 이 브라우저에 둔 사본은 앱을 열 때 같은 기준으로 정리합니다
- 로그인하지 않았으면 앱을 열 때 정리합니다
- 문서를 지웠을 때도 같은 기준입니다. 그 문서만 쓰던 이미지는 그 뒤의 정리 때 사라집니다
- 이미지 세 줄을 복사해 내 다른 문서에 붙여넣으면 같은 파일을 함께 씁니다. 한 문서라도 그 줄을 가지고 있으면 지우지 않습니다
- 정리된 뒤에 실행 취소나 붙여넣기로 세 줄을 되살리면 그 자리에 `이미지를 찾을 수 없습니다`가 보입니다

## 공유하고 내보낼 때

| 어디서 | 이미지는 |
| --- | --- |
| 보기 모드, `PDF (A4 인쇄)` | 보기 모드에서는 편집 모드와 같은 정렬과 폭으로 보이고, 인쇄에도 함께 나옵니다 |
| `링크 복사`, 읽기 전용 링크, 초대 | `링크 복사` 주소에는 이미지가 들어가지 않습니다. 나머지는 [문서를 공유하고 함께 편집하기](/guides/sharing)에 정리했습니다 |
| `.md` | 이미지가 있으면 zip으로 받습니다. 안의 모양은 [한 번 쓴 글을 다른 도구로 옮기기](/guides/markdown-portability)에 있습니다 |
| `HTML 파일` | 이미지를 파일 안에 넣어 HTML 파일 하나로 받습니다 |
| `.txt (평문)` | 이미지 자리에 `[이미지: 설명]`이 들어갑니다 |
| 설정 `데이터` 탭 `전체 내보내기` | 문서와 함께 zip에 들어가고, 같은 탭의 `가져오기…`로 들이면 이미지도 돌아옵니다 |
| `옵시디언 볼트로 내보내기` | 옵시디언의 이미지 문법으로 바뀝니다. [옵시디언 볼트와 오가기](/guides/obsidian-vault)에 있습니다 |

내보낼 때 이 브라우저와 서버 어디에서도 찾지 못한 이미지는 빼거나 자리 표시로 넣고, 몇 개인지 알림으로 알려 줍니다.

## 인터넷 주소의 이미지와 금고 문서의 이미지

이 앱은 인터넷 주소에 있는 이미지를 받아 오지 않습니다. `![설명]` 뒤에 주소를 괄호로 붙이는 마크다운 이미지 문법은 편집 모드에서는 적은 글자 그대로, 보기 모드에서는 `이미지: 설명` 링크로 보이고, 누르면 새 탭에서 그 주소가 열립니다. `attachments/`가 아닌 주소를 쓴 HTML `<img>` 줄도 글자로 남습니다. 오프라인에서도 문서가 같은 모양으로 보이고, 문서를 여는 것만으로 다른 서버에 요청이 가지 않게 하려는 것입니다. 보여 주고 싶은 이미지는 파일로 받아 두었다가 끌어다 놓습니다.

금고 문서에 넣은 이미지는 암호화해서 저장하고, 금고 문서 안에서만 보입니다. 자세한 것은 [금고로 문서 암호화하기](/guides/encryption)에 있습니다. 터미널에서 이미지를 올리는 방법은 [터미널에서 문서 읽고 쓰기](/guides/cli)에 있습니다.

## 정리

- 붙여넣거나 끌어다 놓아 넣고, 본문에는 `attachments/` 파일을 가리키는 HTML 세 줄이 들어갑니다
- 고른 파일은 한 장에 20MB까지이고, 긴 변이 2,000px를 넘으면 줄여서 WebP로 두며, 그래도 5MB를 넘으면 넣지 않습니다
- 정렬과 삭제는 이미지 위 버튼으로, 폭은 손잡이를 끌어 바꿉니다
- 지운 이미지는 어느 문서도 쓰지 않고 넣은 지 24시간이 지나야 정리됩니다
- 로그인한 계정은 이미지 300MB까지이고, 쓴 양은 계정 메뉴에서 봅니다
````

- **사이트 빌드 가드**(`site/guard.ts` `IMAGE_SYNTAX_RE`)는 **코드블록·인라인 코드 안까지** 원문 전체를 본다. 그래서 표준 이미지 문법은 `` `![설명]` 뒤에 주소를 괄호로 붙이는 `` 으로 풀어 썼다. 구현하는 쪽이 이 문장을 `` `![설명](주소)` `` 꼴로 "고치면" 빌드가 실패한다 — 이 정규식은 `![` 뒤 첫 `]` 가 곧바로 `(` 로 이어지지 않으면 걸리지 않는다. 문단 안의 `![설명]` 뒤에는 `` ` `` 이 오고, 같은 파일 뒤쪽 링크의 `](` 까지 `[^\]]*` 가 건너가지 못한다(`[금고로…` 의 `[` 앞에 `]` 가 먼저 나온다). m4 에서 `checkContentFile` 로 확인했다
- 세 줄 예시의 `<div align="center">` 는 가드의 `IMAGE_BLOCK_RE`(`<div class="md-image"`)에 걸리지 않는다 — `markdown-portability.md` 가 같은 모양을 이미 쓴다
- `…` 는 U+2026 한 글자다(`가져오기…` 는 화면 글자 그대로). `←`·`→`·`↓` 는 화살표 글자 그대로
- `이미지 42MB / 300MB` 의 `42` 는 예시 수라 대조하지 않는다. `480px`·`480` 도 예시다
- 알림 인용 `이미지 저장 공간(300MB)이 가득 찼습니다. …하루 뒤 정리됩니다.` 는 코드 글자 그대로(`attachImages.ts` `MESSAGES.quota` = `serverStore.ts` `ATTACHMENT_QUOTA_MESSAGE`). 글의 "24시간" 과 알림의 "하루" 는 같은 값이다

## 4. 사실 표 — 글의 문장과 근거

구현하는 쪽이 문장을 바꿀 때 여기를 먼저 본다. "코드" 칸은 이름으로 적는다. **모든 행을 2026-09-27 현재 코드로 확인했다.** 브라우저 동작에 기댄 행은 그렇다고 적고 10장에 가정으로도 적었다

### 4.1 머리·넣는 방법

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 파일은 따로, 본문에는 HTML 세 줄 | F-156 2.1·2.3 | `buildImageBlock`(세 줄), 저장소 `putAttachment`(문서와 연결 필드 없는 첨부 레코드) |
| 붙여넣기는 커서 줄 다음, 빈 줄이면 그 줄 | F-156 2.6 | `imageInsert` `paste` 가 기준 위치 `selection.main.to`, `computeImagePlacement` — 빈 줄이면 그 줄, 아니면 줄 끝 다음 |
| 끌어 놓기는 놓은 자리 기준, 여러 장은 빈 줄 사이 | F-156 2.5·2.6 | `drop` 의 `posAtCoords`, `computeImagePlacement` 가 블록을 `\n\n` 으로 잇는다. `e2e/image.spec.js` "F-156 A3" |
| Ctrl+Z 한 번에 모두 | F-156 2.6 | 한 `dispatch`(`userEvent` `input.paste`/`input.drop`). "F-156 A3" 이 Ctrl+Z 한 번에 둘 다 사라지는지 본다 |
| 글자가 있으면 글자만 | F-156 2.4 | `paste` — `getData('text/plain')` 이 비어 있지 않으면 `return false`. "F-156 A6". 워드·엑셀 이유는 F-156 2.4 의 문장(코드가 아니라 설계 이유) |
| 편집 영역 밖·`.md` 섞임 알림 | F-156 2.5 | `App.tsx` 창 전체 드롭 — `이미지는 편집 영역에 놓아 넣을 수 있습니다.`, `.md 파일만 가져왔습니다. 이미지는 따로 놓아 주세요.`. "F-156 A8" |
| 코드블록·표·프론트매터·읽기 전용이면 알림 | F-156 2.6 | `isBlockedPosition`(`FencedCode`·`Table`·`Frontmatter`), `handleImageFiles` 의 `blocked \|\| readOnlyDocRef.current` → `이 위치에는 이미지를 넣을 수 없습니다.` |

### 4.2 받는 형식과 크기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| PNG·JPEG·GIF·WebP, 파일 앞부분으로 판정, SVG 불가 | F-156 2.2 | `inspectImageBytes` — 시그니처와 헤더만 본다. SVG·글자 파일은 `null` → `MESSAGES.format` |
| 순서 1: 20MB | F-220 2.2 | `attachImages` 첫 검사 `file.size > MAX_INPUT_BYTES`(20 × 1024 × 1024) |
| 순서 2: 한 변 10,000px, 4,000만 화소 | F-156 2.2 | `info.width > MAX_DIM \|\| info.height > MAX_DIM \|\| w*h > MAX_AREA`(10,000 / 40,000,000). 서버도 `worker/imageSniff.ts` `MAX_PIXELS` 40,000,000 |
| 순서 3: 긴 변 2,000px 넘으면 줄여 WebP | F-220 2.1 | `shrinkImage` — `fitWithin(…, MAX_SIDE)`, 줄였으면 결과 WebP 를 크기와 무관하게 쓴다. "F-220 A2"(저장 2000×1500) |
| 순서 4: 결과 5MB | F-220 2.2 | `shrunk.blob.size > maxResultBytes`(`MAX_RESULT_BYTES` 5 × 1024 × 1024). 금고 문서는 `E2EE_MAX_PLAIN_ATTACHMENT_BYTES` |
| 몇 장만 걸리면 나머지는 넣고 장 수 알림 | F-156 2.2 | `attachImages` 끝 — `이미지 {N}장을 넣지 못했습니다. {첫 사유}` |
| 2,000px 이하도 WebP 로 바꿔 보고 작을 때만 | F-220 2.1 | `shrinkImage` `encoded.size < blob.size`. 로그인하면 `putAttachment` 가 PNG·JPEG 로 남은 것을 `toWebp` 로 한 번 더 시도(작을 때만, F-209 2.5) |
| 원본과 픽셀까지 같지 않음 | F-220 2.1 | `convertToBlob({ type: 'image/webp', quality: QUALITY })`, `QUALITY` 0.85. 품질 값을 주는 인코딩이 손실 압축이라는 것은 브라우저 동작(10장 가정 1) |
| GIF 는 그대로, 움직임 유지, 처음부터 5MB | F-220 2.1·2.2 | `shrinkImage` 첫 줄 `if (info.ext === 'gif') return original`, `putAttachment` 도 `gif` 는 `toWebp` 건너뜀. 알림 `GIF 는 한 장에 5MB 까지 넣을 수 있습니다.`. "F-220 A4" |
| 원래 파일을 그대로 두면 파일 안 정보도 남고, 골라 지우는 기능은 없다 | F-156 2.2("EXIF 제거는 M1 에서 하지 않는다"), F-220 | 원본을 그대로 쓰는 갈래는 셋 — GIF, `encoded.size >= blob.size`, 캔버스 실패(`catch`). 이 갈래는 **받은 바이트를 한 바이트도 바꾸지 않는다.** `src`·`worker` 에 EXIF·메타데이터를 다루는 코드 없음(`exif`·`orientation` grep — `worker-configuration.d.ts` 의 Cloudflare 타입 주석만). 캔버스로 다시 그린 WebP 에 EXIF 가 남지 않는다는 것은 글에 쓰지 않았다(브라우저 동작, 11장 C3) |

### 4.3 원문 모양·정렬·크기·삭제

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 이름은 넣을 때마다 새로, 원래 파일 이름은 안 씀 | F-156 2.1 | `serverStore.ts` `randomAttachmentId`(겹치면 다시 뽑음), 로컬 저장소도 같은 규칙. 원래 이름은 `alt` 로만 간다 |
| `alt`: 끌어 놓은 파일은 확장자 뗀 이름, 붙여넣기는 `이미지` | F-156 2.1 | `attachImages.ts` `altFromFile` |
| `width` 처음 값 = min(원래 폭, 글 폭) | F-156 2.1 | `handleIncomingImages` — `Math.min(img.width, contentWidth)`. "F-156 A4" |
| 모양이 깨지거나 목록·인용 안이면 글자 | F-156 2.1, F-157 2.1, F-158 2.1 | `parseImageBlock` 이 `null`, `blocks.ts` 이미지 갈래가 목록·인용 안이면 위젯을 만들지 않는다. 보기 모드는 최상위만(`renderMarkdown` 이미지 블록 규칙) |
| 커서가 들어가면 원문, ↓·두 번 누르기 | F-157 2.1·2.5 | `imageWidget.ts` `dblclick` → 블록 첫 줄로 커서. "F-157 A7" |
| 도구 묶음 = 이미지 안 오른쪽 위, 아이콘 넷, 이름은 툴팁 | F-157 2.3, F-218 2.1 | `image.css` `.md-image-toolbar`(top·right 8px, 호버·포커스 때만), `buildToolbar` 글자 `왼쪽 정렬`·`가운데 정렬`·`오른쪽 정렬`, `buildDeleteButton` `이미지 삭제`(각 `icon-tooltip`) |
| 정렬은 `align` 값만 | F-157 2.3 | `imageAlignChange`. "F-157 A3" |
| 삭제는 묻지 않고, Ctrl+Z 한 번 | F-218 2.2 | `dispatchDelete` 한 트랜잭션, 확인 대화상자 없음. "F-218 A3" |
| 막대·모서리 손잡이, 끄는 중 `{N}px`, 놓으면 `width`, 비율 유지, 글 폭 상한 | F-157 2.4 | `buildHandles`(가장자리·모서리 `크기 조절`), `ensureSizeLabel` 글자 `${width}px`, `endDrag(true)` → `imageWidthChange`, `measureMaxWidth`. "F-157 A4·A5" |
| Esc 취소, 끈 뒤 ←/→, Shift 더 크게 | F-157 2.4 | `onKeyDownEscape` → `endDrag(false)`, `pointerdown` 의 `handleEl.focus()`(끈 뒤 포커스가 손잡이에 남는다), `keydown` `ArrowLeft`/`ArrowRight` 10·Shift 50. "F-157 A5·A6" |
| 한 번 누르기는 커서를 안 옮김 | F-157 2.5 | `wrap` `mousedown` 에서 `preventDefault` |
| 보기 모드: 같은 모양, 버튼 없음 | F-158 2.1 | `renderMarkdown` 이미지 블록 규칙 + `Viewer.tsx` 채우기. "F-158 A2" |

### 4.4 저장·한도

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 로그인 안 함: 이 브라우저에만, 공간 부족 알림 | F-156 2.3 | 로컬 저장소 `putAttachment`, 실패 → `MESSAGES.save` `저장 공간이 부족해 이미지를 넣지 못했습니다.` |
| 로그인: 브라우저에 먼저, 서버로 올림 | F-209 2.5 | `serverStore.ts` `putAttachment` — 캐시에 넣고 `upload` 항목을 보낼 목록에 넣은 뒤 곧 돌아온다 |
| 다른 기기: 처음 볼 때 받아 그 브라우저에 | F-209 2.5 | `getAttachment` — 캐시에 없으면 `fetchAttachment` 뒤 캐시에 적는다 |
| 300MB, 줄이고 바꾼 뒤 크기로 셈 | F-221 2.1 | `ATTACHMENT_QUOTA_BYTES` 314,572,800, `getUsedBytes` = 저장한 바이트 `size` 합. `putAttachment` 사전 검사도 `converted.size` |
| 계정 메뉴 `이미지 {MB} / 300MB` | F-221 2.5 | `AccountMenu.tsx` 사용량 줄, `formatMegabytes`(10MB 이상은 정수 — 예시 `42MB`) |
| 연결된 상태에서 넘기면 넣지 않고 알림 | F-221 2.3 | `putAttachment` — `fetchUsage` 가 되면 `used + 안 올린 합 + 새 크기 > limit` 에 `QuotaExceededError` → `MESSAGES.quota`. 조회 실패(오프라인)면 검사 없이 넣는다 — 그 뒤는 `offline-sync.md` 몫 |
| 금고 이미지·남의 문서에 넣은 이미지도 넣은 사람 공간 | F-221, F-406, F-212 | `getUsedBytes` 는 `owner_id` 의 모든 행(금고 행 포함), 올리는 요청의 `owner_id` 는 올린 사람(`handleUploadAttachment` `user.id`) |
| 지워도 쓴 양은 바로 안 줄고 정리 뒤 줄어듦 | F-218 2.2, F-219 | 삭제 버튼·문서 삭제가 첨부를 지우지 않는다. `attachments` 행은 정리(`cleanupServerAttachments`)가 지운다 |

### 4.5 정리

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 어느 문서 원문에도 없고 넣은 지 24시간 | F-156 2.7, F-219 2.1 | 앱 `cleanupUnusedAttachments`·worker `cleanupServerAttachments` — `extractAttachmentRefs` 합집합에 없고 `createdAt`/`created_at < now − GRACE_MS`(두 곳 모두 24시간). 서버의 `created_at` 은 올린 때라, 끊긴 동안 넣은 이미지는 24시간보다 늦게 센다 — "24시간이 지난 이미지만" 은 참이다 |
| 여유의 이유(되돌리기·잘라 붙이기) | F-156 2.7 | F-156 2.7 문장(설계 이유) |
| 로그인: 서버 하루 한 번 + 이 브라우저 사본은 앱 열 때 | F-219, F-207 2.5 | `wrangler.jsonc` `"0 18 * * *"` → `scheduled` 가 `cleanupServerAttachments`. `App.tsx` `scheduleGc` — 서버 저장소면 올린 첨부만(`uploaded !== false`) 같은 규칙으로 |
| 로그인 안 함: 앱 열 때 | F-156 2.7 | `scheduleGc` → `scheduleAttachmentGc`(`requestIdleCallback`, 없으면 2초) |
| 문서 삭제도 같은 기준 | F-156 2.7 "문서 삭제 때는 바로 지우지 않는다" | 삭제 경로에 첨부 삭제 없음, 정리가 판정 |
| 복사한 줄은 같은 파일, 한 문서라도 쓰면 안 지움 | F-156 2.1, F-219 2.1 | 참조 판정이 문서 전체의 합집합(서버는 모든 사용자 문서) |
| 정리 뒤 되살리면 `이미지를 찾을 수 없습니다` | F-157 2.2 | `renderPlaceholder` 글자, 보기 쪽 `DEFAULT_MISSING_TEXT` |

### 4.6 공유·내보내기·주소 이미지·금고

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 인쇄에도 나옴 | F-158, F-280 | `printDoc.ts` `fillAttachmentImages(root, resolveAttachment)` |
| `링크 복사` 주소에는 없음 | F-158 2.2 | `ShareMenu.tsx` `공유 링크를 복사했습니다. 이미지는 링크에 담기지 않습니다.`, `SharedView.tsx` 자리 표시 문구 |
| `.md` 는 이미지가 있으면 zip | F-158 2.3 | `exportDoc` — 참조가 있으면 zip |
| `HTML 파일` 은 파일 안에 넣음 | F-280 | `buildExportBody` 주석 "이미지 data: URI" — `fillImages` |
| `.txt (평문)` 은 `[이미지: 설명]` | F-280 | `toPlainText.ts` — `alt` 가 있으면 `[이미지: {alt}]`, 없으면 `[이미지]` |
| `전체 내보내기`·`가져오기…` 로 이미지도 돌아옴 | F-281, F-282 | `exportWorkspace.ts`, `importWorkspace.ts` 첨부 계획 |
| 볼트 내보내기는 옵시디언 문법 | F-2020 | `exportVault.ts`, `obsidianImage.ts` |
| 못 찾은 이미지는 빼거나 자리 표시 + 개수 알림 | F-158 2.3, F-280 | `exportDoc` `이미지 {N}개를 찾을 수 없어 빼고 내보냈습니다.`, HTML `…자리 표시로 넣었습니다.`, 전체·볼트 내보내기도 같은 모양 |
| 주소 이미지 문법: 편집은 글자, 보기는 `이미지: 설명` 링크, 새 탭 | F-123 3.2 | `renderMarkdown` `image` 규칙(`<img>` 대신 `<a … target="_blank">`, 글자 `이미지`/`이미지: {alt}`). 편집 모드에 `Image` 노드 위젯 없음(`links.ts` 는 클릭 판정에서 빼기만) |
| `attachments/` 가 아닌 `<img>` 줄은 글자 | F-156 2.1, F-158 2.1 | `parseImageBlock` `SRC_RE`, markdown-it `html: false` |
| 이유: 오프라인에서 같은 모양, 여는 것만으로 다른 서버 요청 없음 | F-123 3.2, product.md 7장 "오프라인 원칙" | `<img>` 를 만들지 않으므로 요청이 생기지 않는다 |
| 금고 이미지는 암호화, 금고 문서 안에서만 | F-406 | `attachImages` 의 `e2ee` 갈래, `attachmentResolverFor` 가 금고 첨부를 일반 문서에 돌려주지 않는다. `encryption.md` 이미지 행과 같은 사실 |

### 4.7 도움말 절(5장)

| 도움말 문장 | 근거 |
| --- | --- |
| 첫 문단(넣는 방법·형식·20MB·줄이기·5MB) | 4.1 첫 행, 4.2 둘째~다섯째 행 |
| 둘째 문단(정렬 버튼·`이미지 삭제`·손잡이·원문 세 줄) | 4.3 도구 묶음·손잡이 행, 4.1 첫 행 |
| 셋째 문단(300MB·지운 이미지는 나중에 정리) | 4.4 300MB 행, 4.5 첫 행 |

### 4.8 명세·도움말과 코드가 다른 곳 (코드를 따랐다)

| # | 어디 | 지금 글 | 코드 | 이 명세는 |
| --- | --- | --- | --- | --- |
| D1 | 도움말 `## 이미지` | "한 장에 20MB까지 넣을 수 있고" | 고르는 파일 20MB 는 맞다. 줄인 결과 5MB 를 넘으면 막힌다(GIF 는 줄이지 않아 사실상 5MB) | 5장에서 두 수치를 함께 쓴다 |
| D2 | `specs/product.md` 7장 F-209 | "WebP 변환(애니메이션 GIF 제외, EXIF 빠짐)" | EXIF 를 지우는 코드는 없다. 원본을 그대로 두는 갈래 셋(4.2 마지막 행)에서는 EXIF 가 남는다 | 글은 "남습니다". 1.1 갱신 대상, 11장 C3 |
| D3 | `specs/product.md` 5장 F-156 | "장당 5MB" | F-220 뒤 고르는 파일 20MB, 결과 5MB | 1.1 갱신 대상 |
| D4 | `specs/ia.md` 3.26 5번 | "공유 링크에는 이미지가 담기지 않아 자리 표시로 보인다" | 읽기 전용 링크(`handlePublicGetAttachment`)·초대(`handleGetAttachment` `?doc=`)는 이미지를 보여 준다. 담기지 않는 것은 `링크 복사`(F-130) 주소뿐 | 글은 `링크 복사` 만. 1.1 갱신 대상 |

## 5. 도움말 — before / after

### 5.1 자리

`src/app/helpDoc.ts` `APP_SECTIONS` 에서 `## 이미지` 절을 통째로 바꾼다. 절 제목·순서·다른 절은 그대로다.

before (m1):

```
## 이미지

편집 영역에 붙여넣거나 끌어넣어서 이미지를 넣습니다. 한 장에 20MB까지 넣을 수 있고, 계정마다 이미지 저장 공간은 300MB까지입니다.
```

### 5.2 after (m2)

```
## 이미지

편집 영역에 이미지를 붙여넣거나 파일을 끌어 놓아 넣습니다. PNG·JPEG·GIF·WebP를 받고, 고르는 파일은 한 장에 20MB까지입니다. 큰 이미지는 넣을 때 줄여서 저장하고, 줄인 뒤에도 5MB가 넘으면 넣지 않습니다.

편집 모드에서 이미지에 마우스를 올리면 정렬 버튼과 `이미지 삭제`가 뜨고, 가장자리 손잡이를 끌어 폭을 바꿉니다. 원문에는 `attachments/` 파일을 가리키는 HTML 세 줄로 들어갑니다.

로그인한 계정은 이미지를 300MB까지 서버에 둘 수 있습니다. 문서에서 지운 이미지는 곧바로 사라지지 않고, 어느 문서도 쓰지 않게 된 뒤 정리됩니다.

사용법 글: [문서에 이미지 넣기](/guides/images)
```

- `helpDoc.ts` 는 템플릿 리터럴이라 원문의 백틱은 `` \` `` 로 이스케이프한다(지금 절들과 같다)
- 수치는 3개(20MB·5MB·300MB) — R5 절마다 3개 이하. `2,000px`·`24시간` 은 글의 몫
- 이 절도 오프라인에서 읽힌다(R3) — 링크 없이도 "어디서 넣고, 무엇에 막히고, 어떻게 지우나" 가 된다
- 마크다운 문법 `이미지` 카드 캡션(`GROUPS`)은 **고치지 않는다**(9장 Q4)

## 6. 수용 기준

단위 테스트 환경은 `node`(DOM 없음). 새 판정 함수를 만들지 않고 F-2039·F-2045~F-2047 이 쓴 도우미(`appSections`, `readGuide`·`guideBody`)를 그대로 쓴다. 두 상수의 나눗셈이 정수가 아니게 바뀌면(예: 2.5MB) 글 문자열도 바뀌어야 하므로 테스트가 빨개지는 것이 맞다. `MB` 는 모두 2진(× 1024 × 1024)이다 — 코드 상수와 알림 문구가 그렇게 쓴다

### 6.1 도움말 — `src/app/helpDoc.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| U1 | `## 이미지` 절(5.2) | ① 마지막 문단이 정확히 `사용법 글: [문서에 이미지 넣기](/guides/images)` ② `` `한 장에 ${MAX_INPUT_BYTES / 1024 / 1024}MB까지` ``·`` `${MAX_RESULT_BYTES / 1024 / 1024}MB가 넘으면` ``·`` `이미지 삭제` ``(백틱 포함)이 있다 ③ 옛 문장 `한 장에 20MB까지 넣을 수 있고` 가 없다. 두 상수는 `./attachImages` 에서 import |

- 기존 F-2039 U1(600자·4문단)·U2(링크 줄 모양·마지막 문단·절마다 0~1개)·G1(절 순서)·"표준 이미지 문법"·"제품명"·LF·F-2047 U22(`## 이미지` 를 끝 표시로 쓴다)는 그대로 통과한다(m2·m3)

### 6.2 사용법 글 — `site/helpGuides.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| U2 | 글의 앱 쪽 수치 (R5) | `content/guides/images.md` 원문에 다음 여섯 문자열이 있다: `` `고른 파일이 ${MAX_INPUT_BYTES / 1024 / 1024}MB를` ``, `` `한 변이 ${MAX_DIM.toLocaleString('en-US')}px` ``, `` `화소 수가 ${(MAX_AREA / 10_000).toLocaleString('en-US')}만` ``, `` `긴 변이 ${MAX_SIDE.toLocaleString('en-US')}px` ``, `` `결과가 ${MAX_RESULT_BYTES / 1024 / 1024}MB를` ``, `` `넣은 지 ${GRACE_MS / 3_600_000}시간` ``. import 는 `../src/app/attachImages`(넷), `../src/lib/shrinkImage`(`MAX_SIDE`), `../src/app/attachmentGc`(`GRACE_MS`) |
| U3 | 글에 제품명이 없다 (R6) | `images.md` 본문(소문자)에 `brand.name`·`brand.shortName`(소문자)이 없다. F-2045~F-2047 U3 과 같은 모양 |
| — | 기존 F-2039 U4·U5·U6·글 사이 링크 U4 | **코드 변경 없이** 새 글·새 링크 줄이 반복에 들어간다: U4 링크 글자 = `images.md` `title`, U5 `(## 이미지, images.md)` 겹침 0(m6), U6 쓰지 않는 말 0·4,811자·`##` 9개(m4·m8), 글 사이 링크 U4 는 새 글의 `offline-sync`·`sharing`·`markdown-portability`·`obsidian-vault`·`encryption`·`cli` 대상이 있는지 본다 |

### 6.3 서버 한도 — `worker/guideLimits.test.ts`

서버 상수는 앱 `tsconfig` 로 import 할 수 없다(ia.md 6.1 R5 셋째 점, F-2047 m11). 이 파일의 기존 `describe` 는 건드리지 않고 두 개를 더한다. 이름에 `F-2048` 을 붙여 F-2047 의 U4·U5 와 구별한다

| # | 확인 | 기대 |
| --- | --- | --- |
| U4 | 글의 서버 수치 (R5) | `content/guides/images.md` 원문에 `` `저장 공간은 ${ATTACHMENT_QUOTA_BYTES / 1024 / 1024}MB` ``·`` `넣은 지 ${GRACE_MS / 3_600_000}시간` `` 이 있다. `GRACE_MS` 는 `./attachmentGc` 의 것(서버 정리 기준 — 앱 쪽 `GRACE_MS` 와 같은 값이어야 글 문장이 두 경우에 모두 맞다) |
| U5 | 도움말 `## 이미지` 의 서버 수치 (R5) | `HELP_DOC_CONTENT` 의 `## 이미지` 부터 `## 위키링크` 앞까지에 `` `이미지를 ${ATTACHMENT_QUOTA_BYTES / 1024 / 1024}MB까지` `` 가 있다 |

- 글 경로는 `new URL('../content/guides/images.md', import.meta.url)`. `npm run typecheck:worker` 가 이 파일을 본다

### 6.4 동작 — `e2e/site.spec.js` 의 `F-2048 E*`

F-2047 E1~E3 옆에 둔다. 같은 모양이고 주소·제목과 E3 의 개수만 다르다. 모양·문장은 고정하지 않는다(8장)

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| E1 | 글이 뜨고 완결된 정적 페이지다 | `page.goto('/guides/images')`, 그리고 `page.request.get('/guides/images')` | `문서에 이미지 넣기` `h1` 과 `.site-foot` 이 보인다. 응답 `200`, `<title>문서에 이미지 넣기 · Rawdoc</title>`, `rel="canonical"`, `https://rawdoc.app/guides/images`, `<script` 없음 |
| E2 | 목록·색인 | `/guides` 로 가고 `/sitemap.xml` 요청 | `.site-article .markdown-body > ul a[href="/guides/images"]` 이 보이고 글자가 제목과 같다. `sitemap.xml` 에 `<loc>https://rawdoc.app/guides/images</loc>` |
| E3 | 앱 도움말의 링크 | `openApp(page)` → 사이드바 `getByRole('button', { name: '도움말' }).first()` | `.help-page a[href="/guides/images"]` 가 **`toHaveCount(1)`**, 글자 `문서에 이미지 넣기`, `target="_blank"` |

- E1 의 `Rawdoc` 은 F-2045~F-2047 E1 과 같은 방식이다(기존 테스트가 쓰는 글자)
- 이미지 넣기·정리 동작 자체는 e2e 로 다시 고정하지 않는다 — `e2e/image.spec.js`(F-156~F-158·F-218·F-220)·`e2e/imageQuota.spec.js`(F-221)가 이미 본다. 이 명세는 글과 `export` 만 더한다

### 6.5 빌드·회귀·범위

| # | 확인 | 기대 |
| --- | --- | --- |
| A1 | `npm run build` | 성공. `dist/guides/images.html` 이 생긴다(가드 통과) |
| A2 | 관련 회귀 | `npm run e2e:one -- "F-2048\|F-2047\|F-2039\|F-276\|F-274"` 통과 |
| A3 | 단위·타입·린트 | `npm test`, `npm run typecheck`, `npm run typecheck:worker`, 바꾼 파일 eslint 통과 |
| A4 | 소유 범위 | `npm run review -- F-2048` — 1장 표 밖 변경 없음, `specs/**`·`CLAUDE.md` 무변경. `attachImages.ts`·`shrinkImage.ts`·`attachmentGc.ts`(둘)의 diff 가 `export ` 추가 줄뿐 |

## 7. 기존 테스트 회귀 — grep 으로 확인한 것 (2026-09-27, `19bd23c`)

**고쳐야 하는 테스트 없음.** 새 테스트만 더한다.

깨지지 않는 것:

| 파일 | 판정 |
| --- | --- |
| `site/pageNav.test.ts` F-2036 A1·A2, `site/render.test.ts` F-2036 A6 | 도움말 제목 42 그대로(m3). `pageNav.test.ts` 의 `toContain('이미지')` 는 제목 글자 그대로 |
| `src/app/helpDoc.test.ts` G1·G3·"이미지는 결과 예시 없이"·"표준 이미지 문법"·F-2039 U1~U3·U8·F-2045~F-2047 U1·U17·U22·U23·"제품명"·LF | `## 이미지` 한 절만 바뀌고 R2 안(m2), 제목·순서·`GROUPS` 그대로 |
| `worker/guideLimits.test.ts` F-2047 U5 | `## 저장`~`## 이미지` 앞 구간을 본다 — `## 이미지` 제목 그대로라 구간이 같다 |
| `site/helpGuides.test.ts` F-2039 U4~U7·F-2045~F-2047 U2·U3·F-2046 U4·글 사이 링크 U4 | 새 글·새 링크를 반복에 넣고 통과(m4·m6·m8) |
| `site/helpPage.test.ts` F-274 | 같은 원본(`HELP_DOC_CONTENT`)을 비교 |
| `site/build.test.ts`·`site/guard.test.ts` | 내장 픽스처로 부른다 — 저장소 글과 무관 |
| `e2e/site.spec.js` F-276 A10 | 목록 링크 수를 `content/guides` 의 `.md` 수로 센다 — 11편이 되어도 통과 |
| 같은 파일 F-2039·F-2045~F-2047 E* | 각자 자기 링크만 본다. `/guides` 첫 자리가 `images` 가 되어도(m5) 순서를 보지 않는다 |
| `e2e/help.spec.js` | 목차 항목 수를 화면에서 센다(고정 수 없음), 절 이름 `이미지` 그대로 |
| `e2e/image.spec.js`·`e2e/imageQuota.spec.js` | 알림 글자·동작 무변경(`export` 만) |
| `src/app/attachImages.test.ts`·`src/lib/shrinkImage.test.ts`·`src/app/attachmentGc.test.ts`·`worker/attachmentGc.test.ts` | 값·동작 무변경 |
| `src/app/guideDoc.ts`(첫 실행 안내 문서) | 도움말과 다른 원본이다(R1 끝 점). 건드리지 않는다 |

## 8. 사람 확인 필요 (자동 판정 불가)

`specs/human-checks.md` 로 넘긴다.

| # | 확인 |
| --- | --- |
| H1 | 글을 처음부터 끝까지 읽기 — 어조가 다른 사용법 글과 같은지, 표 셋(4줄·2줄·7줄)이 좁은 창에서 읽히는지, 넣는 순서 표(20MB → 10,000px → 2,000px → 5MB)가 한 번에 이해되는지 |
| H2 | 실제 앱에서 글의 흐름을 한 번씩 — ⓐ 탐색기에서 이미지 두 장 끌어 놓기 → 빈 줄 사이, Ctrl+Z 한 번 ⓑ 도구 묶음 네 버튼 이름(툴팁), 손잡이 끌기·Esc·끈 뒤 ←/→ ⓒ 6MB 짜리 GIF → `GIF 는 한 장에 5MB 까지…` ⓓ `이미지 삭제` 뒤 계정 메뉴 쓴 양이 바로 줄지 않는지. 글과 다른 곳이 있으면 적는다 |
| H3 | **Safari(macOS·iOS)** 에서 긴 변 2,000px 넘는 PNG 를 붙여넣기 — 로그인하지 않았을 때 보이는지, 로그인했을 때 `이미지를 서버에 올리지 못했습니다.` 가 뜨지 않는지(11장 C2, 10장 가정 2) |
| H4 | **휴대폰으로 세로로 찍은 JPEG 파일**(EXIF 방향 정보가 든 것)을 끌어 놓기 — 비율이 찌그러지는지(11장 C1, m11 이 Chrome 에서 합성 파일로 재현). 고친 뒤 다시 본다 |

## 9. 열린 질문 — 모두 기본값이 있다

| # | 질문 | 기본값과 근거 | 다른 쪽을 고르면 |
| --- | --- | --- | --- |
| Q1 | 글 주소 slug | **`images`** — main 권장. 짧고 주제가 바로 보인다. 주소는 한 번 나가면 바꾸지 않는다(F-276 Q4) | `image-attachments`·`attachments`: 원문 경로 `attachments/` 와 섞이고 ia.md 6장 `첨부파일` 금지와 어긋난다. 바꾸면 3장 파일 이름, 5.2 링크 줄, U1~U5·E1~E3 의 주소 |
| Q2 | 글 제목 | **`문서에 이미지 넣기`** — F-2039 10장 행 이름 "이미지와 첨부" 는 ia.md 6장 `이미지` 행의 쓰지 않는 말 `첨부파일` 에 걸린다. 다른 글의 "…하기" 끝맺음을 따랐다 | `이미지와 첨부`: 행 이름 그대로(F-2046·F-2047 선례)지만 금지어에 가깝다. `이미지`: 짧지만 목록에서 다른 글과 결이 다르다. 바꾸면 3장 `title`·`h1`, 5.2 링크 글자, U1·E1~E3 |
| Q3 | export 되지 않은 상수를 글에 쓰려면 | **`export` 만 붙인다**(파일 넷, 상수 일곱) — R5 "적은 곳마다 상수와 대조" 를 지키고, 20MB·5MB 처럼 이미 도움말에 있는 수치까지 처음으로 대조가 걸린다. 값·이름·동작은 그대로다. F-2047 이 700ms·30초를 **빼기로** 한 것과 다른 까닭: 그 셋은 판단에 쓰는 수치가 아니었고, 이 일곱은 넣을 수 있나/지운 이미지가 언제 사라지나를 사용자가 판단하는 수치다 | 안 붙이면: 글에서 20MB·10,000px·4,000만·2,000px·5MB·24시간을 "큰 이미지는", "얼마 뒤" 로 바꿔야 해 글의 쓸모가 크게 준다. 또는 상수를 `src/lib/` 한 모듈로 모으는 정리(따로 명세) |
| Q4 | 마크다운 문법 `이미지` 카드 캡션을 고치나 | **안 고친다** — 캡션 사실("붙여넣기·끌어넣기로 넣은 이미지만 이렇게 보입니다", 표준 문법은 `이미지: 설명` 링크)은 코드와 맞다(4.6). "오프라인에서도 항상 보이도록" 이라는 이유 말이 조금 어색하지만 틀리지 않고, `## 마크다운 문법` 은 R2·R4 밖이다 | 고치면 `GROUPS` 이미지 항목 `caption` 한 줄. `helpDoc.test.ts` "표준 이미지 문법" 테스트가 캡션을 보므로 그 테스트가 통과하는지만 본다. tweak 로 해도 된다 |
| Q5 | `alt` 100자·최소 폭 48px·←/→ 10px·Shift 50px 를 글에 쓰나 | **쓰지 않는다** — 함수 안 리터럴이거나 `src/editor/**` 의 지역 상수라 대조하려면 편집기 파일을 소유에 더해야 한다. 판단에 쓰는 수치가 아니다 | 쓰려면 `imageWidget.ts`·`imageBlock.ts` 에서 상수로 뽑아 export 하는 변경이 필요하다(리터럴을 상수로 바꾸는 것이라 `export` 만 붙이는 Q3 보다 크다) |
| Q6 | 파일 안 정보(EXIF) 문장 | **"원래 파일을 그대로 저장할 때는 … 함께 남습니다. 골라 지우는 기능이 없습니다" 만 쓴다** — 코드로 확인한 방향(원본 바이트 보존, 지우는 코드 없음)만이다(결정 4). "WebP 로 바꾼 이미지에서는 지워진다" 는 캔버스의 브라우저 동작이라 쓰지 않았다 | 빼면: 위치 정보가 남을 수 있다는 것을 사용자가 알 길이 없다. 더 쓰려면(바꾼 이미지는 지워진다) 브라우저별 확인을 사람 확인에 더한다 |
| Q7 | 서버 정리 시각(한국 시간 새벽 3시)을 쓰나 | **쓰지 않는다** — `wrangler.jsonc` 설정 값이라 대조할 상수가 없고, 운영 사정으로 바뀐다. "하루에 한 번" 까지 | 쓰면 worker 테스트가 `wrangler.jsonc` 를 읽어 크론 식을 대조하는 행이 필요하다(`writeGate.test.ts` 가 그 파일을 읽는 선례) |

## 10. 가정으로 둔 것 (코드로 확인하지 못했다)

1. **품질 값을 준 WebP 인코딩이 손실 압축이라 "원본과 픽셀까지 같지는 않다" 는 것.** `QUALITY` 0.85 를 넘기는 코드까지는 확인했고, 결과 픽셀을 원본과 견주어 보지는 않았다. 캔버스 `convertToBlob` 의 `quality` 는 손실 형식에 쓰인다는 HTML 표준 설명에 기댔다(이 세션에서 원문을 다시 열지 않았다)
2. **Safari 가 WebP 인코딩을 지원하는지.** Chrome 은 m11 에서 `image/webp` 를 돌려줬다. Safari 는 이 PC 에 없어(Playwright 에 WebKit 미설치) 재지 못했다 — 11장 C2, H3
3. 끌어 놓을 때 여러 파일의 순서가 탐색기에서 고른 순서와 같다는 것 — 코드는 `dataTransfer.files` 순서를 그대로 쓴다. 그 순서를 정하는 것은 운영체제·브라우저다. 그래서 글은 "차례로" 까지만 쓴다
4. 워드·엑셀에서 복사할 때 이미지 조각이 딸려 온다는 것 — F-156 2.4 의 설계 이유를 옮겼다. 이 세션에서 오피스 프로그램으로 복사해 보지 않았다
5. 로컬 `origin/deploy` 참조가 원격과 같다는 것(m9). 원격이 더 나갔다면 결론(모두 배포됨)은 바뀌지 않는다

## 11. 기존 명세·코드와 부딪히는 곳 (보고)

| # | 무엇 | 어디 | 처리 |
| --- | --- | --- | --- |
| C1 | **EXIF 방향 정보가 든 JPEG 가 찌그러진다(버그, 재현함).** `inspectImageBytes` 는 JPEG 헤더(SOF)의 돌리기 전 가로·세로를 읽고, `shrinkImage` 는 그 크기로 캔버스를 만든 뒤 `createImageBitmap` 결과를 그린다. Chrome 의 `createImageBitmap` 은 EXIF 방향을 적용해 돌린 크기를 돌려주므로(m11: 헤더 400×200 → 비트맵 200×400) 세로 사진이 가로 캔버스에 늘어나 그려진다. WebP 결과를 쓰지 않는 갈래에서도 저장하는 `width`·`height` 가 헤더 값이라 표시 비율(`aspect-ratio`)이 뒤집힌다. 휴대폰 세로 사진 파일을 끌어 놓을 때 생긴다(붙여넣기로 오는 클립보드 이미지는 이미 풀린 PNG 라 대개 해당 없음 — 확인은 안 했다) | `src/lib/imageFile.ts` `readJpeg`, `src/lib/shrinkImage.ts` `shrinkImage` | **이 명세 밖.** 버그 수정 한 건(명세 밖 변경 단위)으로 main 이 처리하기를 권한다 — 예: `createImageBitmap` 결과의 `width`·`height` 를 기준으로 `fitWithin` 을 계산하고 그 값을 저장. 글은 이 동작을 적지 않는다. H4 |
| C2 | Safari 가 `convertToBlob({ type: 'image/webp' })` 를 지원하지 않으면 표준대로 PNG 를 돌려준다. `shrinkImage` 는 결과 `type` 을 보지 않고 `mime: 'image/webp'`·`ext: 'webp'` 로 적어, 로그인한 경우 서버가 시그니처(PNG)와 확장자(webp)가 달라 `type_mismatch` 로 거절할 수 있다(`sendOne` → `이미지를 서버에 올리지 못했습니다.`) — **확인하지 못한 가능성**(10장 가정 2) | `src/lib/shrinkImage.ts`, `src/storage/toWebp.ts`(여기는 크기만 보고 형식은 따로 적지 않아 해당 없음) | H3 로 먼저 확인. 맞으면 C1 과 같은 버그 수정 단위(결과 `blob.type` 이 `image/webp` 가 아니면 원본을 쓴다) |
| C3 | product.md F-209 "EXIF 빠짐" 은 코드와 다르다 — 지우는 코드가 없고, 원본을 두는 갈래에서는 남는다 | `specs/product.md` 7장 | 1.1 갱신 대상 |
| C4 | ia.md 3.26 5번 "공유 링크에는 이미지가 담기지 않아" 는 F-130 `링크 복사` 시절 문장이다. 읽기 전용 링크·초대는 보여 준다 | `specs/ia.md` | 1.1 갱신 대상 |
| C5 | `obsidian-vault.md` 가 이미지를 `그림` 으로 부른다(66·68·69·70·98줄 중 넷). ia.md 6장 `이미지` 행의 쓰지 않는 말이다. `FORBIDDEN_WORDS`(U6)에 `그림` 이 없어 테스트가 잡지 않는다 | `content/guides/obsidian-vault.md` | 소유 밖. 글자 바꾸기는 tweak 한 건. U6 에 `그림`·`사진`·`첨부파일` 을 더할지는 F-2039 갱신 거리(`cli.md` 의 예시 파일 이름 `그림.png`·obsidian 문법 예시 `![[그림.png]]` 가 걸리므로 예외 규칙이 필요) |
| C6 | 도움말 `## 이미지` 가 5MB 상한을 빠뜨려 20MB 아래 파일이 막히는 경우를 설명하지 못했다 | `helpDoc.ts` | 5장에서 고친다(4.8 D1) |

## 12. 구현 순서 (TDD)

1. `src/app/attachImages.ts`·`src/lib/shrinkImage.ts`·`src/app/attachmentGc.ts`·`worker/attachmentGc.ts` 에 `export` 만 붙인다 → 기존 테스트 그대로 초록
2. `src/app/helpDoc.test.ts` U1 → 빨강(링크 줄 없음·`5MB가 넘으면` 없음·옛 문장 있음)
3. `site/helpGuides.test.ts` U2·U3 → 빨강(파일 없음)
4. `worker/guideLimits.test.ts` U4·U5 → U4 빨강(파일 없음), U5 빨강(`이미지를 300MB까지` 없음)
5. `content/guides/images.md` 를 3장 그대로(날짜 규칙 R7) → U2·U3·U4 초록, F-2039 U6 초록
6. `src/app/helpDoc.ts` `## 이미지` 를 5.2 after 로 → U1·U5 초록, F-2039 U1·U2·U4·U5 초록. `site/pageNav.test.ts`·`site/render.test.ts` 42 그대로 초록인지 본다
7. `npm run build`(A1), `dist/guides/images.html`·`dist/guides.html`·`dist/help.html` 을 눈으로 한 번 연다
8. `e2e/site.spec.js` E1~E3 → `npm run e2e:one -- "F-2048"`
9. A2~A4
