---
id: F-275
title: 법적 문서
milestone: M2
status: pending
created: 2026-09-21
depends: [F-272, F-273]
---

# F-275 법적 문서

작성일: 2026-09-21
상태: **사람 승인 대기.** `specs/features/F-270.md`(설계 개요, 2026-09-20 사람 승인) 6장 로드맵의 다섯째 항목이다. 개요가 F-275 에 넘긴 것은 `/privacy`·`/terms` 두 글과 자리표시 빌드 가드인데, **가드는 F-272 가 이미 만들었다**(`site/guard.ts:8, 38~40`). 그래서 이 명세의 산출물은 **두 글의 초안 전문**(6·7장)과 꼬리 링크 두 줄이다
**구현 시작 조건: 운영자·연락처·시행일 확정.** F-272 9.1(3)이 "F-275 는 자리표시가 남은 초안을 `content/` 에 커밋하지 않는다. 운영자·연락처가 정해진 뒤 채워서 커밋한다. 초안은 그때까지 F-275 명세 안에 둔다" 로 정했다. 세 값이 정해지기 전에는 이 명세를 승인해도 구현을 시작하지 않는다
근거: `F-272.md`(사이트 틀·`content/` 규칙·주소 매핑·꼬리·빌드 가드, 구현 커밋 `deff27c`)와 그 구현 코드, `F-273.md` 6장(`site/build.test.ts` 회귀 처리 방법), `F-270.md` 4장(법적 문서 내용 규칙), `specs/architecture.md` 2·4·6장, `specs/product.md` 263·397행, 그리고 3장에 파일:행으로 적은 저장소 코드 전부

> **이 문서는 법률 자문이 아니다.** 6·7장 초안은 코드와 설정에서 확인한 사실 관계를 한국어 문장으로 정리한 것이다. 공개 전에 사람이 읽고 판단해야 한다(10장 H1). 사실이 아닌 문장이 들어 있으면 그것이 가장 큰 결함이므로, 3장의 근거를 먼저 검토한다

## 1. 파일 소유

| 파일 | 변경 |
| --- | --- |
| `content/legal/privacy.md` (신규) | 개인정보 처리방침. **6장 초안을 그대로** 넣고 자리표시 3종만 실제 값으로 바꾼다 |
| `content/legal/terms.md` (신규) | 이용약관. **7장 초안을 그대로** 넣고 자리표시 3종만 실제 값으로 바꾼다 |
| `src/lib/siteChrome.ts` | `SITE_FOOTER_LINKS` 에 두 줄 (2.2). `SITE_NAV` 는 건드리지 않는다 |
| `src/lib/siteChrome.test.ts` | `F-275 A2` 추가. **기존 `F-272 A1` 5개와 `F-273 A2` 는 건드리지 않는다** |
| `site/build.test.ts` | `F-275 A1`·`F-275 A3` 추가 (9장). **F-273 6장이 이미 고쳐 둔 저장·복원 구조를 그대로 쓴다** |
| `e2e/site.spec.js` | `F-275 A4`~`A10` 추가. 기존 `F-272 A*`·`F-273 A*` 는 건드리지 않는다 |

- 빌드 코드(`site/build.ts`·`site/render.ts`·`site/pages.ts`·`site/guard.ts`·`vite.config.ts`·`wrangler.jsonc`)는 **한 줄도 고치지 않는다.** 2장에서 하나씩 확인했다
- `content/legal/*.md` 두 개와 `SITE_FOOTER_LINKS` 두 줄은 **반드시 같은 커밋**이다. 링크만 먼저 들어가면 `site/build.ts:85~91` 이 `SITE_FOOTER_LINKS 의 /privacy 페이지가 없습니다` 로 빌드를 실패시킨다
- `src/lib/siteChrome.ts`·`src/lib/siteChrome.test.ts`·`site/build.test.ts`·`e2e/site.spec.js` 는 **F-273·F-274·F-276 과 겹친다.** F-272 5.1 이 그렇게 설계했다("F-273~F-276 이 자기 페이지를 만들면서 한 줄씩 더한다"). 메인은 이 넷을 병렬로 구현하지 않는다

### 1.1 갱신 대상 — 이번에 고치지 않는다

메인이 반영한다. 구현 에이전트는 건드리지 않는다.

| 파일 | 반영할 내용 |
| --- | --- |
| `specs/human-checks.md` | 10장 H1~H6 |
| `specs/architecture.md` 4장 | localStorage 표에 **`md.startScreen`·`md.toolbar`·`md.landingDone` 세 줄이 빠져 있다** — `src/app/prefs.ts:5~22` 의 `PrefMap` 에는 18개가 있는데 표에는 15개뿐이다. 6장 10절(브라우저에 저장하는 것)이 이 목록을 근거로 쓰므로 맞춰 둔다 |
| `specs/architecture.md` 4장 (새 줄) | sessionStorage 키가 문서화돼 있지 않다 — `md.lockSession`(`src/storage/docsApi.ts:41`, 탭 수명), `md.landingReloaded`(`worker/welcomePage.ts:42~44`, 랜딩 되돌이 방지) |
| `specs/architecture.md` 1장·6장 | **F-272 10장이 남긴 갱신 대상이 아직 미반영이다** — 1장 디렉터리에 `content/`·`site/`, 의존 방향 `site → src`, 6장 "없는 경로는 `index.html`" → `404.html`. 이 명세가 새로 만드는 항목은 아니고, 같이 처리하는 편이 낫다 |

- `specs/ia.md` 6장 UI 문구는 **고칠 것이 없다.** `사이트 법적 문서 (F-275) | 개인정보 처리방침 / 이용약관` 줄이 이미 있다(2026-09-21 확인)
- `specs/product.md` 7장 317행(`F-275 | 법적 문서 | /privacy·/terms. 운영자·연락처 자리표시가 남으면 빌드 실패`)도 이 명세와 어긋나지 않는다 — **고칠 것 없음**

## 2. F-272 가 이미 해 둔 것 / 이번에 더하는 것

### 2.1 이미 있다 — 건드리지 않는다

| 항목 | 확인한 위치 | 상태 |
| --- | --- | --- |
| 주소 매핑 | `site/pages.ts:8~9` — `contentUrl('legal/privacy.md')` 가 `'/privacy'`, `contentUrl('legal/terms.md')` 가 `'/terms'` | 있음 |
| `dist` 파일 이름 | `site/pages.ts:18~21` — `urlToFile('/privacy')` 가 `'privacy.html'`. Cloudflare `html_handling: auto-trailing-slash` 와 `vite preview` 둘 다 `/privacy` 로 낸다(F-272 2장) | 있음 |
| `sitemap.xml` 등재 | `site/build.ts:71~78, 94` — 빌드가 만든 페이지 전부가 주소 오름차순으로 들어간다. `lastmod` 는 프론트매터 `updated` → `date` → 빌드 시각 | **자동** |
| 서비스 워커 폴백 제외 | `vite.config.ts:217` — `navigateFallbackDenylist` 에 `/^\/privacy$/`·`/^\/terms$/` 가 이미 있다 | 있음 |
| precache 제외 | `vite.config.ts:223` — `sitePlugin()` 이 `VitePWA()` 뒤라 사이트 HTML 이 precache 목록에 안 들어간다(F-272 3.5) | 있음 |
| 자리표시 빌드 가드 | `site/guard.ts:8` `const PLACEHOLDER_RE = /\{\{[\s\S]*?\}\}/`, `:38~40` — **정해진 이름이 아니라 `{{…}}` 를 일반적으로 막는다.** `{{운영자}}`·`{{연락처}}`·`{{시행일}}` 모두 같은 규칙에 걸린다 | 있음 |
| 링크 목록 검사 | `site/build.ts:81~91` — `SITE_FOOTER_LINKS` 의 모든 `path` 가 실제로 만들어진 페이지인지 본다 | 있음 |
| 머리·꼬리·메타·CSS | `site/render.ts:54~91` — `<title>`·canonical·`og:*`·앱 CSS 링크·스크립트 없음 | 있음 |

**개요 4장이 F-275 에 넘긴 "자리표시 빌드 가드" 는 이미 구현돼 있다.** 이 명세가 새로 만들 코드는 없다.

### 2.2 없다 — 이번에 더한다

| 항목 | 지금 | 이번에 |
| --- | --- | --- |
| 꼬리 링크 | `src/lib/siteChrome.ts:7` `export const SITE_FOOTER_LINKS: SiteLink[] = []` — **빈 배열** | `{ path: '/privacy', label: '개인정보 처리방침' }`, `{ path: '/terms', label: '이용약관' }` 두 줄 |
| 글 파일 | `content/` 에 `.gitkeep` 과 (F-273 이 들어간 뒤) `changelog.md` 뿐 | `content/legal/privacy.md`·`content/legal/terms.md` |

### 2.3 머리(`SITE_NAV`)인가 꼬리(`SITE_FOOTER_LINKS`)인가 — 꼬리

F-272 5.1 이 두 배열의 용도를 주석으로 못박았다.

```ts
export const SITE_NAV: SiteLink[] = []            // 머리 — 사용법·체인지로그·도움말
export const SITE_FOOTER_LINKS: SiteLink[] = []   // 꼬리 — 개인정보 처리방침·이용약관
```

- F-270 3장도 같다 — "머리(로고 + `사용법`·`체인지로그`·`도움말` + `앱 열기`), 꼬리(`개인정보 처리방침`·`이용약관`)"
- **꼬리에 넣는다.** 머리는 읽으러 온 사람이 고르는 글 목록이고, 법적 문서는 필요할 때 찾는 글이다. 관례상으로도 꼬리다
- 꼬리 순서는 `개인정보 처리방침` → `이용약관`. 근거: F-270 3장·F-272 5.1 이 둘 다 그 순서로 적었다
- 꼬리는 랜딩(`worker/welcomePage.ts:76`)·사이트 글(`site/render.ts:60`)·404(`site/build.ts:21`) 세 곳이 모두 같은 함수를 부르므로 **한 줄씩 더하면 세 곳에 동시에 나온다.** 별도 작업이 없다
- `renderSiteFooter` 는 `aria-current` 를 붙이지 않는다(`src/lib/siteChrome.ts:104~112`). `/privacy` 를 보는 중에도 꼬리의 그 링크는 평범한 링크다. 이번 범위에서 고치지 않는다 — 11장 Q9

### 2.4 실측 — 6·7장 초안을 실제로 빌드해 봤다

2026-09-21, 저장소 코드를 Vite `runnerImport` 로 불러 `buildSite` 를 직접 돌렸다(임시 스크립트는 스크래치패드, 저장소 무변경). 입력은 **6·7장 초안 전문에서 자리표시 3종만 임시값으로 바꾼 것**과, 같은 시각 작업 트리에 있던 `content/changelog.md`(F-273 구현 중) 하나다.

```
KEYS:    404.html,changelog.html,privacy.html,robots.txt,sitemap.xml,terms.html
TITLE:   <title>개인정보 처리방침 · Rawdoc</title>
CANON:   <link rel="canonical" href="https://rawdoc.app/privacy" />
H1:      <h1 data-source-line="2">개인정보 처리방침</h1>   (terms 는 <h1 …>이용약관</h1>)
SCRIPT:  0개
BRACES:  {{ 없음
TABLE:   privacy 본문에 <table> 5개, <code> 7개
SITEMAP: <url><loc>https://rawdoc.app/privacy</loc><lastmod>2026-09-30</lastmod></url>
         <url><loc>https://rawdoc.app/terms</loc><lastmod>2026-09-30</lastmod></url>
FOOT:    <footer class="site-foot">
           <span>Rawdoc</span><a href="/privacy">개인정보 처리방침</a><a href="/terms">이용약관</a>
         </footer>
```

- **빌드 가드 G1~G5 를 통과한다.** 프론트매터·`title`·주소 매핑·mermaid·이미지·자리표시 어느 것에도 걸리지 않는다
- 프론트매터 표(`markdown-frontmatter`)가 본문에 새지 않는다 — `site/render.ts:47` 이 떼고 넘긴다
- `updated` 가 그대로 `lastmod` 로 간다(5장 결정의 근거)
- 처리방침 13절의 `privacy.kisa.or.kr` 같은 맨 도메인은 **링크가 되지 않고 글자로 남는다**(`renderMarkdown` 의 `linkify: true` 가 이 형태를 잡지 않는다 — 실측). 눌리는 링크가 필요하면 `[개인정보침해신고센터](https://privacy.kisa.or.kr)` 형태로 적어야 한다 — 11장 Q11
- 꼬리 항목 사이 간격은 `SITE_CHROME_CSS` 의 `.site-foot { display: flex; gap: 16px }`(`src/lib/siteChrome.ts:67~77`)가 준다. 구분 기호(`·`)를 따로 넣지 않는다

## 3. 코드에서 확인한 사실 — 처리방침의 근거

개요 4장이 "실제 수집·저장 항목을 코드에서 확인해 쓴다. 확인하지 못한 항목은 쓰지 않고 보고한다" 로 남긴 부분이다. **2026-09-21 저장소 코드와 Cloudflare 설정을 직접 읽어 확인했다.** 6장 초안의 모든 문장은 이 표를 근거로 한다.

### 3.1 서버(D1)에 저장하는 것 — 마이그레이션 전수

`migrations/0001~0008` 의 모든 표와 칼럼이다. **이 밖에 사용자 정보를 담는 표는 없다.**

| 표 (파일) | 칼럼 | 개인정보인가 |
| --- | --- | --- |
| `users` (`0001_users.sql`) | `id`(uuid), `email`(UNIQUE), `created_at` | **이메일 주소.** 이름·전화번호·생년월일 칼럼이 없다 |
| `folders` (`0002_docs.sql`) | `id`, `owner_id`, `name`, `parent_id`, `created_at`, `updated_at` | 폴더 이름은 사용자가 지은 글자 |
| `docs` (`0002_docs.sql`) | `id`, `owner_id`, `title`, `content`, `line_ending`, `folder_id`, `pinned_at`, `version`, `created_at`, `updated_at` | **본문 전체.** 사용자가 본문에 적은 것은 그대로 저장된다 |
| `share_links` (`0003_links.sql`) | `token`, `owner_id`, `target_type`, `target_id`, `created_at`, `revoked_at` | 열람자 정보 없음. `revoked_at` 이 있어 **끊은 링크의 기록은 행으로 남는다** |
| `attachments` (`0004_attachments.sql`) | `owner_id`, `id`, `ext`, `mime`, `size`, `width`, `height`, `created_at` | 이미지 메타. 파일 원본은 R2 (3.2) |
| `grants` (`0005_grants.sql`) | `target_type`, `target_id`, `owner_id`, `grantee_email`, `role`, `created_at` | **초대받은 사람의 이메일 주소.** 제3자 개인정보 |
| `doc_locks` (`0006_locks.sql`) | `doc_id`, `user_id`, `email`, `session_id`, `expires_at` | **편집자 이메일.** 같은 문서를 연 다른 사람에게 보여 준다(`worker/locks.ts:45~47`). 유효기간 60초(`worker/locks.ts:7` `LOCK_DURATION_MS = 60_000`) |
| `api_tokens` (`0007_api_tokens.sql`) | `id`, `user_id`, `name`, `token_hash`(SHA-256 hex, UNIQUE), `prefix`(앞 11자), `created_at`, `last_used_at`, `revoked_at` | **토큰 원본은 저장하지 않는다** — `worker/apiTokens.ts:13~16` 이 SHA-256 해시만 넣는다 |
| `share_link_docs` (`0008_share_link_docs.sql`) | `token`, `doc_id` | 묶음 공유 대상 목록 |

- IP 주소·User-Agent·접속 시각·페이지 조회를 담는 표가 **하나도 없다.** `grep "DELETE FROM"`·마이그레이션 전수로 확인
- 운영 중인 원격 D1 의 표는 10개다(`npx wrangler d1 info md-editor-db`, 2026-09-21 — 위 9개 + wrangler 가 만드는 `d1_migrations`)
- `docs` 삭제는 소프트 삭제가 아니다 — `worker/docs.ts:338` `DELETE FROM docs WHERE id = ? AND owner_id = ?`. 휴지통이 없다

### 3.2 서버(R2)에 저장하는 것

- 키 형식 `att/{owner_id}/{id}.{ext}` — `worker/attachments.ts:77`
- **공개 접근 없음.** 2026-09-21 `npx wrangler r2 bucket dev-url get md-editor-attachments` → `Public access via the r2.dev URL is disabled.`, `npx wrangler r2 bucket domain list` → `There are no custom domains connected to this bucket.` 모든 읽기는 워커가 권한을 확인한 뒤 `env.BUCKET.get` 으로 내보낸다(`worker/attachments.ts:158, 187, 226, 268, 310`)
- 이미지 크기 한도: 넣을 때 원본 20MB(`src/app/attachImages.ts:7`), 변환 뒤 5MB(`:8`), 서버 5,242,880바이트(`worker/attachments.ts:11`), 계정당 314,572,800바이트(`:13`). 문서 본문 1,000,000바이트(`worker/validate.ts:3`), 제목 500자(`:5`), API 토큰 10개(`worker/apiTokens.ts:6`)

### 3.3 저장 위치 — 국외

| 자원 | 지역 | 확인 방법 (2026-09-21) |
| --- | --- | --- |
| D1 `md-editor-db` | `running_in_region: APAC`, `jurisdiction: null` | `npx wrangler d1 info md-editor-db` |
| R2 `md-editor-attachments` | `location: APAC` | `npx wrangler r2 bucket info md-editor-attachments` |

`specs/product.md:263` 의 기록(`APAC`)과 실제가 일치한다. 나라 단위로는 고정돼 있지 않으므로 초안은 **"아시아·태평양 지역"** 으로만 적는다. 더 좁혀 적으려면 Cloudflare 대시보드에서 확인이 필요하다 — 10장 H5.

### 3.4 브라우저에만 남는 것

| 종류 | 이름 | 내용 | 근거 |
| --- | --- | --- | --- |
| IndexedDB | `md-docs` (버전 4) | `docs`·`folders`·`attachments`(Blob 포함)·`meta`·`fileHandles` | `src/storage/idbStore.ts:7~14` |
| IndexedDB | `md-remote` (버전 2) | 로그인 상태의 서버 문서 사본·보낼 목록(`outbox`)·첨부 Blob | `src/storage/remoteCache.ts:6, 85~99` |
| localStorage | `md.` 로 시작하는 **18개** | `viewMode` `openFolders` `headingFont` `bodyFont` `theme` `sidebar` `sidebarWidth` `lineNumbers` `fontSize` `indent` `lastDocId` `firstRunDone` `persistNoticeShown` `account` `localMigrated` `startScreen` `toolbar` `landingDone` | `src/app/prefs.ts:5~22`(`PrefMap`) — 접근은 전부 이 파일을 거친다 |
| sessionStorage | `md.lockSession` | 탭마다 하나인 임의의 uuid. 서버가 "내 잠금" 을 알아보는 데 쓴다 | `src/storage/docsApi.ts:41~55` |
| sessionStorage | `md.landingReloaded` | 쿠키가 막힌 브라우저에서 랜딩 되돌이를 끊는 표시 | `worker/welcomePage.ts:42~44` |
| 쿠키 | `md_app=1` | `Path=/; Max-Age=31536000; SameSite=Lax; Secure`. 이 브라우저가 앱을 쓰기로 했는지 | `src/lib/appEntry.ts:9`, 쓰는 곳 `src/app/markAppEntry.ts:10` |
| 쿠키 | `CF_Authorization` | Cloudflare Access 가 만드는 로그인 세션. 워커가 읽기만 한다 | `worker/auth.ts:146`. 세션 168시간 — `specs/product.md:263` |
| 캐시 저장소 | Workbox 기본 이름(`workbox-precache-v2-{scope}`) | 앱 HTML·JS·CSS·글꼴 | `dist/workbox-2fbc6a65.js` 에 `precache-v2` 문자열 확인. **제품명이 들어가지 않는다**(불변조건) |

- **`md-remote` 는 로그아웃해도 지우지 않는다** — `src/storage/remoteCache.ts:1` 주석이 명시한다. 공용 컴퓨터 경고를 초안에 넣는 근거
- 워커는 `Set-Cookie` 를 하나도 내보내지 않는다(`grep "Set-Cookie" worker/` — 타입 선언 파일 외 0건). `md_app` 은 브라우저 자바스크립트가 쓴다

### 3.5 로그인하지 않은 사용자 — 서버로 무엇이 가는가

개요가 미뤄 둔 항목이다. **"아무것도 안 간다" 는 사실이 아니다.**

| 가는 것 | 근거 |
| --- | --- |
| 앱 파일(HTML·JS·CSS·글꼴·아이콘) 요청 | 정적 자산. 서비스 워커가 캐시한 뒤로는 줄어든다 |
| `GET /api/me` **부팅 때 1회** | `src/app/App.tsx:581` 이 `boot()` 안에서 `fetchAccount()` 를 무조건 부른다 → `src/app/account.ts:14` `fetch('/api/me', { credentials: 'same-origin' })`. 로그인 안 했으면 `401`. **요청 본문이 없고 문서 제목·본문이 실리지 않는다** |
| `online` 이벤트가 날 때 `GET /api/me` 한 번 더 | `src/app/App.tsx:554~559` |

- 문서·폴더·이미지·설정은 **서버로 가지 않는다.** 저장소 선택이 `src/storage/openStore.ts:27~46` 에서 갈리고, 로그인 안 한 상태는 `createIdbStore`(IndexedDB) 또는 `createMemoryStore` 다
- 초안은 이 구분을 그대로 적는다 — "서버로 가는 요청은 두 가지뿐" (6장 2절)

### 3.6 제3자 — Cloudflare 말고 없다

| 확인 | 결과 |
| --- | --- |
| 분석·추적 의존성 | `package.json` 의 dependencies 17개·devDependencies 17개에 분석·추적 라이브러리가 **없다** |
| 글꼴 | `@fontsource/noto-serif-kr`·`pretendard`·`@kfonts/d2coding` — **npm 패키지로 번들에 들어간다.** `specs/design.md:58, 88` 이 "외부 CDN·Google Fonts 에서 불러오지 않는다" 로 못박았고 빌드 결과에도 원격 글꼴 주소가 없다 |
| 아이콘 | `@material-symbols/svg-400` SVG 파일을 인라인 — `specs/design.md:165` |
| 빌드 결과의 외부 주소 | `dist/assets/*.js`·`*.css`·`index.html` 에서 뽑은 `https://` 호스트는 `chevrotain.io`(39)·`github.com`(11)·`example.com`(5)·`rawdoc.app`(3)·`react.dev`(2)·`en.wikipedia.org`(2)·`langium.org`(1) 뿐이고 **전부 오류 메시지·도움말 본문 안의 글자다.** 실행 중에 부르는 주소가 아니다 |
| `index.html` | 외부 스크립트·스타일 링크가 없다. `google-site-verification` **메타 태그**가 있으나(6행) 이는 글자일 뿐 요청을 만들지 않는다 |
| `vite.config.ts` | `workbox.runtimeCaching` 설정이 없다 — 외부 출처를 캐시하지 않는다. 매니페스트·OG 태그만 주입한다 |
| 워커가 밖으로 부르는 것 | `worker/auth.ts:13` `fetch(${ACCESS_TEAM_DOMAIN}/cdn-cgi/access/certs)` 하나. Cloudflare Access 의 공개키를 받는 요청이고 사용자 정보를 보내지 않는다 |
| 로그아웃 | `src/app/account.ts:64` `/cdn-cgi/access/logout` — 같은 출처 |

**결론: 위탁처는 Cloudflare 하나다.** 로그인 수단으로 Google 을 고르면 그 절차가 Google 을 거친다(`specs/product.md:263, 397` — Access 선택 화면에 Google·일회용 코드). 앱 코드에는 Google 관련 코드가 없다.

### 3.7 보유·삭제

| 대상 | 규칙 | 근거 |
| --- | --- | --- |
| 문서·폴더 | 사용자가 지우면 즉시 하드 삭제 | `worker/docs.ts:338`, `worker/folders.ts:177~203` |
| 이미지 | **어느 문서 원문에도 없고 24시간 지난 것**을 매일 UTC 18시에 지운다 | `worker/attachmentGc.ts:6`(`GRACE_MS = 24h`), `:26~57`, `wrangler.jsonc` `crons: ["0 18 * * *"]` |
| 잠금 | 60초 뒤 만료. 문서를 닫으면 그 전에 `DELETE` | `worker/locks.ts:7, 84` |
| 공유 링크·API 토큰 | `revoked_at` 을 채우는 방식 — **행은 남는다** | `0003`·`0007` 스키마 |
| 계정 | **지우는 기능이 없다** | `worker/` 전체에 `DELETE FROM users` 가 없고 `/api/*` 라우트 표(`worker/index.ts:109~171`)에 계정 삭제 경로가 없다 |

문서를 지워도 그 문서에만 있던 이미지는 다음 정리 때 사라진다 — 초안 5절이 이 순서를 그대로 적는다.

### 3.8 지금 없는 것 — 초안에 쓰지 않는다

- 계정 삭제 화면 (3.7). 개요 4장이 "지금 계정 삭제 기능이 없다면 그 사실을 그대로 적고 요청 방법(연락처)을 둔다" 로 지시했다 — 그대로 따른다
- 접속 로그 표·열람 기록 (3.1)
- 분석·광고·A/B 도구 (3.6). F-270 5장이 "넣게 되면 처리방침을 먼저 고친다" 로 정했다
- 쿠키 동의 배너 (F-270 5장 — 추적 쿠키가 없어서)
- `wrangler.jsonc` 에 `observability` 설정이 없다. Cloudflare 가 플랫폼 차원에서 요청을 얼마나 오래 들고 있는지는 **코드로 확인할 수 없다.** 초안 11절은 "Cloudflare 가 통신에 필요한 정보를 다룬다" 까지만 적고 기간을 쓰지 않는다

## 4. 자리표시

| 자리표시 | 채울 값 | 나오는 곳 |
| --- | --- | --- |
| `{{운영자}}` | 운영자 표기(개인 이름 또는 상호) | privacy 머리말·13절, terms 제1조 |
| `{{연락처}}` | 문의·요청을 받을 이메일 주소 | privacy 1·5·9·13절, terms 제12조 |
| `{{시행일}}` | 배포하는 날짜 `YYYY-MM-DD` | 두 글의 프론트매터 `date`·`updated`, 본문 맨 끝 `시행일:` 줄 |

- **가드는 이름을 가리지 않는다.** `site/guard.ts:8` 의 `/\{\{[\s\S]*?\}\}/` 가 `content/**/*.md` **원문 아무 데나** `{{…}}` 가 있으면 `content/{경로}: {{…}} 자리표시가 남아 있습니다` 로 빌드를 실패시킨다(`:38~40`). 프론트매터도 원문에 포함되므로 `date: {{시행일}}` 도 걸린다
- 이 명세 파일은 `specs/` 아래라 가드 대상이 아니다. 6·7장에 `{{…}}` 가 남아 있어도 빌드가 실패하지 않는다
- **자리표시를 더 두지 않는다.** 확인한 이유:
  - 주소(사업장 소재지) — 개인정보 보호법 제30조가 처리방침에 적으라고 정한 항목에 소재지가 없다. 연락처만 둔다
  - 관할 법원 — 특정 법원을 전속관할로 못박지 않고 "민사소송법이 정한 관할 법원" 으로 적는다(7장 제15조). 소비자에게 불리한 전속관할 조항을 피하려는 것이다
  - 서비스 이름 — 글에 쓰지 않는다(11장 Q1)

## 5. 시행일과 개정 이력

개요·F-272 가 정해 두지 않은 항목이라 여기서 닫는다.

| 결정 | 이유 |
| --- | --- |
| 본문 **맨 끝에 `시행일: {{시행일}}` 한 줄**을 둔다 | 프론트매터는 화면에 나오지 않는다 — `site/render.ts:47` 이 `textAfterFrontmatter` 로 떼고 본문만 넘긴다(F-272 6.1). 법적 문서는 시행일이 화면에 보여야 한다 |
| 프론트매터 `date`·`updated` 를 **시행일과 같은 값**으로 둔다 | `updated` 는 `sitemap.xml` 의 `lastmod` 로만 쓰인다(`site/build.ts:77`). 화면에 나오는 값과 용도가 달라 중복이 아니다. 개정할 때 두 군데를 같이 고친다 |
| **개정 이력 표를 두지 않는다** | 첫 판이라 이력이 없다. 개정 절차(시행 7일 전 공지, 불리한 변경은 30일 전)는 본문 조항으로 적는다(privacy 14절, terms 제3조) |
| 개정할 때 **이전 판을 따로 보관하지 않는다** | 글 원본이 git 에 있어 이력이 남는다(F-270 3장). 화면에 옛 판을 남기려면 주소 매핑(`site/pages.ts`)을 늘려야 하고 그것은 F-272 를 고치는 일이다 — 11장 Q8 |

## 6. 초안 전문 — `content/legal/privacy.md`

**아래를 그대로 파일로 만든다.** 구현하는 쪽이 문장을 새로 지어내지 않는다. 자리표시 3종만 확정된 값으로 바꾼다. 모든 사실 문장의 근거는 3장이다.

````markdown
---
title: 개인정보 처리방침
summary: 어떤 정보를 어디에 저장하고 언제 지우는지 적습니다.
date: {{시행일}}
updated: {{시행일}}
---

# 개인정보 처리방침

{{운영자}}(이하 "운영자")는 이 웹사이트에서 제공하는 마크다운 편집·보관·공유 서비스(이하 "서비스")를 운영하면서 아래와 같이 개인정보를 처리합니다.

## 1. 한눈에 보기

- 로그인하지 않고 쓰면 문서는 사용자의 브라우저 안에만 저장되고 서버로 보내지 않습니다.
- 로그인하면 이메일 주소 하나를 받습니다. 이름·전화번호·생년월일·주소는 받지 않습니다.
- 광고, 접속 통계, 행동 분석 도구를 쓰지 않습니다. 추적을 위한 쿠키도 두지 않습니다.
- 서버 운영은 Cloudflare 에 맡기고 있습니다. 그 밖의 업체에 개인정보를 넘기지 않습니다.
- 문의와 요청은 {{연락처}} 로 받습니다.

## 2. 로그인하지 않고 쓸 때

로그인하지 않은 상태에서 만든 문서, 폴더, 넣은 이미지, 화면 설정은 **사용자의 브라우저 안에만** 저장합니다. 서버로 보내지 않고 운영자는 볼 수 없습니다.

이 상태에서 서버로 가는 요청은 두 가지뿐입니다.

- 앱 화면을 이루는 파일(HTML, 자바스크립트, 글꼴 등)을 내려받는 요청
- 앱을 열 때 로그인 상태인지 확인하는 요청. 이 요청에는 문서 제목이나 본문이 들어가지 않습니다.

브라우저를 바꾸거나 브라우저의 사이트 데이터를 지우면 저장한 문서도 함께 사라집니다. 중요한 문서는 내보내기로 파일을 따로 보관해 주세요.

## 3. 로그인해서 쓸 때 저장하는 항목

| 항목 | 언제 생기나 |
| --- | --- |
| 이메일 주소 | 로그인할 때 |
| 계정 식별자 | 처음 로그인할 때 자동으로 만드는 임의의 문자열 |
| 가입 시각 | 처음 로그인할 때 |
| 문서의 제목, 본문, 줄바꿈 형식, 속한 폴더, 상단 고정 여부, 만든 시각, 고친 시각 | 문서를 쓰고 저장할 때 |
| 폴더 이름, 상위 폴더, 만든 시각, 고친 시각 | 폴더를 만들 때 |
| 넣은 이미지 파일과 그 형식, 크기, 가로세로 | 이미지를 넣을 때 |
| 공유 링크 주소, 무엇을 가리키는지, 만든 시각, 끊은 시각 | 공유 링크를 만들 때 |
| 초대한 사람의 이메일 주소와 권한 | 사람을 초대할 때 |
| 편집 중 표시(문서, 계정, 이메일, 만료 시각) | 로그인 상태로 문서를 편집하는 동안 |
| API 토큰의 이름, 앞 11자, 만든 시각, 마지막 사용 시각, 끊은 시각 | API 토큰을 발급할 때 |

- **본문은 사용자가 쓴 그대로 저장합니다.** 본문에 이름이나 연락처를 적으면 그것도 함께 저장됩니다.
- **API 토큰 값 자체는 저장하지 않습니다.** 되돌릴 수 없게 변환한 값만 두므로 운영자도 발급된 토큰을 다시 볼 수 없습니다.
- 편집 중 표시에는 같은 문서를 함께 보는 사람에게 알려 줄 이메일 주소가 들어갑니다. 1분이 지나면 만료되고 다음 편집 때 덮어씁니다.
- 접속한 사람의 IP 주소, 브라우저 종류, 열어 본 문서 목록을 따로 쌓아 두지 않습니다(11절).

## 4. 처리 목적

- 이메일 주소: 로그인한 사람을 알아보고 그 사람의 문서만 보여주기 위해. 공유와 초대에서 누가 누구에게 권한을 주었는지 가리기 위해
- 문서, 폴더, 이미지: 서비스의 본래 기능인 저장과 기기 사이 동기화를 위해
- 공유 링크, 초대 기록, 편집 중 표시: 사용자가 요청한 공유와 동시 편집 안내를 위해
- API 토큰 기록: 사용자가 발급한 토큰을 목록에서 확인하고 끊을 수 있게 하기 위해

광고, 마케팅, 프로파일링에는 쓰지 않습니다. 문서 내용을 읽어 통계나 기계 학습 자료로 쓰지 않습니다.

## 5. 보유 기간과 파기

| 대상 | 언제까지 |
| --- | --- |
| 문서, 폴더 | 사용자가 지울 때까지. 지우면 서버에서 곧바로 지웁니다 |
| 이미지 | 어느 문서에도 들어 있지 않고 24시간이 지난 이미지를 하루 한 번 자동으로 지웁니다. 그래서 문서를 지운 다음 날 그 문서에만 있던 이미지가 사라집니다 |
| 공유 링크 | 사용자가 끊을 때까지. 끊은 뒤에도 끊었다는 기록은 남습니다 |
| 초대 기록 | 사용자가 그 사람의 권한을 지울 때까지 |
| 편집 중 표시 | 1분. 사용자가 문서를 닫으면 그보다 먼저 지웁니다 |
| API 토큰 기록 | 사용자가 끊을 때까지. 끊은 뒤에도 끊었다는 기록은 남습니다 |
| 계정(이메일, 식별자, 가입 시각) | 계정 삭제 요청을 처리할 때까지 |

**지금은 화면에서 계정을 지우는 기능이 없습니다.** 계정과 그 계정의 문서를 모두 지우고 싶으면 {{연락처}} 로 알려 주세요. 본인 확인 뒤 지우고 처리 결과를 알려 드립니다.

## 6. 제3자 제공

개인정보를 다른 사람이나 업체에 팔거나 넘기지 않습니다. 법령에 따라 수사기관이 적법한 절차로 요구하는 경우에만 그 범위에서 제공합니다.

사용자가 직접 만든 공유 링크는 그 주소를 아는 사람이면 누구나 로그인 없이 볼 수 있습니다. 이것은 사용자의 선택에 따른 공개이고, 링크를 끊으면 더 이상 열리지 않습니다.

## 7. 처리 위탁

| 받는 곳 | 맡기는 일 |
| --- | --- |
| Cloudflare, Inc. | 서버 운영, 데이터베이스와 파일 저장, 로그인 확인 |

로그인할 때 Google 계정으로 로그인하는 방법을 고르면 그 확인 절차는 Google LLC 를 거칩니다. 운영자는 그 과정에서 이메일 주소만 전달받습니다.

## 8. 국외 이전

서버와 저장소는 Cloudflare 의 아시아·태평양 지역 데이터센터에 있습니다.

| 항목 | 내용 |
| --- | --- |
| 이전받는 자 | Cloudflare, Inc. |
| 이전되는 곳 | 아시아·태평양 지역 데이터센터 |
| 이전 항목 | 3절 표에 적은 항목 |
| 이전 목적 | 서비스 제공을 위한 저장과 전송 |
| 보유 기간 | 5절과 같습니다 |

로그인해서 쓰려면 이 이전이 필요합니다. 이전을 원하지 않으면 로그인하지 않고 쓸 수 있고, 그 경우 국외로 옮기는 정보가 없습니다.

## 9. 사용자의 권리

사용자는 언제든지 자기 개인정보를 보고, 고치고, 지우고, 처리를 멈추라고 요구할 수 있습니다.

- 문서, 폴더, 이미지, 공유 링크, 초대, API 토큰은 앱 화면에서 직접 보고 고치고 지울 수 있습니다.
- 이메일 주소는 로그인에 쓰는 값이라 바꿀 수 없습니다. 다른 이메일로 로그인하면 별개의 계정이 됩니다.
- 그 밖의 요청과 계정 삭제는 {{연락처}} 로 받습니다. 법정대리인도 같은 방법으로 요청할 수 있습니다.

만 14세 미만 아동은 이 서비스에 로그인하지 말아 주세요. 운영자는 만 14세 미만 아동의 개인정보를 일부러 수집하지 않습니다.

## 10. 브라우저에 저장하는 것

광고나 분석을 위한 쿠키를 두지 않습니다. 아래는 서비스가 동작하는 데 쓰는 값입니다.

| 이름 | 종류 | 무엇에 쓰나 | 언제까지 |
| --- | --- | --- | --- |
| `md_app` | 쿠키 | 이 브라우저가 앱을 쓰기로 했는지. 처음 온 사람에게만 소개 화면을 보이기 위해 | 1년 |
| `CF_Authorization` | 쿠키 | 로그인 상태. Cloudflare 가 만들고 관리합니다 | 로그인이 유지되는 동안 |
| `md.` 로 시작하는 값 | 로컬 저장소 | 테마, 서체, 글자 크기, 사이드바 너비, 줄 번호, 마지막에 연 문서 같은 화면 설정과 소개 화면을 이미 보았는지 | 지울 때까지 |
| `md.lockSession`, `md.landingReloaded` | 세션 저장소 | 이 탭의 편집을 구분하는 임의의 값과, 소개 화면이 되풀이되지 않게 하는 표시 | 탭을 닫을 때까지 |
| `md-docs`, `md-remote` | 인덱스드DB | 로그인하지 않았을 때의 문서와, 로그인했을 때 서버 문서의 사본. 인터넷이 끊겨도 쓸 수 있게 둡니다 | 지울 때까지 |
| 앱 파일 캐시 | 캐시 저장소 | 오프라인에서도 앱이 열리도록 저장한 HTML, 자바스크립트, 글꼴 | 새 버전이 나오면 바뀝니다 |

- 위 값은 모두 브라우저의 사이트 데이터 삭제로 지울 수 있습니다.
- **로그아웃해도 브라우저에 남은 문서 사본은 지워지지 않습니다.** 공용 컴퓨터에서 썼다면 브라우저의 사이트 데이터를 지워 주세요.

## 11. 접속 기록

서비스는 접속 기록을 따로 쌓지 않습니다. 접속한 사람의 IP 주소, 브라우저 종류, 열어 본 주소를 저장하는 표가 없습니다.

다만 서버를 운영하는 Cloudflare 는 요청을 처리하면서 통신에 필요한 정보를 다룹니다. 이는 Cloudflare 의 개인정보 처리 기준을 따릅니다.

## 12. 안전성 확보 조치

- 모든 통신은 암호화된 연결(HTTPS)로만 오갑니다.
- 로그인은 Cloudflare 가 이메일 일회용 코드 또는 Google 계정으로 확인하고, 서버가 그 결과를 다시 검증합니다.
- 다른 사람의 문서, 폴더, 이미지를 요청하면 있는지 없는지도 알려 주지 않고 없는 것으로 답합니다.
- 이미지 저장소는 바깥에서 바로 열 수 없고, 서버가 권한을 확인한 요청에만 내보냅니다.
- API 토큰은 원래 값을 저장하지 않고 되돌릴 수 없게 변환한 값만 둡니다.
- 올라온 이미지는 확장자가 아니라 파일 자체를 확인해 형식을 판정합니다.

## 13. 개인정보 보호책임자

- 책임자: {{운영자}}
- 연락처: {{연락처}}

개인정보 처리와 관련한 문의, 열람·정정·삭제 요구, 피해 구제는 위 연락처로 받습니다. 그 밖에 개인정보 침해에 대한 상담과 신고는 아래에 하실 수 있습니다.

- 개인정보침해신고센터 (privacy.kisa.or.kr / 국번 없이 118)
- 개인정보 분쟁조정위원회 (kopico.go.kr / 1833-6972)
- 대검찰청 사이버수사과 (spo.go.kr / 국번 없이 1301)
- 경찰청 사이버수사국 (ecrm.police.go.kr / 국번 없이 182)

## 14. 방침 변경

이 방침을 고칠 때는 고친 내용과 시행일을 이 페이지에 적고 시행일 7일 전부터 알립니다. 사용자에게 불리하게 바뀌는 경우에는 30일 전부터 알립니다.

시행일: {{시행일}}
````

- 13절의 네 기관 이름·주소·번호는 **코드에서 확인할 수 없는 값**이다. 작성자의 기존 지식으로 적었으므로 공개 전에 사람이 확인한다 — 10장 H2
- 8절을 별도 장으로 둔 이유는 3.3 이다. 저장 지역이 국내가 아니다
- 이 초안에 이미지 문법·mermaid 펜스가 없다는 것은 `site/guard.ts` 의 G3·G4 기준으로 확인했다. `{{…}}` 는 의도한 자리표시 9곳(`{{시행일}}` 3회, `{{운영자}}` 2회, `{{연락처}}` 4회)뿐이다

## 7. 초안 전문 — `content/legal/terms.md`

**아래를 그대로 파일로 만든다.**

````markdown
---
title: 이용약관
summary: 서비스를 쓸 때 지켜야 할 것과 운영자의 책임 범위를 적습니다.
date: {{시행일}}
updated: {{시행일}}
---

# 이용약관

## 제1조 (목적)

이 약관은 {{운영자}}(이하 "운영자")가 이 웹사이트에서 제공하는 마크다운 편집·보관·공유 서비스(이하 "서비스")를 이용하는 데 필요한 사항을 정합니다.

## 제2조 (용어)

1. "이용자" 는 이 약관에 따라 서비스를 쓰는 사람을 말합니다.
2. "문서" 는 이용자가 서비스에서 만들고 저장하는 글과 그 안에 넣은 이미지를 말합니다.
3. "계정" 은 이용자가 이메일 주소로 로그인해 얻는 서비스 이용 자격을 말합니다.
4. "공유 링크" 는 이용자가 문서나 폴더를 로그인 없이 볼 수 있게 만든 주소를 말합니다.

## 제3조 (약관의 게시와 개정)

1. 운영자는 이 약관을 서비스 화면에서 언제든 볼 수 있게 둡니다.
2. 운영자는 관련 법령을 어기지 않는 범위에서 이 약관을 고칠 수 있습니다.
3. 약관을 고칠 때는 고친 내용과 시행일을 이 페이지에 적고 시행일 7일 전부터 알립니다. 이용자에게 불리하게 바뀌는 경우에는 30일 전부터 알립니다.
4. 이용자가 고친 약관에 동의하지 않으면 서비스 이용을 그만둘 수 있습니다. 시행일 뒤에도 계속 쓰면 고친 약관에 동의한 것으로 봅니다.

## 제4조 (이용 계약)

1. 서비스는 로그인하지 않고도 쓸 수 있습니다. 이 경우 문서는 이용자의 브라우저에만 저장되고 운영자는 이를 보관하지 않습니다.
2. 이용자가 이메일 주소로 로그인하면 그때 이용 계약이 성립합니다.
3. 이메일 주소 하나가 계정 하나입니다. 다른 이메일로 로그인하면 별개의 계정이 되고 문서는 서로 옮겨지지 않습니다.
4. 이용자는 자기 계정을 다른 사람이 쓰게 해서는 안 됩니다.

## 제5조 (문서의 권리)

1. 이용자가 만든 문서의 권리는 이용자에게 있습니다. 운영자는 이를 가져가지 않습니다.
2. 운영자는 서비스를 제공하는 데 필요한 범위(저장, 전송, 이용자가 요청한 공유)에서만 문서를 처리합니다. 문서를 읽어 광고, 통계, 기계 학습 자료로 쓰지 않습니다.
3. 이용자는 자기가 권리를 가지고 있거나 올려도 되는 내용만 서비스에 저장해야 합니다.

## 제6조 (공유와 초대)

1. 공유 링크는 그 주소를 아는 사람이면 누구나 로그인 없이 볼 수 있습니다. 이용자는 이 점을 알고 링크를 만들어야 합니다.
2. 이용자가 공유 링크를 끊으면 그 뒤로는 열리지 않습니다. 다만 이미 열어 본 사람이 따로 저장한 내용까지 되돌릴 수는 없습니다.
3. 다른 사람을 초대하려면 그 사람의 이메일 주소를 입력해야 합니다. 이용자는 그 주소를 적법하게 알고 있어야 하고, 그 주소가 서비스에 저장된다는 점을 알고 입력해야 합니다.

## 제7조 (금지 행위)

이용자는 다음을 해서는 안 됩니다.

1. 법령을 어기거나 다른 사람의 권리를 침해하는 내용을 저장하거나 공유하는 일
2. 다른 사람의 계정, 공유 링크, API 토큰을 허락 없이 쓰는 일
3. 서비스의 취약점을 찾아 악용하거나 다른 이용자의 문서에 접근하려 하는 일
4. 자동화된 방법으로 지나치게 많은 요청을 보내 서비스 운영을 방해하는 일
5. 저장 용량 한도를 피하려고 계정을 여러 개 만드는 일

## 제8조 (사용량 한도)

운영자는 서비스를 고르게 제공하기 위해 문서 하나의 크기, 이미지 한 장의 크기, 계정마다 쓸 수 있는 이미지 저장 공간, 발급할 수 있는 API 토큰 개수에 한도를 둡니다. 현재 한도는 도움말에서 볼 수 있습니다. 운영자는 운영 사정에 따라 한도를 바꿀 수 있고, 바뀌면 화면에 알립니다.

## 제9조 (백업)

1. 운영자는 서버에 저장한 문서가 사라지지 않도록 노력하지만, 이용자의 문서에 대한 별도의 백업본 보관을 보증하지 않습니다.
2. 이용자는 중요한 문서를 내보내기 기능으로 자기 기기에 따로 보관해 두시기 바랍니다.
3. 로그인하지 않고 쓸 때의 문서는 이용자의 브라우저에만 있습니다. 브라우저의 사이트 데이터를 지우거나 기기를 바꾸면 문서도 사라지고, 운영자는 이를 되살릴 수 없습니다.

## 제10조 (서비스의 변경과 중지)

1. 운영자는 서비스의 내용을 바꾸거나 일부를 그만둘 수 있습니다. 중요한 변경은 미리 알립니다.
2. 설비 점검, 장애, 천재지변처럼 어쩔 수 없는 사정이 있으면 서비스를 일시적으로 멈출 수 있습니다.
3. 서비스 전부를 그만두는 경우에는 30일 전에 알리고, 그동안 이용자가 문서를 내보낼 수 있게 합니다.

## 제11조 (이용 제한)

이용자가 제7조를 어기면 운영자는 미리 알린 뒤 서비스 이용을 제한할 수 있습니다. 긴급하거나 법령 위반이 명백한 경우에는 먼저 제한하고 곧바로 알립니다.

## 제12조 (계약 해지)

1. 이용자는 언제든지 서비스 이용을 그만둘 수 있습니다.
2. 계정과 서버에 저장된 문서를 모두 지우려면 {{연락처}} 로 알려 주세요. 본인 확인 뒤 처리하고 결과를 알려 드립니다.

## 제13조 (무상 제공과 보증의 부인)

1. 서비스는 현재 요금 없이 제공합니다.
2. 운영자는 서비스가 끊기지 않는다거나 오류가 없다고 보증하지 않습니다.
3. 운영자는 이용자가 저장한 문서의 내용에 대해 책임지지 않습니다.

## 제14조 (책임)

1. 운영자는 고의나 중대한 과실이 없는 한, 서비스 이용으로 이용자에게 생긴 손해에 대해 책임지지 않습니다.
2. 운영자는 이용자가 만든 공유 링크가 퍼져서 생긴 손해, 이용자가 계정을 관리하지 못해 생긴 손해, 이용자가 스스로 지운 문서에 대해 책임지지 않습니다.
3. 이 조항은 관련 법령이 허용하는 범위에서만 적용합니다.

## 제15조 (준거법과 분쟁)

이 약관은 대한민국 법에 따릅니다. 서비스 이용으로 분쟁이 생기면 서로 협의해 풀고, 협의가 되지 않으면 민사소송법이 정한 관할 법원에 소를 제기할 수 있습니다.

시행일: {{시행일}}
````

- 제8조에 숫자를 쓰지 않는 이유는 11장 Q2 다. 한도가 바뀔 때마다 약관 개정 절차(제3조)가 따라붙게 하지 않으려는 것이다. 현재 값은 3.2 에 적어 두었다
- 제4조 3항의 "이메일 주소 하나가 계정 하나" 는 `migrations/0001_users.sql` 의 `email TEXT NOT NULL UNIQUE` 와 `worker/auth.ts:118~128` 의 `ON CONFLICT(email) DO NOTHING` 에서 확인했다
- 제6조 1항은 `worker/links.ts` 의 `/pub/*` 경로가 로그인을 보지 않는다는 사실(`specs/architecture.md` 6장)에서 나온다
- 자리표시는 `{{시행일}}` 3회, `{{운영자}}` 1회, `{{연락처}}` 1회뿐이다

## 8. 문구 표 (`specs/ia.md` 6장 형식)

| 개념 | 화면 문구 | 쓰지 않는 말 |
| --- | --- | --- |
| 꼬리 링크 1 | `개인정보 처리방침` | 개인정보보호정책, 프라이버시 정책 |
| 꼬리 링크 2 | `이용약관` | 약관, 서비스 약관, 이용 규약 |

- 두 문구 모두 `specs/ia.md` 6장에 **이미 등록돼 있다**(`사이트 법적 문서 (F-275)` 줄). 새로 더할 문구가 없다
- 두 글의 본문은 문장형이므로 `~습니다` 체다(`specs/ia.md` 6장 규칙)

## 9. 기존 테스트 회귀 — 반드시 함께 고친다

`SITE_FOOTER_LINKS` 에 항목이 들어가면 `SITE_NAV` 때와 **완전히 같은 벽**에 부딪힌다. `site/build.ts:81~91` 이 두 배열을 같은 반복문으로 검사하기 때문이다.

| 위치 | 지금 | 깨지나 |
| --- | --- | --- |
| `site/build.test.ts:12` (`F-272 A5`), `:18` (`F-272 A6`) | `buildSite({ content: {}, … })` | **F-273 이 먼저 들어가 있으면 안 깨진다.** F-273 6장이 파일 최상위에서 두 배열을 `splice` 로 비우고 `afterAll` 로 되돌리는 구조를 넣기로 했고, 그 구조가 `SITE_FOOTER_LINKS` 도 함께 비운다 |
| `site/build.test.ts:74~98` (`F-272 A8`) | 자기 항목을 넣고 `finally` 에서 `SITE_NAV.length = 0` | F-273 이 고친 뒤에도 그대로 통과한다 |
| `src/lib/siteChrome.test.ts:6~9` | `beforeEach` 가 두 배열을 비운다 | **안 깨진다.** `F-272 A1` 5개와 `F-273 A2` 모두 영향 없다. 특히 `:41~47` 은 `SITE_FOOTER_LINKS` 에 `/privacy` 를 직접 넣고 보는 테스트라 실제 값과 무관하다 |
| `worker/welcomePage.test.ts:77~82` | `<footer class="site-foot">…</footer>` 안에 제품명이 있는지만 본다 | **안 깨진다.** 링크가 늘어도 정규식이 그대로 잡는다 |
| `worker/welcomePage.ts:76` | 모듈 최상위에서 `renderSiteFooter` 를 1회 호출 | **안 깨진다.** `SITE_FOOTER_LINKS` 도 모듈 최상위 배열이라 값이 채워진 뒤 읽힌다 |
| `e2e/site.spec.js:18, 43` | `.site-foot` 가 보이는지 / `Rawdoc` 을 포함하는지 | **안 깨진다.** 링크가 늘어도 통과한다 |
| `e2e/site.spec.js:35` (`F-272 A11`) | `sitemapBody` 에 `404` 가 없는지 | **안 깨진다.** `/privacy`·`/terms` 주소에 `404` 라는 글자가 없다 |
| `site/render.test.ts`, `site/pages.test.ts`, `site/guard.test.ts` | `SITE_FOOTER_LINKS` 를 쓰지 않는다 | **안 깨진다** |

**의존 관계**: 이 회귀를 푸는 방법은 `F-273.md` 6장이 이미 적었다. 여기서 되풀이하지 않는다. 따라서

- **F-273 이 먼저 구현돼 있으면** 이 명세는 `site/build.test.ts` 의 기존 테스트를 한 줄도 고치지 않고, 자기 테스트(A1·A3)만 더한다
- **F-273 보다 먼저 구현하게 되면** `F-273.md` 6장의 저장·복원 구조를 이 명세가 대신 넣는다. 그때는 `site/build.test.ts` 의 `F-272 A6` `describe` 본문에 있는 `buildSite` 호출을 `it` 안으로 옮기는 일까지 같이 한다
- 프론트매터 `depends: [F-272, F-273]` 은 이 때문이다

2026-09-21 이 명세를 쓰는 시점에 F-273 구현이 작업 트리에 들어와 있었고(`git diff site/build.test.ts src/lib/siteChrome.test.ts`), **F-273 6장이 권고한 그대로** `site/build.test.ts` 최상위에 `savedNav`·`savedFooter` `splice` + `afterAll` 복원이 들어갔고 `F-272 A6` 의 `buildSite` 호출이 `beforeEach` 로 옮겨졌다. `src/lib/siteChrome.test.ts` 에도 `const INITIAL_NAV = [...SITE_NAV]` 가 모듈 최상위에 생겼다. **A2 의 `INITIAL_FOOTER` 는 그 줄 바로 옆에 둔다.**

## 10. 사람 확인 필요 (자동 판정 불가)

`specs/human-checks.md` 로 넘긴다.

| # | 확인 |
| --- | --- |
| H1 | **6·7장 초안 전문을 사람이 읽고 공개해도 되는지 판단.** 이 명세는 법률 자문이 아니다. 사실 관계는 3장에서 코드로 확인했지만, 조항의 법적 효력과 빠진 항목은 판단하지 않았다. 필요하면 전문가 검토 |
| H2 | 처리방침 13절의 네 기관(개인정보침해신고센터·개인정보 분쟁조정위원회·대검찰청 사이버수사과·경찰청 사이버수사국) 이름·주소·전화번호가 지금도 맞는지. 코드로 확인할 수 없는 값이다 |
| H3 | `{{운영자}}` 를 개인 이름으로 할지 상호로 할지. 사업자등록이 있으면 상호·대표자·사업자등록번호를 더 적을지 |
| H4 | `{{연락처}}` 로 쓸 이메일 주소. 공개되는 주소라 스팸을 받는다는 점을 감안 |
| H5 | 8절 "아시아·태평양 지역 데이터센터" 표기를 그대로 둘지, Cloudflare 대시보드에서 더 좁혀 확인해 나라를 적을지 |
| H6 | 꼬리에 항목이 셋(제품명 + 링크 2개)이 된 모양. 랜딩·사이트 글·404 세 화면에서 줄이 넘치거나 답답하지 않은지 |

## 11. 열린 질문 / 사람 결정 필요

| # | 질문 | 기본값과 근거 | 다른 쪽을 고르면 |
| --- | --- | --- | --- |
| Q1 | 두 글에 서비스 이름(`Rawdoc`)을 쓸지 | **쓰지 않는다.** "이 웹사이트에서 제공하는 마크다운 편집·보관·공유 서비스" 로 정의한다. 제품명이 바뀌면 법적 문서를 고쳐야 하는데, 개정에는 시행일과 공지 절차가 따라붙는다(privacy 14절·terms 제3조). F-273 도 같은 이유로 체인지로그에 제품명을 쓰지 않았다. 머리·꼬리에 제품명이 이미 나온다 | 쓰기로 하면 `{{서비스명}}` 자리표시를 넷째로 두거나 `.md` 본문에 직접 적는다(F-270 3장이 글 본문은 예외로 둔다). 이름이 바뀔 때 두 글이 함께 개정 대상이 된다 |
| Q2 | 이용약관 제8조에 한도 숫자(5MB·300MB·1MB·10개)를 박을지 | **박지 않는다.** "현재 한도는 도움말에서 볼 수 있습니다" 로 넘긴다. 숫자를 박으면 한도를 바꿀 때마다 약관 개정 절차가 필요하다. 도움말은 이미 이미지 한도를 적고 있다(`src/app/helpDoc.ts:144`) | 박으면 이용자가 약관 한 장에서 전부 볼 수 있다. 대신 `worker/attachments.ts:11, 13`·`worker/validate.ts:3` 값을 바꿀 때 약관도 같이 고쳐야 하고, 그 규칙을 `CLAUDE.md` 에 한 줄 더해야 한다 |
| Q3 | 시행일을 `{{시행일}}` 자리표시로 둘지 | **둔다.** 빌드 가드가 `{{…}}` 를 일반적으로 막으므로(`site/guard.ts:8`) 날짜를 안 채우면 배포가 안 된다. 잘못된 날짜가 공개되는 것보다 낫다 | 초안에 임시 날짜를 적으면 가드를 통과해 버려서, 커밋하는 사람이 날짜를 고치는 것을 잊어도 아무도 모른다 |
| Q4 | 링크를 꼬리에 둘지 머리에 둘지 | **꼬리(`SITE_FOOTER_LINKS`).** F-270 3장·F-272 5.1 이 둘 다 그렇게 적었다(2.3) | 머리에 두면 `사용법`·`체인지로그`·`도움말` 과 섞여 머리가 다섯 칸이 된다. F-272 5.1 의 주석을 고쳐야 하므로 F-272 를 건드리는 일이 된다 |
| Q5 | 만 14세 미만 조항을 둘지 | **둔다.** 개인정보 보호법 제22조의2 가 만 14세 미만의 개인정보 처리에 법정대리인 동의를 요구한다. 지금 나이를 묻는 화면이 없으므로 "로그인하지 말아 주세요" 로 적는다 | 빼면 미성년자가 로그인했을 때 근거가 없다. 나이를 묻는 화면을 만들면 로그인 흐름(F-205)을 고쳐야 하고, Cloudflare Access 가 로그인을 처리하므로 앱이 끼어들 자리가 마땅치 않다 |
| Q6 | 두 글을 한 커밋으로 낼지 | **한 커밋.** 꼬리 링크 두 줄과 글 두 개가 나뉘면 중간 커밋에서 빌드가 실패한다(1장) | 나누려면 링크를 나중에 켜야 하는데, 그러면 두 번째 커밋까지 글이 있어도 아무 데서도 링크로 갈 수 없다 |
| Q7 | 처리방침에 국외 이전 장(8절)을 따로 둘지 | **둔다.** 저장 지역이 APAC 이고 국내가 아니다(3.3). 개인정보 보호법 제28조의8 이 국외 이전에 별도 고지를 요구한다 | 위탁(7절)에 한 줄로 합치면 짧아지지만, 이전받는 자·국가·항목·목적·기간을 적으라는 법령 요구를 한 줄로 담기 어렵다 |
| Q8 | 개정할 때 이전 판을 사이트에 남길지 | **남기지 않는다.** 글 원본이 git 에 있어 이력이 남는다(F-270 3장). 화면에 옛 판을 두려면 `site/pages.ts` 의 주소 매핑을 늘려야 하고 그것은 F-272 를 고치는 일이다 | 남기기로 하면 `/privacy/2026-09-21` 같은 주소 규칙과 목록 페이지가 필요하다. 별도 명세를 쓴다 |
| Q9 | 꼬리 링크에 `aria-current="page"` 를 붙일지 | **붙이지 않는다.** `renderSiteFooter` 가 지금 그 기능을 갖고 있지 않고(`src/lib/siteChrome.ts:104~112`), 고치면 F-272 가 만든 함수의 시그니처를 바꾸게 된다 | 붙이면 `/privacy` 를 보는 중임을 스크린 리더가 알 수 있다. 그러려면 `renderSiteFooter` 에 `current` 인자를 더하고 부르는 세 곳(`site/render.ts:60`·`site/build.ts:21`·`worker/welcomePage.ts:76`)을 고쳐야 한다 — F-272 소유 파일이므로 갱신 대상으로 올린다 |
| Q10 | 처리방침을 `/help` 나 앱 안에도 넣을지 | **넣지 않는다.** 사이트 꼬리 세 화면(랜딩·사이트 글·404)에서 닿는다. 앱에서 사이트로 가는 링크는 F-276 범위다 | 앱 안에도 두려면 사이드바나 설정에 링크가 필요하고, 그것은 F-276 이 정할 일이다 |
| Q11 | 처리방침 13절의 기관 주소를 누를 수 있는 링크로 만들지 | **만들지 않는다.** 글자로만 둔다(2.4 실측 — 맨 도메인은 링크가 되지 않는다). 사이트 본문 링크는 전부 새 탭으로 열리고(F-272 11장 M3), 기관 주소는 바뀔 수 있어 링크가 깨지면 더 나쁘다 | 링크로 만들려면 `[개인정보침해신고센터](https://privacy.kisa.or.kr)` 형태로 적는다. 주소가 바뀔 때마다 글을 고쳐야 하고, 고치면 개정 절차(14절) 대상인지 판단해야 한다 |

## 12. 수용 기준

단위는 `site/build.test.ts`·`src/lib/siteChrome.test.ts`, 동작은 `e2e/site.spec.js` 의 `F-275 A*`.

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| A1 (단위) | 두 글로 페이지·sitemap 이 난다 | `site/build.test.ts` 에 `F-275 A1`. `SITE_FOOTER_LINKS` 에 두 줄을 넣고 `content` 에 `legal/privacy.md`(`title: 개인정보 처리방침`, `updated: 2026-09-21`)·`legal/terms.md`(`title: 이용약관`, `updated: 2026-09-21`)를 넣어 `buildSite` | 키에 `privacy.html`·`terms.html` 이 있다. `sitemap.xml` 에 `<loc>https://rawdoc.app/privacy</loc><lastmod>2026-09-21</lastmod>` 와 `.../terms` 가 있다. `privacy.html` 에 `<title>개인정보 처리방침 · Rawdoc</title>` 과 `<link rel="canonical" href="https://rawdoc.app/privacy">` 가 있다 |
| A2 (단위) | 꼬리 링크가 실제로 등록돼 있다 | `src/lib/siteChrome.test.ts` **맨 위**(import 바로 아래, `beforeEach` 가 비우기 전)에서 `const INITIAL_FOOTER = [...SITE_FOOTER_LINKS]` 를 잡고 `F-275 A2` describe 에서 검사 | `INITIAL_FOOTER` 가 정확히 `[{ path: '/privacy', label: '개인정보 처리방침' }, { path: '/terms', label: '이용약관' }]` 이다(순서 포함) |
| A3 (단위) | 가드는 자리표시 이름을 가리지 않는다 | `site/build.test.ts` 에 `F-275 A3`. `content` 에 `legal/privacy.md` 를 넣되 본문에 `{{시행일}}` 하나만 남긴다 | `Error` 를 던지고 메시지에 `legal/privacy.md` 가 들어 있다. `{{운영자}}` 만 검사하는 것이 아님을 못박는다 |
| A4 (e2e) | `F-275 A4` 처리방침 페이지가 뜬다 | `npm run build` 뒤 preview 에서 `page.goto('/privacy')` | `개인정보 처리방침` 제목(`h1`)이 보이고 `.site-foot` 안에 `href="/terms"` 인 `이용약관` 링크가 보인다 |
| A5 (e2e) | `F-275 A5` 이용약관 페이지가 뜬다 | `page.goto('/terms')` | `이용약관` 제목(`h1`)이 보이고 `.site-foot` 안에 `href="/privacy"` 인 `개인정보 처리방침` 링크가 보인다 |
| A6 (e2e) | `F-275 A6` 완결된 정적 페이지다 | `page.request.get('/privacy')`·`/terms` | 둘 다 `200`. 본문에 `rel="canonical"` 과 각각 `https://rawdoc.app/privacy`·`https://rawdoc.app/terms` 가 있고 **`<script` 가 하나도 없다** |
| A7 (e2e) | `F-275 A7` 자리표시가 남아 있지 않다 | 같은 두 응답 본문 | `{{` 가 **하나도 없다.** 배포된 글에 자리표시가 남지 않았다는 최종 판정 |
| A8 (e2e) | `F-275 A8` 색인 | `page.request.get('/sitemap.xml')` | `<loc>https://rawdoc.app/privacy</loc>` 와 `<loc>https://rawdoc.app/terms</loc>` 가 있다 |
| A9 (e2e) | `F-275 A9` 서비스 워커에서 빠진다 | `page.request.get('/sw.js')` | 본문에 `privacy.html`·`terms.html` 이 **없다** (F-272 A9 와 같은 방식) |
| A10 (e2e) | `F-275 A10` 랜딩 꼬리에도 보인다 | `mockLanding(page)` 뒤 `/` 로 간다 | `.site-foot` 안에 `href="/privacy"`·`href="/terms"` 인 링크 둘이 보인다 |
| A11 | 빌드 가드 통과 | `npm run build` | 성공한다. `dist/privacy.html`·`dist/terms.html` 이 생긴다 (가드가 걸리면 빌드가 실패하므로 이것이 G1~G5 통과 판정이다) |
| A12 | 기존 e2e 회귀 | `npm run e2e:one -- "F-272\|F-273\|F-271"` | 통과 |
| A13 | 타입·린트·단위 | `npm test`, `npm run typecheck`, `npm run typecheck:worker`, 바꾼 파일 eslint | 통과. `site/build.test.ts` 의 `F-272 A5`~`A8` 과 `src/lib/siteChrome.test.ts` 의 `F-272 A1` 5개가 모두 초록 |
| A14 | 소유 범위 | `npm run review -- F-275` | 1장 표 밖 변경 없음. `specs/**`·`CLAUDE.md` 무변경 |

- 문장의 내용·어조·법적 타당성은 자동으로 판정하지 않는다. 10장 H1 이 본다
- `content/legal/*.md` 는 `.md` 라 `npm run review` 의 제품명·색상 하드코딩 검사 대상이 아니다(`scripts/review-diff.mjs` 가 코드·CSS 가 아니면 통과). 그래도 이 두 글은 제품명을 쓰지 않는다(11장 Q1)

## 13. 구현 순서 (TDD)

**선행 조건: `{{운영자}}`·`{{연락처}}`·`{{시행일}}` 세 값이 확정돼 있어야 한다.** 없으면 시작하지 않고 메인에 보고한다.

1. `site/build.test.ts` 에 `F-275 A1`·`A3` 을 먼저 쓴다 → 빨강(`privacy.html`·`terms.html` 이 안 나온다)
2. `content/legal/privacy.md`·`content/legal/terms.md` 를 6·7장 그대로 만들고 자리표시 3종을 확정값으로 바꾼다 → A1 초록
3. `src/lib/siteChrome.ts` `SITE_FOOTER_LINKS` 에 두 줄 → `npm test` 로 `F-272 A5`·`A6` 이 여전히 초록인지 확인(9장 — F-273 의 저장·복원 구조가 들어가 있어야 한다. 없으면 여기서 멈추고 보고)
4. `src/lib/siteChrome.test.ts` 에 `INITIAL_FOOTER` + `F-275 A2`
5. `npm run build` → `dist/privacy.html`·`dist/terms.html`·`dist/sitemap.xml` 을 눈으로 확인 (A11)
6. `e2e/site.spec.js` 에 `F-275 A4`~`A10` → `npm run e2e:one -- "F-275"`
7. A12~A14
