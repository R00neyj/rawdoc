---
id: F-245
title: 한글 입력 중 목록 줄 엔터가 두 번 넘어가는 문제
milestone: M2
status: done
created: 2026-09-18
implemented: 711b686
---

# F-245 한글 입력 중 목록 줄 엔터가 두 번 넘어가는 문제

작성일: 2026-09-18
상태: 사용자 승인 대기 (2026-09-18 사용자 "ul태그 ( - ) 생성에 오류 — 테스트 적고 엔터로 다음줄 넘기면 두번 줄넘김되서 … 띄우고 `- 여기` 위치에 써짐")
근거: `specs/features/F-103.md` 3.2(조합 종료 재계산), `F-104.md` 2.3(IME 보류 규칙), CLAUDE.md 불변조건("IME 조합 중 재계산을 보류하면, 조합 종료 시 밀린 재계산을 반드시 따라잡는다"), `.workflow/tasks/T-004/verify.md` 6.5·7장

## 1. 범위

### 문제
목록 항목(`- 테스트`)을 치고 그 줄 끝에서 엔터를 치면 **줄이 두 번 넘어간다**. 사용자가 본 결과:

```
- 테스트
(빈 줄)
- |        ← 커서
```

기대:

```
- 테스트
- |
```

2026-09-18 실측으로 조건을 좁혔다:

| 경로 | 결과 |
| --- | --- |
| 합성 키 입력(IME 없이 `- 테스트` → Enter) | **정상** — `- 테스트\n- X`. 줄 끝 공백 1·2개, 체크박스(`- [ ]`), 중첩 목록도 전부 정상 |
| CDP `Input.imeSetComposition` 으로 한글을 조합해 넣은 뒤 Enter | **이상** — `- 테스트\r\nX\r\n`. 목록 이어쓰기(`- `)가 안 붙고, 문서 끝에 개행이 하나 더 남는다 |

즉 **한글 IME 조합을 거친 목록 줄에서만** 엔터 처리가 어긋난다. 조합을 거치지 않으면 `insertNewlineContinueMarkdown`(`@codemirror/lang-markdown` 의 `markdownKeymap`, `createEditor.ts` 285행 `markdown()` 이 싣는다)이 정상 동작한다.

CDP 재현은 실제 IME 와 완전히 같지는 않다(`cm-composing` 클래스가 붙지 않았다) — 그래서 사용자가 본 "빈 줄 + `- ` 는 붙음" 과 실측한 "`- ` 안 붙음 + 개행 하나 더" 가 세부에서 다르다. **둘 다 같은 뿌리(조합 경로에서 Enter 가 두 번 반영되거나 키맵을 건너뛴다)로 보고 조사한다.**

### 한다
- 한글(및 다른 IME) 조합으로 친 목록 항목 줄 끝에서 엔터를 쳤을 때, 조합 없이 친 경우와 **같은 결과**가 되게 한다
  - `- 항목` → 다음 줄 `- `
  - `1. 항목` → 다음 줄 `2. `
  - `- [ ] 항목` → 다음 줄 `- [ ] `
  - 중첩 목록은 같은 들여쓰기 유지
- 빈 목록 항목에서 엔터를 치면 지금처럼 목록을 끝낸다(기호를 지우고 빈 줄) — 조합 경로에서도 같아야 한다
- 원인을 먼저 특정하고(아래 3.1), 고친 뒤 회귀 테스트를 남긴다

### 하지 않는다
- 목록 이어쓰기 규칙 자체를 새로 만들기 — `insertNewlineContinueMarkdown` 동작을 그대로 두고, 조합 경로에서 그것이 제대로 불리게만 한다
- 조합 중 재계산 보류 규칙(F-104 2.3) 변경 — 불변조건이다. 보류를 없애는 방식으로 고치지 않는다
- 다른 키(Backspace·Tab 등)의 조합 중 동작 손대기
- 목록 기호 자동 정렬·번호 다시 매기기

## 2. 파일 소유

| 파일 | 변경 |
| --- | --- |
| `src/editor/createEditor.ts` | Enter 키 처리 순서·조합 판정 (원인에 따라) |
| `src/editor/composition.ts` | 조합 종료 처리 (원인에 따라) |
| `src/editor/listEnter.test.ts` | 신규. 조합 상태를 흉내낸 단위 테스트 |
| `e2e/listEnter.spec.js` | 신규. `F-245 A*` |

원인이 위 두 파일 밖(예: `blockCommands.ts`)으로 밝혀지면 **멈추고 보고한다** — 파일 소유를 늘리려면 명세를 고쳐야 한다

## 3. 요구사항

### 3.1 원인 조사 (구현 전 단계)

고치기 전에 다음을 확인해 보고에 적는다:

1. 조합 중/조합 직후 Enter 가 **몇 번** CM6 트랜잭션으로 들어오는가 (`EditorView.updateListener` 로 `tr.isUserEvent('input')`·`docChanged` 를 찍어 본다)
2. `insertNewlineContinueMarkdown` 이 그때 **불리는가, 건너뛰어지는가** (`markdownKeymap` 보다 먼저 `Prec.high(keymap.of(shortcutKeymap))`·`defaultKeymap` 중 누가 Enter 를 먹는지)
3. `compositionCatchup` 의 `setTimeout(0)` dispatch 가 Enter 트랜잭션과 겹쳐 들어오는지
4. `view.composing` 이 Enter keydown 시점에 무엇인지(`.workflow/architecture.md` 3장: compositionstart 시점의 `view.composing` 은 false 다)

### 3.2 고치기
- 조사 결과에 맞는 최소 수정. "Enter 를 한 번만, 목록 이어쓰기 키맵을 거쳐 처리한다" 가 목표다
- 조합 보류·따라잡기 규칙(CLAUDE.md 불변조건)은 유지한다. 따라잡기 신호가 문서를 바꾸는 일이 있어선 안 된다(지금 `forceRecalc` 는 effect 뿐이라 문서를 안 바꾼다 — 그 성질을 유지한다)

### 3.3 테스트 방법
- 실제 한글 IME 는 자동 판정 대상이 아니다(CLAUDE.md "구현 담당 서브에이전트 규칙"). 대신 **CDP `Input.imeSetComposition`** 으로 조합을 흉내내 e2e 로 고정한다:
  ```js
  const cdp = await page.context().newCDPSession(page)
  await cdp.send('Input.imeSetComposition', { text: '테스트', selectionStart: 3, selectionEnd: 3 })
  ```
- 이 방법이 실제 IME 와 다른 점(위 1장)을 테스트 파일 주석에 한 줄로 남긴다
- 실제 한글 IME 확인은 `specs/human-checks.md` 로 올린다

## 4. 수용 기준

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| A1 | 조합 경로 — 글머리 | e2e: `- ` 친 뒤 CDP 조합으로 `테스트` 넣고 Enter → `X` | 원문이 `- 테스트\n- X`. 빈 줄 없음 |
| A2 | 조합 경로 — 순서 목록 | 같은 방식, `1. ` | `1. 테스트\n2. X` |
| A3 | 조합 경로 — 체크박스 | `- [ ] ` | `- [ ] 테스트\n- [ ] X` |
| A4 | 조합 경로 — 중첩 | `  - ` (2단계) | 들여쓰기 유지한 채 다음 줄 `  - X` |
| A5 | 조합 경로 — 빈 항목에서 끝내기 | 목록 다음 줄에서 아무것도 안 치고 Enter | 지금 동작 그대로(기호 지우고 빈 줄), 조합 없이 친 경우와 같음 |
| A6 | 비조합 회귀 | 합성 키 입력만으로 A1~A5 | 지금처럼 정상(이미 통과하는 동작을 깨지 않았는지) |
| A7 | 원문 불변 | A1 결과 문서를 `.md` 로 내보내기 | 화면에서 본 줄 수와 같다(빈 줄이 몰래 들어가지 않는다) |
| A8 | 회귀 | `e2e/editor.spec.js`, `e2e/lineNumbers.spec.js`, `e2e/outline.spec.js` | 통과 |
| A9 | 타입·린트 | `npm run typecheck`, eslint | 통과 |
| A10 | 실제 한글 IME | 사람이 실제로 한글로 `- 테스트` 치고 엔터 | **사람 확인 항목** — CDP 흉내와 실제 IME 가 다를 수 있다 |

## 5. 구현 순서 (TDD)

1. 3.1 조사를 먼저 하고 결과를 보고에 적는다(고치기 전)
2. A1 을 e2e 로 쓰고 **지금 코드에서 실패하는 것을 확인**한다. 실패하지 않으면 재현 조건이 아직 안 맞는 것이니 멈추고 보고한다
3. 3.2 수정 → A1 통과
4. A2~A5 추가 → 통과
5. A6·A8 회귀

## 6. 재조사 — 진짜 원인은 IME 가 아니었다 (2026-09-18)

첫 구현(커밋 `711b686`)은 CDP `Input.imeSetComposition` 으로 재현한 것을 고쳤는데, 배포 확인에서 **실제 IME 에서는 그대로였다**(H110 실패). 사용자가 콘솔 로그와 문서 원문을 줬고, 그것으로 원인을 다시 잡았다.

### 6.1 IME 가 아니라는 근거

실제 한글 IME 의 이벤트 순서(사용자 제공, 운영 `rawdoc.app`):

```
compositionend key= type= data="트"
keydown key=Enter type= data=null
```

- `compositionend` 가 Enter `keydown` **앞**에 온다 → Enter 시점에 조합은 이미 끝났다
- Enter 뒤에 `beforeinput` 이 **하나도 없다** → 브라우저 기본 개행이 들어가지 않고, 이중 처리도 없다

즉 1장의 "조합 경로에서 Enter 가 두 번 반영된다" 는 가정이 틀렸다. 3.2 의 조합 중 Enter 가로채기는 실제 IME 경로에서 **아예 동작하지 않는다**(`view.composing` 이 false).

### 6.2 진짜 원인 — 항목 2개짜리 목록의 빈 항목 Enter

IME 없이 합성 키 입력만으로 100% 재현된다:

| 문서(커서는 끝) | Enter 결과 | 판정 |
| --- | --- | --- |
| `- 하나` + `- `(빈 항목, 항목 2개) | `- 하나` + **빈 줄** + `- ` | **틀림** |
| `- 하나` + `- 둘` + `- `(항목 3개) | `- 하나` + `- 둘` + 빈 줄 없이 목록 끝 | 맞음 |
| `- `(항목 1개) | 목록 끝 | 맞음 |
| `- [x] test` + `- [ ] `(항목 2개) | `- [x] test` + **빈 줄** + `- [ ] ` | **틀림** |

`insertNewlineContinueMarkup`(`@codemirror/lang-markdown`)은 **항목이 정확히 2개인 tight 목록**의 마지막 빈 항목에서 Enter 를 받으면 목록을 끝내지 않고 목록을 **loose(non-tight)** 로 바꾼다 — 그래서 빈 줄이 하나 생기고 기호가 남는다. 체크박스 여부와 무관하다(사용자가 `- [x]` 를 의심했지만 일반 글머리도 같다).

한 번 loose 가 되면 그 뒤 모든 Enter 가 loose 목록을 이어써서 **항목마다 빈 줄이 들어간다**:

```
- [x] 완료
(빈 줄)
- test
(빈 줄)
- |
```

이것이 사용자가 처음 보고한 "두 번 줄넘김" 의 실체다. 사용자 문서 원문도 정확히 이 모양이었다.

### 6.3 고치기
- **빈 목록 항목에서 Enter 를 치면 항목 수와 무관하게 목록을 끝낸다** — 기호를 지우고 빈 줄로 만든다(항목 1개·3개일 때 지금 되는 동작을 2개일 때도 같게)
- 목록을 loose 로 바꾸는 동작은 하지 않는다. 사용자가 직접 빈 줄을 넣어 loose 로 만든 목록을 이어쓰는 것은 그대로 둔다(이미 loose 인 목록에서 Enter 가 빈 줄을 유지하는 것은 마크다운으로 맞다)
- `insertNewlineContinueMarkup` 을 대체하지 않는다. **이 한 경우만** 앞에서 처리하고 나머지는 그대로 넘긴다
- 1장·3.2 의 조합 중 Enter 가로채기(`handleComposingEnter`)는 **그대로 둔다** — `view.composing` 이 true 인 환경(다른 IME·플랫폼)에서만 동작하고, 지금 A1~A7 e2e 가 그 경로를 지키고 있다. 6.3 의 새 처리는 조합 여부와 무관하게 같은 결과를 내야 한다

### 6.4 소유 파일

| 파일 | 변경 |
| --- | --- |
| `src/editor/listEnter.ts` | 신규. 빈 목록 항목 Enter 처리 |
| `src/editor/listEnter.test.ts` | 기존 파일에 6.5 단위 기준 추가 |
| `src/editor/createEditor.ts` | 새 처리를 `markdownKeymap` 보다 앞선 우선순위로 |
| `src/editor/composition.ts` | 조합 경로도 같은 처리를 거치게(필요하면) |
| `e2e/listEnter.spec.js` | `F-245 A11~A16` 추가 |

### 6.5 수용 기준 (추가)

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| A11 | 항목 2개 — 빈 항목 Enter | `- 하나` + `- ` 끝에서 Enter → `X` | `- 하나\nX`. **빈 줄 없고 기호도 없다** |
| A12 | 항목 2개 — 체크박스 | `- [x] test` + `- [ ] ` 끝에서 Enter → `X` | `- [x] test\nX` |
| A13 | 고치기 전 실패 확인 | A11·A12 를 고치기 전 코드에서 | 실패한다(빈 줄 + 기호). 실패를 보고에 적는다 |
| A14 | 항목 1개·3개 회귀 | `- ` / `- 하나\n- 둘\n- ` | 지금 동작 그대로(목록 끝남) |
| A15 | ~~이미 loose 인 목록~~ | — | **F-253 으로 대체(2026-09-18).** loose 여도 빈 줄을 새로 넣지 않는다 |
| A16 | 순서 목록·중첩 | `1. 하나\n2. ` / `  - 하나\n  - ` | 각각 목록이 끝난다 |
| A17 | 조합 경로 회귀 | 기존 A1~A7(CDP 조합) | 통과 |
| A18 | 회귀 | `e2e/editor.spec.js`·`e2e/listView.spec.js`·`listEnter.test.ts` | 통과 |
| A19 | 원문 불변 | A11 결과를 `.md` 로 내보내기 | 화면에서 본 줄 수와 같다 |
| A20 | 실제 한글 IME | 사람이 실제로 한글로 치고 Enter | **사람 확인 항목** (H110 재확인) |

### 6.6 구현 순서 (TDD)
1. A11·A12 를 단위·e2e 로 쓰고 **고치기 전 실패 확인**(A13)
2. 6.3 구현 → A11·A12 통과
3. A14·A15·A16 으로 나머지 경우가 안 바뀌었는지
4. A17·A18 회귀
