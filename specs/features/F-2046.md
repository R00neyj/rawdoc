---
id: F-2046
title: 옵시디언 볼트와 오가기 사용법 글
milestone: M2
status: approved
created: 2026-09-27
approved: 2026-09-27
depends: [F-2039, F-2016, F-2017, F-2018, F-2019, F-2020, F-281, F-282, F-409, F-257]
---

# F-2046 옵시디언 볼트와 오가기 사용법 글

작성일: 2026-09-27
상태: 승인(2026-09-27). 따로 검토하지 않았고 상시 지시로 승인했다 — 열린 질문(9장)은 모두 기본값이 있다 — 사람이 따로 고르지 않으면 기본값대로 구현한다(CLAUDE.md "A spec that leaves no real fork goes straight to implementation"). 사람 판단이 걸린 것은 Q7(설정 화면 설명 글자, tweak 후보) 하나이고, 이 명세의 구현을 막지 않는다
범위: `specs/features/F-2039.md` 10장 "후속 사용법 글 후보" 둘째 행 "옵시디언 볼트와 오가기". `specs/ia.md` 6.1 R9(사용법 글 한 편 = 명세 한 개)에 따라 **글 1편(`content/guides/obsidian-vault.md`) + 도움말 `## 내보내기·가져오기` 절 고쳐 쓰기와 새 `## 옵시디언 볼트` 절(그 절 끝 `사용법 글:` 줄) + `markdown-portability.md` 표 한 칸에 이 글로 가는 링크 + 수치 대조 테스트**가 이번 구현 단위다
범위 확인: `specs/product.md` 7장 "공개 사이트" 표에 F-276·F-2039·F-2045 가 있고, 8장(범위 밖)에 사용법 글이나 옵시디언 볼트를 빼는 문장이 없다(2026-09-27 grep `사용법`·`옵시디언`·`볼트`). 원천 명세가 모두 `done` 이고 구현 커밋이 모두 `origin/deploy`(`fedc340`)의 조상이다 — 곧 **글에 쓰는 기능은 모두 사용자에게 나가 있다**(m10)

사용자가 이미 정한 것(바꾸지 않는다, 2026-09-27 main 전달):

| # | 결정 | 이 명세 |
| --- | --- | --- |
| 1 | 글은 **한 편**, slug `obsidian-vault`(F-2039 12장 Q1 이 `vault` 를 금고에 쓰지 않은 이유와 맞물림) | 3장 |
| 2 | 형식 모델은 F-2045 — 사실 표, 수치 대조, U*/E*, 소유 표, 사람 확인, 글 전문을 명세 안에, 스크래치 실측 | 전체 |
| 3 | 이 명세 → 바로 구현(ship-feature). 갈림길마다 기본값 | 9장 |
| 4 | 표기는 도움말·설정 화면과 같은 `옵시디언`. 영어 `Obsidian` 은 글 머리·요약에 한 번 괄호로 병기(`markdown-portability.md` 가 `Obsidian` 으로 쓰기 때문) | 3장, 9장 Q4 |
| 5 | 금고 문서의 동작은 짧게 쓰고 금고 글(`/guides/encryption`)로 링크(R8) | 3장 "옮겨지지 않는 것" 표 한 행 |
| 6 | 명세와 코드가 다르면 코드를 따르고 갱신 대상에 적는다 | 4.8, 1.1 |

근거(2026-09-27 HEAD `77a2157` 에서 읽은 것. 작업 트리에 다른 변경 없음 — `git status` 는 `.playwright-mcp/` 하나):
명세: `specs/ia.md` 6장 표·6.1 R1~R9, `F-2045.md` 전문(형식 모델), `F-2039.md` 10장·12장, `F-2016.md` 1~6장, `F-2019.md` 2~4장·7.1·7.6~7.8·10.4, `F-2020.md` 2~4장·6.5·14.2, `F-409.md` 5장, `F-257.md` 2장 표, `specs/product.md` 7·8장, `specs/human-checks.md` H345~H348·H509~H514, `content/guides/*.md` 8편 전문.
코드: `src/app/exportVault.ts`(`planVaultExport`·`selectExportScope`·`planExportPaths`·`toVaultMarkdown`·알림 문구), `src/app/importVault.ts`(`scanVault`·`defaultVaultTarget`·`vaultTargetOptions`·`planVaultImport`·`applyVaultImport`·경고 문구), `src/app/importWorkspace.ts`(`MAX_CONTENT_BYTES`·`MAX_ATTACHMENT_BYTES`·`isNoiseEntry`·`titleFromPath`), `src/storage/toWebp.ts`, `src/lib/filename.ts`(`OBSIDIAN_FORBIDDEN_CHARS`·`toObsidianFileName`·`toObsidianFolderName`), `src/app/exportWorkspace.ts`(`resolveSiblingNames`), `src/lib/obsidianImage.ts`(`findVaultEmbeds`), `src/lib/wikiLink.ts`, `src/app/App.tsx`(`handleExportVault`·`handleExportFolderVault`·`exportOffline`·`previewImportZip`·`confirmVaultImport`·끌어 놓기 알림), `src/app/SettingsDialog.tsx` 데이터 탭, `src/app/ImportPreviewDialog.tsx`, `src/app/Sidebar.tsx` 폴더 메뉴, `src/app/ExportMenu.tsx`, `src/app/helpDoc.ts` `APP_SECTIONS`.
테스트: `src/app/helpDoc.test.ts` 전문, `site/helpGuides.test.ts` 전문, `site/pageNav.test.ts` F-2036 A1·A2, `site/render.test.ts` F-2036 A6, `e2e/site.spec.js` 전문, `e2e/help.spec.js`, `e2e/exportVault.spec.js`·`e2e/importWorkspace.spec.js`·`e2e/e2eeSearchExport.spec.js` 의 옵시디언 항목

**직접 돌리거나 재어 확인한 것** (2026-09-27, 이 PC, Node v24. 스크립트는 스크래치패드 `f2046/check.ts`·`build.mjs`·`patch.mjs` 에만 있다 — esbuild 로 저장소의 `site/`·`src/` 모듈을 그대로 묶고, `helpDoc.ts` 의 `HELP_DOC_CONTENT` 만 5장 after 로 바꿔 끼우며 `?raw`·`.css` 가져오기만 흉내. 저장소 무변경)

| # | 무엇 | 결과 |
| --- | --- | --- |
| m1 | 지금 도움말 `## 내보내기·가져오기` 절 본문(절 제목·링크 줄 뺀, 코드 포인트) | **379자, 3문단 + 링크 줄**(`0d56037` tweak 이 단 `markdown-portability` 링크). 셋째 문단이 볼트 가져오기를 한 문장으로만 말하고 `넣을 폴더`·zip 가져오기 경로·지운 파일 처리는 없다. 첫 문단의 `` `.md 내보내기` `` 는 상단바 메뉴의 실제 글자(`ExportMenu.tsx` 항목 `.md`, 메뉴 이름 `내보내기`)와 다르고, `불러오면` 은 ia.md 6장 쓰지 않는 말이다(4.8, 11장 C3) |
| m2 | 5장 after 두 절 | `## 내보내기·가져오기` **200자, 2문단** + 링크 줄. `## 옵시디언 볼트` **391자, 3문단** + 링크 줄(R2 상한 600·4). 두 절 모두 마지막 문단이 R3 링크 줄 하나 |
| m3 | 도움말 제목 수 | 원래 빌드 41개(기존 테스트 기대값과 같음 — 기준선 확인), 5장 after 로 바꾼 빌드 **42개**. `render.test.ts` 방식(`renderSitePage` 목차 `li`)으로도 42 |
| m4 | 3장 글 초안 | 본문 **4,627자**, `##` **8개**, 93줄(R7 상한 6,000·10). `checkContentFile('guides/obsidian-vault.md', …)` 통과(코드 블록 안 `![[…]]` 도 없음 — 인라인 코드로만 씀). `renderSitePage` 결과 HTML 25,616 B, `<script` 없음, `<table>` 2, `<h2>` 8. 인라인 코드 안 `[[…]]` 는 위키링크로 바뀌지 않고 글자 그대로 나온다 |
| m5 | 3장 초안 + 지금 `content/guides/*.md` 8편 + 5장의 `markdown-portability.md` + `changelog.md`·`legal/*.md` 로 `buildSite` | 통과. `guides/obsidian-vault.html` 이 생기고 `sitemap.xml` `<loc>`·`llms.txt` 줄에 들어간다. `/guides` 목록 첫 링크는 `/guides/obsidian-vault`(`sharing` 과 `date` 가 같아 url 오름차순). `/help` 에 새 링크가 있고, 링크 줄 HTML 에 `target="_blank"` |
| m6 | R4(U5 방법) — 20자 이상 문장이 반대쪽에 그대로 있나 | `## 옵시디언 볼트` 6문장 ↔ 초안 55문장: **양방향 0**. `## 내보내기·가져오기` 3문장 ↔ `markdown-portability.md` 28문장: **양방향 0**. 기존 쌍(금고·위키링크·템플릿·공유)도 0 그대로 |
| m7 | 초안과 다른 글 사이 20자 이상 문장 겹침(`markdown-portability`·`encryption`·`wiki-links`·`command-palette-templates`) | **0** — 링크로 가리키기만 한다. 지금 도움말 셋째 문단(옛 문장) ↔ 초안도 0 |
| m8 | 초안·두 절에 `helpGuides.test.ts` `FORBIDDEN_WORDS` + ia.md 6장 쓰지 않는 말(`불러오기`·`불러오`·`업로드`·`다운로드`·`페이지`·`노트`·`미리보기`·`vault`·`보관함`·`저장소`, 제품명) | **0개** |
| m9 | 초안의 수치를 상수에서 만든 문자열과 대조 | `${MAX_CONTENT_BYTES / 1_000_000}MB를 넘는 문서` → `1MB를 넘는 문서`, `한 장에 ${MAX_ATTACHMENT_BYTES / 1024 / 1024}MB를 넘는 그림` → `한 장에 5MB를 넘는 그림` — **둘 다 초안에 있다.** `src/app/importWorkspace.ts` 는 `fflate`·`decodeMarkdown`·`imageFile` 만 가져와 node 에서 부수효과 없이 import 된다(`importWorkspace.test.ts` 가 이미 node 에서 돈다) |
| m10 | 원천 명세가 사용자에게 나갔나 | `git merge-base --is-ancestor` — F-2017 `b2f332c`, F-2018 `d1063ab`, F-2019 `c767dad`, F-2020 `b0af139`, F-281 `6869f98`, F-282 `a79a3ac`, F-409 `759f6b7` 모두 `origin/deploy`(`fedc340`, 2026-09-27 00:19) 조상. `fedc340..HEAD` 에 볼트 코드 변경 없음. **빼야 할 미배포 기능 없음**(로컬 `origin/deploy` 참조로 판정) |
| m11 | 왕복 실측 — `planVaultExport` → zip → `scanVault` → `planVaultImport`(메모리 저장소, 문서 4개·폴더 2개·PNG 1개, 같은 이름 `회의록` 이 두 폴더에) | 아래 4.9 표. 전체 내보내기 zip 을 되가져올 때 `넣을 폴더` 처음 값이 `새 폴더 "2026-09-27"` 이라 **그대로 두면 4개 모두 새로 생긴다**는 것을 여기서 찾았다 — 글 "옵시디언에서 고친 뒤 다시 가져오기" 마지막 문단의 근거 |

단위 테스트(2026-09-27 HEAD): `importVault`·`exportVault`·`obsidianImage`·`helpGuides`·`helpDoc` 테스트 5파일 152개 통과 — 이 명세가 기대는 코드 동작이 지금 초록임을 확인

---

## 1. 파일 소유

| 파일 | 변경 |
| --- | --- |
| `content/guides/obsidian-vault.md` (신규) | 옵시디언 볼트 글. **3장 전문을 그대로** 넣는다 |
| `content/guides/markdown-portability.md` | 두 곳만 — frontmatter `updated` 를 구현 커밋 날짜로, "옮겨 갈 곳별로 알아 둘 것" 표 `Obsidian` 행 셋째 칸 끝에 링크 문장 하나(5.3). 다른 줄은 건드리지 않는다 |
| `src/app/helpDoc.ts` | `APP_SECTIONS` 의 `## 내보내기·가져오기` 절을 5장 after 로 통째 바꾸고, 그 바로 뒤(`## 설치와 오프라인` 앞)에 새 `## 옵시디언 볼트` 절을 넣는다. 다른 절은 건드리지 않는다 |
| `src/app/helpDoc.test.ts` | `SECTION_ORDER` 에 `'옵시디언 볼트'` 를 `'내보내기·가져오기'` 뒤에 더한다. 6.1 U1 추가 |
| `site/helpGuides.test.ts` | 6.2 U2~U4 추가. 기존 테스트는 고치지 않는다 |
| `site/pageNav.test.ts` | F-2036 A2 의 도움말 제목 수 41 → 42 (테스트 이름 `'도움말 41개, …'` 의 숫자도) |
| `site/render.test.ts` | F-2036 A6 의 목차 항목 수 41 → 42 |
| `e2e/site.spec.js` | `F-2046 E1`~`E3` 추가. 기존 테스트는 건드리지 않는다 |

- `site/**` 소스·`vite.config.ts`·`src/app/HelpPage.tsx`·`src/viewer/**`·볼트 코드(`exportVault.ts`·`importVault.ts`·`importWorkspace.ts`) **변경 없음.** 글 하나를 더하면 목록·주소·`sitemap.xml`·`llms.txt`·왼쪽 문서 목록(F-2036)이 저절로 따라온다(m5). 링크 줄의 새 탭 속성은 `renderMarkdown` `link_open`(F-123)이 붙인다
- `src/app/importWorkspace.ts` 는 테스트가 두 상수를 **읽기만** 한다
- 도움말 목차는 새 절 제목 하나만큼 늘어난다(m3). 새 id 는 `headingSlug('옵시디언 볼트')` = `옵시디언-볼트` — 기존 id 와 겹치지 않는다
- 새 의존성·저장소 식별자·설정 키 없음
- 줄 끝은 LF. `markdown-portability.md` 는 저장소에 LF 로 들어 있고 이 PC 작업 트리에서만 CRLF 로 보인다(autocrlf) — 편집기가 CRLF 로 되돌려 커밋하지 않게 한다(11장 C4)

### 1.1 갱신 대상 — 이번에 고치지 않는다

메인이 반영한다. 구현 에이전트는 건드리지 않는다.

| 파일 | 위치 | 반영할 내용 |
| --- | --- | --- |
| `specs/product.md` | 7장 "공개 사이트" 표, F-2045 행 아래 | `F-2046 | 옵시디언 볼트와 오가기 사용법 글 | /guides/obsidian-vault, 도움말 내보내기·가져오기 절 고쳐 쓰기와 새 옵시디언 볼트 절, markdown-portability 표에서 링크` |
| `specs/features/F-2039.md` | 10장 둘째 행 "옵시디언 볼트와 오가기" | 메모 칸 끝에 "→ F-2046 (`/guides/obsidian-vault`)". 같은 행 "F-2019 볼트 가져오기(지금 `approved`, 구현 중)" 는 `done`(`c767dad`)으로. 이어질 도움말 절 칸은 `## 옵시디언 볼트`(새 절)로 |
| `specs/features/F-257.md` | 2장 절 표 `내보내기·가져오기` 행 | 내용 칸을 "상단바 `내보내기` 메뉴의 `.md`, 사이드바 `가져오기`, 설정 `전체 내보내기`·`가져오기…` — 사용법 글 링크(`markdown-portability`)" 로. 근거 칸에 F-281·F-282 |
| `specs/features/F-257.md` | 2장 절 표, 위 행 바로 아래 새 행 | `옵시디언 볼트 | 볼트로 내보내기(설정·폴더 ⋯), 가져오는 세 길, 넣을 폴더, 다시 가져오기의 맞춤·(가져오기 전) — 사용법 글 링크 (F-2046) | F-2019·F-2020` |
| `specs/features/F-2019.md` | 10.4(도움말 문단) | 도움말의 볼트 가져오기 문단이 F-2046 에서 `## 옵시디언 볼트` 절로 옮겨졌다는 한 줄 |
| `specs/features/F-2020.md` | 6.5(도움말 문단) | 같은 이유로 볼트 내보내기 문단이 `## 옵시디언 볼트` 절로 옮겨졌다는 한 줄 |
| `specs/features/F-2019.md` | 7.1 기본값 | "정확히 같은(대소문자 구분)" → 코드는 NFC + 소문자로 접어 **대소문자를 가리지 않는다**(`importVault.ts` `defaultVaultTarget`). 11장 C1 |
| `specs/features/F-2016.md` | 4.2 기본값 | 같은 내용 한 줄. 11장 C1 |
| `specs/human-checks.md` | 새 절 | 8장 H1~H4 |
| `specs/ia.md` | — | **이번에는 고치지 않는다.** 6장 문자열 표에 새 화면 글자가 없다. 설정 화면 설명의 `불러옵니다`(11장 C2)는 9장 Q7 에서 사람이 고를 때까지 기록만 한다 |

확인한 결과 **고칠 것이 없는 문서**: `specs/design.md`(새 시각 요소 없음 — 표·목록·굵게만), `specs/architecture.md`(모듈·키 변화 없음), `F-276.md`(목록 문구가 주제를 나열하지 않는다 — `site/guidesIndex.ts` `guidesIndexContent`), `F-2045.md`(공유 글은 볼트를 말하지 않는다)

## 2. 글 — 목차와 판단

| 절 | 무엇을 | 왜 이 자리 |
| --- | --- | --- |
| (머리) | 내보내기·가져오기 두 방향, 한 번 옮기기와 오가기, 단서는 경로 하나. 손대지 않는 문법과 바뀌는 셋. `markdown-portability` 링크 | "무엇이 그대로이고 무엇이 바뀌나" 가 이 글을 찾는 첫 물음 |
| 볼트로 내보내기 | 두 자리(설정 전체·폴더 `⋯`), 로그인 시 내 문서만·온라인만, zip 구조, 파일 이름 규칙, ` (2)`, `manifest.json` 없음·빈 폴더 없음 | 순서대로 첫 단계 |
| 옵시디언에서 열기 | 이미지 임베드 모양, 고치는 링크와 고치지 않는 링크, 알림의 수 | 옵시디언 쪽에서 무엇이 보이는지 |
| 볼트를 가져오기 | 세 길, `가져오기` 창의 수, `넣을 폴더` 처음 값, 볼트 이름, 제목 규칙, 무시하는 파일 | 반대 방향 첫 단계 |
| 옵시디언에서 고친 뒤 다시 가져오기 | 경로 맞춤, 대소문자, 여섯 경우 표, 사본, **전체 내보내기 zip 을 되가져올 때 `넣을 폴더`** | 오가기의 핵심. m11 에서 찾은 함정을 여기 적는다 |
| 이미지는 이렇게 바뀝니다 | 한 줄을 혼자 차지한 이미지만, 형식·5MB, 새 이름·WebP, 되돌아온 그림 | 양방향 이미지 변환을 한곳에 |
| 위키링크는 이렇게 맞춥니다 | 되돌리는 규칙, 건너뜀이 되는 이유와 예외, 경로·제목 링크, 별칭 접기, 블록 링크 | 양방향 링크 변환을 한곳에 |
| 옮겨지지 않는 것 | 문서 임베드·태그·각주·`aliases`·캔버스·댓글·공유받은 문서·금고·1MB | 사용자가 잃을 수 있는 것을 표로 |
| 정리 | 다섯 줄 | 다른 글과 같은 끝맺음 |

- **slug `obsidian-vault`**(결정 1)·**제목 `옵시디언 볼트와 오가기`**(9장 Q2): F-2039 10장 행 이름 그대로. "오가기" 가 한 번 옮기기와 되가져오기를 함께 담는다
- **`markdown-portability.md` 와 나누기**(9장 Q3): 그 글은 "문서 하나를 `.md` 로 받아 다른 도구로" 가 주제이고 옵시디언은 표 한 행이다. 볼트 내보내기·가져오기의 자세한 동작은 모두 이 글이 맡는다. 그 글에서 내용을 **빼지 않는다** — 지금 행의 문장("`옵시디언 볼트로 내보내기`를 씁니다")은 맞고, 그 뒤에 이 글로 가는 링크 문장만 붙인다(R8). 이 글 머리에서도 그 글을 링크한다. 두 글 사이 20자 이상 문장 겹침 0(m7)
- **금고**(결정 5): "옮겨지지 않는 것" 표 한 행 — 열림/잠김 두 경우와, 잠긴 금고 문서와는 맞춰 보지 않는다는 것만. 자세한 것은 `encryption.md` "내보내기" 행이 이미 말한다
- **`넣을 폴더` 경고 문단**: 전체 내보내기 zip 의 이름이 `{날짜}-vault.zip` 이라 볼트 이름이 날짜가 되고(`scanVault` 가 끝 `-vault` 를 뗌), 그 이름의 최상위 폴더가 없으니 처음 값이 `new` 가 된다(m11). 코드 동작이 맞고 글로 알려 줄 일이다. 폴더 하나를 내보낸 zip(`{폴더 이름}-vault.zip`)은 처음 값이 그 폴더라 문제가 없다
- 글에 **넣지 않은 것**과 이유
  - 파일 이름 100자 제한(`filename.ts` `MAX_CODE_POINTS`)·그림 새 이름의 16자리: 두 상수가 **export 되지 않은 모듈 지역 상수**라 R5 대조를 걸 수 없고, 사용자가 판단에 쓰는 수치가 아니다. 글은 "영문과 숫자로 된 새 이름" 으로만 쓴다(9장 Q6)
  - 서버 쪽 한도(이미지 공간 300MB·문서 수·하루 쓰기 수, `worker/`): 가져오기 한 번이 몇 번의 쓰기로 세어지는지 재지 않았다. 창의 공간 경고(`importVault.ts` 경고 목록 끝)가 알려 준다(9장 Q6)
  - zip·폴더 이름 끝 `-vault` 를 떼는 규칙: `vault` 가 ia.md 6장 쓰지 않는 말이고, 사용자는 `넣을 폴더` 처음 값으로 결과를 본다(9장 Q8)
  - 끌어 놓기가 막히는 다른 경우(파일과 폴더를 섞음 등)와 창 `취소`: 창의 알림 글자로 충분하다. 여러 폴더를 한 번에 놓을 때만 글에 적었다(`폴더는 한 번에 하나만 가져올 수 있습니다.`)
  - 옵시디언 자체의 동작(볼트 열기 화면, 링크 설정 "새 링크 형식" 등): 이 앱 코드가 아니라 확인할 수 없다. 글은 "그 폴더를 볼트로 엽니다" 까지만 쓴다(10장 가정 1)
  - 가져오기 경고 문구 전부: 글은 사용자가 가장 자주 볼 `볼트에 없어 그대로 둔 문서 {N}개` 하나만 글자로 쓰고, 나머지는 "창에 개수를 알려 줍니다" 로 쓴다

## 3. 글 전문 — `content/guides/obsidian-vault.md`

**아래를 그대로 파일로 만든다.** 구현하는 쪽이 문장을 새로 짓지 않는다. 문장의 근거는 4장 표에 있다. `date`·`updated` 는 구현 커밋 날짜가 2026-09-27 이 아니면 그 날짜로 두 줄 모두 바꾼다(R7). "옵시디언에서 고친 뒤 다시 가져오기" 마지막 문단의 `새 폴더 "2026-09-27"` 는 예시 날짜라 바꾸지 않는다. 줄 끝은 LF.

````markdown
---
title: 옵시디언 볼트와 오가기
summary: 문서를 옵시디언(Obsidian) 볼트로 내보내고, 옵시디언에서 고친 볼트를 다시 가져올 때 무엇이 맞춰지고 무엇이 바뀌는지.
date: 2026-09-27
updated: 2026-09-27
---

# 옵시디언 볼트와 오가기

이 앱의 문서와 폴더는 옵시디언(Obsidian) 볼트로 내보낼 수 있고, 옵시디언 볼트를 이 앱으로 가져올 수도 있습니다. 한 번 옮기고 끝내도 되고, 옵시디언에서 고친 볼트를 다시 가져와 이 앱의 문서를 새 내용으로 바꿔도 됩니다. 두 쪽을 잇는 단서는 **폴더 경로와 파일 이름** 하나입니다.

콜아웃·`==하이라이트==`·`%%주석%%`·체크박스·문서 맨 위 속성은 오갈 때 손대지 않습니다. 바뀌는 것은 이미지, 일부 위키링크, 파일 이름 셋입니다. 문서 하나를 `.md`로 받거나 다른 도구로 옮기는 이야기는 [한 번 쓴 글을 다른 도구로 옮기기](/guides/markdown-portability)에 있습니다.

## 볼트로 내보내기

설정 `데이터` 탭의 `옵시디언 볼트로 내보내기`는 문서 전부를, 사이드바 폴더 `⋯` 메뉴의 같은 항목은 그 폴더와 하위 폴더를 zip 하나로 받습니다. 로그인해서 쓰면 내 문서만 들어가고 공유받은 문서는 빠지며, 인터넷에 연결되어 있을 때만 내보낼 수 있습니다.

- zip 안에는 사이드바 폴더 구조대로 `.md` 파일이 들어 있고, 이미지는 맨 위 `attachments` 폴더 한 곳에 모입니다
- 문서 제목이 곧 파일 이름입니다. 본문에 제목 줄을 더하지 않습니다
- 옵시디언 링크에서 뜻이 있는 `#` `^` `[` `]`와 파일 이름에 쓸 수 없는 `/` `:` `?` 같은 글자는 `_`로 바뀝니다. `왜?`라는 문서는 `왜_.md`가 됩니다
- 한 폴더에 같은 제목이 둘이면 나중에 만든 문서 이름 뒤에 ` (2)`가 붙습니다
- 전체 내보내기 zip에 있는 목록 파일 `manifest.json`은 넣지 않고, 문서가 하나도 없는 폴더는 만들지 않습니다

## 옵시디언에서 열기

받은 zip을 풀고, 옵시디언에서 그 폴더를 볼트로 엽니다.

- 이미지 자리에는 `![[0123456789abcdef.png|320]]`처럼 옵시디언의 이미지 넣기 문법이 한 줄로 들어갑니다. `|` 뒤 숫자는 이 앱에서 정한 폭입니다. 가운데·오른쪽 정렬과 이미지 설명은 옵시디언에 같은 문법이 없어 빠집니다
- 볼트 안에서 한 문서로 정해지지 않거나 이름이 바뀐 문서를 가리키는 위키링크만 고칩니다. 같은 이름 문서가 볼트에 둘 이상이면 `[[교안/회의록|회의록]]`처럼 폴더 이름을 붙이고, 이름이 바뀐 파일로 가는 링크는 `[[왜_|왜?]]`처럼 바뀐 이름을 가리킵니다. 원래 글자는 `|` 뒤 별칭으로 남깁니다. 표 안 링크에는 별칭을 붙이지 않습니다
- 고친 링크가 있으면 내보낸 뒤 알림 끝에 그 수가 붙습니다
- 볼트 밖 문서를 가리키는 링크(폴더 하나만 내보냈을 때 그 폴더 밖 문서)와 원래 끊긴 링크는 고치지 않습니다

## 볼트를 가져오기

가져오는 길은 셋이고, 모두 같은 `가져오기` 창으로 이어집니다.

- 설정 `데이터` 탭 `폴더 가져오기…`에서 볼트 폴더를 고릅니다
- 탐색기나 Finder에서 볼트 폴더를 앱 창에 끌어 놓습니다. 폴더는 한 번에 하나만 받습니다
- 볼트를 압축한 zip은 같은 탭의 `가져오기…`로 고릅니다. zip 안에 `manifest.json`이 있으면 이 앱의 전체 내보내기로 보고, 없으면 볼트로 봅니다

창에는 새로 만들 문서, 갱신할 문서, 건너뛸 문서, 들일 이미지의 수가 먼저 나옵니다. `넣을 폴더`에서 볼트를 들일 자리를 바꾸면 수가 곧바로 다시 계산됩니다. 처음 값은 볼트 이름과 같은 최상위 폴더가 하나 있으면 그 폴더이고, 없으면 `새 폴더 "{볼트 이름}"`입니다. 볼트 이름은 고른 폴더나 zip의 이름에서 정합니다. 볼트의 폴더 구조는 고른 자리 아래에 깊이 그대로 만들어집니다.

- `.md`·`.markdown` 파일이 문서가 되고, 파일 이름에서 확장자를 뗀 것이 제목입니다. 문서 맨 위 속성의 `title`은 제목으로 쓰지 않습니다
- 이름이 `.`으로 시작하는 폴더와 파일(`.obsidian`·`.trash` 등)은 보지 않습니다
- 문서도 이미지도 아닌 파일(PDF·캔버스 등)은 들이지 않고 창에 개수만 알려 줍니다. 어느 문서도 쓰지 않는 이미지 파일도 들이지 않습니다

## 옵시디언에서 고친 뒤 다시 가져오기

같은 볼트를 다시 가져오면 `넣을 폴더` 아래에서 폴더 경로와 파일 이름이 같은 문서를 찾아 맞춥니다. 볼트에는 이 앱의 문서 번호가 없어서 경로가 유일한 단서입니다. 대소문자는 가리지 않고, 이 앱 문서의 이름은 내보낼 때와 같은 규칙(`왜?` → `왜_`)으로 바꿔서 비교합니다.

| 볼트 쪽 | 이 앱에서 |
| --- | --- |
| 내용이 같은 파일 | 건너뜁니다. 아무것도 바꾸지 않습니다 |
| 내용이 바뀐 파일 | 바뀌기 전 내용을 같은 폴더에 `{제목} (가져오기 전)` 문서로 남기고 새 내용으로 갱신합니다. 문서 주소와 읽기 전용 링크는 그대로입니다 |
| 새로 만든 파일 | 같은 경로에 새 문서를 만듭니다 |
| 이름을 바꾸거나 다른 폴더로 옮긴 파일 | 새 자리에 새 문서가 생기고, 옛 문서는 그대로 남습니다 |
| 지운 파일 | 이 앱의 문서를 지우지 않습니다 |
| 대소문자만 바꾼 이름 | 제목을 볼트 쪽 이름으로 바꾸고 갱신으로 셉니다 |

볼트에 없어 손대지 않은 문서가 있으면 창에 `볼트에 없어 그대로 둔 문서 {N}개`가 뜹니다. `(가져오기 전)` 사본도 볼트에는 없으니 다음 가져오기 때 여기에 들어갑니다. 필요 없어진 사본은 직접 지웁니다.

이 앱에서 내보낸 볼트를 되돌릴 때는 `넣을 폴더`를 확인합니다. 처음 값이 원래 넣어 두었던 폴더가 아니면 목록에서 그 폴더를 고릅니다. 전체를 내보낸 볼트는 처음 값이 `새 폴더 "2026-09-27"`처럼 날짜 이름일 수 있는데, 그때는 `최상위`로 바꿔야 원래 문서와 맞춰집니다. 그대로 두면 모든 문서가 새 폴더 안에 새로 생깁니다.

## 이미지는 이렇게 바뀝니다

가져올 때는 **한 줄을 혼자 차지한** 이미지만 이 앱의 이미지로 바꿉니다. `![[그림.png]]` 같은 옵시디언 문법이든 마크다운 이미지 문법이든 같습니다. `|` 뒤 숫자는 폭이 되고, 글자는 이미지 설명이 됩니다. 문장 가운데나 목록 안에 있거나 한 줄에 여럿 있는 이미지는 원문 그대로 두고 창에 개수를 알려 줍니다. 인터넷 주소로 건 이미지는 바꾸지 않습니다.

- PNG·JPEG·GIF·WebP만 이미지로 들입니다. SVG 같은 다른 형식과 한 장에 5MB를 넘는 그림은 원문 그대로 둡니다
- 들인 그림은 영문과 숫자로 된 새 이름을 받습니다. 로그인해서 쓰면 PNG·JPEG는 WebP로 바꿨을 때 더 작으면 WebP로 저장합니다. 그래서 다시 내보낸 볼트에는 옵시디언에서 붙였던 원래 파일 이름이 남지 않습니다
- 이 앱에서 내보냈던 그림은 같은 이름으로 돌아와 이미 있으면 다시 올리지 않고, 정렬과 이미지 설명도 원래 값을 되찾습니다

## 위키링크는 이렇게 맞춥니다

가져올 때는 내보낼 때 붙였던 경로와 바뀐 이름을 걷어 원래 모양으로 되돌립니다. 이 앱에서 다른 문서로 가거나 끊길 링크만 고치고, 이 앱에서도 같은 문서로 가는 링크는 그대로 둡니다. 그래서 이 앱에서 내보낸 볼트를 손대지 않고 되가져오면 문서는 건너뜀으로 셉니다. 다만 따로 별칭을 달아 둔 링크에 경로가 붙었던 문서는 처음 한 번 갱신으로 셉니다.

- 옵시디언에서 쓴 `[[폴더/문서]]` 경로 링크와 `[[문서#제목]]` 제목 링크는 이 앱에서도 그대로 이어집니다. 쓰는 법은 [위키링크로 문서 잇기](/guides/wiki-links)에 있습니다
- 별칭이 이 앱에서 가리킬 이름과 같으면 별칭을 걷어 냅니다. `[[교안/회의록|회의록]]`이 이 앱에서 `[[회의록]]`만으로 같은 문서를 가리키면 `[[회의록]]`이 됩니다
- 블록을 가리키는 `[[문서#^abc123]]`은 그 문서만 엽니다

## 옮겨지지 않는 것

| 무엇 | 어떻게 되나 |
| --- | --- |
| 이미지가 아닌 `![[…]]`(문서·PDF 등) | 원문 그대로 두고 창에 개수를 알려 줍니다 |
| `#태그`, 각주, `aliases` 속성 | 원문 그대로 둡니다. `aliases`에 적은 다른 이름으로는 위키링크가 이어지지 않습니다 |
| 캔버스처럼 `.md`가 아닌 파일 | 들이지 않습니다 |
| 댓글 | 내보내는 파일에 들어가지 않습니다 |
| 공유받은 문서 | 내보내지 않고, 다시 가져올 때 맞춰 보지도 않습니다 |
| 금고 문서 | 열려 있으면 암호화되지 않은 파일로 나가고, 잠겨 있으면 빠집니다. 잠긴 금고 문서와는 맞춰 보지 않으니 금고 문서가 든 폴더로 다시 가져올 때는 금고를 먼저 엽니다. 자세한 것은 [금고로 문서 암호화하기](/guides/encryption)에 있습니다 |
| 로그인해서 쓸 때 1MB를 넘는 문서 | 건너뛰고 창에 개수를 알려 줍니다 |

## 정리

- 내보내기는 설정 `데이터` 탭(전체)이나 폴더 `⋯` 메뉴(폴더 하나)의 `옵시디언 볼트로 내보내기`입니다
- 가져오기는 `폴더 가져오기…`, 폴더 끌어 놓기, zip `가져오기…` 셋이고, 들이기 전에 `넣을 폴더`를 확인합니다
- 다시 가져오면 폴더 경로와 파일 이름으로 맞추고, 바뀐 문서만 `(가져오기 전)` 사본을 남긴 뒤 갱신합니다
- 옵시디언에서 지우거나 옮긴 파일의 옛 문서는 이 앱에 남습니다
- 한 줄을 혼자 차지한 이미지만 이 앱의 이미지가 되고, 그 그림의 파일 이름은 바뀝니다
````

- `…` 는 U+2026 한 글자다(`폴더 가져오기…`·`가져오기…` 는 화면 글자 그대로, `![[…]]` 는 자리 표시)
- `![[…]]`·`![[그림.png]]`·`![[0123456789abcdef.png|320]]` 는 모두 **인라인 코드 안**이다. 사이트 가드 `checkContentFile` 은 `![..](..)` 표준 이미지 문법을 코드 안에서도 거절하지만 `![[` 는 보지 않는다(m4 통과). 구현하는 쪽이 표준 이미지 문법 예시(`![설명](주소)`)를 더하면 빌드가 깨진다
- 표 안 `|` 이스케이프는 없다 — 표 셀 안에 `|` 가 든 인라인 코드가 없게 문장을 지었다(m4 에서 `<table>` 2개가 깨지지 않고 렌더됨)

## 4. 사실 표 — 글의 문장과 근거

구현하는 쪽이 문장을 바꿀 때 여기를 먼저 본다. "코드" 칸은 이름으로 적는다. **모든 행을 2026-09-27 현재 코드로 확인했다.** 명세와 코드가 다른 곳은 4.8

### 4.1 머리·볼트로 내보내기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 콜아웃·하이라이트·주석·체크박스·속성은 손대지 않음, 바뀌는 것은 이미지·일부 위키링크·파일 이름 | F-2020 5장, F-2019 2장 "하지 않는다" | `exportVault.ts` `toVaultMarkdown` 은 이미지 블록과 위키링크만 바꾼다. 가져오기 `planVaultImport` 도 임베드·링크만 고친다 |
| 설정 `데이터` 탭 = 전체, 폴더 `⋯` = 그 폴더와 하위 | F-2020 6.1·6.2 | `SettingsDialog.tsx` 버튼 `옵시디언 볼트로 내보내기` → `App.tsx` `handleExportVault`(범위 전체). `Sidebar.tsx` 폴더 메뉴 같은 글자 → `handleExportFolderVault` |
| 로그인 시 내 문서만, 공유받은 문서 빠짐 | F-2020 3.2 | `planVaultExport` → `selectExportScope`(소유 문서만) |
| 인터넷 연결 때만(로그인 시) | F-2020 6.1·6.3 | `App.tsx` `exportOffline = store.kind === 'server' && offline`, 폴더 경로 알림 `온라인일 때 내보낼 수 있습니다.` |
| 폴더 구조대로 `.md`, 이미지는 맨 위 `attachments` | F-2020 4장·3.2 | `planExportPaths`, `exportWorkspace.ts` 의 예약 폴더 이름 `attachments` |
| 제목 = 파일 이름, 제목 줄 더하지 않음 | F-2020 3장·5장 | `planExportPaths` 가 `toObsidianFileName(title)`, 본문은 `toVaultMarkdown` 결과 그대로 |
| `#` `^` `[` `]` 와 파일 이름 금지 글자 → `_`, `왜?` → `왜_.md` | F-2020 3.1 | `filename.ts` `OBSIDIAN_FORBIDDEN_CHARS`(기본 금지 글자에 `#^[]` 추가). m11 에서 `왜?` → `왜_.md` 확인 |
| 같은 제목 둘이면 나중 것에 ` (2)` | F-281, F-2020 3.2 | `exportWorkspace.ts` `resolveSiblingNames`(대소문자 무시 비교, 뒤 항목에 ` (2)`) |
| `manifest.json` 없음, 빈 폴더 없음 | F-2020 2장 "하지 않는다"·4장 | `exportVault.ts` 는 파일만 zip 에 넣고 manifest 를 쓰지 않는다 |
| zip 이름(글에는 날짜 예시로만) | F-2020 4장 | 전체 `${date}-vault.zip`, 폴더 `${toFolderName(folder)}-vault.zip` |

### 4.2 옵시디언에서 열기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 이미지 → `![[{id}.{ext}|{폭}]]` 한 줄, 정렬·설명 빠짐 | F-2020 5.3 | `toVaultMarkdown` 이미지 블록 변환(정렬·alt 를 버린다) |
| 한 문서로 정해지지 않거나 이름이 바뀐 대상만 고침, 원래 글자는 별칭 | F-2020 5.4 | `toVaultMarkdown` 링크 재작성 — 볼트 안에서 유일하게 가리키면 그대로, 아니면 경로·새 이름을 넣고 원래 글자를 `|` 뒤로. m11: `[[회의록|지난 회의]]` 가 `[[교안/회의록|지난 회의]]` 로 |
| 표 안 링크에는 별칭 없음 | F-2020 5.4·5.5 | 표 행에서는 별칭을 붙이지 않는다(`|` 가 셀을 가름). m11: 표 안 `[[왜?]]` → `[[왜_]]` |
| 알림 끝에 고친 수 | F-2020 6.4 | `옵시디언 볼트로 내보냈습니다.` / `옵시디언 볼트로 내보냈습니다. 위키링크 {N}개에 경로를 붙였습니다.`(`e2e/exportVault.spec.js` 가 같은 글자를 본다) |
| 볼트 밖·끊긴 링크는 그대로 | F-2020 5.4 | 대상이 볼트 목록에 없으면 재작성하지 않는다 |

### 4.3 볼트를 가져오기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 세 길이 같은 `가져오기` 창 | F-2019 4장·10.2 | `SettingsDialog.tsx` `폴더 가져오기…`·`가져오기…`, `App.tsx` 끌어 놓기, `ImportPreviewDialog.tsx` h2 `가져오기` |
| 폴더는 한 번에 하나 | F-2019 4.3 | `App.tsx` 알림 `폴더는 한 번에 하나만 가져올 수 있습니다.` |
| zip 에 `manifest.json` 있으면 전체 내보내기, 없으면 볼트 | F-2019 2장(결정 4) | `App.tsx` `previewImportZip` 분기 |
| 창의 네 수, `넣을 폴더` 바꾸면 다시 계산 | F-2019 10.2 | `ImportPreviewDialog.tsx` `새로 N개 · 갱신 N개 · 건너뜀 N개 · 이미지 N개`, 라벨 `넣을 폴더` |
| 처음 값: 볼트 이름과 같은 최상위 폴더 하나면 그 폴더, 아니면 `새 폴더 "{볼트 이름}"` | F-2019 7.1(4.8 C1) | `defaultVaultTarget`(NFC + 소문자 접기), `vaultTargetOptions` 라벨 `새 폴더 "{vaultName}"`·`최상위`·폴더 경로 |
| 볼트 이름 = 고른 폴더나 zip 이름 | F-2019 4.1·4.4 | `scanVault` — 공통 최상위 폴더 하나면 그 이름, 아니면 zip 파일 이름. 끝의 소문자 `-vault` 를 뗀다 |
| 폴더 구조 깊이 그대로 | F-2017 | `applyVaultImport` 폴더 생성(깊이 제한 없음, F-2017) |
| `.md`·`.markdown` → 문서, 확장자 뗀 이름이 제목, 속성 `title` 안 씀 | F-2019 6.1 | `importWorkspace.ts` `titleFromPath` |
| `.` 으로 시작하는 폴더·파일 무시 | F-2019 4.2·6.1 | `isNoiseEntry`(`__MACOSX/`, `.` 으로 시작하는 경로 조각, `Thumbs.db`, `desktop.ini`) |
| 문서도 이미지도 아닌 파일은 안 들이고 개수만, 안 쓰이는 이미지도 안 들임 | F-2019 6.1·7.7 | 경고 `.md 가 아니라 건너뛴 파일 {N}개`. 이미지는 문서가 참조할 때만 계획에 들어간다 |

### 4.4 다시 가져오기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| `넣을 폴더` 아래 경로·파일 이름으로 맞춤, 대소문자 무시, 앱 쪽 이름은 내보낼 때 규칙으로 | F-2019 7.2~7.4 | `planVaultImport` 맞춤 키 — 내보낼 때 이름 먼저, 그다음 원래 제목. NFC + 소문자 |
| 같음 → 건너뜀 / 다름 → 사본 + 갱신 / 새 파일 → 생성 | F-2019 7.6·9장 | 판정 `skip`·`update`·`create`. `applyVaultImport` 갱신은 먼저 `truncatedCopyTitle` 로 `{제목} (가져오기 전)` 사본을 같은 폴더에 만들고, 원래 문서를 제자리 갱신(문서 id 유지) |
| `(가져오기 전)` 안내 | F-2019 10.2 | `ImportPreviewDialog.tsx` `덮이는 문서는 이전 내용을 "(가져오기 전)" 문서로 남깁니다.` |
| 이름 바꿈·옮김 → 새 문서, 옛 문서 남음 / 지운 파일 → 지우지 않음 | F-2019 7.6·7.7 | 볼트에 없는 앱 문서는 계획에 넣지 않고 경고 `볼트에 없어 그대로 둔 문서 {N}개`. m11 옵시디언 편집 시나리오(이름 바꿈 1·삭제 1) → 경고 2개 |
| 대소문자만 다름 → 제목을 볼트 쪽으로, 갱신 | F-2019 7.4·7.6 | `planVaultImport` 대소문자 차이 분기(제목 바꿈 + `update`) |
| 전체 내보내기 zip 되가져오기의 처음 값이 날짜 이름 | — (코드 동작) | m11: `2026-09-27-vault.zip` → 볼트 이름 `2026-09-27` → 같은 이름 최상위 폴더 없음 → `new`(생성 4). `최상위` 로 바꾸면 건너뜀 3·갱신 1 |

### 4.5 이미지

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 한 줄을 혼자 차지한 이미지만 변환, 옵시디언·마크다운 문법 둘 다 | F-2019 5.1·5.2 | `obsidianImage.ts` `findVaultEmbeds` — 0열에서 시작해 그 줄에 혼자일 때만 |
| `|` 뒤 숫자 = 폭, 글자 = 설명 | F-2019 5.3 | 같은 함수의 크기·설명 해석 |
| 그 밖은 원문 그대로 + 개수 | F-2019 5.2·7.7 | 경고 `원문 그대로 둔 이미지·임베드 {N}개`. m11: 목록 안 임베드 1 + 문서 임베드 1 → 2개 |
| 인터넷 주소 이미지는 안 바꿈 | F-2019 5.1 | `findVaultEmbeds` 가 외부 URL 을 건너뜀 |
| PNG·JPEG·GIF·WebP 만, 5MB 넘으면 그대로 | F-2019 6.2 | `scanVault` 이미지 분류(내용으로 형식 판정), `MAX_ATTACHMENT_BYTES`(5,242,880) 초과는 `tooLarge` |
| 새 이름(영문·숫자), 로그인 시 PNG·JPEG 는 더 작으면 WebP | F-2019 6.2·9.3 | id = SHA-256 앞 16 hex. `applyVaultImport` 서버 저장소에서 `toWebp` — 결과가 더 작을 때만 바꾼 blob |
| 이 앱에서 나간 그림은 같은 이름으로 돌아와 다시 올리지 않고 정렬·설명 되찾음 | F-2019 6.2·7.5 | `scanVault` 가 16 hex 이름을 그대로 id 로 둔다. m11: 두 번째 왕복 건너뜀 4 |

### 4.6 위키링크

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 붙였던 경로·바뀐 이름을 되돌림, 같은 문서로 가는 링크는 그대로 | F-2019 7.8 | `planVaultImport` 링크 복원 |
| 손대지 않은 왕복은 건너뜀, 별칭 링크에 경로가 붙었던 문서는 처음 한 번 갱신 | F-2019 7.8 | m11: 첫 되가져오기 건너뜀 3·갱신 1(`[[교안/회의록|지난 회의]]` 가 든 `목차` — 별칭이 원래 글자와 달라 경로를 남긴다), 두 번째는 건너뜀 4 |
| `[[폴더/문서]]`·`[[문서#제목]]` 이어짐 | F-2018, F-2016 3장 | `wikiLink.ts` 경로·제목 해석 |
| 별칭이 가리킬 이름과 같으면 걷어 냄 | F-2019 7.8 | m11: `[[교안/회의록|회의록]]` → `[[회의록]]` |
| `[[문서#^abc123]]` 은 문서만 엶 | F-2018 | `wikiLink.ts` — `^` 로 시작하는 제목 조각은 `null`. m11: 가져오기에서 글자 그대로 |

### 4.7 옮겨지지 않는 것

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 이미지 아닌 `![[…]]` → 그대로 + 개수 | F-2019 5.2·7.7 | m11: `![[회의록]]` 이 그대로 남고 `원문 그대로 둔 이미지·임베드` 에 셈 |
| `#태그`·각주·`aliases` 그대로, `aliases` 로 링크 안 이어짐 | F-2019 2장 "하지 않는다" | `aliases` 를 읽는 코드 없음(grep `aliases` — 볼트·위키링크 코드 0건) |
| 캔버스 등 `.md` 아닌 파일 안 들임 | F-2019 2장 "하지 않는다"·6.1 | m11: `.canvas` → `.md 가 아니라 건너뛴 파일 1개` |
| 댓글은 파일에 안 들어감 | F-2020 5장 | `planVaultExport` 는 본문(`content`)만 쓴다 |
| 공유받은 문서: 안 내보냄, 맞춰 보지 않음 | F-2020 3.2, F-2019 7.2 | `selectExportScope`, 가져오기 맞춤 대상도 소유 문서 |
| 금고: 열림 → 평문, 잠김 → 빠짐, 잠긴 문서와 맞춰 보지 않음 | F-409 2.2 | `selectExportScope` 가 잠긴 금고 문서를 빼고 `lockedCount` 로 알림(`금고가 잠겨 있어 금고 문서 {N}개는 빼고 내보냈습니다.`), 가져오기 경고 끝의 금고 안내. `encryption.md` "내보내기" 행과 같은 사실 |
| 로그인 시 1MB 넘는 문서 건너뜀 + 개수 | F-2019 7.7 | `MAX_CONTENT_BYTES`(1,000,000), 경고 `내용이 1MB 를 넘어 건너뛴 문서 {N}개`(서버 저장소일 때만) |

### 4.8 명세와 코드가 다른 곳 (코드를 따랐다)

| 명세 | 명세의 글 | 코드 | 글은 |
| --- | --- | --- | --- |
| F-2019 7.1, F-2016 4.2 | `넣을 폴더` 처음 값은 볼트 이름과 "정확히 같은(대소문자 구분)" 최상위 폴더 | `defaultVaultTarget` 은 NFC + 소문자로 접어 비교 — **대소문자를 가리지 않는다** | "볼트 이름과 같은 최상위 폴더" — 대소문자를 말하지 않는다. 11장 C1 |
| 도움말 `## 내보내기·가져오기`(F-257, F-2020 6.5) | `` `.md 내보내기`를 누르면 `` | 상단바 메뉴는 `내보내기`(aria-label), 항목 글자는 `.md`(`ExportMenu.tsx`) | 5장에서 도움말을 고친다. 11장 C3 |
| 같은 절 | `` `가져오기`로 `.md` 파일을 불러오면 `` | `불러오` 는 ia.md 6장 쓰지 않는 말 | 5장에서 고친다 |

### 4.9 왕복 실측 (m11)

| 시나리오 | 결과 |
| --- | --- |
| 전체 내보내기 → 그대로 가져오기(처음 값) | zip `2026-09-27-vault.zip`, 볼트 이름 `2026-09-27`, 처음 값 `new` → **생성 4** |
| 같은 zip, `넣을 폴더` = `최상위` | 건너뜀 3·갱신 1(`목차`) |
| 그 결과를 다시 내보내고 `최상위` 로 가져오기 | 건너뜀 4 |
| 옵시디언 편집(한 문서 고침, `과제/회의록` 이름 바꿈, `교안/목차` 삭제, 목록 안 임베드와 없는 그림을 넣은 새 문서, `.canvas`, `.obsidian/`) | 생성 2·갱신 1·건너뜀 1. 경고 `.md 가 아니라 건너뛴 파일 1개`·`원문 그대로 둔 이미지·임베드 2개`·`볼트에 없어 그대로 둔 문서 2개` |
| 폴더 `교안` 내보내기 zip(`교안-vault.zip`) | 볼트 이름 `교안`, 처음 값 = 기존 `교안` 폴더, 건너뜀 2 |
| 폴더 경로 `교안-vault/`·`2026-09-27-vault/` 로 가져오기 | 각각 기존 폴더·`new` — zip 과 같음 |

## 5. 도움말 — before / after

### 5.1 자리

`src/app/helpDoc.ts` `APP_SECTIONS` 에서 `## 내보내기·가져오기` 절을 통째로 바꾸고, 바로 뒤에 `## 옵시디언 볼트` 절을 새로 넣는다. 순서는 `… ## 금고` → `## 내보내기·가져오기` → **`## 옵시디언 볼트`** → `## 설치와 오프라인` …

before (링크 줄 뺀 본문 379자, 3문단 — m1):

```
`.md 내보내기`를 누르면 지금 화면의 원문을 그대로 파일로 받습니다. 이미지가 있는 문서는 zip으로 받습니다. `가져오기`로 `.md` 파일을 불러오면 그 내용 그대로 새 문서가 됩니다.

설정의 `데이터` 탭이나 폴더 메뉴에서 `옵시디언 볼트로 내보내기`를 누르면 옵시디언에서 바로 열 수 있는 zip으로 받습니다. 이미지는 옵시디언 모양으로 바뀌고, 옵시디언에서 다른 문서로 이어질 위키링크에는 폴더 이름을 붙입니다.

설정의 `데이터` 탭에서 `폴더 가져오기…`를 누르거나 폴더를 창에 끌어 놓으면 옵시디언 볼트를 폴더 구조 그대로 들여옵니다. 같은 볼트를 다시 가져오면 같은 자리의 문서만 새 내용으로 바꾸고, 바뀌기 전 내용은 `(가져오기 전)` 문서로 남깁니다.

사용법 글: [한 번 쓴 글을 다른 도구로 옮기기](/guides/markdown-portability)
```

### 5.2 after (m2)

```
## 내보내기·가져오기

상단바 `내보내기` 메뉴에서 `.md`를 고르면 지금 문서의 원문을 그대로 파일로 받고, 이미지가 있는 문서는 zip으로 받습니다. 사이드바 `가져오기`로 고른 `.md` 파일은 그 내용 그대로 새 문서가 됩니다.

설정 `데이터` 탭의 `전체 내보내기`는 모든 문서·폴더·이미지를 zip 하나에 담고, 같은 탭의 `가져오기…`로 그 zip을 다시 들입니다.

사용법 글: [한 번 쓴 글을 다른 도구로 옮기기](/guides/markdown-portability)

## 옵시디언 볼트

설정 `데이터` 탭이나 폴더 `⋯` 메뉴의 `옵시디언 볼트로 내보내기`는 옵시디언에서 바로 열 수 있는 zip을 만듭니다. 이미지는 옵시디언 모양으로 바꿔 한 폴더에 모으고, 같은 이름 문서가 여럿인 위키링크에는 폴더 이름을 붙입니다.

옵시디언 볼트를 들일 때는 설정 `데이터` 탭 `폴더 가져오기…`로 볼트 폴더를 고르거나, 폴더를 창에 끌어 놓거나, 볼트 zip을 `가져오기…`로 고릅니다. 가져오기 창의 `넣을 폴더`에서 볼트를 넣을 자리를 정합니다.

같은 볼트를 다시 가져오면 폴더 경로와 파일 이름이 같은 문서끼리 맞춰 내용이 달라진 문서만 새 내용으로 바꾸고, 바뀌기 전 내용은 `(가져오기 전)` 문서로 남깁니다. 옵시디언에서 지우거나 옮긴 파일의 문서는 지우지 않고 그대로 둡니다.

사용법 글: [옵시디언 볼트와 오가기](/guides/obsidian-vault)
```

- `helpDoc.ts` 는 템플릿 리터럴이라 원문의 백틱은 `` \` `` 로 이스케이프한다(지금 절들과 같다)
- **왜 절을 나누나**(9장 Q1 기본값 (c)): 지금 절 하나에 R3 링크 줄은 하나뿐이고(`0d56037` tweak 이 `markdown-portability` 를 달았다), `.md` 문단과 볼트 문단이 각기 다른 글을 가리켜야 한다. 절을 나누면 두 링크가 모두 제 문단 아래에 서고, 목차에 `옵시디언 볼트` 항목이 생겨 찾기 쉽다
- **`## 내보내기·가져오기` 를 고쳐 쓰는 이유**: 볼트 문단이 빠지면 1문단만 남는데, 전체 내보내기(F-281)·전체 가져오기(F-282)가 도움말 어디에도 없다(grep `전체 내보내기` — `helpDoc.ts` 0건). 옛 첫 문단의 버튼 글자(`.md 내보내기`)와 `불러오면` 도 고친다(4.8)
- `## 옵시디언 볼트` 둘째 문단이 zip 경로와 `넣을 폴더` 를 말하는 이유: 링크 없이도(오프라인에서) 가져오는 세 길과 자리 고르기를 찾을 수 있어야 한다(R3)
- 넣지 않은 것: 수치(두 절 모두 수치 0개), 금고(`## 금고` 절이 이미 내보내기를 말한다), 이미지·링크 변환 세부·옮겨지지 않는 것(글의 몫)

### 5.3 `content/guides/markdown-portability.md`

바꾸는 곳 두 줄. 다른 줄은 그대로.

- frontmatter `updated: 2026-09-23` → 구현 커밋 날짜(2026-09-27 예정). `date` 는 그대로
- "옮겨 갈 곳별로 알아 둘 것" 표의 `Obsidian` 행 — 셋째 칸 끝에 문장 하나를 붙인다

before:

```
| Obsidian | `[[제목]]`·`[[제목\|별칭]]` 문법이 같습니다 | 이미지·위키링크까지 그대로 열리려면 `옵시디언 볼트로 내보내기`를 씁니다 |
```

after:

```
| Obsidian | `[[제목]]`·`[[제목\|별칭]]` 문법이 같습니다 | 이미지·위키링크까지 그대로 열리려면 `옵시디언 볼트로 내보내기`를 씁니다. 자세한 것은 [옵시디언 볼트와 오가기](/guides/obsidian-vault)에 있습니다 |
```

- 표 첫 칸의 영어 `Obsidian` 은 그 글의 다른 행(`Notion` 등)과 맞춘 것이라 그대로 둔다(9장 Q4)
- 링크 문장은 "자세한 것은 …에 있습니다" — 이 글·`sharing.md` 가 다른 글을 가리킬 때 쓴 모양과 같다

## 6. 수용 기준

단위 테스트 환경은 `node`(DOM 없음). 새 판정 함수를 만들지 않고 F-2039·F-2045 가 쓴 도우미(`appSections`, `readGuide`·`guideBody`)를 그대로 쓴다.

### 6.1 도움말 — `src/app/helpDoc.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| G1 갱신 | `SECTION_ORDER` | `'내보내기·가져오기'` 와 `'설치와 오프라인'` 사이에 `'옵시디언 볼트'`. 기존 G1 테스트가 순서를 본다 |
| U1 | 두 절 (5.2) | ① `옵시디언 볼트` 절의 마지막 문단이 정확히 `사용법 글: [옵시디언 볼트와 오가기](/guides/obsidian-vault)` ② 그 절 본문에 `` `옵시디언 볼트로 내보내기` ``·`` `폴더 가져오기…` ``·`` `넣을 폴더` ``·`` `(가져오기 전)` `` 이 있다 ③ `내보내기·가져오기` 절의 마지막 문단이 정확히 `사용법 글: [한 번 쓴 글을 다른 도구로 옮기기](/guides/markdown-portability)` ④ 그 절에 `옵시디언` 과 `불러오` 가 없고 `` `전체 내보내기` `` 가 있다. 기존 F-2039 U1(600자·4문단)·U2(링크 줄 모양)·U17(금고 절이 `## 내보내기·가져오기` 앞)이 그대로 통과한다 |

- ② 의 화면 글자는 `SettingsDialog.tsx`·`Sidebar.tsx`·`ImportPreviewDialog.tsx` 의 글자와 같다. 컴포넌트를 import 하지 않는 이유는 F-2045 6.1 과 같다(`.tsx` 가 아이콘·CSS 가져오기를 끌고 온다). 화면 글자가 바뀌면 사람 확인 H3 이 잡는다

### 6.2 사용법 글 — `site/helpGuides.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| U2 | 옵시디언 글의 수치 (R5) | `content/guides/obsidian-vault.md` 에 아래 두 문자열이 있다. 기대 문자열은 **테스트가 상수에서 만든다** |
| U3 | 옵시디언 글에 제품명이 없다 (R6) | `obsidian-vault.md` 본문(소문자)에 `brand.name`·`brand.shortName`(소문자)이 없다. F-2045 U3 과 같은 모양 |
| U4 | `markdown-portability` 가 이 글을 가리킨다 (R8) | `markdown-portability.md` 본문에 `](/guides/obsidian-vault)` 가 있다 |
| — | 기존 F-2039 U4·U5·U6, F-2045 U4 | **코드 변경 없이** 새 절·새 글이 반복에 들어간다: F-2039 U4 링크 글자 = `obsidian-vault.md` `title`, U5 `(## 옵시디언 볼트, obsidian-vault.md)` 쌍과 `(## 내보내기·가져오기, markdown-portability.md)` 쌍 문장 겹침 0(m6), U6 `obsidian-vault.md` 쓰지 않는 말 0·4,627자·`##` 8개(m4·m8). F-2045 U4 는 새 글의 `wiki-links`·`encryption`·`markdown-portability` 링크와 `markdown-portability.md` 의 새 `obsidian-vault` 링크가 모두 있는지 본다 |

U2 대조 행(m9 에서 초안이 모두 통과):

| 글의 문자열 | 만드는 상수 (import 위치) |
| --- | --- |
| `{N}MB를 넘는 문서` (`MAX_CONTENT_BYTES / 1_000_000`) | `MAX_CONTENT_BYTES` (`src/app/importWorkspace.ts`) |
| `한 장에 {N}MB를 넘는 그림` (`MAX_ATTACHMENT_BYTES / 1024 / 1024`) | `MAX_ATTACHMENT_BYTES` (같은 파일) |

- 두 상수의 나눗셈이 정수가 아니게 바뀌면(예: 1.5MB) 글의 문자열도 바뀌어야 하므로 테스트가 빨개지는 것이 맞다
- `1MB` 는 10진(1,000,000), `5MB` 는 2진(5,242,880 = 5 × 1024²)이다 — 코드의 두 상수가 단위를 다르게 쓰기 때문이고, 도움말·글·경고 문구(`내용이 1MB 를 넘어…`) 모두 이 두 값을 `1MB`·`5MB` 로 부른다. 테스트는 각 상수를 제 단위로 나눈다

### 6.3 동작 — `e2e/site.spec.js` 의 `F-2046 E*`

빌드된 사이트가 있어야 도는 테스트라 F-2045 E* 옆에 둔다. F-2045 E1~E3 과 같은 모양이고 주소·제목만 다르다. 모양·문장은 고정하지 않는다(8장).

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| E1 | 글이 뜨고 완결된 정적 페이지다 | `page.goto('/guides/obsidian-vault')`, 그리고 `page.request.get('/guides/obsidian-vault')` | `옵시디언 볼트와 오가기` `h1` 과 `.site-foot` 이 보인다. 응답 `200`, `<title>옵시디언 볼트와 오가기 · Rawdoc</title>`, `rel="canonical"`, `https://rawdoc.app/guides/obsidian-vault`, `<script` 없음 |
| E2 | 목록·색인 | `/guides` 로 가고 `/sitemap.xml` 요청 | `.site-article .markdown-body > ul a[href="/guides/obsidian-vault"]` 이 보이고 글자가 제목과 같다. `sitemap.xml` 에 `<loc>https://rawdoc.app/guides/obsidian-vault</loc>` |
| E3 | 앱 도움말에 링크가 있다 | `openApp(page)` → 사이드바 `getByRole('button', { name: '도움말' }).first()` | `.help-page a[href="/guides/obsidian-vault"]` 이 보이고 글자 `옵시디언 볼트와 오가기`, `target="_blank"` |

- E1 의 `Rawdoc` 은 F-2045 E1 과 같은 방식이다
- `markdown-portability` 표 칸의 링크는 U4 가 원본에서 보고, 렌더는 표 셀 안 링크라 특별한 것이 없다(m5 빌드 통과) — e2e 를 더하지 않는다

### 6.4 빌드·회귀·범위

| # | 확인 | 기대 |
| --- | --- | --- |
| A1 | `npm run build` | 성공. `dist/guides/obsidian-vault.html` 이 생긴다(가드 통과) |
| A2 | 관련 회귀 | `npm run e2e:one -- "F-2046|F-2045|F-2039|F-276|F-274"` 통과 |
| A3 | 단위·타입·린트 | `npm test`, `npm run typecheck`, 바꾼 파일 eslint 통과 |
| A4 | 소유 범위 | `npm run review -- F-2046` — 1장 표 밖 변경 없음, `specs/**`·`CLAUDE.md` 무변경 |

## 7. 기존 테스트 회귀 — grep 으로 확인한 것 (2026-09-27, `77a2157`)

**고쳐야 하는 테스트 셋**(모두 1장 소유):

| 파일 | 테스트 | 바꿀 것 |
| --- | --- | --- |
| `src/app/helpDoc.test.ts` | G1 절 순서(`SECTION_ORDER`) | `'옵시디언 볼트'` 를 `'내보내기·가져오기'` 뒤에 더한다. 안 더해도 G1 은 통과하지만(목록에 있는 것만 본다) 새 절의 자리를 고정하려고 더한다 |
| `site/pageNav.test.ts` | F-2036 A2 `'도움말 41개, …'` | 이름과 `toHaveLength(41)` 을 42 로 |
| `site/render.test.ts` | F-2036 A6 목차 `toHaveLength(41)` | 42 로 |

깨지지 않는 것:

| 파일 | 판정 |
| --- | --- |
| `src/app/helpDoc.test.ts` U17(F-404) | `## 금고` 가 `## 내보내기·가져오기` 앞인지 본다 — 제목·자리 그대로 |
| 같은 파일 F-2039 U1·U2, F-2045 U1 | 두 절 모두 600자·4문단 안, 링크 줄이 마지막(m2). `## 공유` 는 건드리지 않는다 |
| 같은 파일 "제품명 문자열을 직접 쓰지 않는다"·"표준 이미지 문법을 쓰지 않는다"·`{{` | 해당 글자 없음 |
| `site/pageNav.test.ts` F-2036 A1 | `headingSlug('내보내기·가져오기')` 만 본다 — 제목 글자 그대로 |
| `site/helpGuides.test.ts` F-2039 U4~U7, F-2045 U2~U4 | 새 쌍·새 글을 반복에 넣고 통과(m4·m6·m8). `sharing.md` 는 건드리지 않는다 |
| `site/helpPage.test.ts` F-274 | 같은 원본(`HELP_DOC_CONTENT`)을 비교 |
| `site/build.test.ts` | 내장 픽스처로 부른다 — 저장소 글과 무관 |
| `e2e/site.spec.js` F-276 A10 | 목록 링크 수를 `content/guides` 의 `.md` 수로 센다 — 9편이 되어도 통과 |
| 같은 파일 F-276 A11 | `markdown-portability` 의 `h1` 만 본다 — 제목 그대로 |
| 같은 파일 F-2039 E3·E5, F-2045 E1~E3 | 각자 자기 링크만 본다. `/guides` 첫 줄이 `obsidian-vault` 로 바뀌어도 통과 |
| 같은 파일 precache·sitemap 합친 테스트 | 경로를 나열해 **있는지** 본다 |
| `e2e/help.spec.js` | 목차 항목 수를 화면에서 센 제목 수와 비교한다(고정 수 없음) |
| `e2e/exportVault.spec.js`·`importWorkspace.spec.js`·`e2eeSearchExport.spec.js` | 볼트 코드를 바꾸지 않는다. 도움말을 보지 않는다 |
| `src/app/importVault.test.ts`·`exportVault.test.ts`·`importWorkspace.test.ts` | 코드 변경 없음(상수만 읽힌다) |

## 8. 사람 확인 필요 (자동 판정 불가)

`specs/human-checks.md` 로 넘긴다.

| # | 확인 |
| --- | --- |
| H1 | 글을 처음부터 끝까지 읽기 — 어조가 다른 사용법 글과 같은지, 두 표(6줄 × 2칸, 7줄 × 2칸)가 좁은 창에서 읽히는지, "옵시디언에서 고친 뒤 다시 가져오기" 마지막 문단(`넣을 폴더` → `최상위`)이 이해되는지 |
| H2 | 실제 옵시디언으로 한 번 왕복 — 설정에서 전체 `옵시디언 볼트로 내보내기` → 풀어서 옵시디언으로 열기(이미지·경로 붙은 링크가 이어지는지) → 한 문서 고치고 한 파일 이름 바꾸기 → zip 으로 되가져오기(**처음 값이 `새 폴더 "{날짜}"` 인지, `최상위` 로 바꾸면 수가 글대로 나오는지**) → 폴더 끌어 놓기로도 한 번. 글과 다른 곳이 있으면 적는다(10장 가정 1·2) |
| H3 | 앱 도움말 두 절 — 목차에 `옵시디언 볼트` 가 `내보내기·가져오기` 다음에 서는지, 화면 글자(`옵시디언 볼트로 내보내기`·`폴더 가져오기…`·`가져오기…`·`넣을 폴더`·`전체 내보내기`)가 지금 화면과 같은지, 두 링크 줄이 각기 맞는 글을 여는지 |
| H4 | `/guides/markdown-portability` 표 `Obsidian` 행 — 좁은 창에서 늘어난 셋째 칸이 읽히는지, 링크가 이 글로 가는지 |

## 9. 열린 질문 — 모두 기본값이 있다

| # | 질문 | 기본값과 근거 | 다른 쪽을 고르면 |
| --- | --- | --- | --- |
| Q1 | 도움말 링크 줄을 어디에 두나 | **(c) 절을 나눈다** — `## 내보내기·가져오기`(→ `markdown-portability`) + 새 `## 옵시디언 볼트`(→ `obsidian-vault`). R3 은 절에 링크 한 줄이라 두 글을 한 절에서 가리킬 수 없다. 목차 항목도 생긴다. 대가: 도움말 제목 41 → 42 로 테스트 두 개 수정(7장) | (a) 지금 절의 링크를 `obsidian-vault` 로 바꾼다: `0d56037` tweak(사용자가 단 `markdown-portability` 링크)을 되돌리고 `.md` 문단이 엉뚱한 글을 가리킨다. 5장 after 한 절로 합치고 U1 ③④, 테스트 41 그대로. (b) 지금 링크를 두고 볼트 글은 링크하지 않는다: 볼트 문단의 자세한 내용이 `markdown-portability` 로 가서 R3 취지에 어긋나고, 이 글로 가는 앱 안 길은 사이트 목록뿐. U1 ①②·E3 없어짐 |
| Q2 | 글 제목 | **`옵시디언 볼트와 오가기`** — F-2039 10장 행 이름. "오가기" 가 되가져오기까지 담는다 | `옵시디언 볼트로 옮기기`: 한 방향만 읽힌다. 바꾸면 3장 `title`·`h1`, 5.2·5.3 링크 글자, U1·E1~E3 |
| Q3 | `markdown-portability.md` 를 얼마나 고치나 | **링크 문장 하나 + `updated`** — 그 글의 옵시디언 행은 사실이 맞고 짧다. 내용을 이 글로 옮겨 빼면 그 글의 "옮겨 갈 곳별" 표가 빈다 | 안 고친다: 두 글이 서로 모르게 된다(이 글만 그 글을 가리킴). U4·H4 없어지고 1장에서 그 파일이 빠진다. 더 고친다(행 문장을 줄임): R4 겹침 재측정 필요 |
| Q4 | 영어 `Obsidian` 병기 | **글 머리·요약에 `옵시디언(Obsidian)` 한 번씩**, 나머지는 `옵시디언`. 검색으로 영어 이름을 치는 사람이 있고, 도움말·설정 화면은 한글이다. `markdown-portability` 표의 `Obsidian` 은 그 글 체계대로 둔다 | 병기 없음: 영어 검색에 덜 걸린다. 영어로 통일: 화면 글자(`옵시디언 볼트로 내보내기`)와 어긋난다 |
| Q5 | 도움말 `## 내보내기·가져오기` 를 볼트 문단만 빼고 둘까, 고쳐 쓸까 | **고쳐 쓴다** — 볼트 문단이 빠지면 1문단만 남고, 전체 내보내기·가져오기가 도움말에 없으며, 옛 문장에 틀린 버튼 글자와 `불러오` 가 있다(4.8) | 빼기만: 옛 첫 문단(`.md 내보내기`·`불러오면`)이 남는다. U1 ④의 `불러오` 없음·`전체 내보내기` 있음을 빼야 한다 |
| Q6 | 글에 넣을 수치 | **1MB·5MB 둘** — export 된 상수로 R5 대조가 된다. 파일 이름 100자·그림 이름 16자리는 export 되지 않은 상수, 서버 한도는 가져오기 한 번의 쓰기 수를 재지 않았다 | 100자를 넣으려면 `filename.ts` 를 1장에 더해 `MAX_CODE_POINTS` 를 export 하고 U2 에 한 행. 서버 한도는 별도 측정이 먼저다 |
| **Q7 (사람, tweak 후보)** | 설정 `데이터` 탭 설명 두 줄의 `불러옵니다`(11장 C2) | **이 명세는 고치지 않는다** — 코드 글자라 소유 밖이다. 글·도움말은 `불러오` 를 쓰지 않는다 | 고치면(권장: `가져옵니다`) `tweak` 한 줄. `SettingsDialog.tsx` 두 설명 글자만 바뀌고, 이 명세의 글·테스트는 영향 없다(글자를 인용하지 않는다) |
| Q8 | zip·폴더 이름 끝 `-vault` 를 떼는 규칙을 글에 쓰나 | **쓰지 않는다** — `vault` 는 ia.md 6장 쓰지 않는 말이고, 사용자는 `넣을 폴더` 처음 값으로 결과를 본다. 글은 "볼트 이름은 고른 폴더나 zip의 이름에서 정합니다" 까지 | 쓰면 U6 금지어에 걸려 `FORBIDDEN_WORDS` 예외가 필요하다 |

## 10. 가정으로 둔 것 (코드로 확인하지 못했다)

1. **옵시디언이 내보낸 폴더를 볼트로 열고 `![[{id}.png|320]]` 임베드와 경로 붙은 위키링크를 이어 준다는 것.** 옵시디언 문법 문서와 F-2020 설계를 근거로 했고, 이 세션에서 옵시디언을 돌려 보지 않았다. F-2020 사람 확인 H345~H348 이 아직 `대기` 다. 글은 "그 폴더를 볼트로 엽니다" 이상 옵시디언 동작을 약속하지 않는다. H2 에서 본다
2. 실제 브라우저에서 폴더 끌어 놓기가 볼트를 읽는다는 것 — `App.tsx` 코드와 e2e(`importWorkspace.spec.js`)가 있으나 OS 탐색기에서 끄는 동작은 F-2019 H509 가 `대기` 다
3. `toWebp` 가 PNG·JPEG 를 더 작을 때만 바꾼다는 것 — 코드를 읽었고, 브라우저 `OffscreenCanvas` 로 돌려 보지는 않았다(node 에 캔버스가 없다). 글은 "더 작으면" 으로 코드 조건 그대로 쓴다
4. 로컬 `origin/deploy` 참조가 원격과 같다는 것(m10). 원격이 더 나갔다면 결론(모두 배포됨)은 바뀌지 않는다
5. m11 왕복은 메모리 저장소(로그인 안 한 경로와 같은 계획 함수)로 돌렸다. 서버 저장소에서만 도는 분기(1MB 경고·WebP 변환·공간 경고)는 코드를 읽었을 뿐 돌리지 않았다

## 11. 기존 명세·코드와 부딪히는 곳 (보고)

| # | 무엇 | 어디 | 처리 |
| --- | --- | --- | --- |
| C1 | `넣을 폴더` 처음 값: 명세는 "정확히 같은(대소문자 구분)" 폴더, 코드는 NFC + 소문자로 접어 **대소문자 무시** | F-2019 7.1, F-2016 4.2 / `importVault.ts` `defaultVaultTarget` | 글은 코드를 따르되 대소문자를 말하지 않는다. 1.1 갱신 대상 |
| C2 | 설정 `데이터` 탭 `가져오기…`·`폴더 가져오기…` 설명 글자가 `…불러옵니다`. `specs/ia.md` 6장이 `불러오기` 를 쓰지 않는 말로 둔다 | `SettingsDialog.tsx` | 9장 Q7(tweak 후보). 이 명세는 인용하지 않는다 |
| C3 | 지금 도움말의 `` `.md 내보내기` `` 는 상단바 버튼 글자가 아니다(메뉴 `내보내기` → 항목 `.md`). 같은 절의 `불러오면` 은 C2 와 같은 쓰지 않는 말 | `helpDoc.ts` | 5장에서 고친다 |
| C4 | `markdown-portability.md`·`wiki-links.md` 가 이 PC 작업 트리에서 CRLF, 저장소(`git show HEAD:…`)는 LF — autocrlf 때문이라 실제 충돌은 아니다 | 작업 트리 | 참고. 1장에 LF 유지만 적었다 |

## 12. 구현 순서 (TDD)

1. `src/app/helpDoc.test.ts` — `SECTION_ORDER` 에 `'옵시디언 볼트'`, U1 추가 → 빨강(절 없음, `불러오` 있음, `전체 내보내기` 없음)
2. `site/helpGuides.test.ts` 에 U2~U4 → 빨강(파일 없음, 링크 없음)
3. `content/guides/obsidian-vault.md` 를 3장 그대로(날짜 규칙 R7) → U2·U3 초록, F-2039 U6·F-2045 U4 초록
4. `content/guides/markdown-portability.md` 5.3 → U4 초록
5. `src/app/helpDoc.ts` 두 절을 5.2 after 로 → U1·G1 초록, F-2039 U1·U2·U4·U5 초록. 이때 `site/pageNav.test.ts` F-2036 A2·`site/render.test.ts` F-2036 A6 이 42 로 빨개지는 것을 보고 두 수를 42 로 고친다
6. `npm run build`(A1), `dist/guides/obsidian-vault.html`·`dist/guides.html`·`dist/help.html` 을 눈으로 한 번 연다
7. `e2e/site.spec.js` E1~E3 → `npm run e2e:one -- "F-2046"`
8. A2~A4
