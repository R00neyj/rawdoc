---
id: F-2045
title: 공유와 실시간 함께 편집 사용법 글
milestone: M2
status: done
created: 2026-09-27
approved: 2026-09-27
implemented: ea8e06a
depends: [F-2039, F-130, F-210, F-211, F-212, F-214, F-243, F-305, F-307, F-506]
---

# F-2045 공유와 실시간 함께 편집 사용법 글

작성일: 2026-09-27
상태: 승인(2026-09-27). 따로 검토하지 않았고, 열린 질문(9장)이 모두 기본값이 있어 상시 지시(CLAUDE.md "A spec that leaves no real fork goes straight to implementation")로 승인한 것이다 — 기본값대로 구현한다
범위: `specs/features/F-2039.md` 10장 "후속 사용법 글 후보" 첫 행. `specs/ia.md` 6.1 R9(사용법 글 한 편 = 명세 한 개)에 따라 **글 1편(`content/guides/sharing.md`) + 도움말 `## 공유` 절 고쳐 쓰기와 그 절 끝 `사용법 글:` 줄 + 수치 대조 테스트**가 이번 구현 단위다
범위 확인: `specs/product.md` 7장 "공개 사이트" 표에 F-276·F-2039 가 있고, 8장(범위 밖)에 사용법 글을 빼는 문장이 없다(2026-09-27 grep `사용법`·`가이드`). 원천 명세 21개가 모두 `done` 이고 구현 커밋이 모두 `origin/deploy`(`fedc340`)의 조상이다 — 곧 **글에 쓰는 기능은 모두 사용자에게 나가 있다**(m9)

사용자가 이미 정한 것(바꾸지 않는다, 2026-09-27 main 전달):

| # | 결정 | 이 명세 |
| --- | --- | --- |
| 1 | 글은 **한 편**이다. 공유와 실시간 함께 편집을 나누지 않는다. R7 한도(본문 6,000자·`##` 10개)를 넘으면 그때만 둘로 나눈다 | 2장, 9장 Q3 |
| 2 | 이어질 도움말 절은 `## 공유` 하나. 그 절 끝에 R3 모양의 `사용법 글:` 줄 | 5장 |
| 3 | 금고 문서가 공유에서 빠지는 내용은 이 글에서 설명하지 않고 `/guides/encryption` 으로 링크한다(R8) | 3장 "되는 것과 안 되는 것" 표 한 행 |
| 4 | 이 명세 → 바로 구현(ship-feature). 결정이 필요한 곳은 기본값을 정해 둔다 | 9장 |
| 5 | 도움말 현재 모양은 `0d56037`(도움말에 `## 위키링크`·`## 템플릿`·`## 내보내기·가져오기` 링크 줄을 단 tweak) 기준 | 5장·7장 |

근거(2026-09-27 HEAD `0d56037` 에서 읽은 것. 작업 트리에 다른 변경 없음 — `git status` 는 `.playwright-mcp/` 하나):
`specs/ia.md` 6장 표·6.1 R1~R9 전문, `specs/features/F-2039.md` 전문(형식 모델), `F-130.md` 전문, `F-210.md` 전문, `F-211.md` 전문, `F-212.md` 전문, `F-214.md` 전문, `F-230.md` 0~2장, `F-243.md` 표시 규칙(88~91행), `F-305.md` 3장·7.3~7.5·8장·10장·11장·15장, `F-307.md` 6.2~6.6·7.1·8장, `F-506.md` 머리말·결정 표·k1~k8, `specs/product.md` 7장, `content/guides/*.md` 7편 전문, `content/changelog.md` 2026-09-24~27.
코드: `src/app/ShareMenu.tsx` 전문, `InviteDialog.tsx` 전문, `SharesPage.tsx` 전문, `ShareSetDialog.tsx` 문자열, `SharedView.tsx` 전문, `PublicView.tsx`(`PublicDocView` 불러오기 effect·`retry`·문자열), `PublicFolderList.tsx` 문자열, `PeerAvatars.tsx` 전문, `FolderMenu.tsx` `allItems`, `AccountMenu.tsx` `actionItems`, `Sidebar.tsx`(`canShare`, 공유받음 묶음·행), `TopBar.tsx` 렌더 순서, `StatusBar.tsx` `LIVE_STATUS`·`LIVE_FALLBACK_TEXT`, `useDocLock.ts`(`lockedMessage`·재시도), `useTabSync.ts` 알림 문구, `docPath.ts` 전문, `liveDoc.ts` 상수·`disconnectedLong`, `App.tsx`(`READ_ONLY_LIVE_NOTICE`·`LIVE_NOTICE`·N5~N8 effect·`canInviteCurrentDoc`·`shareLinkDocId`·`보기 권한만 있는 문서입니다.` effect·`useDocLock` 인자·`claimDocId`·`exportDisabled`), `src/lib/peers.ts` 전문, `src/lib/comments.ts` 머리, `src/editor/remoteCursors.ts` 상수·`CaretWidget`, `src/styles/peers.css` 라벨 애니메이션, `src/editor/yBinding.ts` 전문, `src/storage/serverStore.ts`(`list`·`get`·`handleConflict`), `worker/links.ts`(`pubResponse`·`handleCreateDocLink`·폴더 링크 만들기·공개 폴더 목록 SQL), `worker/publicPage.ts` 전문, `worker/http.ts`, `worker/token.ts`, `worker/grants.ts`(PUT·DELETE 와 `notifyRevalidate`), `worker/docRoomRpc.ts`, `worker/docRoomCore.ts`(`revalidateConnections`·`stillAllowed`·`mayRelayAwareness`·`REVALIDATE_INTERVAL_MS`), `worker/docRoom.ts`(awareness 중계), `worker/attachments.ts`(올리기 `ownerId`·공개 첨부 `Cache-Control`).
테스트: `src/app/helpDoc.test.ts` 전문, `site/helpGuides.test.ts` 전문, `site/pageNav.test.ts`·`site/render.test.ts` 제목 수, `e2e/site.spec.js` 전문, `e2e/fontSubset.spec.js` 머리

**직접 돌리거나 재어 확인한 것** (2026-09-27, 이 PC, Node v24.12.0. 스크립트는 스크래치패드 `f2045/check.ts`·`build.mjs`(esbuild 로 저장소의 `site/`·`src/` 모듈을 그대로 묶고 `?raw`·`.css` 가져오기만 흉내)에만 있다 — 저장소 무변경)

| # | 무엇 | 결과 |
| --- | --- | --- |
| m1 | 지금 도움말 `## 공유` 절 본문(절 제목 뺀, 코드 포인트) | **80자, 1문단.** 공유 메뉴의 자리, 실시간 함께 편집(접속자·원격 커서·한 명씩 편집), `공유 관리` 가 도움말 어디에도 없다(grep `접속자`·`커서`·`공유 관리`·`한 명씩` — `## 공유` 에 0건, 다른 절에도 0건) |
| m2 | 5장 뒤 `## 공유` 절 | `사용법 글:` 줄 뺀 본문 **445자, 4문단**(R2 상한 600·4). 링크 줄 `renderMarkdown` 결과 `<p>사용법 글: <a href="/guides/sharing" target="_blank" rel="noopener noreferrer">문서를 공유하고 함께 편집하기</a></p>` |
| m3 | 3장 글 초안 | 본문 **4,782자**, `##` **9개**, 94줄(R7 상한 6,000·10). `checkContentFile('guides/sharing.md', …)` 통과(`title` 반환). `renderSitePage` 결과 HTML 26,7xx B, `<script` 없음, `<table>` 2, `<h2>` 9 |
| m4 | 3장 초안 + 지금 `content/guides/*.md` 7편 + `changelog.md` + `legal/*.md` 로 `buildSite` | 통과. `guides/sharing.html` 이 생기고 `/guides` 목록 첫 링크가 `/guides/sharing`(가장 최근 `date`), `sitemap.xml`·`llms.txt` 에 들어간다 |
| m5 | R4(U5 방법) — 5장 뒤 `## 공유` 절의 20자 이상 문장 10개가 초안에 그대로 있나 / 초안의 20자 이상 문장 72개가 절에 그대로 있나 | **둘 다 0개** |
| m6 | 초안과 `wiki-links.md`·`encryption.md` 사이 20자 이상 문장 겹침(양방향) | **0개** — 링크로 가리키기만 한다 |
| m7 | 초안에 `helpGuides.test.ts` `FORBIDDEN_WORDS` 25개 + ia.md 6장 쓰지 않는 말 가운데 이 글에 걸릴 만한 것(`참여자`·`협업자`·`멤버`·`온라인`·`사용자`·`잠금`·`잠김`·`사용 중`·`로딩`·`페이지`·`노트`·`업로드`·`다운로드`·`뷰어`·`팝업`·`새 창`·`미리보기`·`불러오기`, 제품명) | **0개** — 초안 첫 판의 `미리보기 카드` 를 `카드` 로 바꾼 뒤 |
| m8 | 초안의 수치를 상수에서 만든 문자열과 대조 | `넓은 창에서는 4명까지`(`PEER_AVATARS_MAX` 4) · `좁은 창에서는 1명까지`(`PEER_AVATARS_MAX_NARROW` 1) · `10초 넘게`(`DISCONNECT_NOTICE_MS` 10,000) — **셋 다 일치.** 두 모듈 모두 node 에서 부수효과 없이 import 된다(`peers.test.ts`·`liveDoc.test.ts` 가 이미 node 에서 돈다) |
| m9 | 원천 명세가 사용자에게 나갔나 | `git merge-base --is-ancestor` — F-2034 `e930fd5`, F-243 `06de1ed`, F-259 `ee0ce8d`, F-296 `40bb960`, F-297 `0317415`, F-304 `32d27ba`, F-305 `42ab51a`, F-306 `ebd64b2`, F-307 `4ef0ba4`, F-506 `60a2de2` 모두 `origin/deploy`(`fedc340`, 2026-09-27 00:19) 조상. 체인지로그 2026-09-25 "같은 문서를 여러 사람이 동시에 고칠 수 있습니다…커서가 함께 보입니다", 2026-09-27 "보기 권한만 받은 문서에도 댓글" 이 있다. `fedc340..HEAD` 는 도움말·댓글 서랍 tweak 뿐이고 공유 코드를 건드리지 않는다. **빼야 할 미배포 기능 없음.** (로컬 `origin/deploy` 참조로 판정했다 — 그 뒤 원격이 더 나갔을 수는 있어도 뒤로 갈 수는 없다) |

---

## 1. 파일 소유

| 파일 | 변경 |
| --- | --- |
| `content/guides/sharing.md` (신규) | 공유 글. **3장 전문을 그대로** 넣는다 |
| `src/app/helpDoc.ts` | `APP_SECTIONS` 의 `## 공유` 절만 — 5장 after 로 통째 바꾼다. 다른 절은 건드리지 않는다 |
| `src/app/helpDoc.test.ts` | 6.1 U1 추가. 기존 테스트는 고치지 않는다 |
| `site/helpGuides.test.ts` | 6.2 U2~U4 추가. 기존 `U4`~`U7`(F-2039)은 고치지 않는다 — 새 링크 줄이 기존 `U4`·`U5` 반복에 저절로 들어간다 |
| `e2e/site.spec.js` | `F-2045 E1`~`E3` 추가. 기존 테스트는 건드리지 않는다 |

- `site/**` 소스·`vite.config.ts`·`src/app/HelpPage.tsx`·`src/viewer/**` **변경 없음.** 글 하나를 더하면 목록·주소·`sitemap.xml`·`llms.txt`·왼쪽 문서 목록(F-2036)이 저절로 따라온다(m4). 링크 줄의 새 탭 속성은 `renderMarkdown` `link_open`(F-123)이 붙인다(m2)
- `src/lib/peers.ts`·`src/app/liveDoc.ts` 는 테스트가 상수를 **읽기만** 한다
- 도움말에 `##`·`###` 제목을 더하지 않는다 — `site/pageNav.test.ts` F-2036 A2·`site/render.test.ts` F-2036 A6 의 도움말 제목 41개가 그대로다(7장)
- 새 의존성·저장소 식별자·설정 키 없음

### 1.1 갱신 대상 — 이번에 고치지 않는다

메인이 반영한다. 구현 에이전트는 건드리지 않는다.

| 파일 | 위치 | 반영할 내용 |
| --- | --- | --- |
| `specs/product.md` | 7장 "공개 사이트" 표, F-2039 행 아래 | `F-2045 | 공유와 실시간 함께 편집 사용법 글 | /guides/sharing, 도움말 공유 절 고쳐 쓰기와 링크` |
| `specs/features/F-2039.md` | 10장 첫 행 "공유와 실시간 함께 편집" | 메모 칸 끝에 "→ F-2045 (`/guides/sharing`)" |
| `specs/features/F-257.md` | 2장 절 표 `공유` 행 | 내용 칸을 "공유 메뉴 세 항목, 폴더 링크, 사람 초대(`보기`·`편집`), 실시간 함께 편집(접속자·커서·한 명씩 편집), `공유 관리` — 사용법 글 링크 (F-2045)" 로. 근거 칸에 F-243·F-305·F-307 |
| `specs/features/F-307.md` | 8장 표 첫 행 "보기 권한 문서 — 연결을 시도하지 않는다" | F-506 이 바꿨다는 한 줄: 온라인 `view` 는 읽기 전용 실시간 세션으로 편집 내용을 받고, 서버가 그 연결의 awareness 를 중계하지 않아 **편집자 쪽 아바타·커서에는 여전히 안 나온다**(`worker/docRoomCore.ts` `mayRelayAwareness`). 4장 C2 |
| `specs/features/F-305.md` | 15장 L4 "보기 권한·보기 모드(뷰어)는 실시간으로 따라오지 않는다" | "보기 권한은 F-506 이 해결함(온라인일 때). 보기 **모드**(뷰어)는 그대로" 한 줄. 4장 C2 |
| `specs/features/F-210.md` | 2.6 복사 주소 `{origin}/#/p/{토큰}` | F-238 이 경로 기반 `/p/{토큰}` 으로 바꿨다는 한 줄(`ShareMenu.tsx` `handleCopyReadOnlyLink`). 4장 C3 |
| `specs/features/F-211.md` | 2.4 복사 주소 `{origin}/#/p/f/{토큰}` | 같은 이유로 `/p/f/{토큰}`(`FolderMenu.tsx`·`SharesPage.tsx` `linkUrl`). 4장 C3 |
| `specs/human-checks.md` | 새 절 | 8장 H1~H4 |
| `specs/ia.md` | — | **이번에는 고치지 않는다.** 4장 C1(공유 관리 `내보내기` 버튼 글자)은 9장 Q6 에서 사람이 고를 때까지 기록만 한다 |

확인한 결과 **고칠 것이 없는 문서**: `specs/design.md`(새 시각 요소 없음 — 표·목록·굵게만), `specs/architecture.md`(모듈·키 변화 없음), `F-276.md`(목록 문구는 F-2039 가 이미 주제를 나열하지 않게 바꿨다 — `site/guidesIndex.ts` `guidesIndexContent` 확인)

## 2. 글 — 목차와 판단

| 절 | 무엇을 | 왜 이 자리 |
| --- | --- | --- |
| (머리) | 길이 셋, 누가 보고 거둘 수 있는지가 다르다 | 보내기 **전에** 고를 판단이 먼저다 |
| 세 가지 방법 비교 | `링크 복사`·`읽기 전용 링크 복사`·`사람 초대…` 여섯 줄 표 + 해시 링크 한 문단 | 세 항목이 같은 메뉴에 나란히 있어 차이를 한눈에. 해시 링크는 따로 절을 둘 만큼 할 일이 없어 표 아래 한 문단 |
| 읽기 전용 링크로 보여 주기 | 문서·폴더, 같은 주소, 폴더 범위, 위키링크 묶음(링크), 메신저 카드 | "누구에게 무엇을 어떻게 여는지" 첫째 |
| 사람을 초대해 권한 주기 | `보기`·`편집`, 메일 없음, 같은 이메일, 미리 초대, 폴더 범위, 높은 권한, `편집` 이 못 하는 것, 이미지 공간 | 둘째 |
| 받은 사람에게 보이는 것 | 링크 화면, 해시 링크 화면, 주석, 초대받은 사람 화면·`보기` 권한·오프라인 | 목적별 순서 둘째 묶음 |
| 여럿이 동시에 고치기 | 실시간, 아바타, 커서·라벨, 가려지는 커서, `보기` 는 안 보임, `Ctrl+Z`, 제목, 연결 끊김 | 셋째 묶음 |
| 실시간으로 이어지지 않을 때 | `실시간 연결 실패 · 한 명씩 편집`, 한 명씩 문구, 다시 열기, 충돌 사본, 방금 만든 문서 | 사용자가 알아채고 벗어날 길을 적는다 — 도움말에는 한 문장만(5장) |
| 공유 거두기 | 링크 끊기, 초대 지우기·낮추기, `공유 관리`, 거둘 수 없는 것 | 넷째 묶음 |
| 되는 것과 안 되는 것 | 로그인 없음·공유받은 문서·금고·이미지·댓글 표 | 다섯째 묶음. 금고는 링크만(결정 3) |
| 정리 | 다섯 줄 | `wiki-links.md`·`encryption.md` 와 같은 끝맺음 |

- **slug `sharing`**(9장 Q1): `encryption` 처럼 기능 이름 영어 명사 하나. 실시간 함께 편집은 초대로만 생기므로 `sharing` 아래에 들어간다
- **제목 `문서를 공유하고 함께 편집하기`**(9장 Q2): 검색으로 들어올 두 낱말(`공유`·`함께 편집`)을 모두 담는다. 지금 글 제목의 "…하기" 끝맺음과 맞춘다
- **한 편으로 둔다**(결정 1): m3 — 4,782자·9절로 R7 안이다. 여유 1,218자·1절. 뒤에 절을 더해 6,000자나 `##` 10개를 넘기게 되면 그때 `sharing`(링크·초대·거두기)과 `live-editing`(여럿이 동시에 고치기·이어지지 않을 때)으로 나눈다 — 나눌 때 도움말 `## 공유` 링크 줄은 `sharing` 을 그대로 가리킨다(R3: 절에 한 줄)
- **`wiki-links.md` 의 "이어진 문서를 함께 공유하기" 절과 겹침**: 이 글은 불릿 한 줄 + `[위키링크로 문서 잇기](/guides/wiki-links)` 로 가리키기만 한다(R8). 묶음 창의 글자(`함께 공유할 문서`)·끊긴 링크 표시는 그 글이 이미 적었다. 20자 이상 문장 겹침 0(m6)
- 글에 **넣지 않은 것**과 이유
  - 해시 링크 길이 경계(8,000자 경고·2,000,000자 거절, `ShareMenu.tsx` `WARN_LINK_LENGTH`·`MAX_LINK_LENGTH`): 두 상수가 **export 되지 않은 모듈 지역 상수**라 R5 대조 테스트를 걸 수 없고, 글에는 "길면 메신저에서 잘릴 수 있다" 로 충분하다. 넣으려면 `ShareMenu.tsx` 를 소유 표에 더해 export 해야 한다(9장 Q4)
  - 메신저 카드의 본문 길이(100자, `worker/publicPage.ts` `excerpt` 안 숫자): 같은 이유로 "본문 첫머리" 로만 쓴다
  - 링크 토큰 길이(43자·32바이트, `worker/token.ts`): 사용자가 판단에 쓰는 수치가 아니다
  - 첫 동기화 시간 제한(`FIRST_SYNC_TIMEOUT_MS` 10초·큰 문서 `FIRST_SYNC_HARD_TIMEOUT_MS` 30초): 두 단계라 한 문장으로 옳게 쓰기 어렵고, 사용자는 상태바 글자로 알아챈다
  - 원격 커서 라벨이 사라지는 시간(1.5초): 실제 값이 `src/styles/peers.css` 의 `animation … 1500ms` 에 있고 `CURSOR_LABEL_MS` 상수는 어디서도 쓰이지 않는다(grep — 정의 한 곳뿐). 상수와 대조해도 실제 동작을 묶지 못해 "잠깐" 으로 쓴다. 4장 C4
  - 사람 색 개수(7), 연결 끊김 뒤 재연결 백오프(1~30초), keepalive: 구현 세부
  - N5 의 다른 문구(`…그 전에 창을 닫으면 끊긴 뒤의 편집은 사라집니다.` — 브라우저 저장이 망가진 세션에만): 드물다. 글은 보통 경우(`LIVE_NOTICE.disconnected`)만 쓴다
  - 오프라인에서 처음 여는 서버 문서가 읽기 전용(`offline-view`)으로 열리는 것, 첫 동기화 전 `4404`·로그인 만료 문구: 후속 "오프라인과 동기화" 글(F-2039 10장)의 몫
  - 같은 계정의 다른 창이 한 명씩 편집을 잡은 경우의 문구(`다른 창에서 편집 중입니다. …`): 드물고, 글의 `… 님이 편집 중입니다.` 와 뜻이 같다
  - 계정 삭제가 공유 링크·초대를 지우는 것: 계정 삭제 쪽 안내(F-2038 D-15)가 이미 말한다
  - 읽기 전용 링크 화면의 이미지가 받은 사람 브라우저에 최대 5분 남을 수 있는 것(`worker/attachments.ts` 공개 첨부 `Cache-Control: private, max-age=300`): 헤더 값은 확인했지만 실제 브라우저가 얼마나 들고 있는지는 재지 않았다 — "가정으로 둔 것"(10장)

## 3. 글 전문 — `content/guides/sharing.md`

**아래를 그대로 파일로 만든다.** 구현하는 쪽이 문장을 새로 짓지 않는다. 문장의 근거는 4장 표에 있다. `date`·`updated` 는 구현 커밋 날짜가 2026-09-27 이 아니면 그 날짜로 두 줄 모두 바꾼다(R7). 줄 끝은 LF.

````markdown
---
title: 문서를 공유하고 함께 편집하기
summary: 링크로 보여 줄지, 사람을 초대해 함께 고칠지 고르는 법. 받은 사람에게 보이는 것, 여럿이 동시에 고칠 때와 공유를 거둘 때 알아 둘 것.
date: 2026-09-27
updated: 2026-09-27
---

# 문서를 공유하고 함께 편집하기

문서를 남에게 보여 주는 길은 셋입니다. 내용을 주소에 담아 보내는 링크, 로그인 없이 볼 수 있는 읽기 전용 링크, 그리고 특정한 사람을 초대하는 방법입니다. 누가 볼 수 있는지, 나중에 거둘 수 있는지가 서로 다르므로 보내기 전에 고릅니다.

## 세 가지 방법 비교

| | `링크 복사` | `읽기 전용 링크 복사` | `사람 초대…` |
| --- | --- | --- | --- |
| 볼 수 있는 사람 | 링크를 받은 누구나 | 주소를 아는 누구나 | 초대한 이메일로 로그인한 사람 |
| 받는 사람 로그인 | 필요 없음 | 필요 없음 | 필요 |
| 고칠 수 있나 | 없음. 자기 문서로 가져가서 고침 | 없음 | `편집` 권한이면 함께 고침 |
| 원본을 고치면 | 반영되지 않음 | 다시 열 때 반영 | 바로 반영 |
| 거둘 수 있나 | 없음 | 있음 | 있음 |
| 쓸 수 있는 곳 | 로그인과 상관없이 | 로그인해서 쓰는 내 문서·폴더 | 로그인해서 쓰는 내 문서·폴더 |

`링크 복사`로 만든 주소에는 문서 내용이 압축되어 들어 있습니다. 서버에 아무것도 남기지 않아 로그인하지 않아도 쓸 수 있지만, 주소를 받은 사람은 그 내용을 그대로 가집니다. 이미지는 담기지 않고, 문서가 길면 주소도 길어져 메신저에서 잘릴 수 있습니다. 그럴 때는 읽기 전용 링크나 `.md 내보내기`로 받은 파일을 씁니다.

## 읽기 전용 링크로 보여 주기

상단바 `공유` 메뉴에서 `읽기 전용 링크 복사`를 누르면 주소가 복사됩니다. 폴더는 사이드바에서 폴더의 `⋯` 메뉴를 열어 같은 항목을 고릅니다. 한 문서에는 링크가 하나라서 다시 눌러도 같은 주소가 나옵니다.

- 폴더 링크는 하위 폴더까지 모든 문서를 보여 줍니다. 링크를 만든 뒤 폴더에 넣은 문서도 보이고, 폴더 밖으로 옮긴 문서는 바로 빠집니다
- 문서의 `[[위키링크]]`가 내 다른 문서를 가리키면, 그 문서들도 함께 열지 고르는 창이 먼저 뜹니다. 자세한 흐름은 [위키링크로 문서 잇기](/guides/wiki-links)에 있습니다
- 메신저나 SNS 에 주소를 붙이면 문서 제목과 본문 첫머리가 든 카드로 보일 수 있습니다. 폴더 링크의 카드에는 폴더 이름만 들어가고 문서 내용은 들어가지 않습니다. 본문 첫머리에 남에게 보이면 안 되는 말이 있다면 링크를 만들기 전에 고칩니다

## 사람을 초대해 권한 주기

상단바 `공유` 메뉴나 폴더의 `⋯` 메뉴에서 `사람 초대…`를 누르고 이메일 주소와 권한을 정합니다. 권한은 `보기`와 `편집` 둘이고, 처음 값은 `보기`입니다.

초대해도 메일은 가지 않습니다. 이 앱의 주소를 직접 알려 주면, 받은 사람이 **초대한 이메일과 같은 주소**의 Google 또는 GitHub 계정으로 로그인했을 때 사이드바 `공유받음` 묶음에 문서가 나타납니다. 아직 가입하지 않은 주소로도 미리 초대할 수 있습니다.

- 폴더에 초대하면 그 폴더와 하위 폴더의 문서 모두에 권한이 생깁니다. 나중에 넣은 문서에도 생기고, 밖으로 옮긴 문서에서는 사라집니다
- 문서와 그 문서가 든 폴더 양쪽에 초대했다면 더 높은 권한이 적용됩니다
- `편집` 권한으로 받은 사람도 문서를 옮기거나 지우거나, 읽기 전용 링크를 만들거나 남을 초대할 수는 없습니다
- `편집` 권한인 사람이 넣은 이미지는 그 사람의 이미지 저장 공간에 잡힙니다

## 받은 사람에게 보이는 것

**읽기 전용 링크**를 연 사람에게는 앱의 사이드바 없이 문서 제목·본문·목차가 보이고, 폴더 링크라면 왼쪽에 문서 목록이 붙습니다. 오른쪽 위 `설정`에서 테마·서체·글자 크기를 바꾸고, `.md 내보내기`로 원문을 파일로 받을 수 있습니다. 화면은 연 순간의 내용이라, 원본을 고친 뒤에는 새로고침해야 바뀐 내용이 보입니다.

**`링크 복사`로 만든 주소**를 연 사람에게는 `공유받은 문서입니다. 아직 내 문서에 저장되지 않았습니다.` 띠와 함께 `내 문서로 가져오기` 버튼이 보입니다. 가져오면 그 사람 문서가 되고, 그 뒤로는 원본과 이어지지 않습니다.

두 링크 모두 본문에서 `%%`나 `<!-- -->`로 감싼 주석을 빼고 보여 줍니다. 받은 사람이 내려받는 `.md` 파일에도 주석은 들어가지 않습니다. 다만 초대받은 사람의 화면과 `마크다운 복사`로 복사한 글에는 주석이 그대로 있습니다.

**초대받은 사람**은 사이드바 `공유받음`에서 문서를 엽니다. 행 오른쪽에 문서 주인의 이메일 앞부분이 보입니다. `보기` 권한이면 `보기 권한만 있는 문서입니다.`가 뜨고 글자를 고칠 수 없지만, 다른 사람이 고친 내용은 바로 따라오고 댓글은 달 수 있습니다. 공유받은 문서는 인터넷에 연결되어 있을 때만 목록에 나오고 열립니다.

## 여럿이 동시에 고치기

`편집` 권한이 있는 사람과 문서 주인이 같은 문서를 열면 함께 고칩니다. 누가 친 글자든 다른 사람 화면에 바로 들어가고, 제목도 함께 바뀝니다. 먼저 연 사람을 기다릴 필요가 없습니다.

- 상단바 보기 모드 버튼 왼쪽에 지금 이 문서를 연 다른 사람이 이메일 첫 글자로 보입니다. 넓은 창에서는 4명까지, 좁은 창에서는 1명까지 보이고 나머지는 `+2`처럼 수로 보입니다. 마우스를 올리면 이메일이 나옵니다
- 본문에는 그 사람의 커서와 고른 범위가 사람마다 정해진 색으로 보입니다. 커서가 움직이면 이메일 `@` 앞부분이 잠깐 떴다가 사라지고, 커서에 마우스를 올리면 다시 보입니다
- 표·코드블록·이미지처럼 그림으로 바뀐 부분 안에 있는 커서는 `편집` 모드에서 보이지 않습니다. `원문` 모드에서는 보입니다
- `보기` 권한으로 연 사람은 상단바에도 본문에도 나오지 않습니다
- `Ctrl+Z`는 내가 고친 것만 되돌립니다. 다른 사람이 친 글자는 되돌리지 않습니다
- 둘이 동시에 제목을 고치면 마지막으로 입력한 쪽의 제목이 남습니다

연결이 끊겨도 계속 고칠 수 있습니다. 상태바에 `연결 끊김 · 다시 연결 중`이 뜨고, 10초 넘게 이어지면 화면 위에도 알림이 뜹니다. 끊긴 동안 고친 내용은 이 브라우저에 저장되었다가 다시 연결되면 합쳐집니다. 그사이 같은 곳을 다른 사람도 고쳤다면 두 사람의 글이 섞일 수 있어, 합쳤다는 알림이 뜨면 그 부분을 한 번 읽어 봅니다.

## 실시간으로 이어지지 않을 때

문서를 열 때 서버와 실시간 연결이 되지 않으면 상태바에 `실시간 연결 실패 · 한 명씩 편집`이 뜹니다. 이때는 이렇게 연 사람 가운데 한 명만 고칠 수 있고, 다른 사람에게는 `… 님이 편집 중입니다. 읽기만 할 수 있습니다.`가 뜹니다. 앞사람이 문서를 닫으면 `이제 편집할 수 있습니다.`가 뜨고 이어서 고칩니다.

이 상태는 그 문서를 다시 열 때까지 이어집니다. 다른 문서를 열었다 돌아오거나 새로고침하면 다시 실시간 연결을 시도합니다. 같은 곳을 실시간으로 고치던 사람이 있었다면 한 명씩 편집 쪽 글이 `(충돌 사본)`이 붙은 새 문서로 남을 수 있습니다.

방금 만든 문서처럼 아직 서버에 다 올라가지 않은 문서도, 그 창에서는 다른 사람의 편집이 바로 보이지 않습니다. `편집` 권한인 사람이 같은 문서를 열었는데 상단바에 보이지 않으면, 다른 문서를 열었다 돌아와 봅니다.

## 공유 거두기

- 읽기 전용 링크는 `공유` 메뉴나 폴더 `⋯` 메뉴의 `읽기 전용 링크 끊기`로 거둡니다. 그 뒤로 그 주소를 열면 `링크가 없거나 끊겼습니다.`만 보입니다. 다시 링크를 만들면 새 주소가 나옵니다
- 초대는 `사람 초대…` 창의 목록에서 `삭제`하거나 권한을 `보기`로 바꿉니다. 문서에 준 초대라면 그 문서를 열어 둔 사람도 곧바로 고칠 수 없게 됩니다. 폴더에 준 초대는 그 사람이 문서를 다음에 열 때부터 막히고, 이미 열어 둔 창은 바로 끊기지 않을 수 있습니다
- 상단바 맨 오른쪽 계정 메뉴의 `공유 관리`에서 만든 읽기 전용 링크와 초대한 사람을 한 화면에 모아 봅니다. 링크 줄의 `해제`는 링크를 끊고, 초대 줄의 `내보내기`는 그 사람의 초대를 지웁니다. 둘 다 확인 없이 바로 실행됩니다

거둘 수 없는 것도 있습니다. 읽기 전용 링크로 이미 열어 둔 화면은 새로고침하기 전까지 남습니다. 받은 사람이 `.md 내보내기`나 `내 문서로 가져오기`로 가져간 사본, `링크 복사`로 보낸 주소는 되돌릴 방법이 없습니다. 초대받은 사람도 `.md 내보내기`와 `마크다운 복사`로 내용을 가져갈 수 있습니다.

## 되는 것과 안 되는 것

| 경우 | 어떻게 되나 |
| --- | --- |
| 로그인하지 않고 쓰는 문서 | `링크 복사`·`마크다운 복사`만 됩니다. 같은 브라우저의 두 탭에서 열면 한 탭만 고칩니다 |
| 공유받은 문서 | `편집` 권한이어도 읽기 전용 링크를 만들거나 남을 초대할 수 없습니다. `링크 복사`·`마크다운 복사`는 됩니다 |
| 금고 문서 | 링크도 초대도 쓸 수 없고, 폴더 링크에서도 빠집니다. 이유는 [금고로 문서 암호화하기](/guides/encryption)에 있습니다 |
| 이미지 | 읽기 전용 링크와 초대에서는 보입니다. `링크 복사` 주소에는 담기지 않습니다 |
| 댓글 | 문서 주인과 초대받은 사람이 봅니다. `보기` 권한이어도 달 수 있습니다. 두 가지 링크 화면에는 나오지 않습니다 |

## 정리

- 로그인 없이 보여 주려면 읽기 전용 링크, 함께 고치려면 `편집` 권한 초대를 씁니다
- 초대는 메일을 보내지 않습니다. 같은 이메일로 로그인해야 `공유받음`에 보입니다
- 같은 문서를 연 사람은 상단바와 커서로 보이고, `Ctrl+Z`는 내 편집만 되돌립니다
- `실시간 연결 실패 · 한 명씩 편집`이 뜨면 문서를 다시 열어 봅니다
- 링크와 초대는 `공유 관리`에서 거두지만, 이미 가져간 사본은 거둘 수 없습니다
````

- `…` 는 U+2026 한 글자다(`사람 초대…` 는 화면 글자 그대로, `… 님이 편집 중입니다.` 의 앞 `…` 는 이메일 자리 표시)
- 표 첫 칸 머리가 빈 칸(`| |`)인 것은 의도다 — m3 에서 `<table>` 로 렌더됨을 확인했다
- `공유 관리` 의 초대 줄 버튼 글자 `내보내기` 는 ia.md 6장 "공유 문맥에서 쓰지 않는 말" 과 부딪힌다(4장 C1). 글은 R6 대로 **코드의 글자**를 쓰고, 무엇을 하는지 바로 뒤에 풀어 적었다. 버튼 글자가 바뀌면(9장 Q6) 이 문장도 같이 바꾼다

## 4. 사실 표 — 글의 문장과 근거

구현하는 쪽이 문장을 바꿀 때 여기를 먼저 본다. "코드" 칸은 이름으로 적는다(줄 번호는 `0d56037` 기준 힌트). **모든 행을 2026-09-27 현재 코드로 확인했다.** 명세와 코드가 다른 곳은 4.8

### 4.1 머리·세 가지 방법

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 공유 메뉴 세 항목 이름 `링크 복사`·`읽기 전용 링크 복사`·`사람 초대…` (+ `마크다운 복사`) | F-130 2장, F-210 2.6, F-212 2.5 | `ShareMenu.tsx` `items` 의 `label` |
| `링크 복사` = 내용을 압축해 주소에 담음, 서버에 남기지 않음 | F-130 3.1 | `ShareMenu.tsx` `handleCopyLink` → `encodeShare`(deflate-raw + base64url) → `formatShareHash`. 해시(`#` 뒤)는 서버 요청에 실리지 않는다(RFC 3986 3.5, F-130 3.1) |
| `링크 복사` 는 로그인과 상관없이, 거둘 수 없음, 원본이 바뀌어도 반영 안 됨 | F-130 2·4장 | `link` 항목에 서버 조건이 없다(`disabled: e2eeDoc` 뿐). 주소 자체가 내용이라 끊을 대상이 없다 |
| 받은 사람은 자기 문서로 가져가 고침 | F-130 4장 | `SharedView.tsx` `내 문서로 가져오기` → `App.tsx` `내 문서로 가져왔습니다.` |
| 읽기 전용 링크: 주소를 아는 누구나, 로그인 없이, 고칠 수 없음 | F-210 2.3 | `worker/links.ts` `/pub` 는 읽기만(`pubResponse`), 알림 `읽기 전용 링크를 복사했습니다. 링크를 아는 사람은 로그인 없이 볼 수 있습니다.` |
| 읽기 전용: 다시 열 때 반영 | F-210 2.3 "다음 조회부터" | `/pub` 응답 `Cache-Control: no-store`(`worker/http.ts` `JSON_HEADERS`), 화면은 4.4 "연 순간" 행 |
| 초대: 로그인한 사람, `편집` 이면 함께 고침, 바로 반영 | F-212, F-305 | `docPath.ts` `decideDocPath` — `owner`·`edit` 온라인은 `realtime` |
| 읽기 전용 링크·초대는 로그인해서 쓰는 **내** 문서·폴더에서만 | F-210 2.6, F-212 2.5 | `App.tsx` `shareLinkDocId`(`store.kind === 'server' && !isSharedDoc`)·`canInviteCurrentDoc`, `Sidebar.tsx` `canShare`(`ctx.isServerStore && !isE2eeFolder`) |
| 이미지는 해시 링크에 안 담김, 길면 메신저에서 잘릴 수 있음 | F-158 2.2, F-130 3.2 | `ShareMenu.tsx` 알림 `공유 링크를 복사했습니다. 이미지는 링크에 담기지 않습니다.`·`링크가 깁니다(약 {N}KB). 일부 메신저에서 잘릴 수 있어 .md 내보내기를 권장합니다.` |

### 4.2 읽기 전용 링크로 보여 주기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 폴더는 `⋯` 메뉴에서 같은 항목 | F-211 2.4 | `FolderMenu.tsx` `allItems` `share-link` `읽기 전용 링크 복사` |
| 한 문서(폴더)에 링크 하나, 다시 눌러도 같은 주소 | F-210 2.2, F-259 | `worker/links.ts` `handleCreateDocLink` — 살아 있는 링크가 있으면 그 토큰을 돌려준다. 폴더 링크 만들기도 `findActiveLink` 가 있으면 그대로 |
| 폴더 링크는 모든 하위 폴더 문서, 조회할 때 기준(넣으면 보이고 옮기면 빠짐) | F-211 2.2(F-2017 개정) | `worker/links.ts` 공개 폴더 목록 — 요청 때마다 소유자의 폴더·문서를 읽어 트리를 거른다(`e2ee = 0`·`e2ee_key IS NULL`) |
| 위키링크가 내 다른 문서를 가리키면 함께 열지 고르는 창 | F-252 3.1 | `ShareMenu.tsx` `hasWikiTargets`(`wikiResolver.resolve` 가 문서를 찾을 때만) → `ShareSetDialog` 제목 `함께 공유할 문서` |
| 메신저·SNS 카드: 문서 = 제목 + 본문 첫머리, 폴더 = 폴더 이름(문서 내용 없음) | F-238 3장 | `worker/publicPage.ts` `renderPublicPage` — 문서는 `excerpt(stripComments(doc.content))`(공백 접고 100자), 폴더는 `SITE_DESCRIPTION`. `og:`·`twitter:` 메타에 넣는다 |

### 4.3 사람을 초대해 권한 주기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 공유 메뉴·폴더 `⋯` 에서 `사람 초대…` | F-212 2.5 | `ShareMenu.tsx` `invite`, `FolderMenu.tsx` `invite` |
| 권한 `보기`·`편집`, 처음 값 `보기` | F-212 2.5 | `InviteDialog.tsx` `ROLE_LABEL`, `useState<GrantRole>('view')` |
| 메일은 가지 않음, 주소를 직접 알려 줌 | F-212 2.5 | 알림 `{이메일} 님에게 {보기|편집} 권한을 줬습니다. 앱 주소를 직접 알려 주세요.` |
| 같은 이메일의 Google·GitHub 로그인이면 `공유받음` 에 보임 | F-2034, F-212 2.4 | `InviteDialog.tsx` `invite-hint` `같은 이메일의 Google 또는 GitHub 계정으로 로그인하면 사이드바 공유받음에 보입니다.`, `Sidebar.tsx` 묶음 제목 `공유받음` |
| 가입 전 주소로도 초대 | F-212 2.1 | `worker/grants.ts` PUT — 형식·자기 자신만 거르고 `users` 를 보지 않고 `INSERT … ON CONFLICT` |
| 폴더 초대는 하위 폴더 문서까지, 조회 때 기준 | F-212 2.2·2.3(F-2017 개정) | `worker/access.ts` `FOLDER_CHAIN_SQL`(재귀) |
| 문서·폴더 양쪽이면 높은 권한 | F-212 2.2 | `worker/access.ts` `higherRole` |
| `편집` 도 옮기기·지우기·읽기 전용 링크·초대 불가 | F-212 2.2 표·2.4 | `access.ts` 판정, `Sidebar.tsx` 공유받음 행(끌기·`⋯` 없음), `App.tsx` `shareLinkDocId`·`canInviteCurrentDoc` 이 공유받은 문서를 뺀다 |
| `편집` 인 사람이 넣은 이미지는 그 사람 공간 | F-212 2.2 | `worker/attachments.ts` 올리기 — `ownerId` 로 `getUsedBytes`·`att/{ownerId}/…`·`INSERT … owner_id` |

### 4.4 받은 사람에게 보이는 것

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 링크 화면: 앱 사이드바 없음, 제목·본문·목차, 폴더면 왼쪽 문서 목록 | F-210 2.4, F-211 2.3 | `PublicView.tsx`, `PublicFolderList.tsx` |
| 오른쪽 위 `설정` 에서 테마·서체·글자 크기, `.md 내보내기` | F-230 2.1, F-210 2.4 | `PublicView.tsx` 머리 버튼 `aria-label="설정"`·`".md 내보내기"` |
| 연 순간의 내용, 새로고침해야 바뀜 | F-210 2.3 | `PublicDocView` 불러오기 effect 가 `[token]` 에만 걸려 한 번 부른다. 주기적으로 다시 읽는 코드 없음(grep `setInterval`·`visibilitychange` 0건) |
| 해시 링크 화면 띠·`내 문서로 가져오기`, 가져오면 원본과 무관 | F-130 4장 | `SharedView.tsx` 문자열 |
| 두 링크 모두 `%%`·`<!-- -->` 주석 제거, 받은 `.md` 에도 없음 | F-214 2.1·2.2 | `worker/links.ts` 공개 응답 `content: stripComments(…)`, `ShareMenu.tsx` `handleCopyLink` 의 `stripComments`, `SharedView.tsx` 렌더 전 `stripComments` |
| 초대받은 사람 화면·`마크다운 복사` 에는 주석이 그대로 | F-214 2.2 "바꾸지 않는 곳" | `worker/docs.ts` 에 `stripComments` 없음(grep), `ShareMenu.tsx` `handleCopyMarkdown` 은 `doc.content` 그대로 |
| 공유받음 행 오른쪽에 주인 이메일 앞부분 | F-212 2.4 | `Sidebar.tsx` 공유받음 행 `emailPrefix` |
| `보기` 면 `보기 권한만 있는 문서입니다.`, 고칠 수 없음 | F-212 2.4 | `App.tsx` 해당 effect, `isReadOnlyByRole` |
| `보기` 여도 다른 사람 편집이 바로 따라옴 | F-506 3.1 | `docPath.ts` `role === 'view'` 이고 온라인이면 `{ kind: 'realtime', readOnly: true }` |
| `보기` 여도 댓글을 달 수 있음 | F-506 | 체인지로그 2026-09-27, 도움말 `## 댓글` 절 |
| 공유받은 문서는 인터넷에 연결돼 있을 때만 목록·열기 | F-212 2.4(2026-09-26 개정) | `serverStore.ts` `list` — 공유 목록은 캐시하지 않고 오프라인·오류면 빈 목록, `get` 은 `knownSharedIds` 면 캐시에 넣지 않는다 |

### 4.5 여럿이 동시에 고치기

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 주인·`편집` 은 같은 문서를 함께 고침, 먼저 연 사람을 기다리지 않음 | F-301, F-305 3장 | `docPath.ts`, 실시간 경로에서 `useDocLock` 의 `docId` 가 `null`(`App.tsx` — `pending`·`fallback` 일 때만 잠금) |
| 제목도 함께 바뀜, 동시에 고치면 마지막 입력이 남음 | F-305 9.3·L9 | `src/editor/liveTitle.ts` `writeTitle`·`observeTitle` |
| 아바타 자리: 보기 모드 버튼 왼쪽 | F-307 7.1 | `TopBar.tsx` — `PeerAvatars` 가 `.view-mode-seg` 바로 앞 |
| 나는 빠짐, 이메일 첫 글자 | F-307 7.1 | `peers.ts` `visiblePeers`(`selfUserId` 제외)·`peerInitial` |
| 넓은 창 4명, 좁은 창 1명, 나머지 `+N`, 마우스를 올리면 이메일 | F-307 7.1·7.3 | `PEER_AVATARS_MAX = 4`·`PEER_AVATARS_MAX_NARROW = 1`, `PeerAvatars.tsx` `+{hidden.length}`·툴팁 `peer.email`, 좁은 창 = `App.tsx` `NARROW_QUERY` `(max-width: 1023px)` |
| 커서·고른 범위, 사람마다 정해진 색 | F-307 3.3·6.4 | `peers.ts` `peerColorIndex`(userId 해시 → 7색. 두 사람이 같은 색일 수 있어 "다른 색" 이 아니라 "정해진 색" 으로 썼다) |
| 움직이면 `@` 앞부분이 잠깐, 마우스를 올리면 다시 | F-307 3.2·6.4 | `peers.ts` `peerLabel`, `peers.css` `.cm-remote-caret-label` `animation … 1500ms`·`.cm-remote-caret:hover` |
| 그림으로 바뀐 부분 안 커서는 `편집` 에서 안 보이고 `원문` 에서 보임 | F-307 6.5(실측 (b)) | 가리는 `replace` 장식 안 점 장식이 빠지는 CM6 동작. 코드로 더 판정할 것 없음 |
| `보기` 권한은 상단바·본문에 안 나옴 | F-500 Q4, F-506 | `docRoomCore.ts` `mayRelayAwareness` 가 읽기 전용 연결의 awareness 를 중계하지 않는다(`docRoom.ts` 102행 부근) |
| `Ctrl+Z` 는 내 편집만 | F-302 5.1 | `yBinding.ts` 머리 주석 "내 편집만 되돌리기", `Y.UndoManager` 를 `yCollab` 에 넘긴다 |
| 끊기면 상태바 `연결 끊김 · 다시 연결 중`, 10초 넘으면 알림 | F-305 11.1·11.2 N5, F-306 | `StatusBar.tsx` `LIVE_STATUS.reconnecting`, `liveDoc.ts` `DISCONNECT_NOTICE_MS` → `disconnectedLong`, `App.tsx` `LIVE_NOTICE.disconnected` `서버와 연결이 끊겼습니다. 편집은 이 브라우저에 저장되고 다시 연결되면 합쳐집니다` |
| 합치고 알림, 같은 곳이면 섞일 수 있음 | F-306 7.3 N8 | `LIVE_NOTICE.merged` `연결이 끊긴 동안 한 편집을 합쳤습니다. 같은 곳을 다른 사람도 고쳤다면 문장이 섞였을 수 있습니다` |

### 4.6 실시간으로 이어지지 않을 때

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| 첫 동기화 전에 실패하면 상태바 `실시간 연결 실패 · 한 명씩 편집` | F-305 8장·11.1 | `StatusBar.tsx` `LIVE_FALLBACK_TEXT` |
| 한 명만 고침, 다른 사람에게 `… 님이 편집 중입니다. 읽기만 할 수 있습니다.` | F-213 2.3, F-305 10.2 | `useDocLock.ts` `lockedMessage`, `App.tsx` `useDocLock` 의 `docId` 조건(`pending`·`fallback`) |
| 앞사람이 닫으면 `이제 편집할 수 있습니다.` | F-213 | `useDocLock.ts` `startRetry`(`RETRY_INTERVAL_MS` 15초마다) 알림 |
| 다시 열 때까지 이어짐, 다른 문서 갔다 오기·새로고침으로 다시 시도 | F-305 3장·8장 | `App.tsx` `DocSession` — `currentDocId` 가 바뀔 때 새 세션 |
| 실시간 쪽과 겹치면 `(충돌 사본)` | F-305 10.2·L3, F-304 18장 Q4 | `serverStore.ts` `handleConflict` — 제목 `… (충돌 사본)` |
| 방금 만든 문서처럼 안 올라간 문서는 그 창에서 실시간 아님, 다시 열어 보기 | F-305 L8 | `docPath.ts` `hasPendingChanges` → `pending`(실시간 아님, 상태바 표시도 없음 — F-305 11.1) |

### 4.7 거두기·되는 것과 안 되는 것

| 글의 사실 | 원천 명세 | 코드에서 확인한 곳 |
| --- | --- | --- |
| `읽기 전용 링크 끊기`, 뒤로는 `링크가 없거나 끊겼습니다.`, 다시 만들면 새 주소 | F-210 2.2·2.4·2.6 | `ShareMenu.tsx`·`FolderMenu.tsx` `읽기 전용 링크 끊기`, `PublicView.tsx` `notFoundMessage`, `handleCreateDocLink` 는 살아 있는 링크가 없을 때 `generateToken` |
| 초대 `삭제`·`보기` 로 낮추기 — 문서 초대면 열어 둔 사람도 곧바로 못 고침 | F-304 9.1, F-305 8장 N2 | `worker/grants.ts` DELETE·PUT(`role === 'view'`)의 `notifyRevalidate` → `docRoomCore.ts` `revalidateConnections` → `4403`(`revoked`) → `App.tsx` `LIVE_NOTICE.revoked`/`READ_ONLY_LIVE_NOTICE.revoked` |
| 폴더 초대는 다음 열 때부터, 열어 둔 창은 바로 안 끊길 수 있음 | F-304 9.1 "폴더 초대는 주기 점검" | `grants.ts` 는 `targetType === 'doc'` 일 때만 알린다. 방의 주기 점검은 저장(flush) 뒤 `REVALIDATE_INTERVAL_MS`(60초)에 한 번까지 — 편집이 없으면 돌지 않는다. 그래서 시간을 적지 않았다 |
| 상단바 맨 오른쪽 계정 메뉴 `공유 관리`, `해제`·`내보내기`, 확인 없음 | F-243 3.3·3.5 | `TopBar.tsx` 마지막 `AccountMenu`, `AccountMenu.tsx` `공유 관리`(로그인일 때), `SharesPage.tsx` 버튼 글자와 `revokeLink`·`revokeGrant` 가 확인 없이 부른다 |
| 이미 연 링크 화면은 새로고침 전까지 남음, 가져간 사본·해시 주소는 못 거둠 | F-210 2.4, F-130 | 4.4 "연 순간" 행, 공개 화면 `.md 내보내기`, 해시 링크는 내용 자체 |
| 초대받은 사람도 `.md 내보내기`·`마크다운 복사` | — | `App.tsx` `exportDisabled` 조건에 역할이 없다, `ShareMenu.tsx` `markdown` 항목은 늘 있다 |
| 로그인 안 한 문서: `링크 복사`·`마크다운 복사` 만, 두 탭이면 한 탭만 | F-130, F-296 6.4 | `shareLinkDocId`·`canInviteCurrentDoc` 이 `store.kind === 'server'` 를 요구, `useTabSync.ts` `다른 탭에서 편집 중입니다. 읽기만 할 수 있습니다.`, `App.tsx` `claimDocId`(`store.kind === 'idb'`) |
| 공유받은 문서: 링크·초대 불가, `링크 복사`·`마크다운 복사` 됨 | F-212 2.4 | 위 두 조건의 `!isSharedDoc`, `ShareMenu.tsx` `link`·`markdown` 항목 |
| 금고 문서: 링크·초대 불가, 폴더 링크에서 빠짐 | F-409 6장 | `ShareMenu.tsx` `disabled: e2eeDoc`, `worker/links.ts` `e2ee_doc`·`e2ee_folder` 409, 공개 폴더 목록 `e2ee_key IS NULL` |
| 이미지: 링크·초대에서 보임, 해시 링크엔 없음 | F-209·F-211 2.2, F-158 2.2 | `worker/attachments.ts` 공개 첨부 경로(참조·소유 확인), `SharedView.tsx` `missingImageText="공유 링크에는 이미지가 담기지 않습니다"` |
| 댓글: 주인·초대받은 사람, `보기` 도 달 수 있음, 두 링크 화면엔 없음 | F-500, F-506 | `PublicView.tsx` 에 댓글 코드 없음(grep `comment` 0건), `SharedView.tsx` 는 `content` 만 렌더 |

### 4.8 명세와 코드가 다른 곳 (코드를 따랐다)

| 명세 | 명세의 글 | 코드 | 글은 |
| --- | --- | --- | --- |
| F-307 8장 첫 행, F-305 15장 L4 | 보기 권한 문서는 연결하지 않는다 — 실시간으로 따라오지 않는다 | F-506 뒤로 온라인 `view` 는 읽기 전용 실시간 세션(`docPath.ts`). 편집자 쪽에는 여전히 안 보인다(`mayRelayAwareness`) | "다른 사람이 고친 내용은 바로 따라오고", "`보기` 권한으로 연 사람은 상단바에도 본문에도 나오지 않습니다" |
| F-210 2.6, F-211 2.4 | 복사 주소 `{origin}/#/p/{토큰}`·`#/p/f/{토큰}` | `/p/{토큰}`·`/p/f/{토큰}`(F-238) | 주소 모양을 쓰지 않는다 — 영향 없음 |
| 도움말 `## 공유`(F-257) | `읽기·편집 권한` | 화면 글자는 `보기`·`편집`(`InviteDialog.tsx` `ROLE_LABEL`) | 5장에서 도움말도 고친다 |
| F-243 3.3 | 초대 줄 버튼 `내보내기` | 같다 | 글자는 그대로 쓰고 뜻을 풀었다. ia.md 와의 충돌은 C1 |

## 5. 도움말 `## 공유` 절 — before / after

`src/app/helpDoc.ts` `APP_SECTIONS` 의 이 절만 통째로 바꾼다. 절 제목과 자리(`## 템플릿` 뒤, `## 댓글` 앞)는 그대로다.

before (80자, 1문단):

```
문서는 읽기 전용 링크로 공유할 수 있고, 폴더 전체를 공개할 수도 있습니다. 로그인한 사람을 초대해 읽기·편집 권한을 나눠 줄 수도 있습니다.
```

after (`사용법 글:` 줄 뺀 본문 445자, 4문단 — m2):

```
상단바 `공유` 메뉴에서 `링크 복사`를 누르면 문서 내용을 주소에 담은 링크가 복사되고, 로그인하지 않아도 쓸 수 있습니다. 로그인해서 쓰는 내 문서는 `읽기 전용 링크 복사`로 로그인 없이 볼 수 있는 주소를 만들고, `읽기 전용 링크 끊기`로 거둡니다. 폴더는 `⋯` 메뉴에서 같은 항목을 고릅니다.

`사람 초대…`에서 이메일과 `보기`·`편집` 권한을 정합니다. 메일은 가지 않으니 앱 주소를 따로 알려 주세요. 초대받은 사람이 같은 이메일로 로그인하면 사이드바 `공유받음`에 문서가 보입니다.

`편집` 권한이 있는 사람이 같은 문서를 열면 함께 고칩니다. 상단바에 지금 문서를 연 사람이, 본문에 그 사람의 커서가 보입니다. 상태바에 `실시간 연결 실패 · 한 명씩 편집`이 뜨면 한 사람씩만 고칠 수 있습니다.

만든 링크와 초대는 계정 메뉴 `공유 관리`에서 모아 보고 거둡니다.

사용법 글: [문서를 공유하고 함께 편집하기](/guides/sharing)
```

- `helpDoc.ts` 는 템플릿 리터럴이라 원문의 백틱은 `` \` `` 로 이스케이프한다(지금 절들과 같다)
- **링크 줄만 달지 않고 고쳐 쓰는 이유**: R3 은 "도움말 절은 링크 없이도 그 기능을 쓸 수 있을 만큼" 적으라고 한다. 지금 절은 메뉴가 어디 있는지, 초대받은 사람이 어디서 보는지, 실시간 함께 편집과 그 폴백을 전혀 말하지 않는다(m1). 오프라인에서 사용법 글을 못 여는 사람이 "왜 읽기만 되나" 를 찾을 곳이 없다. 넷째 문단의 `공유 관리` 는 거두는 곳 하나를 알린다
- 넣지 않은 것: 수치(R5 — 이 절은 수치 0개), 금고 문서 제외(`## 금고` 절 넷째 문단이 이미 `금고 문서는 공유할 수 없고` 라고 말한다), 해시 링크의 이미지·길이, 초대 범위 규칙(글의 몫)
- 문장 모양: 말투·인라인 코드 규칙은 F-257 1장. 20자 이상 문장 10개가 글과 하나도 겹치지 않는다(m5, U5)

## 6. 수용 기준

단위 테스트 환경은 `node`(DOM 없음). 새 판정 함수를 만들지 않고 F-2039 가 만든 두 테스트 파일의 도우미(`appSections`, `readGuide`·`guideBody`)를 그대로 쓴다.

### 6.1 도움말 — `src/app/helpDoc.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| U1 | `## 공유` 절 (5장) | `appSections()` 의 `공유` 절에서 ① 마지막 문단이 정확히 `사용법 글: [문서를 공유하고 함께 편집하기](/guides/sharing)` ② 본문에 `` `사람 초대…` ``·`` `공유받음` ``·`` `실시간 연결 실패 · 한 명씩 편집` ``·`` `공유 관리` ``·`` `보기`·`편집` `` 이 있다 ③ `읽기·편집 권한` 이 없다(옛 문구). 기존 F-2039 U1(600자·4문단)·U2(링크 줄 모양)·G1(절 순서)·U17·U23(절 앞뒤)이 그대로 통과한다 |

- ② 의 네 화면 글자는 `ShareMenu.tsx`·`Sidebar.tsx`·`StatusBar.tsx`·`AccountMenu.tsx` 의 글자와 같다. 테스트가 컴포넌트를 import 하지 않는 이유: `.tsx` 컴포넌트를 node 테스트로 끌어오면 아이콘·CSS 가져오기까지 따라온다(스크래치 측정에서 esbuild 가 `?raw`·`.css` 로더 없이 실패했다). 화면 글자가 바뀌면 이 테스트가 아니라 사람 확인 H3 이 잡는다 — 9장 Q5

### 6.2 사용법 글 — `site/helpGuides.test.ts`

| # | 확인 | 기대 |
| --- | --- | --- |
| U2 | 공유 글의 수치 (R5) | `content/guides/sharing.md` 에 아래 세 문자열이 있다. 기대 문자열은 **테스트가 상수에서 만든다** |
| U3 | 공유 글에 제품명이 없다 (R6) | `sharing.md` 본문(소문자)에 `brand.name`·`brand.shortName`(소문자)이 없다. 기존 `encryption.md` 테스트와 같은 모양으로 한 `it` 을 더한다 |
| U4 | 글 사이 링크 대상 (R8) | **모든** `content/guides/*.md` 본문의 `](/guides/{slug})` 마다 `content/guides/{slug}.md` 가 있다. 지금 `wiki-map.md` → `wiki-links`, 새 `sharing.md` → `wiki-links`·`encryption` 세 개 — 모두 있다(2026-09-27 grep) |
| — | 기존 F-2039 U4·U5·U6 | **코드 변경 없이** 새 링크 줄과 새 글을 반복에 넣는다: U4 링크 글자 = `sharing.md` `title`, U5 `(## 공유, sharing.md)` 쌍 문장 겹침 0(m5), U6 `sharing.md` 쓰지 않는 말 0·4,782자·`##` 9개(m3·m7) |

U2 대조 행(m8 에서 초안이 모두 통과):

| 글의 문자열 | 만드는 상수 (import 위치) |
| --- | --- |
| `넓은 창에서는 {N}명까지` | `PEER_AVATARS_MAX` (`src/lib/peers.ts`) |
| `좁은 창에서는 {N}명까지` | `PEER_AVATARS_MAX_NARROW` (같은 파일) |
| `{N}초 넘게` (`DISCONNECT_NOTICE_MS / 1000`) | `DISCONNECT_NOTICE_MS` (`src/app/liveDoc.ts`) |

- `peers.ts` 는 `docRoomProtocol.ts`·`docComments.ts` 만, `liveDoc.ts` 는 타입과 `docRoomProtocol.ts` 만 가져온다 — node 에서 부수효과 없이 불린다(m8)
- `site/**/*.test.ts` 는 `vite.config.ts` `test.include` 에 들어 있다(F-2039 8.2)

### 6.3 동작 — `e2e/site.spec.js` 의 `F-2045 E*`

빌드된 사이트가 있어야 도는 테스트라 F-2039 E* 옆에 둔다. 모양·문장은 고정하지 않는다(8장).

| # | 확인 | 방법 | 기대 |
| --- | --- | --- | --- |
| E1 | 글이 뜨고 완결된 정적 페이지다 | `page.goto('/guides/sharing')`, 그리고 `page.request.get('/guides/sharing')` | `문서를 공유하고 함께 편집하기` `h1` 과 `.site-foot` 이 보인다. 응답 `200`, `<title>문서를 공유하고 함께 편집하기 · Rawdoc</title>`, `https://rawdoc.app/guides/sharing`, `<script` 없음 |
| E2 | 목록·색인 | `/guides` 로 가고 `/sitemap.xml` 요청 | `.site-article .markdown-body > ul a[href="/guides/sharing"]` 이 보이고 글자가 제목과 같다. `sitemap.xml` 에 `<loc>https://rawdoc.app/guides/sharing</loc>` |
| E3 | 앱 도움말에 링크가 있다 | `openApp(page)` → 사이드바 `getByRole('button', { name: '도움말' }).first()` | `.help-page a[href="/guides/sharing"]` 이 보이고 글자 `문서를 공유하고 함께 편집하기`, `target="_blank"` |

- E1 의 `Rawdoc` 은 F-2039 E2·F-272 등 기존 e2e 와 같은 방식이다(e2e 는 JS 이고 기존 테스트가 이미 제품명 글자를 쓴다)
- 새 탭 팝업 동작은 F-2039 E4 가 같은 경로(`renderMarkdown` `link_open`)로 이미 본다 — 다시 재지 않는다. 서비스 워커 precache 제외는 경로 규칙(`/^\/guides(\/|\?|$)/`)이라 글마다 볼 필요가 없다(F-2039 E3 가 한 번 본다)

### 6.4 빌드·회귀·범위

| # | 확인 | 기대 |
| --- | --- | --- |
| A1 | `npm run build` | 성공. `dist/guides/sharing.html` 이 생긴다(가드 G1~G5 통과) |
| A2 | 관련 회귀 | `npm run e2e:one -- "F-2045|F-2039|F-276|F-274"` 통과 |
| A3 | 단위·타입·린트 | `npm test`, `npm run typecheck`, 바꾼 파일 eslint 통과 |
| A4 | 소유 범위 | `npm run review -- F-2045` — 1장 표 밖 변경 없음, `specs/**`·`CLAUDE.md` 무변경 |

## 7. 기존 테스트 회귀 — grep 으로 확인한 것 (2026-09-27, `0d56037`)

**깨지는 테스트는 없다.** 확인한 것:

| 파일 | 판정 |
| --- | --- |
| `src/app/helpDoc.test.ts` G1 `SECTION_ORDER` | 절 제목·순서를 바꾸지 않는다 |
| 같은 파일 U17(F-404)·U23(F-505) | `indexOf('## 공유')` 로 **자리**만 본다 — `## 공유` 제목과 자리는 그대로 |
| 같은 파일 F-2039 U1·U2 | 5장 after 가 445자·4문단, 링크 줄이 마지막 문단(m2) |
| 같은 파일 "제품명 문자열을 직접 쓰지 않는다" | 상대 주소 링크, 제품명 없음 |
| 같은 파일 "표준 이미지 문법을 쓰지 않는다"·U6 `{{` | 해당 글자 없음 |
| `site/helpGuides.test.ts` F-2039 U4·U5·U6 | 새 쌍·새 글을 반복에 넣고 통과(m3·m5·m7) |
| `site/helpPage.test.ts` F-274 A1~A3 | 같은 원본(`HELP_DOC_CONTENT`)을 비교 — 링크 줄도 같게 나온다 |
| `site/pageNav.test.ts` F-2036 A2·`site/render.test.ts` F-2036 A6 (도움말 제목 **41개**) | 도움말에 `#`·`##`·`###` 를 더하지 않는다 — 41개 그대로 |
| `site/build.test.ts` | 내장 픽스처 `content` 로 부른다 — 저장소 글과 무관 |
| `e2e/site.spec.js` F-276 A10 | 목록 링크 수를 `content/guides` 의 `.md` 수로 센다 — 8편이 되어도 통과 |
| 같은 파일 precache·sitemap 합친 테스트 | 경로를 나열해 **있는지** 본다 — 새 글과 무관 |
| 같은 파일 F-2039 E3 | `/guides` 목록에서 `encryption` 링크만 본다 — 첫 줄이 `sharing` 으로 바뀌어도 통과 |
| 같은 파일 F-2039 E5 | `/help` 에서 `금고로 문서 암호화하기` 링크 — 그대로 |
| `e2e/siteToc.spec.js` | `/guides/wiki-links` 의 `aria-current` 하나 — 목록 항목이 늘어도 통과 |
| `e2e/help.spec.js` | 목차 항목 수를 화면에서 센 제목 수와 비교(`headingCount`) — 제목이 늘지 않는다 |
| `e2e/fontSubset.spec.js` | `zettelkasten.md` 만 읽는다 |
| `e2e/topbar.spec.js`·`invite.spec.js`·`publicView.spec.js`·`peers.spec.js` | 도움말·사용법 글을 보지 않는다 |

## 8. 사람 확인 필요 (자동 판정 불가)

`specs/human-checks.md` 로 넘긴다.

| # | 확인 |
| --- | --- |
| H1 | 글을 처음부터 끝까지 읽기 — 어조가 다른 사용법 글과 같은지, 첫 표(6줄 × 3칸)가 좁은 창에서 읽히는지, 겁주는 말이나 과장이 없는지, "거둘 수 없는 것" 이 이해되는지 |
| H2 | 글의 흐름을 실제 배포 앱에서 두 계정으로 한 번씩 — `읽기 전용 링크 복사`(시크릿 창에서 열기·`설정`·`.md 내보내기`·`%%주석%%` 빠짐), 끊은 뒤 새로고침, `사람 초대…` → 받은 계정 `공유받음`, 둘이 같은 문서 편집(아바타·커서 라벨이 잠깐 떴다 사라짐·`Ctrl+Z` 내 것만), **`보기` 계정이 편집자 커서를 보는지**(10장 가정 1), 초대 `삭제` 때 상대 화면, `공유 관리` 의 `해제`·`내보내기`. 글과 다른 곳이 있으면 적는다 |
| H3 | 앱 도움말 `## 공유` 네 문단이 링크 없이도 메뉴를 찾게 하는지, 화면 글자(`사람 초대…`·`공유받음`·`실시간 연결 실패 · 한 명씩 편집`·`공유 관리`)가 지금 화면과 같은지 |
| H4 | 메신저(카카오톡 등)에 읽기 전용 링크를 붙였을 때 카드에 제목·본문 첫머리가 보이는지, 폴더 링크 카드에 문서 내용이 없는지 |

## 9. 열린 질문 — 모두 기본값이 있다

| # | 질문 | 기본값과 근거 | 다른 쪽을 고르면 |
| --- | --- | --- | --- |
| Q1 | 글 주소 slug | **`sharing`** — `encryption` 과 같은 "기능 이름 영어 명사 하나". 실시간 함께 편집은 초대로만 생겨 공유의 한 갈래다. 주소는 한 번 나가면 바꾸지 않는다(F-276 Q4) | `collaboration`: 함께 편집을 앞세우지만 읽기 전용 링크(혼자 보여 주기)가 어색하게 걸린다. `share`: 동사형. 바꾸면 파일 이름, 5장 링크 줄, U1·E1~E3 주소 문자열 |
| Q2 | 글 제목 | **`문서를 공유하고 함께 편집하기`** — 검색 낱말 둘을 모두 담는다 | `링크와 초대로 함께 쓰기`: 짧지만 `공유`·`편집` 이 빠진다. 바꾸면 3장 `title`·`h1`, 5장 링크 글자, U1·E1~E3 |
| Q3 | 한 편 유지 | **한 편**(결정 1) — 4,782자·9절(m3). 여유 1,218자·1절 | 넘길 때 나누는 선은 2장("링크·초대·거두기" / "동시에 고치기·이어지지 않을 때") |
| Q4 | 해시 링크 길이 경계(8,000·2,000,000)를 글에 넣나 | **넣지 않는다** — 두 상수가 export 되지 않아 R5 대조를 걸 수 없고, 글은 "길면 잘릴 수 있다" 로 충분 | 넣으면 `ShareMenu.tsx` 를 1장에 더해 두 상수를 export 하고 U2 에 두 행. 알림 문구가 이미 길이를 KB 로 알려 준다 |
| Q5 | 도움말 화면 글자를 컴포넌트 상수와 테스트로 묶나 | **묶지 않는다** — 글자는 컴포넌트 안 문자열이라 테스트가 `.tsx` 를 끌어와야 한다(6.1). H3 이 사람 눈으로 본다 | 묶으려면 네 글자를 순수 모듈(예: `src/app/shareLabels.ts`)로 빼는 리팩터가 먼저다 — 이 명세 범위를 넘는다 |
| **Q6 (사람)** | `공유 관리` 초대 줄 버튼 글자 `내보내기`(4장 C1) | **이 명세는 고치지 않는다** — 글은 지금 글자를 쓰고 뜻을 풀어 적는다. 버튼 글자를 바꾸는 것은 코드 변경이라 `tweak` 한 줄이면 된다 | 바꾸면(권장 후보 `초대 삭제` — `InviteDialog.tsx` 의 `삭제` 와 맞음) 3장 "공유 거두기" 셋째 불릿의 `` `내보내기`는 그 사람의 초대를 지웁니다 `` 를 `` `초대 삭제`로 그 사람의 초대를 지웁니다 `` 로. `e2e/shares.spec.js` `F-243 A11 내보내기` 와 `src/app/SharesPage.test.tsx` 의 글자도 함께 |
| Q7 | 폴더 초대를 거둘 때의 늦음을 수치로 적나 | **적지 않는다** — 열린 연결의 재확인은 방이 저장할 때 60초에 한 번까지라, 편집이 없으면 한없이 늦을 수 있다(4.7). "다음에 열 때부터" 만 확실하다 | 적으려면 서버가 폴더 초대 변경 때도 방에 알리게 바꾸는 명세가 필요하다(`worker/grants.ts` 는 지금 `doc` 일 때만 `notifyRevalidate`) |

## 10. 가정으로 둔 것 (코드로 확인하지 못했다)

1. **`보기` 권한인 사람의 화면에 편집자의 원격 커서가 그려지는지.** 서버가 편집자 awareness 를 모든 연결에 보내고(`docRoom.ts` 중계 반복이 연결을 가리지 않는다), 앱은 읽기 전용 세션에도 `livePeers` 를 상단바에 넘긴다(`App.tsx` `peers=`)는 것까지 읽었다. 읽기 전용 편집기에 `remoteCursors` 장식이 실제로 그려지는지는 돌려 보지 않았다 — **그래서 글에는 이 문장을 쓰지 않았다.** H2 에서 본다
2. 메신저·SNS 가 카드를 어떻게 만들고 얼마나 들고 있는지. 서버가 넣는 메타(`og:title`·`og:description`)는 코드로 확인했지만 받는 쪽 동작은 외부다. 글은 "카드로 보일 수 있습니다" 까지만 쓰고, "링크를 끊어도 카드가 남는다" 는 쓰지 않았다
3. 공개 첨부의 `Cache-Control: private, max-age=300` 이 받은 사람 브라우저에 이미지를 최대 5분 남긴다는 것 — 헤더 뜻으로는 그렇지만 재지 않았다. 글에 넣지 않았다
4. 로컬 `origin/deploy` 참조가 원격과 같다는 것(m9). 원격이 더 나갔다면 결론(모두 배포됨)은 바뀌지 않는다
5. `Ctrl+Z` 가 원격 편집을 되돌리지 않는다는 것 — `yBinding.ts` 머리 주석과 F-302 5.1(`y-codemirror.next` 가 자기 origin 만 추적) 을 근거로 했고, 이 세션에서 두 Doc 을 붙여 돌려 보지는 않았다. F-302·F-305 e2e 가 이미 다룬 동작이다

## 11. 기존 명세·코드와 부딪히는 곳 (보고)

| # | 무엇 | 어디 | 처리 |
| --- | --- | --- | --- |
| C1 | `공유 관리` 초대 줄 버튼 글자 `내보내기`. `specs/ia.md` 6장 "다른 사람에게 보내기" 행이 공유 문맥의 `내보내기` 를 쓰지 않는 말로 두고(파일 다운로드와 섞지 않음), 같은 화면 머리에는 `.md 내보내기` 가 있다 | `SharesPage.tsx`, F-243 3.3 | 9장 Q6(사람). 이 명세는 글에서 뜻을 풀어 쓰는 데서 멈춘다 |
| C2 | F-307 8장 첫 행·F-305 L4 가 F-506 뒤 낡았다 — 온라인 `view` 는 실시간으로 받는다 | F-307, F-305 | 1.1 갱신 대상 |
| C3 | F-210 2.6·F-211 2.4 의 복사 주소가 해시 경로(`#/p/…`)로 남아 있다. 코드는 F-238 경로 기반 | F-210, F-211 | 1.1 갱신 대상 |
| C4 | `src/editor/remoteCursors.ts` 의 `CURSOR_LABEL_MS = 1500` 이 export 만 되고 쓰이지 않는다. 실제 값은 `src/styles/peers.css` `animation … 1500ms` 에 따로 있어 둘이 어긋날 수 있다 | `remoteCursors.ts`, `peers.css` | 이 명세 범위 밖. 글에 이 수치를 쓰지 않아 영향 없음(2장). 정리하려면 별도 변경(CSS 변수로 한 곳에 두거나 상수를 지우기) |
| C5 | 도움말 `## 공유` 의 `읽기·편집 권한` 이 화면 글자 `보기`·`편집` 과 다르다 | `helpDoc.ts` | 5장에서 고친다 |

## 12. 구현 순서 (TDD)

1. `src/app/helpDoc.test.ts` 에 U1 → 빨강(링크 줄·네 화면 글자 없음, `읽기·편집 권한` 있음)
2. `site/helpGuides.test.ts` 에 U2~U4 → U2·U3 빨강(파일 없음), U4 초록(지금 링크 하나가 이미 맞다 — 규칙을 고정하는 테스트)
3. `content/guides/sharing.md` 를 3장 그대로(날짜 규칙 R7) → U2·U3·U4 초록, F-2039 U6 초록
4. `src/app/helpDoc.ts` `## 공유` 를 5장 after 로 → U1 초록, F-2039 U1·U2·U4·U5 초록, 나머지 helpDoc·helpPage·pageNav·render 테스트 초록
5. `npm run build`(A1), `dist/guides/sharing.html`·`dist/guides.html` 을 눈으로 한 번 연다
6. `e2e/site.spec.js` E1~E3 → `npm run e2e:one -- "F-2045"`
7. A2~A4
