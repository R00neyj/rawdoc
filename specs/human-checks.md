# 사람 확인 목록

자동 테스트(`npm test`, `npm run test:e2e`)로 판정할 수 없는 항목만 모은다. 메인이 명세 완료마다 갱신한다
판정 환경: 데스크톱 Chrome, 한국어 Windows IME (`specs/product.md` Q7)

상태: `대기` / `통과 (날짜)` / `실패 (날짜, 내용)`

## 1. 한글 IME (실제 입력)

기준: 글자 중복·누락·커서 튐 없음

| # | 명세 | 확인 | 상태 |
| --- | --- | --- | --- |
| H1 | F-103 A8 | 본문에 한글 연속 입력 | 통과 (2026-09-14) |
| H2 | F-104 A4 | 굵게(`**`) 안 한글 연속 입력, 기호 경계에서 조합, 조합 중 방향키 | 통과 (2026-09-14) |
| H3 | F-105 A4 | 제목·목록·인용 줄에서 한글 입력 | 통과 (2026-09-14) |
| H4 | F-106 A5 | 코드블록 안 한글 입력, 블록 경계에서 조합 중 방향키 | 통과 (2026-09-14, 비고: 코드블록 한글이 작게 보임 — F-154 D2Coding 과 관련) |
| H5 | F-125 A8, F-135 A3 | 표 칸 안 한글 연속 입력, 조합 중 Tab·Enter·방향키 | 통과 (2026-09-15, 이전 실패 2026-09-14 → F-161 수정) |
| H6 | F-139 | 표를 줄 단위로 치며 한글 입력 | 실패 (2026-09-15, 줄 단위 한글 입력은 정상. Alt+Enter 줄바꿈만 안 됨 — F-162 는 표 칸 편집 중에만 동작, 입력 위치 확인 필요) |
| H7 | F-127 A5 | 한글 조합 직후 `(`·`*`, 짝 안에서 한글 연속 입력 | 통과 (2026-09-14) |
| H8 | F-128 A7 | 콜아웃 제목·본문 한글 입력 | 대기 (2026-09-14 비고: 색은 나오나 아이콘 없음 → F-148 반영, 재확인) |
| H9 | F-129 A7 | 링크 글자 안 한글 입력 | 통과 (2026-09-14) |
| H10 | F-131 A9 | `[[` 뒤 한글 제목 입력하며 자동완성, 조합 중 Esc | 통과 (2026-09-14) |
| H11 | F-133 A8 | 프론트매터 값에 한글 입력 | 통과 (2026-09-14) |
| H12 | F-134 A2 | 제목·목록 윗줄 끝에서 한글 입력 | 통과 (2026-09-14) |
| H13 | F-146 A10 | 한글 조합 중 문서 칸 좌우 여백 클릭, 실제 Tab 키로 편집기 포커스 복귀(e2e 는 `focus()` 로 대신 판정) | 대기 |
| H14 | F-166 A7 | 편집 모드 목록 줄(글머리·순서·할일)에서 한글 입력하며 커서 출입, 기호가 거터 쪽으로 흔들리지 않는지 | 대기 |
| H15 | F-202 A7 | TS 이전 뒤 회귀: 편집 모드 연속 한글 입력, 표 칸 입력, 제목 줄 기호 토글에서 커서 튐·글자 중복 없는지 (배포 주소 rawdoc.app) | 대기 |
| H122 | F-261 | 실제 한글 IME 로 찾기·치환 패널 입력 필드에 타이핑하는 감각(자동화는 영문으로 우회 판정) | 대기 |

## 2. OS·시스템 창

| # | 명세 | 확인 | 기대 | 상태 |
| --- | --- | --- | --- | --- |
| H20 | F-119 A3 | 설치한 앱을 Windows 탐색기 `.md` 연결 프로그램으로 열기 | 새 문서로 가져와짐 | 재확인 필요 (2026-09-14 실패는 dev 서버 localhost:5173 에서 설치 — dev 는 매니페스트 미생성 `vite.config.js` `devOptions.enabled: false`. `npm run build` 후 preview 주소에서 기존 앱 제거 → 재설치 → 탐색기 `.md` 우클릭 "연결 프로그램 > 다른 앱 선택" 에서 확인) |
| H21 | F-130 A8 | 공유 → 파일로 공유 | 시스템 공유 창이 뜸 | 해당 없음 (2026-09-14 실패 "공유하지 못했습니다" — Chromium 공유 허용 목록에 `.md` 없음. F-163 으로 항목 제거) |
| H22 | F-145 A9 | 탐색기에서 `.md` 파일을 앱 창에 끌어다 놓기, 덮개 모양, 에디터 안 글자 끌어 옮기기(A7) | 덮개 표시, 새 문서로 열림. 글자 옮기기는 지금과 같음 | 통과 (2026-09-14) |
| H23 | F-120 | Cloudflare 로그인 후 실제 배포 | 배포 주소에서 앱·PWA 설치 동작 | 배포 통과 (2026-09-15 메인 `rawdoc.app` 배포, `/api/health` db true, `/` 200). PWA 설치·오프라인은 사람 확인 대기 |

## 3. 눈으로 판단

| # | 명세 | 확인 | 상태 |
| --- | --- | --- | --- |
| H30 | F-124 A7 | 편집·보기 모드가 GitHub 스타일과 비슷하고 색이 어울리는지 | 통과 (2026-09-15) |
| H31 | F-139 3.1 | 커서가 표 원문 안이면 원문, 밖이면 표로 바뀌는 방식이 괜찮은지 | 통과 (2026-09-15) |
| H32 | F-140 A10 | 표 호버 모양, 칸 서식 모양, 편집 칸 강조 이동 느낌 | 통과 (2026-09-15) |
| H33 | F-141 A10·A14 | 세피아·다크 색감, 세리프 본문 가독성, 스크롤바 모양 | 통과 (2026-09-15) |
| H34 | F-142 A11 | 상단바 아이콘 모양·크기, 툴팁 문구, 폴더 가이드 선 | 통과 (2026-09-15) |
| H35 | F-143 A13 | 사이드바 레일 모양, 목록 행 여백, 아이콘 선택, 편집 영역 개방감 | 통과 (2026-09-15) |
| H36 | F-144 A11 | 목차 노션 느낌, 선 길이·색, 카드 위치 | 통과 (2026-09-15) |
| H37 | F-148 A8 | 콜아웃 아이콘이 예시 이모지 뜻과 맞는지, 크기 | 대기 |
| H38 | F-149 A8 | 호버·툴팁 전환 느낌(탄성 곡선), 트리에서 문서 끌 때 놓기 강조(`.tree-row--drop`) 반응이 늦지 않은지, 알림 띠·공유 화면 버튼 호버 전환(e2e 미확인) | 재확인 필요 (2026-09-15 통과 후 사용자 "호버가 두 번 겹쳐 들어가는 느낌" → F-173 색 곡선 변경. display 로 켜고 끄는 요소 전환은 F-172) |
| H39 | F-151 A9 | 상단바 제품명·토글·검색 묶음 모양, 접힘 전환 느낌 | 통과 (2026-09-15) |
| H40 | F-152 A10 | 편집 모드 목록 내어쓰기, 제목·구분선·콜아웃 제목 줄 번호 위치, 보기 모드 코드블록 안 글자 바탕 없음 | 대기 |
| H41 | F-153 A9 | 상단바 버튼 선택 표시 무게감, 사이드바 현재 행, 세피아 색감 | 통과 (2026-09-15) |
| H42 | F-154 A9 | 원문 모드·코드 고정폭(2026-09-15 영문·한글 모두 D2Coding, 크기 보정 없음)이 영문·한글 크기 어울리는지, 글자 크기 3단계(작게·보통·크게) 느낌 | 대기 (2026-09-14 120% 맞춤은 사용자 "한글이 너무 커보임" 으로 변경) |
| H43 | F-155 A9 | 편집 모드 프론트매터 표 모양, 표 근처 한글 입력 | 대기 |
| H44 | — | 모바일 화면 제목 위 여백 (2026-09-14 사용자 "모바일은 아직 미확인") | 대기 (모바일 레이아웃 명세 후) |
| H46 | F-159 A11 | 사이드바가 창 위까지 이어진 모양, 너비 손잡이 선·끌기 느낌 | 대기 |
| H47 | F-156 A13 | 캡처 도구(Win+Shift+S) 붙여넣기, 탐색기에서 이미지 끌어놓기, 한글 조합 직후 붙여넣기, 공유 화면에서 붙여넣기가 막히는지(에이전트가 별도 가드를 넣지 않음) | 대기 |
| H48 | F-157 A12 | 이미지 모서리·정렬 버튼 모양, 끌어 크기 조절 느낌, 이미지 앞뒤 줄 한글 입력 | 대기 |
| H49 | F-158 A11 | 내보낸 zip 을 풀어 VS Code·GitHub 미리보기에서 이미지·정렬이 보이는지 | 대기 |
| H50 | F-164 A7 | 세 테마 글자 선택 색 느낌, 줄무늬 없는 표(열·행 많은 표)의 행 구분 가독성, 칸 편집 중 파란 강조 없이도 편집 칸을 알아볼 수 있는지 | 대기 |
| H51 | F-165 A12 | 표 칸 끌어 범위 선택하는 느낌, 범위 강조 모양, 한글 칸 삭제 직후 한글 입력 | 대기 |
| H52 | F-171 A9 | 긴 문서에서 실제 마우스·키보드로 표 행·열 추가 반복, 화면이 튀지 않는지 | 통과 (2026-09-15) |
| H53 | F-172 A9 | 메뉴·대화상자·알림 띠·겹침 사이드바 열고 닫는 느낌, 탄성 곡선이 어색하지 않은지 | 대기 |
| H45 | F-141 A5 | 다크 테마 커서 색. 앱은 `.cm-cursor` 없이 기본 캐럿(`caret-color`)을 쓴다 — e2e 는 `caret-color` 로 대신 판정 | 대기 |
| H90 | F-232 3.5(로고→홈) | 상단바·사이드바 로고 버튼 호버·포커스 모양이 다른 아이콘 버튼과 통일되어 보이는지 | 대기 |
| H91 | F-233 3.2·3.4 | 탭바 아이콘 모양·간격, 제목 드롭다운 느낌 | 대기 |
| H92 | F-234 3.5 | 폴더 경로 클릭 시 사이드바 강조 애니메이션(600ms 배경색 페이드) 느낌·타이밍, 경로 글자 크기·색이 어색하지 않은지 | 대기 |
| H93 | F-170 A14 | 우클릭 메뉴 그림자·호버·2차 메뉴 열림 애니메이션이 공유 메뉴와 같아 보이는지 | 대기 |
| H94 | F-170 A12 | 실제 한글 IME 조합 중 우클릭 시 앱 메뉴가 안 뜨는지(e2e 는 합성 조합만 확인) | 대기 |
| H95 | F-170 A8 | 클립보드 최초 권한 요청 시 Chrome 팝업 문구·동작(자동화 불가) | 대기 |
| H96 | F-233 3.6(2026-09-16 재개정) | 실제 휴대폰에서 상단바 밑 탭바 줄 느낌 — 아이콘 전용 탭 3개의 뜻이 툴팁 없이도 헷갈리지 않는지, 좌측 고정된 탭과 가로 스크롤되는 아이콘 줄이 자연스러운지, 터치 영역이 좁지 않은지 | 대기 |
| H97 | F-235 A8 | 도움말 대화상자 카드 설명 문구·톤이 이해하기 쉬운지, 위키링크 카드 캡션과 이미지 카드 이름(`이미지 (예시)`)의 "예시" 안내가 헷갈리지 않는지 | 대기 |
| H106 | F-241 A11 | 홈 화면 최근 문서 목록 간격·정렬, 제목과 날짜 사이 여백, 좁은 창에서 제목 말줄임이 어색하지 않은지 | 통과 (2026-09-18 사용자 "최근문서 확인") |
| H107 | F-240 A13 | 코드블록 머리줄 — 언어 글자 위치와 복사 버튼(28px)의 시각적 정렬, 머리줄 높이가 코드 첫 줄을 가리거나 뜨지 않는지, 보기 모드 복사 버튼과 같아 보이는지 | 통과 (2026-09-18 사용자 "코드블록 작동 확인") |
| H108 | F-236 A9 | 중첩 목록 안내선 두께·색이 사이드바 폴더 선과 견줘 과하거나 흐리지 않은지, 조상 기호 자리와 맞아 보이는지, 편집 모드와 보기 모드가 같아 보이는지 | 일부 (2026-09-18 사용자 "선표시는 확인" — 선을 기호 가운데로 정렬 요청, F-236 후속) |
| H109 | F-242 A15 | 폴더 삭제 대화상자 버튼 3개(취소·위로 옮기기·전부 삭제) 줄 배치와 보조 문구 위치, 좁은 창에서 줄바꿈이 어색하지 않은지 | 대기 |
| H110 | F-245 A10 | 실제 한글 IME 로 `- ` 뒤 한글을 치고(조합 중) 바로 Enter — 다음 줄에 `- ` 가 붙고 빈 줄이 끼지 않는지, 조합 중이던 글자가 잘리거나 중복되지 않는지. 순서 목록(`1. `)·체크박스도 같이. CDP 흉내와 실제 IME 가 다를 수 있어 반드시 눈으로 확인 | 재확인 대기 (2026-09-18 실패 → F-245 6장에서 진짜 원인(항목 2개 목록의 빈 항목 Enter, IME 무관) 고침. 실제 한글 IME 로 다시 확인 필요) |
| H111 | F-243 A17 | 배포 뒤 계정 메뉴 → `공유 관리` — 목록 줄 정렬, 링크·초대 배지 색과 여백이 design.md 토큰과 어울리는지, `.shares-url` 회색 말줄임이 읽히는지, 좁은 창(560px 안팎)에서 줄이 접히는 모양 | 통과 (2026-09-18 사용자 "공유 관리 확인") |
| H112 | F-244 A13 | 배포 뒤 `#/help` — 문법 묶음 사이 간격, 원문 코드블록과 결과가 눈으로 구분되는지, 긴 문서 스크롤이 답답하지 않은지, 좁은 창에서 `.help-page` 레이아웃. 대화상자였을 때보다 읽기 편해졌는지 | 일부 (2026-09-18 사용자 "도움말에 목차 안뜸" — 오른쪽 목차가 안 뜸, 별도 명세 필요) |
| H113 | F-249 A9 | `#/help` 오른쪽 목차 — 목차와 본문 사이 여백, 머리줄(도움말 제목·`내 문서로 복사`·`닫기`)과 겹치지 않는지, 긴 제목 줄임 | 대기 |
| H114 | F-236 A14 | 중첩 목록 안내선이 글머리 기호 **가운데**로 보이는지, 사이드바 폴더 선과 견줘 어색하지 않은지(색·굵기·정렬) | 대기 |
| H115 | F-251 A11 | 중첩 목록 들여쓰기 2em — 단계 깊이가 한눈에 보이는지, 2em 이 과하지 않은지, 안내선과 어울리는지. `2칸`·`4칸` 설정으로 쓴 문서가 같은 깊이로 보이는지 | 대기 |
| H116 | F-258 A5 | 보기 모드(공유·공개 보기 포함)에서 유효한 mermaid 코드블록이 다이어그램으로 그려지는지, 문법 오류일 때 오류 문구가 보이는지(e2e 예산 제한으로 자동 확인 안 함) | 대기 |
| H117 | F-258 2.5 | 편집·보기 모드 다이어그램 여백·가운데 정렬, 오류 문구 색·톤이 이미지 자리 표시(F-157)와 어울리는지 | 대기 |
| H455 | F-2037 8.4 H1 | 설정 `편집기` 탭 `새 문서 템플릿` 선택칸 모양 — 화이트·세피아·다크, 460px 미만 좁은 창 | 대기 |
| H456 | F-2037 8.4 H2 | 팔레트 `템플릿 삽입` 안내 3줄이 목록 아래에서 너무 길지 않은지 | 대기 |
| H457 | F-2037 8.4 H3 | 도움말 `## 템플릿` 절 문구, 공개 `/help` 표시 | 대기 |
| H458 | F-2037 8.4 H4 | 템플릿으로 만든 새 문서에서 제목 `Enter` 뒤 커서가 본문 맨 앞(프론트매터면 그 안)인 것이 불편하지 않은지 | 대기 |
| H459 | F-149·F-172·F-173·F-228 (e2e 경량화로 이관) | 툴팁·메뉴·사이드바·대화상자·알림이 나타나고 사라지는 전환이 자연스러운지 | 대기 |
| H460 | theme (e2e 경량화로 이관) | 라이트·다크 글자 대비(실제 화면 요소), 선택 영역 색 | 대기 |
| H461 | F-140 A2·A11·A13, F-164 (e2e 경량화로 이관) | 표 칸 여백·정렬·테두리, 편집·보기 화면의 줄무늬 색 | 대기 |
| H462 | F-153 A1·A2·A4, F-142 A2, F-159 A2~A4 (e2e 경량화로 이관) | 사이드바 행 글자 시작선·여백·아이콘 크기, 상단바 버튼 배치, 공유 메뉴가 버튼 아래 제자리에 뜨는지, 선택 행 대비 | 대기 |
| H463 | F-236·F-251 (e2e 경량화로 이관) | 목록 깊이 안내선 x 위치·두께·색, 2칸·4칸 단계 간격 | 대기 |
| H464 | F-224·F-225 (e2e 경량화로 이관) | 대화상자 폭(420·520·368)·뒤 막 blur, 초대 입력·세그먼트·버튼 한 줄, 형식 오류 테두리 색 | 대기 |
| H465 | F-234 A1, F-147 A4, F-229 A2 (e2e 경량화로 이관) | 문서 제목 20px, 넓은 창 본문 열 800px 가운데, 개요 버튼 위치·크기 | 대기 |
| H466 | F-227 A2, F-146 A11 (e2e 경량화로 이관) | 터치 사이드바 버튼 44×44, 자동완성 목록 여백 | 대기 |
| H467 | F-226 A1~A5, F-254 C4 (e2e 경량화로 이관) | 편집·보기 모드의 목록 글머리·들여쓰기 위치가 맞는지 | 대기 |
| H468 | F-152 A1·A3·A5·A7·A8a, F-166 A1~A4 (e2e 경량화로 이관) | 줄 번호 거터 정렬, 줄 시작 x, 줄바꿈된 긴 목록의 둘째 줄 내어쓰기 | 대기 |
| H469 | F-2011, bootSkeleton A5·A6·A10 (e2e 경량화로 이관) | 지도 머리 줄 버튼 모양, 부팅 뼈대 모양·크기가 실제 화면과 맞는지 | 대기 |
| H470 | callout A2·A4·A6, F-157 A2, F-158 A2 (e2e 경량화로 이관) | 콜아웃 아이콘 18×18·종류별 색, 이미지 표시 크기 | 대기 |
| H471 | F-2003 A15, F-2010 A2·A3·A5·A7, F-2012 A1·A3, F-2013 (e2e 경량화로 이관) | 3D 지도 카메라 움직임·노드 모양·호버 표시·프레임 부드러움 | 대기 |
| H472 | F-402 | 배포 다음 날 Worker 로그에 `attachment gc` 줄이 있고 `e2ee_refs_unreadable` 가 없는지 | 대기 |
| H473 | F-402, F-406 | 금고 문서 이미지가 이틀 뒤에도 열리는지 (정리 작업이 암호 첨부를 지우지 않음) | 대기 (F-406 구현 후) |
| H474 | F-404 | 실제 한글 IME 조합 중 다른 탭에서 `지금 잠그기` — 조합이 안 깨지고 조합 끝난 뒤 잠기는지 | 대기 |
| H475 | F-404 | 느린 PC 에서 금고 열기·만들기(PBKDF2 600,000회) 걸리는 시간 | 대기 |
| H476 | F-404 | 좁은 창(400px)에서 설정 탭 4개·자동 잠금 버튼 5개·복구 코드 한 줄이 안 넘치는지 | 대기 |
| H477 | F-404 | 설정 위에 겹친 금고 대화상자에서 Esc 한 번이 그 대화상자만 닫고 초점이 `금고 만들기…` 로 돌아오는지 | 대기 |
| H478 | F-404 | 비밀번호 관리자가 금고 암호를 사이트 로그인 암호와 섞지 않는지 | 대기 |
| H479 | F-404 | 절전에서 깨어난 뒤·숨은 탭 7분 뒤 돌아오면 곧바로 잠겨 있는지 | 대기 |
| H480 | F-404 | `recovery-code.txt` 를 메모장·모바일에서 열어 한글이 안 깨지는지 | 대기 |
| H481 | F-404 | 두 창을 나란히 띄우고 한 창에서 로그아웃하면 다른 창의 금고가 곧바로 잠기는지 | 대기 |
| H482 | F-405 | 실제 한글 IME 조합 중 자동 잠금 시간이 지날 때 조합이 끝나고 저장된 뒤 잠기는지, 다시 열면 마지막 글자가 남는지 | 대기 |
| H483 | F-405 | 사이드바 자물쇠 아이콘 자리·크기·색, 좁은 사이드바에서 `잠긴 문서` 잘림, 잠긴 문서 패널(P1) 배치와 어두운 테마 | 대기 |
| H484 | F-405 | 느린 PC 에서 금고 문서 1,000개일 때 사이드바가 멈칫하는 시간 | 대기 |
| H485 | F-405 | 배포 뒤 원격 D1 `docs` 에 금고 문서가 봉투(암호문)로만 들어가는지 (wrangler 로 조회, 본문 값은 출력하지 않고 길이·앞 몇 바이트만) | 대기 (배포 후) |
| H486 | F-405 | 금고 문서 10초 전송 간격 때문에 생기는 충돌 사본 빈도가 견딜 만한지 | 대기 |
| H487 | F-406 | 느린 PC·큰 금고 이미지 여러 장에서 열리는 체감, Chrome DevTools 메모리로 잠근 뒤 `blob:` 이 실제로 풀리는지 | 대기 |
| H488 | F-406 | 일반 문서에 복사한 금고 이미지 자리 표시 `이미지를 찾을 수 없습니다` 가 이해되는지 | 대기 |
| H489 | F-406 | 금고 문서에 다른 금고 문서의 이미지 줄을 붙여 넣었을 때 그림이 뜨는지(e2e 중 틀만 있고 빈 화면을 한 번 봄 — 원인 미확인) | 대기 |
| H490 | F-409 | 금고 문서 공유 메뉴의 비활성 항목 3개와 설명 줄이 비활성으로 읽히는지(어두운 테마·스크린 리더 포함) | 대기 |
| H491 | F-409 | 빈 상태 `최근 문서` 의 `잠긴 문서` 가 사이드바 자물쇠와 헷갈리지 않는지 | 대기 |
| H492 | F-409 | 지도를 연 채 금고를 열거나 잠글 때 배치가 다시 잡히는 움직임이 거슬리는지 | 대기 |
| H493 | F-409 | 문서 1,000개(금고 200개)에서 금고를 연 직후 검색·지도를 처음 열 때 멈칫함 | 대기 |
| H494 | F-409 | 열린 금고 문서가 든 전체 내보내기 zip 이 평문이라는 게 사용자에게 뜻밖이지 않은지 | 대기 |
| H495 | F-407 | 배포 뒤 문서 200개 이상 폴더를 금고로 옮겨 걸린 시간과 429(Workers 로그 `rate_limited`) 가 났는지 | 대기 (배포 후) |
| H496 | F-407 | 금고로 옮긴 뒤 옛 평문 이미지가 R2 에서 사라졌는지(`wrangler r2 object get` 404) | 대기 (배포 후) |
| H497 | F-407 | 실제 한글 IME 로 실시간 문서를 편집하다 조합이 끝나기 전에 옮겼을 때 마지막 글자가 옮긴 글에 들어가는지 | 대기 |
| H498 | F-407 | 진행 알림(`멈추기` 버튼 포함)이 다른 알림에 가려졌다 다시 보이는 모양, D-9·D-10 덧붙임 줄·백업 안내·어두운 테마, 비활성 메뉴 흐린 색 | 대기 |
| H499 | F-407 | 다른 기기에서 실시간으로 열어 둔 문서를 옮겼을 때 그 기기 화면(구현은 서버를 다시 읽어 금고면 알림 없이 세션을 새로 시작) | 대기 (배포 후) |
| H500 | F-408 | 알림 띠 버튼 셋(옮기기·나중에·닫기)이 400px 창에서 안 겹치는지, D-14 1→2단계 문구가 헷갈리지 않는지 | 대기 |
| H501 | F-408 | 이미지 수백 장 규모 로그인 이관의 시간·탭 메모리, 1,000개 규모 429 대기 체감 | 대기 |
| H502 | F-408 | 배포 뒤 원격 D1 에서 이관된 금고 문서 행이 봉투뿐인지 | 대기 (배포 후) |
| H503 | F-408 | 비밀번호 관리자가 `로그인 전 금고 암호` 칸을 자동으로 채우지 않는지 | 대기 |
| H504 | F-2039 H1 | 금고 글(`/guides/encryption`)을 처음부터 끝까지 읽기 — 어조가 다른 사용법 글과 같은지, 5,594자·10절이 한 번에 읽히는지, 과장·겁주는 말이 없는지, "막지 못하는 것" 이 이해되는지 | 대기 |
| H505 | F-2039 H2 | 글의 흐름을 실제 앱에서 한 번씩 — 금고 만들기(복구 코드 파일 이름·체크), 두 탭 잠그기 퍼짐, 자동 잠금 선택지, `금고로 옮기기…` 진행 알림, 로그아웃 뒤 로그인 이관 알림. 글과 다른 곳 적기 | 대기 |
| H506 | F-2039 H3 | 도움말 `## 금고` 끝 `사용법 글:` 줄 모양, 설치한 PWA 창에서 눌렀을 때 브라우저 탭으로 열리는지, 오프라인에서 눌렀을 때 보이는 것 | 대기 |
| H507 | F-2039 H4 | `/guides` 목록의 새 안내 문단이 도움말과 사용법 글의 차이를 알아듣게 하는지 | 대기 |
| H508 | F-2039 H5 | 글의 "서버가 볼 수 있는 것" 표를 `content/legal/privacy.md` 3절과 나란히 읽기 — F-2039 13장 C4 두 차이 말고 어긋난 곳이 없는지 | 대기 |
| H509 | F-2019 H-a | 실제 폴더 끌어놓기(탐색기/Finder)로 볼트 가져오기 — 합성 드롭은 `webkitGetAsEntry()` 가 `null` 이라 e2e 불가 | 대기 |
| H510 | F-2019 H-b | 가져오기 미리보기 `넣을 폴더` 선택칸·width 없는 이미지의 화이트·세피아·다크 모양 | 대기 |
| H511 | F-2019 H-c | 실제 옵시디언 볼트(macOS zip, NFD 파일명) 가져오기 | 대기 |
| H512 | F-2019 H-d | 옵시디언에서 고친 뒤 여러 번 다시 가져오기 — 갱신·건너뜀 판정, `(가져오기 전)` 사본이 쌓이지 않는지 | 대기 |
| H513 | F-2019 H-e | 수백 MB 볼트의 미리보기·적용 시간 | 대기 |
| H514 | F-2019 H-f | 옵시디언이 실제로 `![[그림.png\|설명\|300]]` 확장 문법을 쓰는지 | 대기 |
| H515 | F-504 H1 | 댓글 앵커 배경·밑줄·활성·후보 색이 선택 영역·`==하이라이트==`·원격 선택과 갈리는지, 세 테마에서. 겹친 앵커의 진해짐이 읽기를 해치지 않는지 (F-505 화면이 붙은 뒤) | 대기 |
| H516 | F-504 H2 | 댓글 거터 표시 모양·개수 글자, 첫 댓글이 생길 때 본문이 옆으로 밀리지 않는지 (F-505 뒤) | 대기 |
| H517 | F-504 H3 | 실제 한글 IME 로 앵커 안·끝 바로 뒤에서 조합할 때 글자가 깨지거나 두 번 들어가지 않는지, 조합 중 장식이 번쩍이지 않는지 (F-505 뒤) | 대기 |
| H518 | F-504 H4 | 표·코드블록 안 앵커 — 편집 모드에서 장식이 없고 거터 표시가 위젯 칸에 있는 것이 이해되는지 (F-505 뒤) | 대기 |
| H519 | F-504 H5 | 1MB 문서·댓글 수백 개에서 입력이 느려지지 않는지. 레일 top 은 몇 px 근사(글자 윗변과 1.2~1.4px, 추정 줄 3.4px) | 대기 |
| H520 | F-502 H1 | **배포 전**: 원격 마이그레이션 `0012_comments.sql` 을 코드 배포보다 먼저 적용. 빠지면 DO 불러오기가 복사본 읽기에서 던져 모든 실시간 문서가 열리지 않는다 | 대기 (배포 전) |
| H521 | F-502 H2 | 배포 뒤 댓글을 단 문서에서 `wrangler d1 execute md-editor-db --remote --command "SELECT doc_id, id, bytes FROM doc_comments LIMIT 5"` — 행이 있는지 | 대기 (배포 후) |
| H522 | F-502 H3 | 배포 뒤 1주 DO CPU 시간(대시보드) — 큰 문서에서 마지막 사람이 나간 뒤 "exceeded CPU" 가 보이는지 (F-502 16장 Q2) | 대기 (배포 후) |
| H523 | F-502 H4 | 원격 D1 에서 1MB 가까운 JSON 바인딩이 들어가는지 — 한글로 채운 댓글 500개 문서가 있을 때만 | 대기 (배포 후) |
| H524 | F-503 H1 | F-506 까지 배포한 뒤 view 초대 계정으로 문서를 열면 실시간으로 붙고(본문을 칠 수 없음), 편집자 화면의 `접속자` 에 그 사람이 보이지 않는지 | 대기 (F-506 배포 후) |
| H525 | F-503 H2 | 같은 때 view 계정으로 댓글을 달면 편집자 화면에 곧바로 보이고, 1분 넘게 둔 뒤(방이 잠든 뒤) 다시 달아도 되는지 — hibernation 뒤 역할·속도 칸 복원 | 대기 (F-506 배포 후) |
| H526 | F-503 H3 | 댓글이 많은(한글, 수백 개) 로컬 문서로 로그인 이관 — `POST …/comments/import` 가 200 인지. 503 이 되풀이되면 DO RPC 인자 크기 한도를 의심 | 대기 (F-506 배포 후) |
| H527 | F-503 H4 | 배포 뒤 1주 DO CPU(대시보드) — view 연결이 많은 문서. H522 와 같이 본다 | 대기 (배포 후) |
| H528 | F-2041 H-a | 배포본, 한국에서. 한 번 연 긴 문서(100줄 이상)를 새로고침 10번 — 기록된 본문이 곧바로 보이고, 따라잡기로 화면 위쪽 줄이 한 줄 넘게 움직이지 않는지. 다른 창에서 위쪽을 고쳐 둔 경우도 3번 | 대기 (배포 후) |
| H529 | F-2041 H-b | 배포본. 새로고침 직후 `연결 중…` 동안 실제 한글 IME 로 치기 시작해 조합 도중 동기화가 끝나게 — 글자가 깨지거나 두 번 들어가지 않고 조합 뒤 원격분이 들어오는지 | 대기 (배포 후) |
| H530 | F-2041 H-c | H391 을 둘로 나눠 적기 — 기록 있는 문서(F-2041 뒤)와 기록 없는 첫 열기. F-2041 11장 Q1 판단 자료 | 대기 (배포 후) |
| H531 | F-2041 구현 보고 | `연결 중…` 에서 금고로 옮기기가 막힌 뒤, 연결되자마자 같은 페이지에서 곧바로 다시 누르면 다시 막히는지(F-407 prepareConvertDoc·finishConvertDoc ↔ useLiveDoc docId 경합). 자주 걸리면 F-407 쪽 수정 필요 | 대기 |
| H532 | F-2040 H1 | 서체 모양이 바뀌기 전과 같은지(본문 산세리프, 제목 세리프 700, 본문 세리프 400, 원문 고정폭), 한글 뒤 영문·숫자 간격 — 운영 배포 뒤 이전 스크린샷과 비교 | 대기 (배포 후) |
| H533 | F-2040 H2 | 느린 망(DevTools 30KB/s) 첫 방문에서 조각이 따로 도착하며 서체가 섞여 보이는 시간·문단 출렁임 — 앱 문서·/guides/zettelkasten | 대기 (배포 후) |
| H534 | F-2040 H3 | 네트워크 제한을 건 채 긴 문서 끝에서 처음 나온 음절을 클릭 — 새 조각 도착 직후 커서 자리·스크롤 튐 | 대기 |
| H535 | F-2040 H4 | 설치한 PWA 오프라인에서 처음 치는 글자가 시스템 서체로 보이는 모습, 온라인 복귀 새로고침 뒤 제 서체 | 대기 |
| H536 | F-2040 H5 | 처음 여는 문서를 곧바로 인쇄 — 한글이 폴백 없이 제 서체인지(H149 와 함께, Chrome 은 측정상 위험 낮음) | 대기 |
| H537 | F-2040 H6 | 운영 첫 방문 한 번 뒤 곧바로 오프라인 새로고침 — HTTP 캐시 조각이 나오는지(새 프로필) | 대기 (배포 후) |
| H538 | F-2042 8.3 | 배포본에서 문서 수십 개 계정으로 새로고침 — 셸이 이전보다 빨리 뜨는지, 뒤 맞추기로 사이드바가 바뀔 때(공유받은 문서가 늦게 붙는 것 포함) 거슬리는 깜빡임이 없는지 | 대기 (배포 후) |
| H539 | F-2043 H-1 | 저장값 1400 으로 느린 네트워크 첫 로딩 — 스켈레톤에서 앱으로 넘어갈 때 폭이 튀지 않는지 | 대기 |
| H540 | F-2043 H-2 | `본문 너비` 줄 모양(화이트·세피아·다크, 넓은/좁은 대화상자) — 숫자 칸 88px·간격 8px | 대기 |
| H541 | F-2043 H-3 | 슬라이더를 끄는 동안 흐린 배경 너머 본문 반응이 알아볼 만한지, 긴 문서에서 끊김 없는지 | 대기 |
| H542 | F-2043 H-4 | 1600 폭에서 읽기 편한지, 폭을 바꾼 뒤 스크롤 위치가 튀는지(CM6 다시 재기) | 대기 |
| H543 | F-2043 H-5 | 홈 최근 문서·공유 관리·지도 목록을 1600 으로 봤을 때 이상 없는지(Q2 판단 자료) | 대기 |

## 4. 결정 대기 (확인이 아니라 정해야 하는 것)

`specs/product.md` 9장 미결 Q6·Q14·Q21·Q26, 명세 승인(`product.md`·`ia.md`·`design.md`)

## 5. M2 계정 준비 (Cloudflare, 사람)

| # | 항목 | 기대 | 상태 |
| --- | --- | --- | --- |
| H60 | `npx wrangler login` | `npx wrangler whoami` 에 계정 표시 | 통과 (2026-09-15 사용자 로그인) |
| H61 | R2 구독 켜기 | 버킷 생성 가능 | 통과 (2026-09-15 사용자 켬, 메인이 MCP 로 `md-editor-attachments` APAC 생성) |
| H62 | D1 DB | `md-editor-db` 존재 | 통과 (2026-09-15 메인이 MCP 로 생성, id `073462f8-7a95-4b0f-8d32-4b07f8c4448e`, APAC, 테이블 없음 — 스키마는 F-206) |
| H63 | Zero Trust 조직(팀 도메인)·일회용 코드 로그인 켜기 | API `access/organizations` 조회 성공 | 통과 (2026-09-15 사용자 활성화, 팀 도메인 `fragrant-sky-f428.cloudflareaccess.com`, 메인이 MCP 로 일회용 코드 IdP 추가) |
| H64 | Access 앱(보호 API 경로) | F-205 명세대로 경로·정책 | 통과 (2026-09-15 메인이 MCP 로 `md-editor-api` 자체 호스팅 앱 `rawdoc.app/api`, 정책 `all-emails` 모두 허용, 일회용 코드만, 세션 168h. 2026-09-15 사용자가 `/api/me` 일회용 코드 로그인 → 이메일 JSON 확인. 앱 메뉴 로그인·로그아웃은 F-205 B 뒤 A8) |
| H65 | F-205 A8 | 상단바 계정 메뉴 → 로그인 → 일회용 코드 → 앱 복귀 → 이메일 표시 → 로그아웃 | 통과 (2026-09-15 사용자 "로그인 잘됨") — 로그인 재설계(F-2032) 배포 뒤로는 F-2032 ⑥·⑧ 이 대신함 |
| H66 | F-208·F-207 A8 | 로컬 문서가 있는 브라우저에서 첫 로그인 → 문서·폴더·고정 그대로 옮겨짐, 다른 브라우저 같은 계정에서 보임 | 통과 (2026-09-15 사용자 "로컬 계정 연계 잘됨") |
| H67 | F-210 A11 | 공유 메뉴 `읽기 전용 링크 복사` → 시크릿 창에서 로그인 없이 보임 → `링크 끊기` 뒤 안 열림 | 통과 (2026-09-15 사용자 "문서 읽기 전용 링크 생성/공유 확인") |
| H68 | F-209 A9 | 이미지 붙여넣기 → 공개 링크 시크릿 창에서 이미지 보임, 다른 사이트 `<img>` 로 끌어오지 못함 | 통과 (2026-09-15 사용자 "이미지 공유됨") |
| H69 | F-211 A6 | 폴더 `⋯` → `읽기 전용 링크 복사` → 시크릿 창 목록·본문, 소유자가 문서 추가 후 새로고침에 보임. 목록 순서(폴더 자신 문서 → 하위 폴더 묶음)가 괜찮은지 | 통과 (2026-09-15 사용자 확인) |
| H70 | F-212 A7·A8 | 실제 계정 두 개: 초대(보기·편집) → 받은 쪽 `공유받음` 에서 열기·편집, 보기 문서에서 한글 입력이 막히는지, 편집 중 권한을 빼면 `이 문서를 편집할 권한이 없어졌습니다.` 뒤 읽기 전용. 초대 대화상자·공유받음 묶음 모양 | 대기 |
| H71 | F-213 A7 | 배포 주소에서 편집 권한 계정 두 개(또는 같은 계정 두 창)로 같은 문서 열기: 늦게 연 쪽 읽기 전용 + `{이메일} 님이 편집 중입니다.`(같은 계정이면 `다른 창에서 편집 중입니다.`), 먼저 연 창을 닫으면 15초 안팎 뒤 `이제 편집할 수 있습니다.` 와 최신 내용. 로컬 wrangler A2·A3 미실행 | 대기 |
| H72 | Q5 Google 로그인 | 로그아웃 → 계정 메뉴 로그인 → Access 선택 화면에 Google·일회용 코드 → Google 로 로그인 → 앱 복귀, 기존 이메일과 같으면 기존 문서 그대로. Google 동의 화면 게시 상태 프로덕션 | 통과 (2026-09-15 사용자 확인) |
| H73 | F-215 A5 | 배포 주소의 문서·폴더 읽기 전용 링크를 넓은·좁은 창(휴대폰 포함)에서: 로고·구분선·제목 정렬, 폴더 목록 머리 가로선이 문서 머리 줄과 이어지는지, 로고 누르면 앱 첫 화면 | 대기 (메인이 가짜 응답 스크린샷 4장으로 1차 확인) |
| H74 | F-216 A3 | 실제 휴대폰에서 첫 화면·문서 편집·공유 링크: 가로·세로 불필요한 스크롤 없음, 한글 줄바꿈이 낱말 단위(keep-all), 이미지 정렬 버튼 아이콘이 버튼 안에 맞음 | 대기 (메인이 Pixel 7 흉내 412px 로 1차 확인) |
| H75 | F-218 A5 | 편집 모드 이미지 호버: 정렬 3개 뒤 구분선·휴지통 아이콘 모양, 휴지통 호버 때 빨간색, 눌러 지운 뒤 Ctrl+Z | 통과 (2026-09-15 사용자 "휴지통 확인") |
| H76 | F-219 A4 | 배포 뒤 Cloudflare 대시보드 Worker `md-editor-web` → Settings → Triggers 에 Cron `0 18 * * *`, 다음 날 Logs 에 정리 한 줄(지운·남긴 개수) | 대기 |
| H77 | F-217 A9 | 본문 맨 위 제목에 실제 한글 IME 로 입력·수정(조합 끊김 없음), 긴 제목 줄바꿈, 제목과 본문 `#` 제목이 구분되어 보이는지, 편집·원문·보기 모드 제목 위치 | 대기 |
| H78 | F-220 A6 | 휴대폰 사진(10MB 안팎 JPEG)·큰 화면 캡처 붙여넣기 → 긴 변 2000px WebP 로 줄어든 화질이 교안용으로 충분한지 | 통과 (2026-09-15 사용자 "화질 충분") |
| H79 | F-221 A6 | 배포 뒤 계정 메뉴 `이미지 {N} / 300MB` 가 실제 올린 양과 맞는지(이미지 몇 장 넣고 메뉴 다시 열기) | 통과 (2026-09-15 사용자 확인) |
| H80 | F-222 A7 | 배포 뒤 계정 메뉴 `API 토큰` 에서 만들고 복사 → `curl -H "Authorization: Bearer rd_…" https://rawdoc.app/v1/docs` 가 401 아님, 폐기 뒤 401. 복사 버튼·대화상자 모양 | 대기 |
| H81 | F-223 A6 | 배포 뒤 토큰으로 `curl -X POST https://rawdoc.app/v1/docs` → 앱 사이드바에 보임, `POST /v1/attachments` 응답 `markdown` 을 본문에 넣어 `PUT` → 편집 모드 이미지 표시, `POST /v1/docs/:id/link` 주소가 시크릿 창에서 열림 | 대기 |
| H82 | F-224 A6 | 대화상자 뒤 흐림 6px 세기, 다크 테마에서 막 느낌, 고정 폭에서 각 대화상자 내용이 어색하지 않은지 | 통과 (2026-09-15 사용자 확인) |
| H83 | F-226 A3 | 보기 모드·공개 링크에서 `3. 셋` 으로 시작하는 목록이 `3.` 부터, 중첩 숫자 목록이 로마 숫자 아닌 `1.` 로 보이는지. 글머리 기호·들여쓰기가 편집 모드와 같아 보이는지(여러 단계 중첩 문서) | 대기 |
| H84 | F-225 A9 | 배포 뒤 공유 메뉴 `사람 초대…` — 라이트·다크에서 한 줄 초대 막대 모양, 초대·권한 변경 뒤 행 강조(1.2초)가 과하지 않은지, 휴대폰 폭에서 줄바꿈 | 대기 |
| H85 | F-227 A7 | 배포 뒤 실제 안드로이드·iOS 휴대폰 — 본문 가운데 오른쪽 밀기로 사이드바 열림, 사이드바 위 왼쪽 밀기로 닫힘, 편집 중 글자 선택·표 가로 스크롤·한글 입력 직후와 헷갈리지 않는지, 화면 끝에서 밀 때 시스템 뒤로 가기와 겹치는지, 여닫기 버튼 누르기 쉬운지 | 통과 (2026-09-15 사용자 확인) |
| H86 | F-228 A8 | 상단바 아이콘 툴팁·`⋯`·공유·계정 메뉴·대화상자가 튀어나오는 느낌이 쫀득한지, 과하지 않은지(라이트·다크) | 대기 (2026-09-15 사용자 "애매함 — 들어갈 때 더 쫀득해야, 스케일값 더 크게, 오퍼시티 전환은 짧게" → F-228 2.2·2.3 값 조정, 재확인 필요) |
| H87 | F-229 A9 | 배포 뒤 실제 휴대폰 — 오른쪽 위 `목차` 버튼 크기·위치, 버튼이 제목·첫 줄 오른쪽 끝을 가리는 정도, 카드 스크롤과 화면 밀기(F-227) 헷갈림, 사이드바·대화상자 열 때 카드가 바로 닫히는지 | 통과 (2026-09-15 사용자 확인) |
| H88 | F-231 A4 | 설치된 앱 → 탐색기에서 `.md` 연결 프로그램으로 같은 파일을 두 번 열기 → 문서가 안 늘고 같은 문서가 열리는지. 다른 `.md` 파일은 새 문서로 생기는지 | 대기 |
| H89 | F-231 A5 | 가져온 문서 내용을 앱 안에서 고친 뒤 같은 원본 파일을 다시 OS 로 열기 → 고친 내용이 지워지지 않고 그대로 열리는지(원본 내용으로 덮어쓰지 않음) | 대기 |
| H98 | F-238 A8 | `npm run dev:worker` 로컬 D1 에 문서 만들고 공유 링크 발급 → `curl http://localhost:8790/p/{token} \| grep og:title` 로 문서 제목 반영 확인, 배포 뒤 카카오톡·슬랙 등에 `/p/{token}` 링크 붙여넣어 미리보기에 문서 제목·발췌가 뜨는지 확인 | 대기 |
| H99 | F-239 A5 | `npm run dev:worker` 뒤 `curl -s http://localhost:8790/welcome`(기본 UA·`GPTBot`·`ClaudeBot`)로 헤드라인·예시 문서 텍스트가 원문에 그대로 있는지, 브라우저로 마크 색 전환 동작·CTA 클릭 시 `/` 이동 확인, 카카오톡/슬랙 미리보기, Search Console `/welcome` 색인 요청. 기능 목록 문구(로그인 동기화·공유·초대 편집)가 실제 배포 동작과 맞는지 재확인 | 대기 |
| H104 | F-239 §0.2 | 랜딩 히어로 편집기 데모: 한글 조합 타이핑이 실제 IME 처럼 보이는지, 다 친 뒤 줄을 클릭하면 그 줄 기호가 원문으로 드러나는지, 모바일(실기기)에서 편집기가 뜨고 조작되는지, 첫 화면이 뜨는 데 걸리는 시간이 견딜 만한지(데모 번들이 CM6 전체를 부른다) | 대기 |
| H105 | F-239 §0.2 | 앱과 랜딩의 색·서체가 같은 화면으로 보이는지(웹폰트 로드 전후 FOUT 포함), "만들고 있습니다" 섹션이 지금 되는 기능으로 오해되지 않는지 | 대기 |
| H116 | F-253 B14 | 실제 한글 IME 로 **빈 줄로 끊긴 목록**(`- 하나` / 빈 줄 / `- 둘`) 끝에서 한글을 치고 Enter — 다음 줄에 빈 줄 없이 `- ` 만 붙는지. 이미 있던 빈 줄은 그대로인지 | 대기 |
| H117 | F-254 C14 | 목록이 많은 문서를 편집하며 줄을 오르내릴 때 본문이 좌우로 튀지 않는지. 기호 위(`Home`)로 가면 원문 `- ` 가 제때 보이는지 | 대기 |
| H118 | F-252 D2 | 배포 뒤 위키링크 묶음 링크를 시크릿 창으로 열어 — 딸린 문서로 이동되는지, 묶음 밖 대상이 missing 으로 남는지, 묶음 문서 안 이미지가 보이는지 | 대기 |
| H119 | F-252 D3 | `함께 공유할 문서` 대화상자 모양 — 단계 들여쓰기(16px), 체크박스 정렬, 긴 제목 줄임, 재발급 경고 줄이 눈에 띄는지 | 대기 |
| H120 | F-255 D21 | 사이드바 여러 항목 선택 — 선택된 행 배경이 현재 행·호버와 구분되는지(라이트·다크·세피아), 드래그 중 커서 옆 개수 배지가 가리지 않고 읽히는지 | 대기 |
| H121 | F-257 G10 | 새 도움말(`#/help`) — 앱 사용법 12개 절의 말투가 친절한지, 빠진 기능·틀린 설명이 없는지, 문법 절로 넘어가는 흐름이 자연스러운지 | 대기 |
| H123 | F-261 2.5 | 찾기·바꾸기 패널 색·여백·크기가 라이트·세피아·다크 3테마 모두 앱과 어울리는지(최소 구현) | 대기 |
| H124 | F-261 2.4 | 표(Table) 위젯 안 글자를 찾을 때 매치 하이라이트가 실제로 안 보이는지(알려진 제약, 이번 라운드 대응 안 함) | 대기 |
| H125 | F-271 H1 | `npm run dev:worker`(8790) 또는 배포 뒤 시크릿 창으로 `/` 를 열어 랜딩이 보이는지, `로그인 없이 사용` 뒤 새로고침·재방문에 앱이 바로 뜨는지 | 대기 |
| H126 | F-271 H2 | 이미 앱을 쓰던 브라우저(서비스 워커 있음)와 설치한 PWA 가 `/` 에서 랜딩을 보지 않는지 | 대기 |
| H127 | F-271 H3 | `/welcome` 이 `/` 로 넘어가는지, 카카오톡·슬랙에 `/` 링크 미리보기가 뜨는지 | 대기 |
| H128 | F-271 H4 | 랜딩 → `로그인` → Access 일회용 코드 → 앱까지 한 번에 이어지는지(실제 메일 수신이 필요해 자동화 불가) | 대기 |
| H129 | F-271 H5 | 기존 사용자 브라우저에서 랜딩이 잠깐 보였다가 앱으로 바뀌는 깜빡임이 거슬리는지 | 대기 |
| H130 | F-271 2.3 | 쿠키를 차단한 브라우저에서 랜딩에 `앱으로 바로 가기`(`?app=1`) 안내가 뜨고 그 링크로 앱이 열리는지. 정상 흐름에서는 그 안내가 안 보이는지 | 대기 |
| H131 | F-278 7장 | 내보내기 메뉴가 열릴 때 움직임·그림자·여백이 공유 메뉴와 같아 보이는지 | 대기 |
| H132 | F-278 7장 | `.txt` 결과를 Windows 메모장·VS Code 로 열었을 때 줄바꿈이 깨지지 않는지(특히 `lineEnding: 'lf'` 문서) | 대기 |
| H133 | F-281 A18 | 내려받은 zip 을 Windows 탐색기·VS Code 로 풀어 폴더 구조·한글 이름·`attachments/` 이미지·`manifest.json` 이 정상인지 | 대기 |
| H134 | F-281 A18 | 이미지가 많은(수십~수백 MB) 계정에서 전체 내보내기가 브라우저에서 버티는지, 걸리는 시간 | 대기 |
| H135 | F-281 A18 | 경로가 긴 문서에서 Windows 260자 경로 제한에 걸리는지 | 대기 |
| H136 | F-283 9장 | 하이라이트 배경이 화이트·세피아·다크 세 테마에서 읽을 만한지, 형광펜처럼 보이는지(`--md-highlight-bg` 기본값은 `--callout-question` 22%) | 대기 |
| H137 | F-283 9장 | 드래그 선택 바탕과 하이라이트 배경이 구분되는지. 편집 모드 배경과 보기 모드 `<mark>` 배경이 같은 색으로 보이는지 | 대기 |
| H138 | F-283 9장 | 커서가 하이라이트 줄에 들어올 때 `==` 가 드러나며 글이 밀리는 정도가 거슬리는지 | 대기 |
| H139 | F-283 9장 | 한글 IME 로 하이라이트 안에서 이어 입력할 때 조합 중에도 배경이 자연스러운지 | 대기 |
| H140 | F-283 9장 | 하이라이트 배경을 형광펜 노랑으로 바꾼 뒤(`--highlight-base`, 2026-09-21) 라이트 60% 가 적절한지 — 사용자 확인: 라이트 통과. **다크(`#7a5c0a`)는 미확인** | 부분 통과 (2026-09-21) |
| H141 | F-272 H1 | 배포 후 없는 주소가 실제로 `404` 상태 코드로 오는지(브라우저 개발자도구 Network). SPA 폴백이던 때와 달라진 지점 | 대기 |
| H142 | F-272 H2 | 폐기·없는 공유 토큰(`/p/xxx`)에서 앱이 뜨지 않고 `링크를 찾을 수 없습니다` 가 보이는지 | 대기 |
| H143 | F-272 H3 | 랜딩 머리·꼬리가 본문과 어울리는지(여백·정렬·글자 크기) | 대기 |
| H144 | F-272 H4 | 다크 모드 사용자가 사이트 페이지를 열면 밝은 화면이 나오는데 거슬리는지 — 사이트엔 스크립트가 없어 테마를 못 읽는다 | 대기 |
| H145 | F-272 H5 | PWA 로 설치한 상태에서 사이트 경로(`/changelog` 등)를 열면 앱 창이 아니라 브라우저로 뜨는지 | 대기 |
| H146 | F-272 H6 | Search Console 에서 `sitemap.xml` 이 읽히고 색인되는지 (배포 뒤 며칠) | 대기 |
| H147 | F-279 10장 | 인쇄 미리보기에서 A4 한 쪽 여백이 20mm 로 보이는지, 쪽 나눔이 제목·표·코드블록을 어색하게 자르지 않는지 | 대기 |
| H148 | F-279 10장 | `PDF로 저장` 했을 때 기본 파일명이 문서 제목인지 | 대기 |
| H149 | F-279 10장 | 인쇄 결과에서 한글이 깨지지 않고 설정한 서체·글자 크기가 반영되는지 | 대기 |
| H150 | F-279 10장 | 첨부 이미지와 Mermaid 다이어그램이 인쇄 결과에 제대로 들어가는지 | 대기 |
| H151 | F-279 4.5 | 다크·세피아 테마에서 인쇄할 때 화면이 잠깐 희게 번쩍이는 것이 거슬리는지 | 대기 |
| H152 | F-279 10장 | 브라우저가 붙이는 머리글·바닥글(날짜·주소)이 결과에 섞이는 정도 — 앱이 끌 수 없다 | 대기 |
| H153 | F-279 10장 | 인쇄 창을 닫은 뒤 편집 화면이 그대로 남는지(제목·테마·스크롤 위치) | 대기 |
| H154 | F-279 4.1 | 원문(raw) 모드에서 인쇄해도 마크다운 기호가 아니라 렌더된 모양이 나오는 것이 기대와 맞는지 | 대기 |
| H155 | F-2022 6.1(이전 F-279 6.1) | macOS 에서 `Ctrl+P` 가 CM6 의 `cursorLineUp` 과 겹쳐 커서가 한 줄 오르며 **명령 팔레트**가 열리는 것(대상이 인쇄에서 명령 팔레트로 바뀜, 2026-09-23. Windows·Linux 는 무충돌을 코드로 확인) | 대기 |
| H156 | F-280 7장 | 인터넷 없는 PC 에서 내보낸 `.html` 을 열었을 때 글꼴·색·이미지가 제대로 보이는지 | 대기 |
| H157 | F-280 7장 | 내보낸 `.html` 의 문서 제목 크기(20px, 본문 `#` 제목과 같은 크기)가 괜찮은지 | 대기 |
| H158 | F-280 7장 | `서식 있는 복사` 를 지메일·아웃룩·노션·구글 문서·워드·한글에 붙여넣은 결과 | 대기 |
| H159 | F-280 7장 | 파이어폭스·사파리에서 `서식 있는 복사` 가 되는지 (e2e 는 크롬 채널만 판정) | 대기 |
| H160 | F-280 7장 | 이미지가 10MB 넘는 문서에서 HTML 내보내기가 멈추지 않는지, 20MB 경고 문구가 적절한지 | 대기 |
| H161 | F-290 9장 | 왼쪽 탭 목록 폭·여백·글자 크기가 오른쪽 패널과 균형 맞는지 (라이트·세피아·다크) | 대기 |
| H162 | F-290 9장 | 선택된 탭 표시가 세그먼트 선택 표시와 같은 무게로 보이는지 | 대기 |
| H163 | F-290 9장 | 탭마다 항목 수가 달라 대화상자 높이가 바뀌는 것이 거슬리는지 | 대기 |
| H164 | F-290 9장 | 폭 520px 이 설정 대화상자로 너무 넓어 보이지 않는지 | 대기 |
| H165 | F-290 9장 | 좁은 창(400px 안팎)에서 가로로 누운 탭 3개가 줄바꿈 없이 들어가는지 | 대기 |
| H166 | F-290 9장 | 키보드만으로 탭 이동 → 패널 → 닫기 흐름이 자연스러운지 | 대기 |
| H167 | F-290 9장 | 스크린 리더에서 탭 이름·선택 상태·패널이 제대로 읽히는지 | 대기 |
| H168 | F-290 9장 | `데이터` 탭의 `전체 내보내기` 가 `닫기` 와 같은 모양으로 보이는지, 오프라인 안내 문구가 한 줄에 자연스러운지 | 대기 |
| H169 | F-290 9장 | `시작 화면` 이 `화면` 탭에 있는 것이 말이 되는지 | 대기 |
| H170 | F-282 A18 | Windows 탐색기로 압축한 한글 이름 폴더 zip(CP949) 가져오기 — 이름이 깨지지 않는지 | 대기 |
| H171 | F-282 A18 | 실제 Obsidian 보관함 zip 을 가져와 폴더·문서 구조가 말이 되는지 | 대기 |
| H172 | F-282 A18 | 이미지가 많은(수십~수백MB) zip 가져오기 — 브라우저가 버티는지, 걸리는 시간 | 대기 |
| H173 | F-282 A18 | 미리보기·진행·결과 대화상자 모양(라이트·세피아·다크), 경고 목록이 많을 때 스크롤 | 대기 |
| H174 | F-282 A18 | 다른 계정에서 내보낸 zip 을 로그인 상태로 가져온 뒤 새로고침 — 문서가 남아 있는지 (Q1 의 실제 영향) | 대기 |
| H175 | F-260 A4 | 보기 모드에서 테마를 바꿀 때 이미 그려진 mermaid 다이어그램이 즉시 다시 그려지는지 | 대기 |
| H176 | F-260 2.5 | 다크 테마 mermaid 기본 팔레트가 앱 배경·오류 문구 톤과 어울리는지 | 대기 |
| H177 | F-260 | HTML 내보내기(`exportDoc.ts`)와 인쇄(`fillMarkdownAssets.ts`)의 mermaid 는 앱 테마와 무관하게 밝은 테마로 남는다 — 그대로 둘지 판단 | 대기 |
| H178 | F-293 8장 | 공유 화면 머리줄이 앱과 같은 인상인지 (화이트·세피아·다크) | 대기 |
| H179 | F-293 8장 | 아이콘만 남은 설정·내보내기 버튼을 처음 보는 사람이 짐작할 수 있는지 | 대기 |
| H180 | F-293 8장 | 툴팁 400ms 지연이 답답하지 않은지 | 대기 |
| H181 | F-293 8장 | 코드블록 머리줄 무게감, 언어 없는 코드블록 머리줄이 허전하지 않은지 | 대기 |
| H182 | F-293 8장 | 코드블록 가로 스크롤 때 복사 버튼이 제자리에 있는지 | 대기 |
| H183 | F-293 8장 | 40px 띠 안 32px 버튼이 답답하지 않은지, 현재 문서 행과 호버가 구분되는지 | 대기 |
| H184 | F-293 8장 | 터치 기기에서 아이콘 버튼·툴팁 사용성 | 대기 |
| H185 | F-293 (범위 밖) | 좁은 창 폴더 보기에서 목록이 열려 있을 때 `.public-folder-backdrop` 가 상단 토글 버튼을 덮는다. backdrop 클릭도 같은 결과(닫힘)라 눈에 안 띌 수 있음 — 별도 버그로 다룰지 판단 | 대기 |
| H186 | F-287 11장 | 검색 결과 행 모양·발췌 강조 색이 화이트·세피아·다크에서 읽히는지 | 대기 |
| H187 | F-287 11장 | 520px(`wide`) 대화상자에서 발췌가 읽을 만한 길이인지 | 대기 |
| H188 | F-287 11장 | 휴대폰 폭에 새로 만든 사이드바 검색 버튼 자리가 자연스러운지 | 대기 |
| H189 | F-287 11장 | 한글 IME 켠 채 `Ctrl+Shift+F`, macOS `Cmd+Shift+F`, 확장 깔린 실제 크롬 프로필에서 단축키가 먹는지 | 대기 |
| H190 | F-287 11장 | `role="combobox"`+`listbox` 패턴이 스크린 리더에서 제대로 읽히는지 | 대기 |
| H191 | F-287 (구현 중 발견) | 검색 대화상자가 열린 채로 로그인·로그아웃하면 인덱스가 새 범위로 다시 만들어지지 않을 수 있음. 실사용에서 드문 경로라 테스트로 안 걸림 | 대기 |
| H192 | F-287 A17 | 게이트 (a) 재측정 — 이 PC 에서는 설정 대화상자조차 93~124ms 라 100ms 절대 기준을 자주 넘긴다. 부하 적은 PC 에서 다시 잴 것 (상대 기준 +30ms 는 8/8 통과) | 대기 |
| H193 | F-288 11장 | 안내 줄이 최대 3줄까지 쌓였을 때 화이트·세피아·다크에서 답답하지 않은지 | 대기 |
| H194 | F-288 11장 | `속성이 없거나 읽지 못한 문서 {N}개…` 가 필터를 쓸 때마다 거의 항상 뜬다 — 쓸모 있는지 잔소리인지 | 대기 |
| H195 | F-288 11장 | `필터 tag=(모두)` 표기가 자연스러운지 | 대기 |
| H196 | F-288 11장 | 해석 줄의 소문자화(`Tag:Diary` → `tag=diary`)가 거슬리는지 | 대기 |
| H197 | F-288 11장 | 250ms 문턱이 문서 5,000개 환경에서 짧게 번쩍이지 않는지 | 대기 |
| H198 | F-288 11장 | 꼬리 줄이 결과 스크롤 영역 바깥에 붙은 자리가 자연스러운지 | 대기 |
| H199 | F-288 A7 | 실제로 Wi-Fi 를 끈 오프라인에서도 안내가 뜨는지 — e2e 는 브라우저 `offline` 이벤트로만 확인했다 | 대기 |
| H200 | F-288 A11 | 스크린 리더에서 `role="status"` 가 타이핑마다 과하게 읽히지 않는지 | 대기 |
| H201 | F-273 8장 H1 | `content/changelog.md` 4장 초안의 과거 항목이 사실과 맞는지 — 빠진 기능·틀린 항목·틀린 날짜 | 대기 |
| H202 | F-273 8장 H2 | 체인지로그 문장 어조와 분량, 특히 `2026-09-15` 묶음을 줄일지 | 대기 |
| H203 | F-273 8장 H3 | 긴 목록이 사이트 본문 폭에서 읽히는지, 날짜 `h2` 가 묶음 경계로 충분히 보이는지 | 대기 |
| H204 | F-273 8장 H4 | 머리에 `체인지로그` 하나만 있는 상태의 모양 (F-274·F-276 붙기 전) | 대기 |
| H205 | F-274 9장 H1 | `/help` 본문이 앱 밖에서 읽는 사람에게 어색하지 않은지 | 대기 |
| H206 | F-274 9장 H2 | 사이트 코드블록에 언어 이름·복사 버튼이 없는 모습이 괜찮은지 | 대기 |
| H207 | F-274 9장 H3 | F-274 4.2 로 고친 이미지 캡션 문장이 자연스러운지 | 대기 |
| H208 | F-274 9장 H4 | 머리에 `체인지로그`·`도움말` 둘 있는 모양, 긴 글이 `--site-read` 폭에서 읽히는지 (H204 를 대신한다) | 대기 |
| H209 | F-295 A10 | 라이브 ↔ 보기 전환이 부드럽게 느껴지는지 — 옛 위치가 한 프레임 비치지 않는지 | 대기 |
| H210 | F-276 11장 H1 | 사용법 글 3편의 어조와 분량 — 1,500~2,200자가 `--site-read` 폭에서 읽히는지, 존댓말·짧은 문장이 앱 문구와 어울리는지 | 대기 |
| H211 | F-276 11장 H2 | 8장 표의 바깥 도구 설명이 맞는지 — GitHub 에서 `[[제목]]` 이 글자로 보이는지, zip 을 풀어 VS Code·Obsidian 에서 열리는지 | 대기 |
| H212 | F-276 11장 H3 | 머리 링크가 셋이 된 모양(`사용법`·`체인지로그`·`도움말`). 좁은 화면에서 줄이 넘치지 않는지 (H208 을 대신한다) | 대기 |
| H213 | F-276 11장 H4 | `/guides` 목록의 모양 — 제목 + `—` + 설명 한 줄이 글머리표로 늘어선 것이 읽히는지, 날짜가 없어 허전하지 않은지 | 대기 |
| H214 | F-276 11장 H5 | 사이드바 `사용법` 의 모양과 동작 — `menu_book` 이 `도움말` 과 구분되는지, 오른쪽 새 탭 아이콘이 거슬리지 않는지 | 대기 |
| H215 | F-296 12장 H1 | 이 결정 자체 — Chrome 탭 둘을 끌어내 Windows 스냅으로 붙여 문서 둘을 비교해 보고, 앱 안 분할을 버린 것이 실제로 견딜 만한지 | 대기 |
| H216 | F-296 12장 H2 | 사이드바 문서 라벨이 `<a>` 가 된 뒤 밑줄·글자색·호버 배경·선택 배경·`aria-current` 굵기가 화이트·세피아·다크에서 전과 같아 보이는지 | 대기 |
| H217 | F-296 12장 H3 | 문서 행 끌어 폴더에 놓기(F-255)가 `draggable={false}` 뒤에도 되는지, 끌 때 링크 주소가 따라 붙지 않는지 | 대기 |
| H218 | F-296 12장 H4 | 우클릭 `새 탭에서 열기` 는 앞 탭, 가운데클릭은 배경 탭 — 이 차이가 어색하지 않은지 | 대기 |
| H219 | F-296 12장 H5 | 같은 문서를 두 탭에서 열었을 때 두 번째 탭이 읽기 전용이 되는 것이 놀랍지 않은지, 알림 문구가 읽히는지 | 대기 |
| H220 | F-296 12장 H6 | 문서 200개 이상에서 다른 탭이 문서를 만들거나 지울 때 사이드바가 번쩍이거나 스크롤이 튀지 않는지 | 대기 |
| H221 | F-296 12장 H7 | 스크린 리더에서 사이드바 트리 항목이 버튼에서 링크로 바뀐 것이 어색하지 않은지 | 대기 |
| H222 | F-275 10장 H1 | 개인정보 처리방침·이용약관 초안 전문이 사실과 맞는지. **법률 자문이 아니므로 공개 전 사람이 읽어야 한다** | 대기 |
| H223 | F-275 10장 H2 | 자리표시를 평문으로 채운 문장이 실제 운영자명·연락처가 들어갔을 때도 자연스러운지 (`(이하 "운영자")` 정의절을 뺀 자리) | 대기 |
| H224 | F-275 10장 H3 | `> [!warning] 초안입니다` 콜아웃이 사이트 화면에서 어떻게 보이는지 | 대기 |
| H225 | F-275 10장 H4 | 꼬리에 링크 둘(`개인정보 처리방침`·`이용약관`)이 붙은 모양 | 대기 |
| H226 | F-275 10장 H5 | 두 글의 분량과 어조가 사용법·도움말 글과 어울리는지 | 대기 |
| H227 | F-275 10장 H6 | 프론트매터 `date` 를 생략해 sitemap `lastmod` 에 빌드일이 들어가는 것이 괜찮은지 (시행일 확정 시 다시 본다) | 대기 |
| H228 | F-294 11장 | 검색 결과로 문서를 열었을 때 찾기 카드가 갑자기 뜨는 것이 놀랍지 않은지 (성가시면 F-294 13장 Q10) | 대기 |
| H229 | F-294 11장 | 첫 매치가 선택된 채 포커스가 본문에 있는 것이 위험하게 느껴지지 않는지 (바로 타자를 치면 대체된다) | 대기 |
| H230 | F-294 11장 | 노란 `.cm-searchMatch` 배경과 주황 링이 화이트·세피아·다크에서 본문 위에 잘 읽히는지 | 대기 |
| H231 | F-294 11장 | 휴대폰 폭에서 첫 매치가 오른쪽 위 찾기 카드에 가리지 않는지 | 대기 |
| H232 | F-294 11장 | `F3`·`Ctrl+G` 로 다음 매치 이동이 실제 브라우저에서 먹는지 (e2e 로 판정하지 않았다) | 대기 |
| H233 | F-294 11장 | 한글 IME 를 켠 채 검색 결과를 열었을 때 패널 입력에 조합 중 글자가 끼어들지 않는지 | 대기 |
| H234 | F-294 11장 | 표·코드블록 위젯 안 매치가 강조되지 않는 F-261 2.4 제약이 검색 결과로 열었을 때 더 눈에 띄지 않는지 | 대기 |
| H235 | F-294 11장 | 아주 긴 문서(수십만 자)에서 문서를 연 뒤 강조가 뜨기까지 멈칫하지 않는지 | 대기 |
| H236 | F-291 14장 | 수식 글자 크기가 본문(Pretendard·Noto Serif KR)과 화이트·세피아·다크에서 어울리는지 | 대기 |
| H237 | F-291 14장 | 인라인 수식이 든 줄의 줄 높이가 밀리지 않는지 (서브픽셀) | 대기 |
| H238 | F-291 14장 | 커서가 수식에 들어갈 때 원문이 드러나며 글이 밀리는 정도가 거슬리지 않는지 | 대기 |
| H239 | F-291 14장 | 실제 한글 IME 로 `$수식 안 한글$` 을 칠 때 글자 중복·누락·커서 튐이 없는지 (4.3 C1·C2) | 대기 |
| H240 | F-291 14장 | 긴 수식이 화면을 넘칠 때 가로 스크롤이 보기·편집 모드에서 같게 동작하는지 | 대기 |
| H241 | F-291 14장 | 인터넷 없는 PC 에서 내보낸 `.html` 을 열었을 때 수식 글꼴이 제대로 나오는지 | 대기 |
| H242 | F-291 14장 | 서식 있는 복사로 수식을 지메일·노션·워드에 붙여넣은 결과 | 대기 |
| H243 | F-291 14장 | 인쇄 미리보기·PDF 에서 수식이 쪽 경계에 걸려 잘리지 않는지 | 대기 |
| H244 | F-291 14장 | KaTeX 폰트가 늦게 도착할 때 글이 한 번 출렁이는 정도 | 대기 |
| H245 | F-291 12장 M2 | katex 정적 import 로 첫 화면 번들이 gzip +1,656B(+0.8%) 늘어난 것을 받아들일지. mermaid 가 lazy 로 쓰던 katex 청크(gzip 77KB)가 메인에 흡수됐는데도 증가분이 그만큼 작았다. 동적 import + 자리표시 구조로 안 바꾼 판단의 승인 | 대기 |
| H246 | F-292 A17 | 지도 노드 배치·크기(연결 수 반영)·색·공유 문서 테두리가 F-292 8장 디자인 의도와 맞는지 | 해당 없음 — F-292 개정판(3D)으로 대체 |
| H247 | F-292 A18 | 휠 확대·드래그 이동의 손맛과 최소/최대 배율(0.2~4배)이 적당한지 | 해당 없음 — F-292 개정판(3D)으로 대체 |
| H248 | F-292 A19 | 좁은 창(휴대폰 폭)에서 지도 컨트롤 줄바꿈과 사용성 | 해당 없음 — F-292 개정판(3D)으로 대체 |
| H249 | F-292 A20 | 성능 게이트 최종 판정. 순수 계산 구간은 문서 1,000개에 126ms 로 여유(그래프 2.0ms + 상한 자르기 0.8ms + 배치 123.4ms). 저장소 읽기(IndexedDB)와 SVG 2,000+ 요소 렌더는 재지 못했다 — 1,000개 시딩 도구가 없다 | 해당 없음 — F-292 개정판(3D)으로 대체 |
| H250 | F-292 7.4 | 목록 보기에 폴더 경로를 넣지 않고 제목·연결 수만 둔 것이 충분한지 (MapPage 에 폴더 데이터를 넘기지 않았다) | 대기 |
| H251 | F-297 10장 H1 | 실제 크롬 `탭 복제` 로 F-297 2.3 재현 절차를 밟아, 복제 탭에 `다른 창에서 편집 중입니다.` 가 뜨는지 (Playwright 로 그 메뉴를 누를 수 없어 자동화 밖) | 대기 |
| H252 | F-297 10장 H2 | 크롬 `Ctrl+Shift+T`(닫은 탭 복원)·브라우저 재시작 세션 복원이 `md.lockSession` 을 되살리는지. 되살려도 원본 탭이 없어 겹치지 않는다는 4.5 의 판단이 맞는지 | 대기 |
| H253 | F-297 10장 H3 | 복제 탭이 읽기 전용이 되는 것이 놀랍지 않은지. `다른 창에서 편집 중입니다.` 가 방금 복제한 그 탭으로 읽히는지 (문구를 안 바꾸기로 한 4.3 판단) | 대기 |
| H254 | F-297 10장 H4 | 복제 탭에서 원래 탭을 닫았을 때 편집권이 돌아오기까지 최대 15초 기다리는 느낌이 견딜 만한지 | 대기 |
| H255 | F-2002 12.4 H-a | 테마 3종(화이트·세피아·다크)에서 지도의 노드·간선·바탕 색이 화면 색과 같은지. `setRGB` 의 `SRGBColorSpace` 인자를 빠뜨리면 전체가 밝고 뿌옇게 뜨는데 컴파일도 테스트도 통과한다 | 대기 |
| H256 | F-2002 12.4 H-b | 회전 없이도 3D 로 보이는지. 원근만으로 덩어리가 입체로 느껴지는지, 아니면 평면 점 그림처럼 보이는지 — F-2003·F-2004 를 얼마나 서둘러야 하는지가 여기서 갈린다 | 대기 |
| H257 | F-2002 12.4 H-c | 좁은 창(1024px 미만)에서 머리 줄이 줄바꿈된 뒤에도 캔버스가 쓸 만한 높이를 갖는지 | 대기 |
| H258 | F-2003 14.4 H-a | 우클릭 회전이 "덩어리를 손으로 돌리는" 느낌인지. F-292 결정 1(재미·인상)이 이 커밋에서 처음 성립한다 — 기대에 못 미치면 무엇이 모자란지(속도·관성·깊이감) | 대기 |
| H259 | F-2003 14.4 H-b | 좌클릭 끌기 = 이동, 우클릭 끌기 = 회전이라는 뒤집은 배치가 헷갈리지 않는지 (보통의 3D 뷰어는 반대다) | 대기 |
| H260 | F-2003 14.4 H-c | `dampingFactor` 0.08 의 관성이 적당한지. 손을 뗀 뒤 약 110 프레임(60 fps 에서 1.8초) 이어진다. 끌 때 화면이 커서보다 늦게 따라오는 정도도 함께 | 대기 |
| H261 | F-2003 14.4 H-d | `rotateSpeed` 1.0 이 적당한지 — 캔버스 높이의 1/4 을 끌면 90° 돈다 | 대기 |
| H262 | F-2003 14.4 H-e | 휠 배율(한 칸에 5%)과 확대·축소 한계(`맞춤` 거리의 0.05배~4배)가 적당한지. 끝까지 축소했을 때 너무 작지 않은지, 끝까지 확대했을 때 덩어리 안을 볼 수 있는지 | 대기 |
| H263 | F-2003 14.4 H-f | 실제 터치 기기에서 한 손가락 회전·두 손가락 확대·이동·길게 누르기(500 ms). 특히 길게 누르는 동안 아무 표시가 없어도 되는지와, 노드 위에서 시작한 한 손가락 끌기가 회전하지 않는 것이 거슬리는지 | 대기 |
| H264 | F-2003 14.4 H-g | `여기로 이동`·`Ctrl`+클릭이 보던 각도를 유지한 채 그 노드로만 옮기는 것이 자연스러운지 (전체 맞춤으로 되돌리지 않는다) | 대기 |
| H265 | F-2003 14.4 H-h | 노드 메뉴 아이콘 셋(`IconEdit`·`IconOpenInNew`·`IconRecenter`)이 뜻을 전하는지. 특히 `여기로 이동` 의 `recenter` 아이콘 | 대기 |
| H266 | F-2004 14.4 H-a | 연결 수 차이가 눈에 보이는지. 현재 사용자 데이터(대부분 고립, 몇 개가 연결 1~2)에서 반지름 6.0 대 10.4(1.7배)가 구분되는지. 안 보이면 계수 2.2 를 키울지 밑값 3 을 줄일지 (`NODE_SCALE` 2.0 을 곱한 값이다) | 대기 |
| H267 | F-2004 14.4 H-b | 깊이가 느껴지는지. 먼 노드가 `--panel` 과 65% 섞이는 것이 "뒤에 있다"로 읽히는지, 아니면 그냥 색이 두 종류로 보이는지. 간선에는 깊이를 안 넣었는데(F-2004 6.4) 그것이 거슬리는지 | 대기 |
| H268 | F-2004 14.4 H-c | 테마 3종(화이트·세피아·다크)에서 가장 먼 끊긴 링크 노드(`--muted` 를 65% 섞은 것)가 배경에 묻히지 않는지. 묻히면 `--muted` 에만 섞는 비율을 낮출지 | 대기 |
| H269 | F-2004 14.4 H-d | 호버 이름표의 크기(11px)·색(`--ink-2`)·노드 아래 붙는 자리가 읽을 만한지. 이름표가 겹칠 때(이웃이 많은 허브를 호버) 읽을 수 있는지 — 60개 상한이 너무 큰지 | 대기 |
| H270 | F-2004 14.4 H-e | 호버한 노드 자신에게 아무 표시가 없는 것(이름표만 뜬다)이 헷갈리지 않는지. 커서는 이제 기본 화살표다 (사용자 지시 2026-09-22) | 대기 |
| H271 | F-2004 14.4 H-f | 현재 문서만 깊이 섞기에서 뺀 것(F-2004 6.2)이 자연스러운지 — 덩어리 뒤쪽에 있을 때 "떠 보이는" 것이 도움이 되는지 어색한지 | 대기 |
| H272 | F-2004 14.4 H-g | 바깥 링·테두리를 뺀 판단(F-2004 6.3)이 맞는지. 현재 문서가 `--accent` 색만으로 충분히 눈에 띄는지, 공유받은 문서의 `--ink-2` 가 보통 문서의 `--ink` 와 구분되는지. 안 되면 두 번째 `InstancedMesh` 를 들이는 값을 치를지 | 대기 |
| H273 | F-2005 12.3 H-a | 패널 폭 280px·안쪽 여백·묶음 제목 크기가 적당한지. 180ms 여닫기 전환이 거슬리지 않는지 | 대기 |
| H274 | F-2005 12.3 H-b | `노드 크기` 0.5~3.0 범위가 쓸모 있는지. 3.0 에서 덩어리가 화면을 넘쳐도 자동 `맞춤` 을 안 하는 결정이 괜찮은지 | 대기 |
| H275 | F-2005 12.3 H-c | 좁은 창(560px 미만)에서 설정 패널이 캔버스를 통째로 덮는 것이 괜찮은지 | 대기 |
| H276 | F-2005 12.3 H-d | `선 두께` 가 굵기가 아니라 진하기인 것이 이름과 맞는지(WebGL 이 `lineWidth` 를 무시한다). 0·1 양끝이 쓸모 있는지, `선 진하기` 로 개명할지 | 대기 |
| H277 | F-2005 12.3 H-e | `이름표 표시 거리` 를 올릴 때 가까운 노드부터 이름표가 붙는 것이 읽히는지. 1 일 때 너무 빽빽하지 않은지, 회전 중에 깜빡이거나 우르르 바뀌지 않는지 | 대기 |
| H278 | F-2005 12.3 H-f | 머리 줄 `지도 설정` 아이콘이 사이드바 `설정` 과 헷갈리지 않는지. 슬라이더 `accent-color` 가 테마 3종에서 잘 보이는지 | 대기 |
| H279 | 2026-09-22 버그 수정 | `이름표 표시 거리` 를 1 로 올린 채 노드를 호버했다 뗐다 하며 이름표가 더 이상 깜빡이지 않는지. 자동 판정이 못 하는 한 프레임짜리 현상이라 눈으로만 볼 수 있다 | 대기 |
| H280 | 2026-09-22 사용자 지시 | 지도 이름표 14px 이 캔버스 위에서 읽히는지 (H269 의 11px 을 대신한다). 테마 3종에서 `--ink-2` 대비도 함께 | 대기 |
| H281 | 2026-09-22 사용자 지시 | 노드 위에서만 커서가 손가락으로 바뀌는 것이 "여기를 누를 수 있다"로 읽히는지. 회전·이동 중에도 손가락인 것이 거슬리는지 (H270 을 대신한다) | 대기 |
| H282 | F-2006 14.3 H-a | `장력` 네 축의 양 끝이 쓸모 있는 그림인지. `중심 장력` 0.005 의 성긴 구름과 1 의 빽빽한 공, `반발력` 0 에서 점이 겹쳐 보이지 않는지, `링크 거리` 1 에서 너무 멀어지지 않는지 | 대기 |
| H283 | F-2006 14.3 H-b | ~~슬라이더를 끄는 동안 카메라가 안 흔들리고, 손을 뗀 뒤 각도를 유지한 채 거리만 맞는지~~ | **무효** — `F-2012`(사람 승인 2026-09-22)가 장력을 바꿀 때 카메라를 아예 안 맞추기로 정해 이 동작 자체가 사라진다. 보지 않아도 된다 |
| H284 | F-2006 14.3 H-c | `링크 장력`·`링크 거리` 를 움직일 때 이어진 문서가 뭉치고 흩어지는 것이 보이는지. 자동으로 못 보는 `중심 장력`·`반발력` 은 점 크기 변화로 대신 본다 | 대기 |
| H285 | F-2006 14.3 H-d | 값 표시가 퍼센트(`43%`·`68%`·`54%`·`17%`)인 것이 읽히는지. 내부 단위(`0.02`·`800`)를 띄우지 않기로 한 판단이 맞는지 | 대기 |
| H286 | F-2006 14.3 H-e | `prefers-reduced-motion` 을 켠 채 장력 슬라이더를 끌었을 때 덩어리가 실시간으로 움직이는 것이 괜찮은지 (F-2006 16장 Q5 예외) | 대기 |
| H287 | F-2010 15장 H-a | 흐리기 85% 가 테마 3종에서 적당한지. 강조 안 된 노드가 아주 사라지지는 않는지, 간선이 거의 사라지는 것이 옳은지 | 대기 |
| H288 | F-2010 15장 H-b | 호버한 노드와 그 이웃이 깊이 섞기까지 벗어나 원색으로 튀는 것이 옵시디언 그래프뷰와 같은 느낌인지 | 대기 |
| H289 | F-2010 15장 H-c | ~~전환 없이 즉시 바뀌는 것이 거슬리는지, 마우스를 덩어리 위로 쓸 때 깜빡여 보이는지~~ | **무효** — 사용자가 2026-09-22 에 전환을 원한다고 답했고 F-2013 이 180ms 전환을 넣었다. 이어지는 확인은 H332~H334 |
| H290 | F-2010 15장 H-d | 우클릭 회전을 하는 내내 직전 강조가 유지되는 것이 맞는지 | 대기 |
| H291 | F-2010 15장 H-e | 현재 문서에 닿는 간선을 `--accent` 로 칠한 것이 링크가 많은 문서에서 너무 시끄럽지 않은지 | 대기 |
| H292 | F-2010 15장 H-f | `선 두께` 1 에서 강조 간선이 기본 간선과 구분되는지, 0 에서 강조 간선만 보이는 것이 쓸모 있는지 | 대기 |
| H293 | F-2010 15장 H-g | 끊긴 링크 노드를 호버했을 때 `--muted` 노드 + `--accent` 간선 조합이 어색하지 않은지 | 대기 |
| H294 | F-2012 14.3 H-a | `맞춤` 전환 240 ms 가 적당한지. 느려서 답답하지 않은지, 빨라서 안 보이지 않은지. 곡선이 끝에서 부드럽게 서는지 | 대기 |
| H295 | F-2012 14.3 H-b | 180° 돌려 놓은 상태에서 `맞춤` 을 눌렀을 때 덩어리를 가로지르지 않고 돌아서 가는지. 도중에 화면이 덩어리 속으로 들어가 깜깜해지는 구간이 없는지 | 대기 |
| H296 | F-2012 14.3 H-c | 문서 수십 개짜리 지도에서 `중심 장력`·`반발력` 을 끝에서 끝까지 끌어 점 크기가 그대로인지(덩어리만 오므라들고 벌어지는지). 덩어리가 화면 밖으로 나갔을 때 휠로 물러나 되찾을 수 있는지와 `맞춤` 이 되돌리는지. **휠로 되찾기는 e2e A7 이 못 가른다** — 세 문서 그래프는 반지름이 3배만 커져 옛 확대 한계(4배)에 안 걸린다 | 대기 |
| H297 | F-2012 14.3 H-d | `여기로 이동`·`Ctrl`+클릭의 전환이 자연스러운지. 각도가 유지된 채 노드만 가운데로 미끄러져 오는 것이 읽히는지 | 대기 |
| H298 | F-2012 14.3 H-e | `prefers-reduced-motion` 을 켠 채 `맞춤`·`여기로 이동` 이 즉시인지, 그것이 답답하지 않은지 | 대기 |
| H299 | F-2011 10장 H-a | `맞춤` 의 `fit_screen`(모서리 꺾쇠 + 안쪽 사각형)이 "전체가 보이게 되돌린다" 로 읽히는지. 노드 메뉴의 `여기로 이동`(`recenter`)과 헷갈리지 않는지 | 대기 |
| H300 | F-2011 10장 H-b | `지도`·`목록` 두 아이콘이 테두리 하나로 묶여 **한 벌(배타 선택)** 로 읽히는지. 바로 위 상단바 보기 모드 묶음과 같은 물건으로 보이는지, 가로 막대 둘에 같은 모양 묶음이 겹쳐 보여 거슬리지는 않는지 | 대기 |
| H301 | F-2011 10장 H-c | 머리 줄에서 글자가 다 사라진 뒤 **제목 `지도` 만 글자**인 것이 균형이 맞는지. 낱개 셋(`맞춤`·`지도 설정`·`닫기`)이 테두리 없이 오른쪽에 붙어 있는 것이 묶음과 구분되는지 | 대기 |
| H302 | F-2009 13.4 H-a | 노드가 커서에 붙어 따라오는 느낌인지. 돌려 놓은 각도에서 끌 때 "화면에 평행한 판 위에서 미끄러진다"가 자연스러운지, 깊이까지 움직였으면 하는지 | 대기 |
| H303 | F-2009 13.4 H-b | 놓았을 때가 재미있는지. alpha 0.6 에서 제자리로 "돌아가려다 새 균형에 굳는" 정도가 적당한지(덜 돌아오면 1.0, 너무 튀면 0.3) | 대기 |
| H304 | F-2009 13.4 H-c | `반발력`·`중심 장력` 을 양 끝으로 옮긴 뒤 각각 끌어 놓았을 때 힘의 차이가 손으로 느껴지는지 | 대기 |
| H305 | F-2009 13.4 H-d | 실제 터치 기기에서 (1) 한 손가락 노드 끌기, (2) 끌다가 손가락을 하나 더 얹으면 노드를 놓고 확대로 넘어가는지, (3) 움직이지 않고 누르고 있으면 여전히 메뉴가 뜨는지 | 대기 |
| H306 | F-2009 13.4 H-e | 터치로 끄는 동안 이름표가 안 뜨는 것이 거슬리는지 | 대기 |
| H307 | F-2009 13.4 H-f | 끄는 동안 커서가 `grabbing` 인 것과, 노드가 덮은 자리에서는 이동(pan)을 시작할 수 없는 것이 헷갈리지 않는지 | 대기 |
| H308 | 지도 노드 임포스터 (2026-09-23) | 노드가 각지지 않고 매끈한 원으로 보이는지. 가까이 확대해도 테두리가 계단지지 않는지, 노드 안으로 간선 끝이 비치지 않는지. 3D 입체감(구)이 사라진 것이 괜찮은지 | 대기 |
| H309 | F-2007 16.3 H-a | 테마 3종에서 걸러진 노드가 보이되 뒤로 물러나는지. 9.2 의 계산으로는 배경과 6~12 단계 차이다 — 눈으로 "사라졌다" 고 느껴지면 `MAP_FILTER_DIM` 을 낮춘다 | 대기 |
| H310 | F-2007 16.3 H-b | 폴더 목록 높이 180px 과 패널 폭 280px 에서 폴더 경로가 읽을 만한지. 잘린 경로에 `title` 툴팁이 뜨는지 | 대기 |
| H311 | F-2007 16.3 H-c | `몇 다리` 슬라이더의 값 문자열이 280px 패널에서 줄바꿈되지 않는지(`1단계 — 이 문서와 바로 이어진 문서까지` 는 두 줄이 될 수 있다) | 대기 |
| H312 | F-2007 16.3 H-d | `목록` 보기에서 패널이 열렸을 때 목록이 가려지지 않는지(13장의 `padding-right`), 좁은 창에서는 어떤지 | 대기 |
| H313 | F-2007 16.3 H-e | 조건에 맞는 문서가 0개일 때 화면이 이해되는지 — 흐려진 점만 남고 꼬리 줄에 `N개 중 0개 보임` 이 뜬다. 별도 안내 문구가 필요한지 | 대기 |
| H314 | F-2007 16.3 H-f | 실제 한글 입력기(두벌식·구글 입력기)로 `파일 검색` 을 칠 때 글자가 씹히거나 두 번 걸러지지 않는지. CDP 합성으로는 조립 순서만 확인했다 | 대기 |
| H315 | F-2015 14.3 | 운영(`rawdoc.app`)에서 강력 새로고침(DevTools "Fast 4G") — 흰 화면 없이 첫 페인트부터 스켈레톤이 보이고, ready 때 사이드바 폭·상단바 높이가 튀지 않는지 | 대기 |
| H316 | F-2015 14.3 | 화이트·세피아·다크 세 테마에서 막대가 "흐린 줄" 로 읽히는지. 너무 진하거나 안 보이면 9.1 의 8% 조정 | 대기 |
| H317 | F-2015 14.3 | ready 때 목록 막대 자리에 실제 목록 행이, 문서 막대 자리에 제목·본문 줄이 떨어지는지 (9.2~9.4 수치 조정 여부) | 대기 |
| H318 | F-2015 14.3 | 홈 화면 사용자(기본값)의 ready 전환 — 가운데 막대 → 홈 문구·버튼이 튀어 보이지 않는지 | 대기 |
| H319 | F-2015 14.3 | CPU 4x 감속으로 부팅이 1초를 넘길 때 400ms 뒤 깜빡임이 시작되고 시작 순간 튀지 않는지 | 대기 |
| H320 | F-2015 14.3 | Windows "애니메이션 효과" 끄기에서 깜빡임이 없는지 | 대기 |
| H321 | F-2015 14.3 | NVDA 로 새로고침 — "불러오는 중…" 이 읽히거나 조용하고, 막대 개수·빈 요소가 읽히지 않는지 | 대기 |
| H322 | F-2008 13.3 H-a | 테마 3종에서 그룹 색 여덟 개가 서로 구분되는지. 가장 가까운 쌍은 4번·5번(`question`·`warning`), sRGB 거리 화이트·세피아 41, 다크 32 — 헷갈리면 F-2008 16장 Q5 로 되돌아감 | 대기 |
| H323 | F-2008 13.3 H-b | 8번 색(`--ink-2` 별칭)이 그룹 없는 노드(`--ink`)와 구분되는지. 거리 화이트 75 · 세피아 53 · 다크 79 | 대기 |
| H324 | F-2008 13.3 H-c | 걸러지거나 흐려진 그룹 색이 배경으로 사라지지 않는지. 계산상 최악은 다크 8번 8.2 단계 | 대기 |
| H325 | F-2008 13.3 H-d | 패널 폭 280px 에서 색 칩 여덟 개가 한 줄에 들어가고 고른 표시가 보이는지, 560px 미만 창에서는 어떤지 | 대기 |
| H326 | F-2008 13.3 H-e | `목록` 보기의 색 점이 글자를 밀어내지 않고 읽히는지, 점만으로 그룹을 알 수 있는지 | 대기 |
| H327 | F-2017 14.6 H1 | 8단계 트리를 사이드바 기본 폭(300px)·최소 폭(200px)에서 — 6단계부터 들여쓰기가 멈추는 모양, 이름 칸이 읽을 만한지, 가이드 선(4에서 멈춤)이 토글 아이콘을 지나지 않는지, 끌어놓기 강조가 어느 행인지 분명한지 | 대기 |
| H328 | F-2017 14.6 H2 | 폴더로 이동 대화상자에서 8단계 트리 — 들여쓰기 멈춤, 긴 목록 스크롤 | 대기 |
| H329 | F-2017 14.6 H3 | 공개 폴더 목록에서 4단계 이상 — 제목 줄만 있는 중간 폴더가 어색하지 않은지, 240px 폭에서 70px 들여쓰기 | 대기 |
| H330 | F-2017 14.6 H4 | 문서 제목 위 경로가 7단계일 때 줄바꿈 모양(F-234, 접지 않음) | 대기 |
| H331 | F-2017 14.6 H5 | 배포 주소에서 폴더 초대: 4단계 아래 문서가 초대받은 계정의 `공유받음` 에 보이고 열리며, `viaFolder` 표시가 가장 가까운 초대 폴더인지. 원격 D1 의 `WITH RECURSIVE` 지원(가정 G4)도 이것으로 확인 | 대기 |
| H332 | F-2013 13.3 H-a | 호버 초점 전환 180ms 가 적당한지 — 답답하거나 안 보이지 않는지, 다른 호버 전환(`--transition-fast`)과 같은 속도로 느껴지는지 | 대기 |
| H333 | F-2013 13.3 H-b | 덩어리 위로 마우스를 쓸 때 강조가 노드에서 노드로 즉시 옮겨 가는 것이 읽히는지, 반쯤 흐린 잔상이 없는 게 더 또렷한지 | 대기 |
| H334 | F-2013 13.3 H-c | 되돌아가는 전환이 들어올 때와 같은 속도로 느껴지는지, 이름표는 즉시 뜨고 지는데 색만 서서히 바뀌는 게 어긋나 보이지 않는지 | 대기 |
| H335 | F-2018 15.3 H1 | **실제 옵시디언**으로 만든 헤딩 링크(`# ^ : % |` 등이 든 제목, 굵게 표시가 든 제목)가 이 앱에서 그 제목으로 가는지 — F-2016 G3 | 대기 |
| H336 | F-2018 15.3 H2 | 위키링크 자동완성의 폴더 경로 글자(`.cm-completionDetail`) 색·간격·긴 경로 잘림 | 대기 |
| H337 | F-2018 15.3 H3 | 한글 IME 로 `[[` 뒤 같은 제목 후보를 고를 때, 조합 중 문서가 저장돼 해석기가 바뀌어도 조합 뒤 표시가 맞게 따라잡는지 | 대기 |
| H338 | F-2018 15.3 H4 | 편집·보기·공개 화면에서 헤딩으로 간 뒤 제목이 맨 위 16px 아래에 자연스럽게 오는지(보기·공개는 smooth scroll), 편집 모드에서 커서가 제목 줄에 가 있는 것이 거슬리지 않는지 | 대기 |
| H339 | F-2018 15.3 H5 | 공개 화면 알림 띠(헤딩을 못 찾았을 때)의 자리와 모양 | 대기 |
| H340 | F-2018 15.3 H6 | 배포 뒤 Cloudflare 대시보드에서 문서가 많은 계정의 `/pub/docs/:token/set`·`/api/docs/:id/share-set` CPU 시간 | 대기 |
| H341 | F-302 11.4 H-a | 실제 한글 입력기로 본문에 20자 안팎 문장을 중간에 1초쯤 멈추며 친 뒤 Ctrl+Z 를 한 번씩 — 한 번에 지워지는 양이 이전 배포본과 같고, 글자 중복·누락·커서 튐이 없는지 | 대기 |
| H342 | F-302 11.4 H-b | 표 칸에서 한글 → Alt+Enter → 한글, 칸 안에서 Ctrl+Z 두 번 — 이전 배포본과 같은 순서로 되돌아가고 칸 편집이 튕기지 않는지 | 대기 |
| H343 | F-302 11.4 H-c | 본문을 고친 뒤 Shift+우클릭 → 브라우저 기본 메뉴 `실행 취소` — 한 단계만 되돌아가고 글자가 깨지거나 두 번 되돌아가지 않는지 | 대기 |
| H344 | F-302 11.4 H-d | 여러 곳을 클릭해 가며 고친 뒤 Ctrl+Z 여러 번 — 되돌릴 때마다 커서·스크롤이 그 자리로 가는지, 이전 배포본과 같은 느낌인지 | 대기 |
| H345 | F-2020 11.4 H-a | 실제 옵시디언에서 하위 폴더 문서의 `![[{id}.png|300]]` 이 루트 `attachments/` 그림을 폭 300px 로 보이는지 | 대기 |
| H346 | F-2020 11.4 H-b | 옵시디언에서 `[[교안/회의록|회의록]]`·`[[위키/색인|색인]]` 이 그 파일로 가는지, 최상위 `색인.md` 옆에 `위키/색인.md` 가 있을 때 `[[색인]]` 이 최상위로 가는지 | 대기 |
| H347 | F-2020 11.4 H-c | 내보낸 볼트가 `.obsidian/` 없이 열리는지, CRLF 문서가 문제없이 열리고 편집되는지 | 대기 |
| H348 | F-2020 11.4 H-d | 대소문자만 다른 링크가 옵시디언에서 풀리는지, 표 안 `[[왜_]]` 가 표를 깨지 않는지 | 대기 |
| H349 | F-2020 11.4 H-e | 설정 데이터 탭 버튼 줄(버튼 셋 + 안내)이 좁은 대화상자에서 어떻게 접히는지, 폴더 메뉴 `옵시디언 볼트로 내보내기` 길이 | 대기 |
| H350 | F-2020 11.4 H-f | 문서 수천 개 계정에서 내보내는 동안 화면이 멈추지 않고 진행 알림이 올라가는지 | 대기 |
| H351 | F-303 13.4 H1 | `?ysync` 두 창. A 가 한글 문단을 이어 치는 동안 B 가 같은 문단 뒤쪽에 `BBB` — A 조합이 안 끊기고 다 친 뒤 두 창 본문이 같은지 (준비 절차는 F-303 13.4) | 통과 (2026-09-23 사용자 "한글 자모 조합에서 깨지는거 없음, 역방향도 확인") |
| H352 | F-303 13.4 H2 | 같은 조건에서 B 가 A 커서 앞쪽을 고침 — A 글자가 엉뚱한 자리에 안 들어가고 커서가 안 튀는지 | 통과 (2026-09-23 사용자 확인) |
| H353 | F-303 13.4 H3 | A 가 표 칸에서 한글 입력 중 B 가 같은 표 다른 칸 수정 — 칸 편집이 안 튕기고 양쪽 편집이 남는지 | 통과 (2026-09-23 사용자 "표 안 수정도 잘 따라옴") |
| H354 | F-303 13.4 H4 | A 가 표 칸에서 한글 입력 중 B 가 표 밖 수정 — 칸 편집이 안 튕기는지 | 통과 (2026-09-23 사용자 확인) |
| H355 | F-303 13.4 H5 | A 조합 중 B 가 50줄 안팎 붙여넣기 — 조합이 안 끊기고 제목·목록·표·코드블록·목차가 깨진 채 남지 않는지 | 통과 (2026-09-23 사용자 확인) |
| H356 | F-303 13.4 H6 | A 에서 `하` 까지 조합한 채 B 로 옮겨 `CCC` — A 에 2초 안팎에 보이고, 돌아와 이어 치면 정상인지 | 대기. 참고(2026-09-23 사용자 확인): 조합 중 **다른 Chrome 창**(페이지든 주소표시줄이든)으로 포커스를 옮기면 미확정 음절이 사라진다. 메모장·VS Code·터미널로 옮기면 남는다. 게이트 유무와 무관하고, 옆 창은 페이지를 클릭하지 않아도(주소표시줄) 같아서 A 자신이 조합을 취소하는 Chrome·Windows IME 동작으로 본다. 자동 흉내(CDP)에서도 "취소" 로 끝날 때만 같은 증상이 났다. `?ysync` 에서 눈에 띈 것은 미확정 글자가 이미 상대에게 전달돼 있어서다 |
| H357 | F-303 13.4 H7 | A `가나다` 뒤 B 가 `RRR`, A 조합 중 Ctrl+Z — A 편집만 되돌아가고 `RRR` 가 남는지 | 통과 (2026-09-23 사용자 확인) |
| H358 | F-303 13.4 H0 (선택, 기록) | `?ysync=nogate` 로 H1·H3·H5 — 게이트 없이 무엇이 깨지는지 비고에 적기 (F-301 U10) | 기록함 (2026-09-23 사용자: nogate 로 H1·H3·H5 해 봤고 "깨지는 것 없었음") |
| H359 | F-304 15.4 S1 (배포 뒤) | 로그인한 창 콘솔에서 `/ws/doc/{id}` 연결(코드는 F-304 15.4) — `msg` 1줄 이상, 10초 안에 `close` 없음. `4401` 이면 쿠키가 안 실린 것 → 4.4 2안(티켓). **F-305 명세의 게이트** | 통과 (2026-09-24 배포 `db9f23c`, 사용자 콘솔: `msg 10` 1줄, `close` 없음 — 쿠키가 `/ws` 에 실린다. 4.4 티켓 안은 구현하지 않는다) |
| H360 | F-304 15.4 S2 (배포 뒤) | 시크릿 창에서 같은 코드 — `close 4401 unauthenticated` | 대기 |
| H361 | F-304 15.4 S3 (배포 뒤) | `https://example.com` 콘솔에서 `wss://…/ws/doc/<id>` — `close 1006` | 대기 |
| H362 | F-304 15.4 S4 (배포 뒤) | S1 소켓을 ping 없이 30분 — 끊기면 시각·코드, 안 끊기면 "30분 유지". **F-305 배포 게이트** | 대기 |
| H363 | F-304 15.4 S5 (배포 뒤) | 20초마다 `ping` 30분 — `msg pong`, 대시보드 DO Duration 이 450 GB-s 에 한참 못 미침(값·단위 그대로 적기). **F-305 배포 게이트** | 대기 |
| H364 | F-304 15.4 S6 (배포 뒤) | 평소 사용 중 `npx wrangler tail md-editor-web` — `DocRoom` 오류 없음. 참고: 로컬에서 클라이언트가 끊을 때 `Uncaught Error: Network connection lost` 가 찍혔음(partyserver 가 "무해한 teardown 오류" 라 적은 종류로 추정, 확인 안 함) | 대기 |
| H365 | F-2022 11.4 H1 | 팔레트 위치(위 여백 12vh)·폭·줄 높이·`detail` 흐린 글이 화이트·세피아·다크에서 읽히는지. 거를 때 입력칸이 안 움직이는지 | 대기 |
| H366 | F-2022 11.4 H2 | 열고 닫는 전환이 다른 대화상자와 같은 느낌인지 | 대기 |
| H367 | F-2022 11.4 H3 | **실제 한글 IME** 로 `템플릿` 을 치다 마지막 글자 조합 중 `Enter` — 한 번에 2단계로 가고, 거기서 템플릿이 저절로 들어가지 않는지. 2단계에서도 같은 방식으로 한 번만 들어가는지 | 대기 |
| H368 | F-2022 11.4 H4 | 한글을 치는 도중(`템ㅍ`) 목록이 잠깐 비는 것이 거슬리는지 | 대기 |
| H369 | F-2022 11.4 H5 | 한글 입력 상태에서 `Ctrl+P` 가 팔레트를 여는지. macOS `Cmd+P`, `Ctrl+P`(H155 와 함께). 확장 깔린 실제 크롬 프로필에서 단축키를 뺏기지 않는지 | 대기 |
| H370 | F-2022 11.4 H6 | 내장 템플릿 4개 문구가 적당한지 — 문구는 제품 글이다 | 대기 |
| H371 | F-2022 11.4 H7 | 스크린 리더에서 combobox·listbox 가 읽히는지(H190 과 같은 패턴) | 대기 |
| H372 | F-2022 A10 (구현 추론) | 표 칸에서 우클릭 → 명령 팔레트 → 템플릿 삽입 — 그 칸 자리에 들어가는지, 표가 깨지지 않는지 | 대기 |
| H373 | F-2021 13.4 S1 | Windows 11 Chrome, `dev:worker` 띄우고 `rawdoc login --server http://localhost:8790` — 해시까지 온전한 인증 화면 → 승인 → 성공 문구 → `whoami`·`ls`·`new`·`get` 이 되는지 | 대기 |
| H374 | F-2021 13.4 S2 | 배포·게시 뒤 로그아웃 상태 브라우저에서 `npx -y rawdoc login` → `로그인` → Access 일회용 코드 → 인증 화면으로 복귀 → 승인 → `ls` | 통과 (2026-09-24 Windows 11 Chrome, `npx -y rawdoc login` → 승인 → `whoami`·`ls` 정상). 참고: 첫 시도는 브라우저에 "잘못된 로그인 주소" — 같은 주소를 자동 브라우저로 열면 정상이고, 주소 전체를 붙여 넣자 성공했다. 474자 주소가 터미널 링크에서 잘린 것으로 추정(확인 안 함). 저장소 안에서는 `npx rawdoc` 이 루트 `package.json`(이름 `rawdoc`, bin 없음)을 잡아 `could not determine executable` → F-2023 에서 주소를 102~107자로 줄임(H381 에서 확인) |
| H375 | F-2021 13.4 S3 | Edge·Firefox(데스크톱)에서 S1 — https 페이지에서 `http://127.0.0.1` 로의 최상위 이동이 막히거나 권한 창이 뜨는지 | 대기 |
| H376 | F-2021 13.4 S4 | PWA 로 설치한 Chrome 에서 S2 — 인증 화면이 앱 창으로 열려도 콜백 이동이 되는지 | 대기 |
| H377 | F-2021 13.4 S5 | macOS 또는 Linux 가 있으면 브라우저 열기(`open`·`xdg-open`)가 되는지 | 대기 |
| H378 | F-2021 13.4 S6 | 인증 화면 모양 — 밝게·어둡게, 좁은 창 | 대기 |
| H379 | F-2021 13.4 S7 | AI 도구(Claude Code 등)가 `RAWDOC_TOKEN` 을 넣고 `npx -y rawdoc ls --json`·`new 파일 --json` 을 비대화형으로 부를 수 있는지 | 대기 |
| H380 | F-2021 13.4 S8 | 게시 완료 확인 — `npm view rawdoc version`, 다른 컴퓨터에서 `npx -y rawdoc --version` | 통과 (2026-09-24 `npm view rawdoc version` = 0.1.0, `npx -y rawdoc@latest --version` = 0.1.0. 다른 컴퓨터에서는 아직 안 해 봄) |
| H381 | F-2023 13.4 S1 | 웹 배포·0.2.0 게시 뒤, H374 때와 같은 터미널에서 `npx -y rawdoc@latest login --force` → 찍힌 주소가 한 줄에 들어가는지, 그 링크를 눌러서 연 화면이 승인 화면인지 → `승인` → `whoami` | 대기 |
| H382 | F-2023 13.4 S2 | (원인 확인용, 선택) 같은 터미널에서 `npx -y rawdoc@0.1.0 login --no-browser --force` 로 찍힌 474자 주소를 눌러 열어 잘리는지. 잘리면 F-2023 3장 후보 A 로 확정, 아니면 B·C 가 남는다 | 대기 |
| H383 | F-2023 13.4 S3 | 로그아웃 상태 브라우저에서 0.2.0 `login` → `로그인` → Access 일회용 코드 → 승인 화면으로 돌아오는지 (F-2023 3장 후보 B) | 대기 |
| H384 | F-2023 13.4 S4 | 새 웹 + 0.1.0(`npx -y rawdoc@0.1.0 login --force`)이 지금처럼 끝까지 되는지 | 대기 |
| H385 | F-2023 13.4 S5 | `invalid`(업데이트 줄 포함)·`unsupported` 화면 모양 — 밝게·어둡게, 긴 명령 줄이 좁은 창에서 넘치지 않는지 | 대기 |
| H386 | F-2023 13.4 S6 | Firefox·Edge(가능하면 Safari)에서 0.2.0 승인 — X25519 가 실제로 되는지 | 대기 |
| H387 | F-305 19.4 H1 | 서로 다른 두 브라우저(한 쪽은 초대받은 계정)로 같은 문서 — 서로 친 것이 1~2초 안에 보이고 새로고침 뒤에도 남는지 | 대기 |
| H388 | F-305 19.4 H2 | 한국어 IME 로 두 사람이 같은 문단을 번갈아 침(F-303 H1~H5 를 실제 서버로) — 음절이 깨지거나 두 벌이 되지 않는지 | 대기 |
| H389 | F-305 19.4 H3 | 편집 중 와이파이를 끄고 5초 뒤 켬 — `연결 끊김 · 다시 연결 중` → `저장됨`, 끊긴 동안 친 것이 상대에게 가는지 | 대기 |
| H390 | F-305 19.4 H4 | 편집 중 소유자가 초대를 지움 — 몇 초 안에 읽기 전용 + N2 알림이 뜨는지 | 대기 |
| H391 | F-305 19.4 H5 | 문서 열기부터 편집 가능까지 체감 시간(10번, 짧은 문서·100KB 문서) — 값을 적는다(F-301 U12). 1초를 자주 넘으면 F-305 22장 Q1 재검토 | 대기 |
| H392 | F-305 19.4 H6 | 약 990KB 문서 실시간 열기 — 열리고 편집되는지. 안 되면 코드·시각을 적는다 | 대기 |
| H393 | F-305 19.4 H7 | 상태바 `불러오는 중…`·`연결 끊김 · 다시 연결 중`·`.statusbar-live` 의 모양·자리 — 눈으로 보기에 어색하지 않은지 | 대기 |
| H394 | F-305 19.4 H8 | 하루 쓰고 대시보드 DO Requests·Duration — F-301 9.3 표 범위 안인지, 값 그대로 적기 | 대기 |
| H395 | F-305 13장 keepalive | H362·H363 결과로 K1~K4 중 확정 — 지금 기본값 20,000ms | 대기 |
| H396 | F-307 12.4 P1 | `npm run dev:worker` 를 켜고 일반 창 하나 + 시크릿 창 하나로 같은 서버 문서를 연다(`DEV_AUTH_EMAIL` 로 둘 다 같은 사람이라 아바타는 안 뜨고 원격 커서는 뜬다). 실제 한글 IME — A 창이 한 문단을 한글로 이어 치는 동안 B 창이 같은 줄 여러 자리를 계속 누른다 — A 의 조합이 끊기지 않고 글자가 빠지거나 겹치지 않는지. A 가 조합을 끝내면 B 커서가 마지막 자리에 보이는지 | 대기 |
| H397 | F-307 12.4 P2 | B 커서를 A 가 치는 바로 그 자리에 두고 A 가 한글 긴 문장을 친다 — P1 과 같은지, B 커서 라벨이 A 의 입력을 가리지 않는지 | 대기 |
| H398 | F-307 12.4 P3 | A 가 조합하는 줄을 B 가 여러 줄 선택으로 덮는다 — P1 과 같은지, A 확정 뒤 선택 바탕이 보이는지 | 대기 |
| H399 | F-307 12.4 P4 | A 가 표 칸을 한글로 편집하는 동안 B 가 같은 표 둘레를 누르고 다닌다 — 칸 조합이 끊기지 않고 칸 편집이 튕기지 않는지 | 대기 |
| H400 | F-307 12.4 P5 | 원격 커서·선택·라벨 모양을 화이트·세피아·다크 세 테마에서 본다 — 커서가 본문과 구별되는지, 라벨이 1.5초쯤 보였다 사라지며 커서에 마우스를 올리면 다시 보이는지, 라벨이 첫 줄에서 잘리지 않는지 | 대기 |
| H401 | F-307 12.4 P6 | 배포본에서 서로 다른 두 계정으로 같은 문서를 연다. 창 폭 1440·900·360px — 상대 아바타 하나(머리글자·색), 올리면 이메일이 보이는지. 360px 에서 상단바가 넘치지 않는지. 내 아바타는 안 뜨는지 | 대기 |
| H402 | F-307 12.4 P7 | 배포본에서 P6 두 창을 연 채 1분 넘게 아무것도 안 한 뒤(DO 가 잠들게) B 창을 닫는다 — A 에서 B 아바타·커서가 5초 안에 사라지는지. 이어서 B 를 다시 열면 A·B 둘 다 서로를 보는지(커서를 움직이지 않아도) | 대기 |
| H403 | F-307 12.4 P8 | P6 뒤 Cloudflare 대시보드 DO Requests 를 1시간 공동 편집 전후로 적는다 — 5.4 계산(최악 시간당 720건/사람 + 타이핑)과 같은 대인지 적는다. F-301 9.3 O1 과 함께 | 대기 |
| H404 | F-306 19.4 H1 | 설치한 PWA 에서 실시간으로 한 번 연 문서를, 와이파이를 끄고 **앱을 새로 열어** 연다 — 편집되고, 와이파이를 켜면 다른 브라우저에 1~2초 안에 보이는지 | 대기 |
| H405 | F-306 19.4 H2 | 와이파이를 끈 채 한 번도 연 적 없는 문서를 연다 — 읽기만 되고 N7, 와이파이를 켜면 편집 가능한지 | 대기 |
| H406 | F-306 19.4 H3 | 두 사람이 같은 문단을 한쪽은 오프라인, 한쪽은 온라인으로 고친 뒤 다시 붙인다 — N8 이 뜨고 두 편집이 모두 남는지(문장이 섞여도 글자가 사라지지 않는지) | 대기 |
| H407 | F-306 19.4 H4 | 느린 노트북에서 약 1MB 문서를 1분 이어 치고 멈춘다 — 치는 도중·멈춘 직후 끊김이 거슬리지 않는지 | 대기 |
| H408 | F-306 19.4 H5 | 오프라인 편집 뒤 창을 닫고, 다음 날 온라인에서 앱을 열되 그 문서는 열지 않는다 — 다른 브라우저에서 그 편집이 보이는지 | 대기 |
| H409 | F-306 19.4 H6 | N5·N7·N8 의 모양·자리가 눈으로 보기에 어색하지 않은지 | 대기 |
| H410 | F-2034 8.3 H1 | 초대 대화상자의 새 안내 문구(더 길어짐)가 1280px·400px 창에서 `닫기` 버튼과 겹치지 않고 줄바꿈이 어색하지 않은지 | 대기 |
| H411 | F-308 16.4 S1 | 두 창(다른 계정 편집자)으로 문서를 열고 한쪽에서 치는 동안 CLI `put --base-version` — 치는 쪽 커서가 튀지 않고 CLI 는 409 또는 200, 200 이면 두 창에 곧바로 보이는지 | 대기 (배포 뒤) |
| H412 | F-308 16.4 S2 | 창 하나로 문서를 열어 두기만 하고 CLI `put` — 200, 창에 1초 안에 보이는지. 한글 조합 중이면 조합이 끝난 뒤 보이는지 | 대기 (배포 뒤) |
| H413 | F-308 16.4 S3 | 아무도 안 연 문서에 1분마다 `put` 30분 뒤 대시보드 DO Duration·Requests 값 적기 — 실시간 경로 추정(1회 1.9 GB-s)보다 한참 작은지 | 대기 (배포 뒤) |
| H414 | F-308 16.4 S4 | 문서를 열어 두고 5분 손대지 않은 뒤 CLI `put`, `wrangler tail` 켜 둠 — 200, 창에 곧바로 보이고 `DocRoom load`·`docRoom writeText failed` 오류 없는지 | 대기 (배포 뒤) |
| H415 | F-2032 ①~⑤ / F-2033 11장 | 배포 전 사람 할 일: Google·GitHub OAuth 앱 등록, 비밀 값(`wrangler secret put`), 원격 마이그레이션 0009, Access 앱 삭제(배포 직전), 두 PC `.dev.vars` 갱신 — 11장 순서대로 했는가 | 대기 (배포 전) |
| H416 | F-2032 ⑥ / F-2033 | Google 로그인 → 원래 문서 해시로 돌아옴, 기존 문서가 그대로 보임 | 통과 (2026-09-25, 사용자 "로그인 확인함") |
| H417 | F-2032 ⑦ / F-2033 | GitHub 로그인 — 같은 이메일이면 Google 과 같은 계정(같은 문서 목록) | 대기 (배포 뒤) |
| H418 | F-2032 ⑧ / F-2033·F-2034 | 로그아웃 → 새로 고쳐도 로그아웃 상태 | 대기 (배포 뒤) |
| H419 | F-2032 ⑨ / F-2033 | 실시간 문서의 `/ws` 가 세션 쿠키로 열림 (H359 방법) | 대기 (배포 뒤) |
| H420 | F-2032 ⑩ / F-2033 | 로그아웃 상태에서 `npx -y rawdoc login` → S-9 → `/login` → 로그인 → S-9 `승인` | 대기 (배포 뒤) |
| H421 | F-2032 ⑪ / F-2033 | 브라우저를 닫았다 열어도 로그인 유지 | 대기 (배포 뒤) |
| H422 | F-2032 ⑫ / F-2033 | `/login` 모양 — 사이트 머리·꼬리, 다크 모드 사용자도 밝은 화면, 로고 크기·정렬, 오류 줄(`/login?error=access_denied`). 버튼 CSS 는 임시 값 | 대기 |
| H423 | F-2032 ⑬ / F-2033 | 배포 뒤 1주: Workers 대시보드 CPU 시간(p50·p99), "exceeded CPU" 오류 수, "Worker Startup Time"(로컬 check startup 46~50ms, 첫 요청 인스턴스 생성 11ms). 넘치면 F-2033 12장 Q1 | 대기 (배포 뒤 1주) |
| H424 | F-2032 ⑭ / F-2033 | Google·GitHub 공식 로고 SVG 를 받아 `worker/providerLogos.ts` 에 넣기(지금 빈 문자열) — 사용 조건·Google 버튼 모양 규칙, 받은 URL·날짜 | 넣음 2026-09-24 (메인이 공식 묶음에서 받음 — 받은 곳은 파일 머리 주석). Google 은 버튼 틀을 빼고 viewBox 를 G 영역으로 좁힘. 로컬 `/login` 에서 모양 확인 남음 |
| H425 | F-2033 ⑮ | 로컬 OAuth 앱으로 실제 Google·GitHub 로그인이 `http://localhost:8790` 으로 돌아오는지(`wrangler dev` 가 요청 주소를 바꿔 씀 — 돌려 보지 않음) | 통과 (2026-09-24 사용자 "로그아웃/로그인 둘다 잘됨" — Google, 로컬 dev:worker. 로그아웃 뒤 재로그인은 SW 거부 목록 수정 71fbb4d 뒤) |
| H426 | F-2031 9.3 S1 | F-2026 구현 뒤 로컬 `dev:worker` 에서 `rawdoc mkdir` 250번 연속(또는 분이 바뀐 직후 130번 — 로컬 창은 벽시계 분마다 0으로 돌아감, F-2026 5.4) — 종료 코드 8 과 한도 문구가 나오는지 | 대기 (F-2026 뒤) |
| H427 | F-2031 9.3 S2 | 0.2.0 게시 뒤 `npx -y rawdoc@latest --version` 이 0.2.0, `login --force` 가 짧은 주소를 내는지 | 대기 (게시 뒤) |
| H428 | F-2031 9.3 S3 | 게시 뒤 npm 패키지 페이지 README 에 종료 코드 8 행과 "한도에 걸리면" 절이 보이는지 | 대기 (게시 뒤) |
| H429 | F-2036 11장 H1 | 1280·1440 창에서 세 칸(문서 목록·본문·목차) 균형, 첫 줄이 제목 윗선과 맞는지 | 대기 |
| H430 | F-2036 11장 H2 | 본문 34rem 이 좁아 보이지 않는지, 긴 제목이 두 줄로 꺾일 때 모양 | 대기 |
| H431 | F-2036 11장 H3 | 여백의 원문 기호(#·##)가 왼쪽 문서 목록에 붙어 보이지 않는지(1056px 에서 8px) | 대기 |
| H432 | F-2036 11장 H4 | 390px 접는 `목차` 안 글머리·밑줄·들여쓰기 | 대기 |
| H433 | F-2036 11장 H5 | 도움말 목차가 칸 안에서 스크롤되는지, Chrome 현재 절 강조가 자연스러운지, Firefox 에서 강조 없음이 어색하지 않은지 | 대기 |
| H434 | F-2036 11장 H6 | 문서 목록의 현재 페이지 표시 색·들여쓰기 | 대기 |
| H435 | F-2036 11장 H7 | 랜딩 ↔ 글 페이지를 오갈 때 머리(로고·메뉴)가 제자리인지 | 대기 |
| H436 | F-2030 9.3 | L1~L5·L7 알림 띠 색·줄바꿈 (좁은 창에서 L2·L7 이 두 줄 이상일 때) | 대기 |
| H437 | F-2030 9.3 | 계정 메뉴 두 줄(이미지·문서 사용량) 정렬과 90% 이상일 때 위험 색 | 대기 |
| H438 | F-2030 9.3 | 로컬 `dev:worker` 끝-끝: `wrangler.jsonc` `ratelimits[0].simple.limit` 을 잠시 낮춰(커밋 금지) 429 → L1 → 자동 이어서 저장, 하루 한도 → L2 의 시각이 한국 오전 9:00 (F-2025·F-2026 구현 뒤, 배포 전) | 대기 |
| H439 | F-2030 9.3 | `admin-warn` → L7, `admin-block` → 다른 탭에 10분 넘게 있다 돌아오면 L5·읽기 전용, `admin-unblock` → L5 걷히고 편집 가능 (배포 뒤 1주 관측) | 대기 |
| H440 | F-2030 9.3 | 사이트 `/help` 저장 절에 새 문단이 보이고 목차 줄 수가 그대로인지 (배포 뒤) | 대기 |
| H441 | F-2025 10장 | 배포 직전 `d1 migrations list --remote` 에 `0010_usage.sql` 이 적용됐는지, 누계 합 = 문서 합 확인 질의(3.1 ②) | 대기 |
| H442 | F-2025 10장 | 배포 뒤 `admin-recount --all` 출력 (옛 Worker 가 쓴 몫 다시 셈, F-2029 구현 뒤) | 대기 |
| H443 | F-2025 10장 | 배포 뒤 브라우저에서 `GET /api/usage` 응답에 문서 사용량이 실리는지 | 대기 |
| H444 | F-2025 10장 | 배포 1주 뒤 D1 "Rows written" 대시보드를 배포 전과 비교 (쓰기당 +1 행 예상, 5.2) | 대기 |
| H445 | F-2026 9장 ① | 배포 직후 Workers Builds 로그의 바인딩 목록에 `WRITE_LIMITER` 가 있는지 | 대기 |
| H446 | F-2026 9장 ② | 배포 직후 CLI 토큰으로 쓰기를 빠르게 반복해 429 `minute` 로 바뀌는 시점 확인(근사) | 대기 |
| H447 | F-2026 9장 ③ | 배포 뒤 첫 주 `wrangler tail` 에 `write_limiter_failed`·`write_limiter_missing` 이 안 나오는지 | 대기 |
| H448 | F-2029 10장 ① | 배포 직전(F-2025 3.1 1·2번 뒤) 첫 원격 실행 `node scripts/admin-usage.mjs` 가 읽기만으로 돌고 원격 `--json` 출력 모양이 로컬과 같은지. recount(H442)·warn/block 왕복(H439)은 기존 항목에서 본다 | 대기 |
| H449 | F-2027 10장 ① | 배포 뒤 원격에서 DO 알람이 도는지(느린 저장 중 방을 닫으면 60초 안에 마지막 편집이 D1 에 들어가는지), 원격 D1 batch `RETURNING` 모양 | 대기 |
| H450 | F-2027 10장 ② | 배포 1주 뒤 D1 `Rows written` 에서 실시간 방 몫이 계산(방 하나 최악 약 18,070행/일) 안쪽인지 — H444 와 함께 | 대기 |
| H451 | F-2028 11장 ① | 로컬 OAuth 로 `dev:worker` — 관문을 `(오늘, 20)` 으로 둔 뒤 새 이메일 로그인에 L6 이 보이는지, 기존 계정은 로그인되는지 | 대기 |
| H452 | F-2028 11장 ② | 배포 직후 시험 계정 `admin-block` → 공개 링크 `링크를 찾을 수 없습니다`, 초대받은 쪽 읽기 전용. `admin-unblock` 으로 되돌아오는지 | 대기 |
| H453 | F-2028 11장 ③ | `admin-warn` 뒤 L7, `/api/me` 에 `"warned":true` | 대기 |
| H454 | F-2028 11장 ④ | 첫 주 `admin-usage` 의 오늘 가입 두 값(관문 수·실제 생성 수)이 같은지 | 대기 |

- F-306 19.3 L1~L4 — `npm run dev:worker` 로컬 확인(구현 에이전트가 `.dev.vars` 없어 미실행)
- 원격 커서 hover 로 라벨 다시 보이기는 2px 폭 위에서만 됨(P5 와 함께 볼 것)
- `Prec.low` mark 가 인라인 프리뷰 안쪽에 들어가는지(F-307 U2)
