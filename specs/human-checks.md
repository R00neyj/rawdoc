# 사람 확인 목록

자동 테스트(`npm test`, `npm run test:e2e`)로 판정할 수 없는 항목만 모은다. 메인이 명세 완료마다 갱신한다
판정 환경: 데스크톱 Chrome, 한국어 Windows IME (`specs/product.md` Q7)

상태: `대기` / `통과 (날짜)` / `실패 (날짜, 내용)`

## 1. 한글 IME (실제 입력)

기준: 글자 중복·누락·커서 튐 없음

| # | 명세 | 확인 | 상태 |
| --- | --- | --- | --- |
| H1 | F-103 A8 | 본문에 한글 연속 입력 | 통과 (2026-09-14) |
| H2 | F-104 A4 | 굵게(`**`) 안 한글 연속 입력, 기호 경계에서 조합, 조합 중 방향키 | 통과 (2026-09-14) |
| H3 | F-105 A4 | 제목·목록·인용 줄에서 한글 입력 | 통과 (2026-09-14) |
| H4 | F-106 A5 | 코드블록 안 한글 입력, 블록 경계에서 조합 중 방향키 | 통과 (2026-09-14, 비고: 코드블록 한글이 작게 보임 — F-154 D2Coding 과 관련) |
| H5 | F-125 A8, F-135 A3 | 표 칸 안 한글 연속 입력, 조합 중 Tab·Enter·방향키 | 통과 (2026-09-15, 이전 실패 2026-09-14 → F-161 수정) |
| H6 | F-139 | 표를 줄 단위로 치며 한글 입력 | 실패 (2026-09-15, 줄 단위 한글 입력은 정상. Alt+Enter 줄바꿈만 안 됨 — F-162 는 표 칸 편집 중에만 동작, 입력 위치 확인 필요) |
| H7 | F-127 A5 | 한글 조합 직후 `(`·`*`, 짝 안에서 한글 연속 입력 | 통과 (2026-09-14) |
| H8 | F-128 A7 | 콜아웃 제목·본문 한글 입력 | 대기 (2026-09-14 비고: 색은 나오나 아이콘 없음 → F-148 반영, 재확인) |
| H9 | F-129 A7 | 링크 글자 안 한글 입력 | 통과 (2026-09-14) |
| H10 | F-131 A9 | `[[` 뒤 한글 제목 입력하며 자동완성, 조합 중 Esc | 통과 (2026-09-14) |
| H11 | F-133 A8 | 프론트매터 값에 한글 입력 | 통과 (2026-09-14) |
| H12 | F-134 A2 | 제목·목록 윗줄 끝에서 한글 입력 | 통과 (2026-09-14) |
| H13 | F-146 A10 | 한글 조합 중 문서 칸 좌우 여백 클릭, 실제 Tab 키로 편집기 포커스 복귀(e2e 는 `focus()` 로 대신 판정) | 대기 |
| H14 | F-166 A7 | 편집 모드 목록 줄(글머리·순서·할일)에서 한글 입력하며 커서 출입, 기호가 거터 쪽으로 흔들리지 않는지 | 대기 |
| H15 | F-202 A7 | TS 이전 뒤 회귀: 편집 모드 연속 한글 입력, 표 칸 입력, 제목 줄 기호 토글에서 커서 튐·글자 중복 없는지 (배포 주소 rawdoc.app) | 대기 |

## 2. OS·시스템 창

| # | 명세 | 확인 | 기대 | 상태 |
| --- | --- | --- | --- | --- |
| H20 | F-119 A3 | 설치한 앱을 Windows 탐색기 `.md` 연결 프로그램으로 열기 | 새 문서로 가져와짐 | 재확인 필요 (2026-09-14 실패는 dev 서버 localhost:5173 에서 설치 — dev 는 매니페스트 미생성 `vite.config.js` `devOptions.enabled: false`. `npm run build` 후 preview 주소에서 기존 앱 제거 → 재설치 → 탐색기 `.md` 우클릭 "연결 프로그램 > 다른 앱 선택" 에서 확인) |
| H21 | F-130 A8 | 공유 → 파일로 공유 | 시스템 공유 창이 뜸 | 해당 없음 (2026-09-14 실패 "공유하지 못했습니다" — Chromium 공유 허용 목록에 `.md` 없음. F-163 으로 항목 제거) |
| H22 | F-145 A9 | 탐색기에서 `.md` 파일을 앱 창에 끌어다 놓기, 덮개 모양, 에디터 안 글자 끌어 옮기기(A7) | 덮개 표시, 새 문서로 열림. 글자 옮기기는 지금과 같음 | 통과 (2026-09-14) |
| H23 | F-120 | Cloudflare 로그인 후 실제 배포 | 배포 주소에서 앱·PWA 설치 동작 | 배포 통과 (2026-09-15 메인 `rawdoc.app` 배포, `/api/health` db true, `/` 200). PWA 설치·오프라인은 사람 확인 대기 |

## 3. 눈으로 판단

| # | 명세 | 확인 | 상태 |
| --- | --- | --- | --- |
| H30 | F-124 A7 | 편집·보기 모드가 GitHub 스타일과 비슷하고 색이 어울리는지 | 통과 (2026-09-15) |
| H31 | F-139 3.1 | 커서가 표 원문 안이면 원문, 밖이면 표로 바뀌는 방식이 괜찮은지 | 통과 (2026-09-15) |
| H32 | F-140 A10 | 표 호버 모양, 칸 서식 모양, 편집 칸 강조 이동 느낌 | 통과 (2026-09-15) |
| H33 | F-141 A10·A14 | 세피아·다크 색감, 세리프 본문 가독성, 스크롤바 모양 | 통과 (2026-09-15) |
| H34 | F-142 A11 | 상단바 아이콘 모양·크기, 툴팁 문구, 폴더 가이드 선 | 통과 (2026-09-15) |
| H35 | F-143 A13 | 사이드바 레일 모양, 목록 행 여백, 아이콘 선택, 편집 영역 개방감 | 통과 (2026-09-15) |
| H36 | F-144 A11 | 목차 노션 느낌, 선 길이·색, 카드 위치 | 통과 (2026-09-15) |
| H37 | F-148 A8 | 콜아웃 아이콘이 예시 이모지 뜻과 맞는지, 크기 | 대기 |
| H38 | F-149 A8 | 호버·툴팁 전환 느낌(탄성 곡선), 트리에서 문서 끌 때 놓기 강조(`.tree-row--drop`) 반응이 늦지 않은지, 알림 띠·공유 화면 버튼 호버 전환(e2e 미확인) | 재확인 필요 (2026-09-15 통과 후 사용자 "호버가 두 번 겹쳐 들어가는 느낌" → F-173 색 곡선 변경. display 로 켜고 끄는 요소 전환은 F-172) |
| H39 | F-151 A9 | 상단바 제품명·토글·검색 묶음 모양, 접힘 전환 느낌 | 통과 (2026-09-15) |
| H40 | F-152 A10 | 편집 모드 목록 내어쓰기, 제목·구분선·콜아웃 제목 줄 번호 위치, 보기 모드 코드블록 안 글자 바탕 없음 | 대기 |
| H41 | F-153 A9 | 상단바 버튼 선택 표시 무게감, 사이드바 현재 행, 세피아 색감 | 통과 (2026-09-15) |
| H42 | F-154 A9 | 원문 모드·코드 고정폭(2026-09-15 영문·한글 모두 D2Coding, 크기 보정 없음)이 영문·한글 크기 어울리는지, 글자 크기 3단계(작게·보통·크게) 느낌 | 대기 (2026-09-14 120% 맞춤은 사용자 "한글이 너무 커보임" 으로 변경) |
| H43 | F-155 A9 | 편집 모드 프론트매터 표 모양, 표 근처 한글 입력 | 대기 |
| H44 | — | 모바일 화면 제목 위 여백 (2026-09-14 사용자 "모바일은 아직 미확인") | 대기 (모바일 레이아웃 명세 후) |
| H46 | F-159 A11 | 사이드바가 창 위까지 이어진 모양, 너비 손잡이 선·끌기 느낌 | 대기 |
| H47 | F-156 A13 | 캡처 도구(Win+Shift+S) 붙여넣기, 탐색기에서 이미지 끌어놓기, 한글 조합 직후 붙여넣기, 공유 화면에서 붙여넣기가 막히는지(에이전트가 별도 가드를 넣지 않음) | 대기 |
| H48 | F-157 A12 | 이미지 모서리·정렬 버튼 모양, 끌어 크기 조절 느낌, 이미지 앞뒤 줄 한글 입력 | 대기 |
| H49 | F-158 A11 | 내보낸 zip 을 풀어 VS Code·GitHub 미리보기에서 이미지·정렬이 보이는지 | 대기 |
| H50 | F-164 A7 | 세 테마 글자 선택 색 느낌, 줄무늬 없는 표(열·행 많은 표)의 행 구분 가독성, 칸 편집 중 파란 강조 없이도 편집 칸을 알아볼 수 있는지 | 대기 |
| H51 | F-165 A12 | 표 칸 끌어 범위 선택하는 느낌, 범위 강조 모양, 한글 칸 삭제 직후 한글 입력 | 대기 |
| H52 | F-171 A9 | 긴 문서에서 실제 마우스·키보드로 표 행·열 추가 반복, 화면이 튀지 않는지 | 통과 (2026-09-15) |
| H53 | F-172 A9 | 메뉴·대화상자·알림 띠·겹침 사이드바 열고 닫는 느낌, 탄성 곡선이 어색하지 않은지 | 대기 |
| H45 | F-141 A5 | 다크 테마 커서 색. 앱은 `.cm-cursor` 없이 기본 캐럿(`caret-color`)을 쓴다 — e2e 는 `caret-color` 로 대신 판정 | 대기 |

## 4. 결정 대기 (확인이 아니라 정해야 하는 것)

`specs/product.md` 9장 미결 Q6·Q14·Q21·Q26, 명세 승인(`product.md`·`ia.md`·`design.md`)

## 5. M2 계정 준비 (Cloudflare, 사람)

| # | 항목 | 기대 | 상태 |
| --- | --- | --- | --- |
| H60 | `npx wrangler login` | `npx wrangler whoami` 에 계정 표시 | 통과 (2026-09-15 사용자 로그인) |
| H61 | R2 구독 켜기 | 버킷 생성 가능 | 통과 (2026-09-15 사용자 켬, 메인이 MCP 로 `md-editor-attachments` APAC 생성) |
| H62 | D1 DB | `md-editor-db` 존재 | 통과 (2026-09-15 메인이 MCP 로 생성, id `073462f8-7a95-4b0f-8d32-4b07f8c4448e`, APAC, 테이블 없음 — 스키마는 F-206) |
| H63 | Zero Trust 조직(팀 도메인)·일회용 코드 로그인 켜기 | API `access/organizations` 조회 성공 | 통과 (2026-09-15 사용자 활성화, 팀 도메인 `fragrant-sky-f428.cloudflareaccess.com`, 메인이 MCP 로 일회용 코드 IdP 추가) |
| H64 | Access 앱(보호 API 경로) | F-205 명세대로 경로·정책 | 통과 (2026-09-15 메인이 MCP 로 `md-editor-api` 자체 호스팅 앱 `rawdoc.app/api`, 정책 `all-emails` 모두 허용, 일회용 코드만, 세션 168h. 2026-09-15 사용자가 `/api/me` 일회용 코드 로그인 → 이메일 JSON 확인. 앱 메뉴 로그인·로그아웃은 F-205 B 뒤 A8) |
| H65 | F-205 A8 | 상단바 계정 메뉴 → 로그인 → 일회용 코드 → 앱 복귀 → 이메일 표시 → 로그아웃 | 대기 (2026-09-15 배포됨) |
| H66 | F-208·F-207 A8 | 로컬 문서가 있는 브라우저에서 첫 로그인 → 문서·폴더·고정 그대로 옮겨짐, 다른 브라우저 같은 계정에서 보임 | 대기 (2026-09-15 배포됨) |
| H67 | F-210 A11 | 공유 메뉴 `읽기 전용 링크 복사` → 시크릿 창에서 로그인 없이 보임 → `링크 끊기` 뒤 안 열림 | 대기 (2026-09-15 배포됨) |
| H68 | F-209 A9 | 이미지 붙여넣기 → 공개 링크 시크릿 창에서 이미지 보임, 다른 사이트 `<img>` 로 끌어오지 못함 | 대기 (2026-09-15 배포됨) |
| H69 | F-211 A6 | 폴더 `⋯` → `읽기 전용 링크 복사` → 시크릿 창 목록·본문, 소유자가 문서 추가 후 새로고침에 보임. 목록 순서(폴더 자신 문서 → 하위 폴더 묶음)가 괜찮은지 | 대기 (2026-09-15 배포됨) |
| H70 | F-212 A7·A8 | 실제 계정 두 개: 초대(보기·편집) → 받은 쪽 `공유받음` 에서 열기·편집, 보기 문서에서 한글 입력이 막히는지, 편집 중 권한을 빼면 `이 문서를 편집할 권한이 없어졌습니다.` 뒤 읽기 전용. 초대 대화상자·공유받음 묶음 모양 | 대기 |
| H71 | F-213 A7 | 배포 주소에서 편집 권한 계정 두 개(또는 같은 계정 두 창)로 같은 문서 열기: 늦게 연 쪽 읽기 전용 + `{이메일} 님이 편집 중입니다.`(같은 계정이면 `다른 창에서 편집 중입니다.`), 먼저 연 창을 닫으면 15초 안팎 뒤 `이제 편집할 수 있습니다.` 와 최신 내용. 로컬 wrangler A2·A3 미실행 | 대기 |
| H72 | Q5 Google 로그인 | 로그아웃 → 계정 메뉴 로그인 → Access 선택 화면에 Google·일회용 코드 → Google 로 로그인 → 앱 복귀, 기존 이메일과 같으면 기존 문서 그대로. Google 동의 화면 게시 상태 프로덕션 | 대기 (2026-09-15 MCP 로 `md-editor-api` 에 Google IdP 추가, 자동 이동 끔) |
| H73 | F-215 A5 | 배포 주소의 문서·폴더 읽기 전용 링크를 넓은·좁은 창(휴대폰 포함)에서: 로고·구분선·제목 정렬, 폴더 목록 머리 가로선이 문서 머리 줄과 이어지는지, 로고 누르면 앱 첫 화면 | 대기 (메인이 가짜 응답 스크린샷 4장으로 1차 확인) |
| H74 | F-216 A3 | 실제 휴대폰에서 첫 화면·문서 편집·공유 링크: 가로·세로 불필요한 스크롤 없음, 한글 줄바꿈이 낱말 단위(keep-all), 이미지 정렬 버튼 아이콘이 버튼 안에 맞음 | 대기 (메인이 Pixel 7 흉내 412px 로 1차 확인) |
