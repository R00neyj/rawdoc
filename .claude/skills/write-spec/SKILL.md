---
name: write-spec
description: Rawdoc 명세(specs/features/F-xxx.md)를 Opus 서브에이전트로 작성한다. 설계 개요와 작은 명세 둘 다. "F-273 명세 써줘", "검색 명세 작성", "남은 명세 리스트업 후 작성" 같은 요청에 쓴다. 구현은 ship-feature 가 맡는다.
---

# write-spec

명세 작성만 한다. 구현은 `ship-feature`.

## 1. 준비 — 메인이 먼저 정한다

1. **번호를 정한다.** 단계별 백 단위 — M1 `F-1NN`, M2·M1 보강 `F-2NN`, M3 `F-3NN`. `ls specs/features/` 로 빈 번호를 찾는다
2. **개요인지 작은 명세인지** 정한다
   - 여러 화면·여러 구현 단위에 걸치면 **설계 개요** (`F-270`·`F-277`·`F-284` 형식). 1~5장 결정 + 6장 하위 로드맵 표
   - 하나가 구현 단위 1개면 **작은 명세** (`F-279`·`F-282` 형식)
3. **사용자에게 물을 것을 먼저 묻는다.** 설계가 갈리는 지점(어디서 도나, 어떤 UI 형태, 범위 경계)은 에이전트가 고르게 두지 말고 `AskUserQuestion` 으로 결정받아 프롬프트에 **"이미 정한 것, 바꾸지 말 것"** 으로 박는다. 이게 가장 큰 품질 차이를 만든다
4. `specs/product.md` 가 그 기능을 **범위 밖**으로 적고 있는지 확인한다. 적혀 있으면 프롬프트에 "갱신 대상" 으로 넣게 한다 (에이전트는 기존 `specs/**` 를 못 고친다)
5. **파일 겹침을 본다.** 같은 소스 파일을 건드릴 명세끼리는 병렬로 띄워도 되지만(각자 다른 `F-xxx.md` 를 쓰므로), 그 사실을 양쪽 프롬프트에 알려 서로 모순되는 결정을 못 하게 한다

## 2. 에이전트 띄우기

`Agent`, `subagent_type: "general-purpose"`, **`model: "opus"`** (CLAUDE.md — 명세 작성은 Opus). 병렬 가능하면 한 메시지에 여러 개.

프롬프트 뼈대:

```
`specs/features/F-xxx.md` 를 새로 작성한다. **명세만 쓴다. 구현·코드 수정·커밋은 하지 않는다.**
새로 만드는 파일은 그 하나뿐이다.

## F-xxx 는 무엇인가
{상위 개요의 로드맵 행을 인용} / {선행 명세의 구현 상태}

## 사용자가 이미 정한 것 (바꾸지 말 것)
{1단계에서 받은 결정을 번호로. 이유까지 적는다}

## 먼저 읽을 것 (순서대로)
1. CLAUDE.md
2. specs/product.md {해당 장}
3. specs/ia.md · design.md · architecture.md {해당 장}
4. {상위 개요} 전문
5. {선행 명세} 전문 — 형식 본보기이자 이어받는 것
6. 실제 코드: {파일 목록. 무엇을 확인해야 하는지까지}

## 명세에 반드시 담을 것
{장별로 무엇을 결정해야 하는지. 애매하게 남기면 안 되는 지점을 구체적으로}
- 수용 기준 표 (A1, A2… 각 항목이 자동 테스트 또는 사람 확인으로 판정 가능해야 함)
- 수정 파일 표 (첫 칸 백틱 경로. `npm run review -- F-xxx` 가 읽는다)
- 갱신 대상 (고쳐야 할 기존 문서를 어느 장까지. 직접 고치지는 않는다)
- 열린 질문 / 사람 결정 필요

## 반드시 지킬 것
- 불변조건(CLAUDE.md): 색 hex 는 tokens.css·brand.config.ts 에서만, 제품명 문자열 직접 쓰기 금지,
  저장소 식별자에 제품명 금지, decoration 은 문서 내용을 안 바꿈, 원문 보존
- **새 의존성을 마음대로 확정하지 않는다.** 쓰자고 하려면 대안 비교표 + "사람 결정 필요" 표시.
  기본값은 의존성 없이 직접 구현
- **테스트 환경은 `node`** (jsdom·happy-dom 미설치). DOM 없이 단위 테스트가 되도록 순수 함수로
  분리하는 설계를 한다 — F-278 이 DOMParser 대신 markdown-it 토큰 순회를 택한 이유
- 동작 기준은 `e2e/F-xxx` Playwright, 순수 로직은 단위 테스트, **시각값은 e2e 로 고정하지 말고
  사람 확인으로** (CLAUDE.md "개발 방식")
- 기존 `specs/**` 와 `CLAUDE.md` 는 수정하지 않는다. 고칠 것은 "갱신 대상" 에 적기만 한다

## 보고
주요 결정과 근거, **실제로 코드를 읽거나 돌려 확인한 것과 가정으로 둔 것을 구분해서**,
사람 결정이 필요한 항목. 확인하지 않은 것을 확인했다고 적지 않는다
```

### 품질을 올리는 한 줄

- 상위 개요가 "F-xxx 에서 확인한다" 로 미뤄둔 것이 있으면 → **"조사 항목으로 남기지 말고 네가 실제 코드를 읽고/돌려서 결론을 적는다"**
- 성능·용량이 걸리면 → **"스크래치패드에 스크립트를 만들어 직접 재서 숫자를 넣는다. 추정을 사실처럼 적지 마라"**
- 단축키·API 지원 여부 → **"`node_modules` 의 키맵/타입 정의를 직접 확인하고 적는다"**

## 3. 보고 받으면

1. **"가정으로 둔 것" 을 먼저 읽는다.** 거기 있는 것이 다음 사고의 지점이다
2. 보고가 짚은 **기존 코드·명세와의 불일치**를 메인이 직접 확인한다 (파일 한두 개 읽는 수준). 에이전트 말만 믿고 사용자에게 전하지 않는다
3. `npm run review -- F-xxx` 로 수정 파일 표가 파싱되는지 본다 (제목이 "파일 소유"·"수정 파일"·"바꾸는 파일" 중 무엇이든 읽는다)
4. 절 번호 참조가 어긋난 곳이 있는지 `grep -n "^### \|(N\.M)"` 로 훑는다 — 에이전트가 절을 옮기고 참조를 안 고치는 일이 잦다

## 4. 커밋

- **명세 파일만.** 병렬 중이면 반드시 경로 지정: `git add -- specs/features/F-xxx.md` → `git commit … -- specs/features/F-xxx.md`
- 메시지는 `F-xxx {제목} 명세` 한 줄. 결정이 뒤집힌 게 있으면 본문에 이유

## 5. 사용자 보고 (음슴체, 짧게)

- 실측/코드 확인으로 새로 드러난 사실 — 이게 제일 값어치 있다
- **사람 결정이 필요한 항목을 선택지와 추천으로.** 기본값이 다 타당하면 "기본값대로 갈지만 확인" 으로 묶어 묻는다
- 다음에 쓸 명세와 구현 순서(파일 겹침 기준)

## 멈출 때

- 상위 개요가 없는데 여러 구현 단위에 걸친다 → 개요부터 쓸지 사용자에게 묻는다
- `specs/product.md` 가 범위 밖으로 못박은 기능이다 → 범위를 바꾸는 승인을 먼저 받는다
